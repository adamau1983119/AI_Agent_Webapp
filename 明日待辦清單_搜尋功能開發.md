# 明日待辦清單 - 搜尋功能開發

**日期**：2026-01-21  
**目標**：實作新的主題搜尋 API，整合中文全文搜尋、快取策略、查詢限制、權限控制

---

## 📋 檢查結果（2026-01-20）

### ✅ Trace ID 機制檢查
- **狀態**：✅ 部分實作
- **發現**：圖片搜尋 API (`/api/v1/images/search`) 已有 trace_id 機制
- **需要**：擴展到其他 API 端點（搜尋 API、主題 API 等）
- **檔案位置**：`backend/app/api/v1/images.py` (line 268)

### ❌ Topic 模型 source.style 檢查
- **狀態**：❌ 未實作
- **發現**：`Topic` 模型只有 `source: str` 和 `sources: List[SourceInfo]`
- **問題**：缺少 `source.style` 結構（tone, structure, vocabulary）
- **需要**：擴充 `SourceInfo` 模型，加入 `style` 欄位
- **檔案位置**：`backend/app/models/topic.py` (line 24-51)

### ❌ Prompt style 參數檢查
- **狀態**：❌ 未使用
- **發現**：`article_prompt.py` 和 `script_prompt.py` 都沒有使用 `style` 參數
- **問題**：無法根據原文風格保持生成內容風格
- **需要**：更新 Prompt 模板，加入 `style` 參數支援
- **檔案位置**：
  - `backend/app/prompts/article_prompt.py`
  - `backend/app/prompts/script_prompt.py`

---

## 🎯 優先級說明

- 🔴 **高優先級**：必須完成的核心功能
- 🟡 **中優先級**：重要但可延後的功能
- 🟢 **低優先級**：優化與改進功能

---

## 📋 Phase 0：資料模型與 Prompt 擴充（優先完成）

### 🔴 0.1 擴充 SourceInfo 模型（加入 style 結構）
- [ ] 在 `SourceInfo` 類別中加入 `style` 欄位
- [ ] 定義 `StyleInfo` 類別（tone, structure, vocabulary）
- [ ] 更新 `Topic` 模型以支援 `source.style`
- [ ] 確保向後兼容（現有資料不受影響）

**預估時間**：30 分鐘

**檔案位置**：
- `backend/app/models/topic.py`

**參考結構**：
```python
class StyleInfo(BaseModel):
    tone: Optional[str] = Field(None, description="語氣風格")
    structure: Optional[str] = Field(None, description="結構風格")
    vocabulary: Optional[str] = Field(None, description="用詞風格")

class SourceInfo(BaseModel):
    # ... 現有欄位 ...
    style: Optional[StyleInfo] = Field(None, description="原文風格分析")
```

---

### 🔴 0.2 更新 Prompt 模板支援 style 參數
- [ ] 更新 `build_article_prompt()` 函數，加入 `style` 參數
- [ ] 更新 `build_script_prompt()` 函數，加入 `style` 參數
- [ ] 在 Prompt 中加入風格保持指示
- [ ] 測試 Prompt 生成是否正確

**預估時間**：45 分鐘

**檔案位置**：
- `backend/app/prompts/article_prompt.py`
- `backend/app/prompts/script_prompt.py`

**參考修改**：
```python
def build_article_prompt(
    topic_title: str,
    topic_category: str,
    keywords: List[str],
    target_length: int = 500,
    style: Optional[StyleInfo] = None  # 新增
) -> str:
    # 在 Prompt 中加入風格指示
    if style:
        prompt += f"""
**風格要求**：
- 語氣：{style.tone}
- 結構：{style.structure}
- 用詞：{style.vocabulary}
"""
```

---

### 🟡 0.3 擴展 Trace ID 機制到搜尋 API
- [ ] 在搜尋 API 端點生成 trace_id
- [ ] 將 trace_id 傳遞到所有服務層
- [ ] 確保所有日誌都包含 trace_id
- [ ] 前端錯誤訊息顯示 trace_id

**預估時間**：30 分鐘

**參考實作**：
- `backend/app/api/v1/images.py` (line 268)

---

## 📋 Phase 1：基礎功能（MVP）

### 🔴 1. 建立搜尋 API 端點檔案
- [ ] 創建 `backend/app/api/v1/search.py`
- [ ] 設定 FastAPI Router
- [ ] 定義基本端點結構
- [ ] 加入 API 文檔註解

**預估時間**：30 分鐘

---

### 🔴 2. 實作 MongoDB 基礎查詢
- [ ] 實作 `search_topics()` 函數
- [ ] 使用 `$regex` 進行關鍵字搜尋
- [ ] 支援分類篩選（category 參數）
- [ ] 實作基本分頁（skip/limit）
- [ ] 加入查詢結果投影（只返回必要欄位）

**預估時間**：1 小時

**參考檔案**：
- `backend/app/api/v1/topics.py`
- `backend/app/services/repositories/topic_repository.py`

---

### 🔴 3. 實作輸入驗證與清理
- [ ] 實作 `sanitize_query()` 函數
- [ ] 清理查詢字串（移除危險字符）
- [ ] 驗證查詢字串長度（2-100 字元）
- [ ] 驗證分頁參數（page: 1-100, limit: 1-50）
- [ ] 加入錯誤處理（400 Bad Request）

**預估時間**：30 分鐘

---

### 🔴 4. 實作基本權限控制
- [ ] 建立 `UserRole` Enum（guest, user, premium, admin）
- [ ] 實作 `filter_results_by_role()` 函數
- [ ] 根據角色過濾結果欄位
- [ ] 加入權限檢查（403 Forbidden）

**預估時間**：45 分鐘

**參考檔案**：
- `backend/app/models/user.py`
- `backend/app/dependencies.py`

---

### 🔴 5. 整合到主路由
- [ ] 在 `backend/app/main.py` 註冊搜尋路由
- [ ] 測試 API 端點可訪問性
- [ ] 驗證 API 文檔顯示正確

**預估時間**：15 分鐘

---

## 📋 Phase 2：效能優化

### 🟡 6. 設定 Redis 連接
- [ ] 安裝 `redis` 和 `redis.asyncio` 套件
- [ ] 建立 Redis 連接池配置
- [ ] 設定連接參數（host, port, max_connections）
- [ ] 加入連接錯誤處理

**預估時間**：30 分鐘

**環境變數**：
```env
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
```

---

### 🟡 7. 實作 Redis 快取邏輯
- [ ] 實作 `build_cache_key()` 函數（使用 MD5 hash）
- [ ] 實作快取讀取邏輯（檢查 Redis）
- [ ] 實作快取寫入邏輯（TTL: 5分鐘）
- [ ] 加入快取命中/未命中日誌
- [ ] 處理 Redis 連接失敗（繼續查詢，不使用快取）

**預估時間**：1 小時

**快取 Key 格式**：
```
search:{query_hash}:{category}:{page}:{limit}:{role}
```

---

### 🟡 8. 優化 MongoDB 查詢
- [ ] 使用游標分頁（取代 skip，使用 _id 游標）
- [ ] 優化查詢投影（只返回必要欄位）
- [ ] 驗證索引使用情況（使用 explain()）
- [ ] 加入查詢效能日誌

**預估時間**：45 分鐘

---

### 🟡 9. 實作熱門查詢統計
- [ ] 在查詢時更新 Redis Sorted Set（`hot:queries`）
- [ ] 實作 `GET /api/v1/topics/search/hot-queries` 端點
- [ ] 返回熱門查詢列表（按查詢次數排序）
- [ ] 設定 TTL（1小時）

**預估時間**：30 分鐘

---

## 📋 Phase 3：進階功能

### 🟢 10. 設定 Elasticsearch 連接（可選）
- [ ] 安裝 `elasticsearch` 套件
- [ ] 建立 Elasticsearch 連接池配置
- [ ] 設定連接參數（host, port, maxsize）
- [ ] 加入連接錯誤處理

**預估時間**：30 分鐘

**環境變數**：
```env
ELASTICSEARCH_HOST=http://localhost:9200
```

---

### 🟢 11. 實作 Elasticsearch 搜尋邏輯（可選）
- [ ] 實作 Elasticsearch 查詢邏輯
- [ ] 使用 IK Analyzer 進行中文斷詞
- [ ] 支援分類篩選（filter）
- [ ] 支援相關度排序
- [ ] 實作備援機制（ES 失敗 → MongoDB）

**預估時間**：2 小時

**注意**：需要先安裝 Elasticsearch 和 IK Analyzer 插件

---

### 🟢 12. 實作檢查原文是否已收錄端點
- [ ] 實作 `GET /api/v1/topics/search/check` 端點
- [ ] 使用 `source.url` 唯一索引快速查詢
- [ ] 返回是否存在及主題資訊
- [ ] 加入錯誤處理

**預估時間**：30 分鐘

---

### 🟢 13. 實作健康檢查端點
- [ ] 實作 `GET /api/v1/topics/search/health` 端點
- [ ] 檢查 Elasticsearch 連接狀態
- [ ] 檢查 Redis 連接狀態
- [ ] 檢查 MongoDB 連接狀態
- [ ] 返回服務健康狀態

**預估時間**：30 分鐘

---

### 🟢 14. 實作清除快取端點（管理員）
- [ ] 實作 `DELETE /api/v1/topics/search/cache` 端點
- [ ] 加入管理員權限檢查
- [ ] 支援模式匹配清除快取
- [ ] 返回清除的快取數量

**預估時間**：30 分鐘

---

## 📋 Phase 4：測試與優化

### 🔴 15. 單元測試
- [ ] 測試輸入驗證邏輯
- [ ] 測試權限過濾邏輯
- [ ] 測試快取讀寫邏輯
- [ ] 測試錯誤處理邏輯

**預估時間**：1.5 小時

---

### 🔴 16. API 整合測試
- [ ] 測試搜尋端點（各種參數組合）
- [ ] 測試快取命中/未命中場景
- [ ] 測試權限控制（不同角色）
- [ ] 測試錯誤情況（無效輸入、服務不可用）

**預估時間**：1 小時

---

### 🟡 17. 效能測試
- [ ] 測試查詢效能（無快取）
- [ ] 測試快取效能（有快取）
- [ ] 測試並發查詢效能
- [ ] 分析慢查詢（使用 explain()）

**預估時間**：1 小時

---

### 🟡 18. 日誌與監控
- [ ] 加入查詢日誌記錄
- [ ] 記錄快取命中率
- [ ] 記錄查詢效能指標
- [ ] 記錄錯誤統計

**預估時間**：45 分鐘

---

## 📋 Phase 5：文檔與部署

### 🟡 19. API 文檔更新
- [ ] 更新 OpenAPI/Swagger 文檔
- [ ] 加入請求/回應範例
- [ ] 加入錯誤碼說明
- [ ] 加入使用範例

**預估時間**：30 分鐘

---

### 🟡 20. 程式碼審查
- [ ] 檢查程式碼風格
- [ ] 檢查錯誤處理
- [ ] 檢查安全性問題
- [ ] 檢查效能優化

**預估時間**：30 分鐘

---

### 🟢 21. 部署準備
- [ ] 檢查環境變數配置
- [ ] 檢查依賴套件版本
- [ ] 準備部署說明文檔
- [ ] 準備回滾計劃

**預估時間**：30 分鐘

---

## 📊 時間估算

### 必須完成（Phase 0 + Phase 1）
- **Phase 0**：約 1.75 小時（資料模型擴充、Prompt 更新、Trace ID）
- **Phase 1**：約 3 小時（基礎搜尋 API、輸入驗證、權限控制）
- **總時間**：約 4.75 小時
- **核心功能**：資料模型擴充、Prompt 優化、基礎搜尋 API

### 建議完成（Phase 2）
- **總時間**：約 2.5 小時
- **效能優化**：Redis 快取、查詢優化、熱門查詢統計

### 可選完成（Phase 3）
- **總時間**：約 4 小時
- **進階功能**：Elasticsearch 整合、健康檢查、清除快取

### 測試與優化（Phase 4-5）
- **總時間**：約 4.5 小時
- **測試與文檔**：單元測試、整合測試、API 文檔

---

## 🎯 明日目標

### 最低目標（必須完成）
完成 Phase 0 + Phase 1 的核心任務：
- ✅ 擴充資料模型（source.style）
- ✅ 更新 Prompt 模板（支援 style）
- ✅ 實作基礎搜尋 API

### 理想目標（建議完成）
完成 Phase 0 + Phase 1 + Phase 2：
- ✅ 擴充資料模型與 Prompt
- ✅ 實作基礎搜尋功能
- ✅ 加入 Redis 快取與效能優化

### 最佳目標（可選完成）
完成 Phase 0-3，實作完整搜尋功能（包括 Elasticsearch）。

---

## 📝 注意事項

1. **分支管理**：在 feature 分支進行開發，不要直接修改 main 分支
2. **備份**：修改核心檔案前先備份
3. **測試**：每個功能完成後立即測試
4. **文檔**：及時更新 API 文檔
5. **錯誤處理**：完善錯誤處理與日誌記錄

---

## 🔗 相關檔案

### 後端檔案
- `backend/app/api/v1/search.py`（新建）
- `backend/app/api/v1/topics.py`（參考）
- `backend/app/services/repositories/topic_repository.py`（參考）
- `backend/app/models/user.py`（參考）
- `backend/app/dependencies.py`（參考）
- `backend/app/main.py`（修改）

### 配置檔案
- `backend/.env`（新增環境變數）
- `backend/requirements.txt`（新增依賴）

### 文檔檔案
- `新搜尋邏輯需求.md`（需求文檔）
- `明日待辦清單_搜尋功能開發.md`（本檔案）

---

## ✅ 完成檢查清單

完成後請檢查：
- [ ] 所有 API 端點可正常訪問
- [ ] 輸入驗證正常運作
- [ ] 權限控制正常運作
- [ ] 快取功能正常運作
- [ ] 錯誤處理完善
- [ ] 日誌記錄完整
- [ ] API 文檔更新
- [ ] 程式碼通過審查

---

**最後更新**：2026-01-20  
**檢查日期**：2026-01-20  
**檢查結果**：
- ✅ Trace ID：部分實作（圖片 API 已有，需擴展）
- ❌ source.style：未實作（需擴充 SourceInfo 模型）
- ❌ Prompt style：未使用（需更新 Prompt 模板）

