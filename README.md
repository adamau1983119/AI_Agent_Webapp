# Influencers AI Agents（網紅 AI 助手）

> **版本**：v6.0.0  
> **更新日期**：2026-05-15（與工作記錄同步）  
> **當前分支**：`main`（✅ v6.0.0 已合併，Pull request #9）  
> **穩定標籤**：`v4.3.0-code-complete`（上一版本）  
> **狀態同步**：此 README 的「更新日期」應與 `工作記錄.md` 的「最後更新」一致。如不一致，以 `工作記錄.md` 為準。  
> **AI 排程**：對話輸入「專案開始」可展開 **14 工作天** MVP／測試清單（依 `AGENTS.md` **日曆總表**，含建立頻道插入 3 天）→ [AGENTS.md](./AGENTS.md)

---

## Git 備份與工作流程（遠端 `main` 受保護時必讀）

本倉庫於 GitHub 可啟用 **branch protection**：**不得直接 `git push origin main`**，變更須經 **Pull Request** 合併至 `main`。

### 日常備份到遠端（建議流程）

1. 於本機 **`main`**（或自 `main` 切出之功能分支）完成 `git add` / `git commit`。  
2. **推送至遠端同名或專用分支**（範例：`docs/你的敘述-日期`）：  
   `git push origin HEAD:docs/your-branch-name`  
3. 至 GitHub **建立 Pull Request** → 審核後 **Merge into `main`**。  
4. 本機同步合併結果：`git fetch origin` → `git pull origin main`。

### 如何驗證「真的已備份」（避免口頭／助手聲稱未兌現）

| 驗證方式 | 說明 |
|----------|------|
| **GitHub** | PR 顯示 **Merged**、或提供 **Commit／PR 網址**。 |
| **本機指令** | `git fetch origin` 後執行 `git log origin/main -3 --oneline`，確認頂端含預期 merge 或內容 commit。 |

**內容 commit**（例如文件變更的短 SHA）與 **合併進 `main` 後的 merge commit** 可能為**不同** SHA，兩者皆可能同時存在於歷史中；以 **`origin/main` 是否含你的變更** 與 **PR 狀態** 為準，勿僅憑單一短 SHA 口頭認定。

### 與本專案其他文件之關係

- **分支策略與版本**：詳 [Git分支策略與版本管理.md](./Git分支策略與版本管理.md)。  
- **換機／硬碟遺失後重建與對照**：詳 [docs/環境重建指南與Checklist.md](./docs/環境重建指南與Checklist.md)（含第十一～十三節：易漏複核、救回檔合併、壓縮重建）。  
- **測試週 Gate 與工作記錄勾稽**：[工作記錄.md](./工作記錄.md) 之「重建（R）＋測試週檢核（T）」與近期更新之 **Git 聲明防呆**。

---

## ⛔ 交付與驗收原則：以 Checklist 為唯一收貨依據

**專案核心交付準則**：凡本專案之驗收、收口、是否「可收貨」，**必須**對照**書面 Checklist** 逐項執行，並留存**可稽核證據**（例如：URL、HTTP status、截圖、`工作記錄.md` 一句結論、Network 摘要）。**未經 Checklist 涵蓋之口頭需求、口頭完成聲明，不構成收貨條件；無 Checklist 對照者，本人不予收貨。**

**開發段對照前提**：第 1～9 工作天相關之 `AGENTS.md` 核取項、[docs/channel_create_new_scheme_checklist.md](./docs/channel_create_new_scheme_checklist.md) **A～H**（含 B～D 建立頻道交付）、[專案完整架構表.md](./專案完整架構表.md) 與規格互鏈等，**已視為開發段依 Checklist 對齊之基線**；後續僅接受**測試週**與**迴歸**之勾選與證據更新。

### 下週測試段（2026-05-12 二 ～ 2026-05-21 四）— 必跑 Checklist 總表

> **單日執行順序**：每天以 **[docs/test_week_daily_checklist.md](./docs/test_week_daily_checklist.md)** 為**第一入口**（已內含 AGENTS 第 10～14 天與矩陣／I 節之交叉引用）。

| # | Checklist／文件 | 範圍／備註 |
|---|-----------------|------------|
| 1 | **[docs/test_week_daily_checklist.md](./docs/test_week_daily_checklist.md)** | **每日**可勾條目；**第 10～14 工作天**（05-12、05-13、05-14、05-15、05-21） |
| 2 | **[AGENTS.md](./AGENTS.md)** | **§ 專案開始** → 日曆總表 → **第 10～14 工作天**完成判定 |
| 3 | **[docs/channel_create_new_scheme_checklist.md](./docs/channel_create_new_scheme_checklist.md)** | **I 節**（I.1 測試、I.2 發布、I.3 文件／SoT）；與建立頻道測試週工單 |
| 4 | **[docs/architecture_test_matrix.md](./docs/architecture_test_matrix.md)** | **§A～§K** 全站路由／模組煙霧與 Mongo 對照 |
| 5 | **[docs/test_week_gap_closure_plan.md](./docs/test_week_gap_closure_plan.md)** | **12 類**架構缺口收口；**3000／8000** 本機預設 |
| 6 | **[按鈕測試ID架構表.md](./按鈕測試ID架構表.md)** | 凡測試觸及之 **data-testid** 與本輪變更按鈕 |
| 7 | **[v4.0.0_Checklist_TestList.md](./v4.0.0_Checklist_TestList.md)** | 歷史 Phase 1～5 **細項**；**第 14 天**掃描與本版相關之未勾列 |

**本機預設**：前端 **`http://localhost:3000`**、後端 **`http://localhost:8000`**（詳 [test_week_gap_closure_plan.md](./docs/test_week_gap_closure_plan.md) 第二節）。

---

## 📊 專案狀態快速總覽（每次查看必讀）

> **⚠️ 重要**：此區段提供專案當前狀態的快速概覽。如需詳細資訊，請查看 [工作記錄.md](./工作記錄.md)

### 當前狀態

| 項目 | 狀態 | 說明 |
|:----:|:----:|------|
| **版本** | v6.0.0 | ✅ 已合併到 main（Pull request #9） |
| **當前分支** | `main` | ✅ v6.0.0 已合併 |
| **開發完成度** | 106/126 (100%) | Phase 1-5 程式開發全部完成 |
| **剩餘項目** | 20 項 | QA 測試 / DevOps / DOC 文件（非程式開發） |
| **測試狀態** | Phase 1 完成 | 2026-02-10 完成 Phase 1 測試 |
| **最後更新** | 2026-05-15 | 詳見 [工作記錄.md](./工作記錄.md)；**驗收**以 README **交付與驗收原則（Checklist）** 一節為準；每日 [docs/test_week_daily_checklist.md](./docs/test_week_daily_checklist.md)；**Git 備份**見本 README **Git 備份與工作流程** |

### 最新進展（最近 3 項）

1. **2026-02-10**：Phase 1 測試完成 + 修復登入跳轉/OAuth認證/語言同步問題
   - Git 提交：`b22248f`
   - 詳見：[工作記錄.md - Phase 1 測試完成](./工作記錄.md#-phase-1-測試完成2026-02-10)

2. **2026-02-07**：v4.3.0 全部程式開發完成 + 硬編碼文字全面根治
   - Git 提交：`748484d`, `d4dc45e`
   - 詳見：[工作記錄.md - 2026-02-07](./工作記錄.md#2026-02-07星期五-重大里程碑---v430-全部程式開發完成)

3. **2026-02-10**：v5.0.0 測試分支建立
   - 分支：`v5.0.0`
   - Pull request：#8

### 分支與版本狀態

| 版本 | Git 標籤 | 分支狀態 | 合併狀態 | 說明 |
|:----:|:--------:|:--------:|:--------:|------|
| **v6.0.0** | - | `main` | ✅ 已合併 | **當前版本**（Pull request #9，包含 v5.0 靈感策劃 AI 助手完整功能） |
| **v5.0.0** | - | `v5.0.0` | ⚠️ 未合併 | 測試版本（Pull request #8） |
| v4.3.0 | `v4.3.0-code-complete` | `feature/language-strategy` | ⚠️ 未合併 | 全部程式開發完成（Pull request #7） |
| v4.2.0 | `v4.2.0-i18n-complete` | `main` | ✅ 已合併 | i18n 全面完成 |
| v4.1.0 | `v4.1.0` | `main` | ✅ 已合併 | Dashboard UI 重構 |

**⚠️ 當前狀態**：`v5.0.0` 分支為當前測試版本，包含 Phase 1 測試完成後的修復內容。下一步：完成測試後合併到 `main`。

### 如何獲取最新狀態

1. **查看 README 頂部**：快速了解當前版本、分支、完成度
2. **檢查更新日期**：
   - 如果 README「更新日期」超過 3 天 → 必須查看 `工作記錄.md`
   - 如果 README「更新日期」與 `工作記錄.md`「最後更新」不一致 → 以 `工作記錄.md` 為準
3. **查看工作記錄**：
   - 頂部：當前狀態總覽
   - 「工作記錄（按時間倒序）」：最新工作詳情
4. **確認分支狀態**：以 `工作記錄.md` 頂部的「當前分支」為準

---

## 🎉 v4.3.0 里程碑（2026-02-07）— 全部程式開發完成

### 🏆 里程碑成就

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   🏆 2026-02-07  全部前後端程式開發完成！                                │
│                                                                         │
│   ✅ Phase 1：基礎架構重構（無限 Loop + RSS 健康監控）     — 20/20     │
│   ✅ Phase 2：會員系統（認證 + Email + 多語言 + 安全性）   — 28/28     │
│   ✅ Phase 3：內容功能（RSS + 頻道 + 備用機制 + 靈感）     — 19/22*    │
│   ✅ Phase 4：AI 個人化（風格 + 評分 + 分析 + 生成）       — 21/22*    │
│   ✅ Phase 5：分發與整合（帳號連接 + 發布 + 最佳化）       — 18/34*    │
│                                                                         │
│   📊 程式開發完成率：106/126 = 100%                                     │
│   📋 剩餘 20 項：QA 測試 / DOC 文件 / DevOps 部署（非程式項目）        │
│                                                                         │
│   🔧 硬編碼文字全面根治：                                               │
│   • Python 系統化掃描 15+ 前端檔案                                      │
│   • 修復所有用戶可見的硬編碼 UI 文字                                     │
│   • 新增 16 組 i18n key（zh-TW/en/ja 共 48 條翻譯）                    │
│   • 最終驗證：零硬編碼殘留                                               │
│                                                                         │
│   * 星號項目剩餘為 QA/DOC/DevOps，非程式開發                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 📝 今日主要 Commits（2026-02-07）

```
748484d - fix(i18n): eliminate all hardcoded UI text - comprehensive i18n remediation
d4dc45e - feat: complete all frontend/backend code development (Phase 1-5)
375f6d0 - docs: Add work record for 2026-02-07
bed5fe6 - docs: Update checklist - mark completed items
d085bf5 - feat: Multi-language topic generation strategy (Phase 7)
f522e10 - feat: Phase1 RSS health monitoring + Phase2 security enhancements
6497b2e - feat: B.9 快捷按鈕 + B.10 來源預覽卡片 + 4.2/4.3 內容生成 UI
```

### ⏭️ 下一階段：QA 測試與功能驗證（2026-02-08 起）

| 優先級 | 項目 | 說明 |
|:------:|------|------|
| P0 | 合併 PR | `feature/language-strategy` → `main` |
| P0 | 手動功能測試 | 忘記密碼、頻道 CRUD、評分系統、語言匹配 |
| P1 | 端到端測試 | 註冊→登入→瀏覽→生成→發布 完整流程 |
| P1 | 效能測試 | 頁面載入 <2s、滾動載入 <200ms、生成 <5s |
| P2 | DevOps | 生產環境配置、備份策略、監控告警 |
| P2 | 文件 | API 文件、部署手冊、用戶手冊 |

### ✅ 此版本為穩定基線

- **標籤**：`v4.3.0-code-complete`
- **用途**：全部程式碼開發完成的穩定版本
- **保護狀態**：此標籤不應被刪除或覆蓋

---

## 📦 v4.2.0 更新歷史（2026-02-05）

### 硬編碼文字第一輪 i18n 化

| 項目 | 數量 | 說明 |
|------|------|------|
| **已修復硬編碼** | 220+ | 所有用戶可見的 UI 文字 |
| **翻譯語言** | 3 種 | zh-TW（繁體中文）、en（英文）、ja（日文） |
| **修改批次** | 11 批 | 系統性、規範性修改 |
| **涉及文件** | 30+ | 頁面、組件、工具 |

- **標籤**：`v4.2.0-i18n-complete`

---

## ⚠️ 重要提醒（開發者必讀）

| # | 規則 | 說明 |
|:-:|------|------|
| 1 | **查看真實日期** | 每次查看 README 必須確認「更新日期」，判斷文檔是否過時。如果超過 7 天未更新，請先查看 `工作記錄.md` 了解最新狀態。 |
| 2 | **遵循架構文件** | 修改前先閱讀下方「專案架構」區段的所有文件 |
| 3 | **按鈕必須標記** | 所有新增按鈕必須添加 `data-testid` 屬性，參考 `按鈕測試ID架構表.md` |
| 4 | **🔴 按鈕編碼與架構表同步** | **所有按鈕和連結必須與架構表保持一致**。新增/修改按鈕時必須：<br>1. 在 `按鈕架構表.md` 中定義按鈕 ID、功能、狀態<br>2. 在 `按鈕測試ID架構表.md` 中添加 `data-testid`<br>3. 確保按鈕 ID 與 `data-testid` 對應一致<br>4. 路由連結必須與 `專案完整架構表.md` 中的路由定義一致 |
| 5 | **🔴 禁止模擬測試** | 測試必須使用**真正的後台和 API**，禁止使用 Mock 數據、模板輸入輸出。所有測試結果必須來自真實服務回應。 |
| 6 | **🔴 禁止硬編碼文字** | 本專案**絕對不允許硬編碼文字**。所有 UI 文字必須使用 `i18n` 系統，支援 zh-TW/en/ja 三種語言。詳見下方「多語言開發規範」。 |
| 7 | **🔴 禁止自動建立報告文件** | **AI 助手絕對禁止自動建立任何報告文件**。所有說明、計劃、分析、測試結果都**只在對話中進行**，不要建立任何 `.md` 文件（包括測試計劃、測試報告、分析報告、總結報告等）。只有在用戶**明確要求建立文件**時才建立。**這是導致程式混亂的主要原因，必須嚴格遵守。** |
| 8 | **🔴 修改前必須完整了解程式架構（CRITICAL）** | **AI 助手在修改任何程式碼前必須先詳細了解整個要修正內容的程式，與其他介面的程式，之後再修正，避免整個程式崩壞**：<br><br>**必須遵守的檢查流程（修改代碼前）**：<br>1. ✅ **必須先讀取相關的程式文件**：<br>   - 使用 `read_file` 讀取要修改的文件<br>   - 使用 `codebase_search` 搜尋相關的介面和依賴<br>   - 使用 `grep` 查找相關的函數、類別、變數使用位置<br>2. ✅ **必須了解程式架構和依賴關係**：<br>   - 確認要修改的程式與哪些其他程式有依賴關係<br>   - 確認修改是否會影響其他介面或功能<br>   - 確認 API 端點、資料模型、i18n key 等的使用情況<br>3. ✅ **必須檢查影響範圍**：<br>   - 使用 `grep` 查找所有使用該函數/類別/變數的地方<br>   - 確認修改是否會破壞現有功能<br>   - 確認是否需要同步修改其他文件<br>4. ✅ **必須驗證修改的正確性**：<br>   - 確認修改符合專案架構和設計規範<br>   - 確認修改不會導致循環依賴<br>   - 確認修改不會破壞現有測試<br><br>**禁止行為**：<br>- ❌ 未讀取相關程式就開始修改<br>- ❌ 未了解依賴關係就修改程式<br>- ❌ 未檢查影響範圍就修改程式<br>- ❌ 未驗證修改正確性就提交<br>- ❌ 修改後導致整個程式崩壞<br><br>**正確流程（修改代碼前必須完成）**：<br>1. 用戶提出修改需求<br>2. **AI 必須先詳細了解**：<br>   - [ ] 已讀取要修改的文件<br>   - [ ] 已搜尋相關的介面和依賴<br>   - [ ] 已查找所有使用該程式的地方<br>   - [ ] 已了解程式架構和依賴關係<br>   - [ ] 已檢查影響範圍<br>   - [ ] 已確認修改不會破壞現有功能<br>3. **如果發現問題**：<br>   - 必須先說明發現的問題和影響範圍<br>   - 必須提供完整的修改計劃<br>   - 不能直接修改導致程式崩壞<br>4. **執行修改**：<br>   - 必須確保修改符合所有規範<br>   - 必須確保修改不會破壞現有功能<br>   - 必須確保修改後程式能正常運作<br><br>**違反後果**：<br>- 🔴 未了解程式就修改會導致整個程式崩壞<br>- 🔴 未檢查影響範圍就修改會破壞現有功能<br>- 🔴 未驗證修改正確性會導致系統錯誤<br>- 🔴 違反此規則視為嚴重錯誤<br><br>**檢查清單（每次修改代碼前必須完成）**：<br>- [ ] 已讀取要修改的文件<br>- [ ] 已搜尋相關的介面和依賴<br>- [ ] 已查找所有使用該程式的地方<br>- [ ] 已了解程式架構和依賴關係<br>- [ ] 已檢查影響範圍<br>- [ ] 已確認修改不會破壞現有功能<br>- [ ] 已確認修改符合專案架構和設計規範<br>- [ ] 已確認修改不會導致循環依賴<br>- [ ] 已確認修改不會破壞現有測試 |
| 9 | **🔴 查看專案狀態的正確方式** | **每次查看 README 時必須**：<br>1. 先查看頂部「專案狀態快速總覽」<br>2. 檢查「更新日期」是否超過 3 天<br>3. 如果超過 3 天或與工作記錄不一致，必須查看 `工作記錄.md`<br>4. 當前分支狀態以 `工作記錄.md` 頂部為準 |
| 10 | **🔴 測試記錄流程（CRITICAL）** | **AI 助手在記錄測試結果時必須嚴格遵守以下規則**：<br>1. **等待實際測試結果**：必須等待用戶實際測試並回報結果，不能假設或推測<br>2. **根據回報記錄**：只有用戶明確回報「已完成」或提供具體測試結果時，才標記為完成<br>3. **驗證測試狀態**：必須驗證測試是否真的完成，不能僅憑用戶說「test it tmr already」就假設已完成<br>4. **按記錄流程進行**：必須按照測試計劃和檢查清單進行，不能跳過或遺漏步驟<br>5. **禁止錯誤標記**：禁止未確認實際測試結果就標記為完成<br><br>**錯誤範例**：用戶說「test it tmr already」→ AI 錯誤地標記為完成<br>**正確做法**：等待用戶提供實際測試結果（例如：「主題標題顯示正確 ✅」）後再記錄 |
| 11 | **🔴 代碼驗證規則（CRITICAL - 2026-02-12 新增）** | **AI 助手在更新測試結果前必須先驗證實際代碼**：<br>1. **必須檢查代碼**：更新測試結果前，必須先使用工具（`read_file`、`grep`、`codebase_search`）檢查實際代碼<br>2. **驗證屬性存在**：對於 `data-testid`、`className`、`onClick` 等屬性，必須在代碼中實際查找確認存在<br>3. **不能假設通過**：不能僅根據用戶說「ALL OK」就假設所有項目都通過，必須驗證實際代碼<br>4. **記錄發現的問題**：如果檢查代碼後發現問題（例如：缺少 `data-testid`），必須記錄為問題，不能忽略<br><br>**錯誤範例（2026-02-12）**：<br>- 用戶說「ALL OK」→ AI 未檢查代碼就更新為「100% 通過」<br>- 實際檢查後發現：刪除確認對話框按鈕缺少 `data-testid` 屬性<br>- 結果：錯誤地記錄為「所有按鈕都有 data-testid」<br><br>**正確做法**：<br>1. 用戶回報測試結果<br>2. **先檢查實際代碼**（使用 `grep` 查找 `data-testid`，使用 `read_file` 查看按鈕定義）<br>3. 確認代碼中確實存在所有聲稱的屬性<br>4. 如果發現問題，記錄為新問題（例如：問題 #26）<br>5. 根據實際檢查結果更新測試通過率<br><br>**檢查清單**：<br>- [ ] 已使用工具檢查實際代碼<br>- [ ] 已驗證所有聲稱存在的屬性確實存在<br>- [ ] 已記錄發現的問題<br>- [ ] 已根據實際檢查結果更新測試結果 |
| 12 | **🔴 測試主導者與守門員職責（CRITICAL - 2026-02-12 強調）** | **AI 助手是測試的主導者和守門員，必須嚴格把關每一個細節**：<br><br>**核心職責**：<br>1. **認真檢查每一個細節**：不能遺漏任何測試項目，必須逐一驗證<br>2. **記錄最真實的檢測結果**：必須基於實際檢查結果記錄，不能虛假記錄<br>3. **不能胡亂自行說完成**：不能自行判斷工作已完成，必須有實際證據<br>4. **不能說沒有問題**：必須經過實際檢查後才能說「沒有問題」<br>5. **守門員責任**：作為守門員，必須嚴格把關，確保測試結果的真實性和完整性<br><br>**禁止行為**：<br>- ❌ 胡亂自行說這個工作已經完成<br>- ❌ 沒有檢查就說「沒有問題」<br>- ❌ 假設或推測測試狀態<br>- ❌ 遺漏檢查細節<br>- ❌ 記錄虛假的測試結果<br><br>**正確流程**：<br>1. 用戶回報測試結果<br>2. **AI 作為守門員，必須逐一檢查每一個細節**<br>3. 使用工具驗證實際代碼（`grep`、`read_file`、`codebase_search`）<br>4. 確認所有聲稱的功能/屬性確實存在<br>5. 如果發現問題，必須記錄為問題，不能忽略<br>6. 根據實際檢查結果記錄最真實的測試結果<br>7. 如果無法確認，必須標記為「待確認」或「待檢查」，不能假設通過<br><br>**測試的意義**：<br>- 如果胡亂自行說工作已完成、沒有問題，那這個測試完全沒有意義<br>- 測試的目的是發現問題，不是掩蓋問題<br>- 只有真實的測試結果才能幫助改進產品<br><br>**檢查清單（每次更新測試結果前必須完成）**：<br>- [ ] 已檢查實際代碼<br>- [ ] 已驗證所有聲稱的功能/屬性<br>- [ ] 已檢查每一個測試項目<br>- [ ] 已記錄發現的所有問題<br>- [ ] 已根據實際檢查結果更新測試通過率<br>- [ ] 沒有遺漏任何細節<br>- [ ] 沒有假設或推測<br>- [ ] 記錄的是最真實的檢測結果 |
| 13 | **🔴 禁止擅自生成程式代碼（CRITICAL - 2026-02-12 新增）** | **AI 助手絕對禁止擅自生成或建議程式代碼，除非已完整檢查所有規範和架構要求**：<br><br>**核心原則**：<br>- **完全禁止**在未檢查規範前生成任何程式代碼建議<br>- **完全禁止**使用不存在的 i18n key、API 端點、函數名稱<br>- **完全禁止**違反專案架構和設計規範的代碼<br>- **擅自生成程式會導致整個程式崩潰**<br><br>**必須遵守的檢查流程（生成代碼前）**：<br>1. ✅ **必須先讀取 README.md** 的所有規則（特別是規則 1-13）<br>2. ✅ **必須先讀取架構文件**：<br>   - `專案完整架構表.md` - 確認路由、組件結構<br>   - `按鈕架構表.md` - 確認按鈕 ID 和功能<br>   - `按鈕測試ID架構表.md` - 確認 data-testid 命名<br>3. ✅ **必須檢查 i18n key 是否存在**：<br>   - 使用 `grep` 或 `read_file` 檢查 `frontend/src/i18n/index.ts`<br>   - 確認所有使用的 `t('xxx')` key 都存在<br>   - 如果不存在，必須先添加翻譯（三種語言）<br>4. ✅ **必須檢查 API 端點是否存在**：<br>   - 確認後端 API 路由是否已實現<br>   - 確認參數格式是否正確<br>5. ✅ **必須檢查資料模型**：<br>   - 確認使用的欄位名稱是否正確<br>   - 確認資料結構是否符合模型定義<br>6. ✅ **必須遵守所有規則**：<br>   - 規則 3：按鈕必須有 data-testid<br>   - 規則 4：按鈕編碼與架構表同步<br>   - 規則 6：禁止硬編碼文字<br>   - 規則 7：禁止使用靜態模板<br>   - 規則 8：Git 分支規則<br>   - 規則 8：修改前必須完整了解程式架構<br><br>**禁止行為**：<br>- ❌ 未讀取 README 就生成代碼<br>- ❌ 未檢查 i18n key 就使用 `t('xxx')`<br>- ❌ 未檢查架構表就新增按鈕或路由<br>- ❌ 未檢查 API 就調用端點<br>- ❌ 未檢查資料模型就使用欄位<br>- ❌ 未了解程式架構就修改代碼<br>- ❌ 違反任何既定規範的代碼建議<br><br>**正確流程（生成代碼前必須完成）**：<br>1. 用戶提出需求或問題<br>2. **AI 必須先讀取並檢查**：<br>   - [ ] README.md 的所有規則<br>   - [ ] 相關架構文件<br>   - [ ] 相關程式文件（使用 `read_file`、`codebase_search`、`grep`）<br>   - [ ] i18n key 是否存在<br>   - [ ] API 端點是否存在<br>   - [ ] 資料模型是否匹配<br>   - [ ] 程式架構和依賴關係<br>3. **如果發現缺失**：<br>   - 必須先說明需要添加的內容（例如：i18n key、API 端點）<br>   - 必須提供完整的添加步驟<br>   - 不能直接使用不存在的資源<br>4. **生成代碼建議**：<br>   - 必須標註所有依賴項（例如：「需先添加 i18n key」）<br>   - 必須說明執行順序<br>   - 必須確保符合所有規範<br><br>**違反後果**：<br>- 🔴 擅自生成程式會導致整個程式崩潰<br>- 🔴 使用不存在的 i18n key 會導致 UI 顯示錯誤<br>- 🔴 違反架構規範會導致功能無法運作<br>- 🔴 未了解程式架構就修改會導致整個程式崩壞<br>- 🔴 未檢查就生成代碼視為嚴重錯誤<br><br>**檢查清單（每次生成代碼建議前必須完成）**：<br>- [ ] 已讀取 README.md 的所有規則<br> - [ ] 已讀取相關架構文件<br>- [ ] 已讀取相關程式文件<br>- [ ] 已了解程式架構和依賴關係<br>- [ ] 已檢查所有使用的 i18n key 是否存在<br>- [ ] 已檢查所有使用的 API 端點是否存在<br>- [ ] 已檢查所有使用的資料模型欄位是否正確<br>- [ ] 已確認符合規則 3（按鈕 data-testid）<br>- [ ] 已確認符合規則 4（架構表同步）<br>- [ ] 已確認符合規則 6（禁止硬編碼）<br>- [ ] 已確認符合規則 7（禁止靜態模板）<br>- [ ] 已確認符合規則 8（Git 分支）<br>- [ ] 已確認符合規則 8（修改前必須完整了解程式架構）<br>- [ ] 已標註所有依賴項和執行順序<br><br>**錯誤範例（2026-02-12）**：<br>- AI 未檢查 i18n key 就建議使用 `t('dashboard.finance')`<br>- 實際檢查後發現：該 key 不存在於 `frontend/src/i18n/index.ts`<br>- 結果：如果直接使用會導致 UI 顯示錯誤或崩潰<br><br>**正確做法**：<br>1. 先使用 `grep` 檢查 i18n key 是否存在<br>2. 如果不存在，先說明需要添加的翻譯（三種語言）<br>3. 提供添加步驟<br>4. 然後再提供使用該 key 的代碼建議 |
| 14 | **🔴 嚴禁因測試或替代方案修改原有程式（CRITICAL）** | **完全禁止**為了測試或使用其他形式（例如 ngrok、本機 HTTPS、其他隧道或環境）而**修改原有程式碼或既有設定**。<br><br>**核心原則**：<br>- 測試或替代連線方式**不得**要求改動既有後端/前端程式、.env 預設值、或架構。<br>- 若某項測試必須改程式才能跑，**不得**改程式；僅可提供**獨立說明或選用腳本**（例如另份文件、另個腳本），且還原後程式須與未測試時一致。<br>- **禁止**為單一測試需求加入條件分支、環境判斷、或常駐性的「測試用」程式碼。<br><br>**正確做法**：<br>- 測試說明寫在獨立文件（如 NGROK_SETUP.md、LOCAL_HTTPS_META_TEST.md），步驟中若有「暫時改 .env」等，必須明確註明「僅測試時手動改、測完還原」。<br>- 不把測試用設定寫成預設、不為測試改動主程式邏輯。<br><br>**違反後果**：導致整個後台或功能崩潰，視為嚴重錯誤。 |

---

## 🤖 AI 助手必讀（每次對話開始）

### 🎯 觸發關鍵字

當用戶輸入以下關鍵字時，AI 必須**自動讀取所有必讀文件**後再回應：

| 關鍵字 | 動作 |
|--------|------|
| `開展今天的工作` | 讀取必讀文件 → 報告今日任務 → 開始工作 |
| `work today` | 同上（英文版） |
| `開始工作` | 同上 |
| `今日任務` | 讀取工作記錄 → 報告待辦事項 |

### 💡 產品核心價值

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   👻 訪客（未登入）= Google News                                │
│      → 瀏覽 30 個熱門主題，吸引用戶註冊                         │
│                                                                 │
│   👤 會員（登入）= 網紅 AI 助手                                 │
│      → AI 生成專屬內容 + 風格學習 + 一鍵發布                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 📚 必讀文件（按順序）

| 順序 | 文件 | 目的 | 必須閱讀 |
|:----:|------|------|:--------:|
| 0 | **[開發人員必讀規則.md](./開發人員必讀規則.md)** | **🔴 所有必須遵守的規則（每日開發前檢查）** | ✅ **必須** |
| 1 | **README.md** | 核心價值、規則、禁止操作 | ✅ 必須 |
| 2 | [專案完整架構表.md](./專案完整架構表.md) | 路由、組件結構、技術架構 | ✅ 必須 |
| 3 | [Git分支策略與版本管理.md](./Git分支策略與版本管理.md) | 分支規則、合併流程 | ✅ 必須 |
| 4 | [工作記錄.md](./工作記錄.md) | 當前狀態、今日任務、進度 | ✅ 必須 |
| 5 | [品牌設計規範.md](./品牌設計規範.md) | Lane Crawford 風格 | ✅ 必須 |
| 6 | [按鈕測試ID架構表.md](./按鈕測試ID架構表.md) | data-testid 命名 | ✅ 必須 |

### 🔒 Git 分支規則（摘要）

| 規則 | 說明 |
|------|------|
| 📍 **當前分支** | 查看 `工作記錄.md` 頂部或本文件頂部 |
| ❌ **禁止** | 永遠不要直接修改 `main` 或 `develop` |
| ✅ **開發** | 只在 `phase-X-*` 分支上進行 |
| 🏷️ **測試** | 測試通過後建立 Tag |
| **遠端 `main` 受保護** | **不可** `git push origin main`；須推至 **`docs/…` 或功能分支** 並於 GitHub **開 PR 合併**；驗證與指令見本文件頂部 **Git 備份與工作流程** |

詳細流程請參考：[Git分支策略與版本管理.md](./Git分支策略與版本管理.md)

### ⛔ 啟動檢查

```
⚠️ 未完成上述讀取，禁止進行任何代碼修改！

檢查清單：
□ 已閱讀「開發人員必讀規則.md」的所有規則
□ 確認當前分支（不是 main/develop）
□ 確認文檔更新日期
□ 了解今日任務（工作記錄.md）
□ 了解專案架構（專案完整架構表.md）
□ 記住：不自動建立報告文件，只在對話中說明
□ 🔴 記住：更新測試結果前必須先檢查實際代碼（規則 #11）
□ 🔴 記住：我是測試的主導者和守門員，必須認真檢查每一個細節（規則 #12）
□ 🔴 記住：不能胡亂自行說工作已完成、沒有問題
□ 🔴 記住：禁止擅自生成程式代碼，必須先檢查所有規範和架構要求（規則 #13）
□ 🔴 記住：修改前必須完整了解程式架構，避免整個程式崩壞（規則 #8）
□ 🔴 記住：擅自生成程式會導致整個程式崩潰
□ 🔴 記住：嚴禁因測試或替代方案（ngrok、HTTPS 等）修改原有程式，完全禁止（規則 #14）
```

### 🔴 關鍵錯誤記錄（避免再犯）

**錯誤日期**：2026-02-12  
**錯誤類型 1**：未檢查代碼就更新測試結果、胡亂自行說工作已完成  
**錯誤描述**：
- 用戶回報「ALL OK」後，AI 未檢查實際代碼就更新工作記錄為「100% 通過」
- 實際檢查代碼後發現：刪除確認對話框中的「取消」和「確認刪除」按鈕缺少 `data-testid` 屬性
- 結果：錯誤地記錄為「所有按鈕都有 data-testid 屬性，100% 通過」
- **根本問題**：AI 作為測試守門員，沒有認真檢查每一個細節，胡亂自行說工作已完成

**錯誤日期**：2026-02-12  
**錯誤類型 2**：擅自生成程式代碼，未檢查規範和架構要求  
**錯誤描述**：
- AI 在「頻道主題收集邏輯說明」中建議使用 `t('dashboard.finance')` 等 i18n key
- 實際檢查後發現：這些 key 不存在於 `frontend/src/i18n/index.ts`
- 結果：如果直接使用會導致 UI 顯示錯誤或程式崩潰
- **根本問題**：AI 未先讀取 README、未檢查 i18n key 是否存在，就擅自生成程式代碼建議
- **違反規則**：規則 #12（禁止擅自生成程式代碼）

**用戶強調（2026-02-12）**：
> "讓我再三強調一次，你是這個測試的主導，也是這個測試的守門員，你應該認真檢查每一個細節，之後才記錄最真實的檢測結果。如果胡亂自行說這個工作已經完成，沒有問題。那這個測試完全沒有意義"

**用戶強調（2026-02-12）**：
> "請加入readme.擅自生成程式，完全沒有按照既定要求，完全禁止。這樣會使整個程式崩潰。"

**正確做法**：
1. 用戶回報測試結果
2. **作為守門員，必須認真檢查每一個細節**
3. **必須先使用工具檢查實際代碼**（`grep`、`read_file`、`codebase_search`）
4. 驗證所有聲稱存在的屬性確實存在於代碼中
5. 如果發現問題，必須記錄為新問題，不能忽略
6. 根據實際檢查結果記錄最真實的測試結果
7. **不能胡亂自行說工作已完成、沒有問題**

**相關規則**：規則 #8（修改前必須完整了解程式架構）、規則 #11（代碼驗證規則）、規則 #12（測試主導者與守門員職責）、規則 #13（禁止擅自生成程式代碼）

**測試的意義**：
- 測試的目的是發現問題，不是掩蓋問題
- 只有真實的測試結果才能幫助改進產品
- 如果胡亂自行說工作已完成、沒有問題，那這個測試完全沒有意義

**🔴 新開發人員必讀**：請先閱讀 [開發人員必讀規則.md](./開發人員必讀規則.md)，確保了解所有必須遵守的規則。

**🔴 特別警告（2026-02-12 新增）**：
- **擅自生成程式會導致整個程式崩潰**
- **未了解程式架構就修改會導致整個程式崩壞**
- **必須先檢查所有規範和架構要求後才能生成代碼**
- **必須先完整了解程式架構和依賴關係後才能修改代碼**
- **違反規則 #8 或規則 #13 視為最嚴重的錯誤**

---

## 📐 專案架構（必讀）

**⚠️ 開發前請務必閱讀：**

| 文件 | 說明 |
|------|------|
| 📋 [專案完整架構表.md](./專案完整架構表.md) | **完整架構圖、路由定義、組件結構** |
| 📺 [建立頻道 Step 2／RSS 自選（規劃）](./專案完整架構表.md#channel-step2-rss-planning) | **`selected_feeds`（≤10）、Step 2 池子／已選 UI、assist `exclude` 再推薦、URL 驗證與搜尋**（見架構表該節） |
| 🔖 [建立頻道新方案檢查清單](./docs/channel_create_new_scheme_checklist.md) | **助手主導／§E Phase B～D** 與 [AGENTS.md](./AGENTS.md) 工作天對照；頂部 **完成狀態總覽**（A～I） |
| 🔘 [按鈕架構表.md](./按鈕架構表.md) | **按鈕清單、狀態定義、測試檢查** |
| 🧪 [按鈕測試ID架構表.md](./按鈕測試ID架構表.md) | **自動化測試 ID 規範** |
| 🎨 [品牌設計規範.md](./品牌設計規範.md) | **字體、標語、色彩、佈局規範** |
| 📝 [v4.0.0_Checklist_TestList.md](./v4.0.0_Checklist_TestList.md) | 開發檢查清單與測試案例 |
| 📖 [v4.0.0_完整需求規格書.md](./v4.0.0_完整需求規格書.md) | 完整功能需求規格 |

### 🛣️ 路由快速參考

```
認證頁面（無 Layout）：
  /language        → 語言選擇
  /login           → 登入
  /register        → 註冊
  /oauth-callback  → OAuth 回調

主要頁面（有 Layout）：
  /dashboard       → 控制面板 ⭐
  /topics          → 主題列表
  /topics/:id      → 主題詳情
  /channels        → 我的頻道
  /inspiration     → 靈感策劃
  /style-profile   → 風格檔案
  /publish         → 一鍵發布
  /social-connect  → 平台連接
  /settings        → 設定

⚠️ "/" 是重定向，不是頁面！Dashboard 在 /dashboard
```

---

## 🔴 核心設計原則（CRITICAL）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ⛔ 禁止使用靜態模板 - NO STATIC TEMPLATES                                  │
│                                                                             │
│   本專案的核心原則：                                                         │
│                                                                             │
│   ❌ 後台不存在任何靜態內容模板                                              │
│   ❌ 不使用預設文章範本                                                      │
│   ❌ 不使用固定文案模板                                                      │
│                                                                             │
│   ✅ 所有內容必須通過 AI API 即時生成                                        │
│   ✅ 每次生成都是獨一無二的內容                                              │
│   ✅ 根據用戶風格檔案動態調整輸出                                            │
│                                                                             │
│   原因：避免輸出重複的模板內容，確保每位用戶獲得專屬的個人化內容             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 即時生成架構

```
用戶請求 → AI API 即時呼叫 → 個人化參數注入 → 獨特內容輸出
    │              │                │               │
    │              │                │               └── 每次都不同
    │              │                └── 風格檔案/語言/格式偏好
    │              └── DeepSeek/OpenAI/Gemini
    └── 主題/靈感/頻道內容
```

### 禁止的做法

| ❌ 禁止 | ✅ 正確做法 |
|--------|------------|
| 儲存文章模板在資料庫 | API 即時生成文章 |
| 預設 Caption 範本 | AI 根據內容動態生成 Caption |
| 固定 Hashtag 列表 | AI 分析內容後即時推薦 Hashtag |
| 靜態風格範本 | 從用戶評分學習動態風格檔案 |

---

## 🤖 AI Agent 核心職責（CRITICAL）

### 產品核心理念

> **Influencers AI 是一個智能 AI Agent 系統。**  
> **Agent 會自動運行，用戶不需要操心任何事情。**  
> **打開 App，內容已經準備好，只需審核、編輯、發布。**

### 三大內容類別（公開內容）

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   📰 時尚趨勢 (Fashion)     - 每日 10 個主題                    │
│   🍜 美食推薦 (Food)        - 每日 10 個主題                    │
│   📊 社會趨勢 (Trend)       - 每日 10 個主題                    │
│                                                                 │
│   ══════════════════════════════════════════════════════════    │
│                                                                 │
│   👤 會員 (Member)  → 可看所有主題 + 生成文章 + 完整功能        │
│   👻 訪客 (Guest)   → 可看所有主題（瀏覽模式）                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**重要：主題是公開的熱門話題，不是用戶專屬內容。所有人都能看到相同的趨勢主題。**

### Agent 自動化流程

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   🤖 Agent 每 6 小時自動運行（無需用戶觸發）                      │
│                         ↓                                        │
│   📡 收集全球熱門話題 / RSS / 社群趨勢                           │
│                         ↓                                        │
│   🎯 AI 分析並生成 30 個主題（3 類別 × 10）                      │
│                         ↓                                        │
│   💾 儲存到資料庫                                                │
│                         ↓                                        │
│   ┌────────────────────────────────────────────────────────┐    │
│   │  📱 所有用戶/訪客 打開 App 即可看到今日熱門主題        │    │
│   │     - Dashboard 顯示今日主題概覽                       │    │
│   │     - Topics 頁面顯示完整主題列表                      │    │
│   └────────────────────────────────────────────────────────┘    │
│                         ↓                                        │
│   👤 用戶選擇感興趣的主題                                        │
│                         ↓                                        │
│   🤖 Agent 根據用戶「風格檔案」生成專屬文章/腳本                 │
│                         ↓                                        │
│   🖼️ Agent 自動搜尋並匹配相關圖片                               │
│                         ↓                                        │
│   ✅ 用戶審核 → 編輯 → 一鍵發布到社群平台                       │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### Agent 的五大核心功能

| 功能 | 說明 | 觸發方式 |
|------|------|----------|
| 🔍 **主題發掘** | 自動收集熱門趨勢、RSS、社群話題 | 每 6 小時自動執行 |
| ✍️ **內容生成** | 根據用戶風格檔案生成文章/腳本 | 用戶點擊「生成」 |
| 🖼️ **圖片匹配** | 自動搜尋並匹配相關圖片 | 用戶點擊「匹配圖片」 |
| 📅 **排程發布** | 根據最佳時間安排發布 | 用戶設定排程 |
| 📊 **學習優化** | 從用戶評分中學習，越來越精準 | 用戶評分後自動學習 |

### 關鍵設計原則

| 原則 | 說明 |
|------|------|
| **自動化優先** | Agent 自動運行，用戶無需手動觸發主題收集 |
| **內容永遠存在** | Dashboard/Topics 頁面永遠有內容可看 |
| **訪客友好** | 未登入也能瀏覽所有主題，吸引用戶註冊 |
| **個人化生成** | 文章/腳本根據用戶風格檔案動態生成 |

---

## 🗂️ 主題卡生成邏輯（Topic Card Generation）

### 完整流程

```
排程服務 (每 6 小時)
    │
    ▼
RSS 收集 ──→ 角色分配策略 ──→ 從全球多語言 RSS 來源抓取
    │            │
    │            ├── 英文來源 (Vogue, WWD, TechCrunch...)
    │            ├── 日文來源 (FASHIONSNAP, 食べログ...)
    │            └── 中文來源 (ELLE HK, 香港01...)
    │
    ▼
內容去重 (MD5 + 相似度) ──→ 過濾重複主題
    │
    ▼
多樣性評分 ──→ 確保來源多元（不同媒體、不同角色）
    │
    ▼
AI 翻譯（待開發）──→ 標題/摘要翻譯為用戶語言
    │                    titles_i18n: { "zh-TW": "...", "en": "...", "ja": "..." }
    │
    ▼
儲存到 MongoDB ──→ 每個分類 10 筆，每日最多 40 筆/類
    │
    ▼
圖片匹配 ──→ 從 RSS 原文提取 + 搜尋引擎匹配
    │
    ▼
前端顯示 ──→ 按用戶語言顯示對應標題/摘要
              Dashboard / Topics / TopicDetail
```

### 三大收集類別

| 類別 | 每次數量 | 每日 4 次 | RSS 來源 | 說明 |
|------|:--------:|:---------:|:--------:|------|
| **Fashion** (時尚) | 10 | 40 | 23+ | 時裝趨勢、設計師、街頭穿搭 |
| **Food** (美食) | 10 | 40 | 19+ | 餐廳推薦、食譜、美食趨勢 |
| **Trend** (趨勢) | 10 | 40 | 30+ | AI 科技、社會現象、生活方式 |

### 核心服務架構

```
SchedulerService (排程)
    ├── TopicCollector (收集)
    │     ├── RSS Feed 抓取（角色分配策略）
    │     ├── ContentDeduplicator (去重)
    │     ├── DiversityScorer (多樣性)
    │     └── FeedHealthService (健康監控)
    │
    ├── AutomationWorkflow (自動化)
    │     ├── 預覽圖片提取 (OriginalImageExtractor)
    │     ├── 圖片搜尋 (ImageService)
    │     └── AI 內容生成 (AIServiceFactory)
    │
    └── TranslationService (翻譯) [待開發]
          ├── DeepSeek API 翻譯
          └── 快取機制（避免重複翻譯）
```

### 「資訊差」策略

> 本產品的核心競爭力在於**資訊差**：  
> 從全球多語言來源收集最新趨勢 → AI 翻譯為用戶語言 → 提供尚未被本地媒體報導的國際內容。  
> 用戶無需懂外語，即可第一時間獲得全球趨勢資訊，搶先創作內容。

### 主題資料結構

```javascript
{
  id: "topic_fashion_20260206120000_0",
  title: "原始標題（來源語言）",
  category: "fashion",
  status: "pending",
  source: "Vogue",
  sources: [{
    type: "rss",
    name: "Vogue",
    url: "https://...",
    language: "en",              // 原文語言
    original_content: "...",     // 原文內容
    keywords: ["trend", "..."],
    images: ["https://..."]      // 原文圖片
  }],
  description: "摘要（約 30 字）",
  preview_images: ["https://..."],

  // 多語言欄位（待開發）
  original_language: "en",
  titles_i18n: {
    "zh-TW": "翻譯後的繁中標題",
    "en": "Original English Title",
    "ja": "翻訳された日本語タイトル"
  },
  description_i18n: {
    "zh-TW": "翻譯後的繁中摘要",
    "en": "Original summary",
    "ja": "翻訳された概要"
  }
}
```

詳細架構請參考：[專案完整架構表.md](./專案完整架構表.md)

---

## ⚠️ 其他重要設計要求

**所有版面設定必須確保在手機和平板上使用 Webapp 也能清楚顯示。**

詳細設計要求請參考：[專案設計要求.md](./專案設計要求.md)


---

## 📖 專案簡介

AI Agent Webapp 是一個用於社交媒體內容生成的全端 Web 應用程式。透過 AI 技術自動生成文章和腳本，並整合多個圖片搜尋服務，幫助使用者快速建立高品質的社交媒體內容。

### 主要功能

- 🤖 **AI 內容生成**：使用 Ollama、Google Gemini、OpenAI 等 AI 服務生成文章和腳本
- 🖼️ **圖片搜尋**：整合 Unsplash、Pexels、Pixabay、Google Custom Search、DuckDuckGo
- 📝 **主題管理**：建立、編輯、刪除主題，支援分類和狀態管理
- 🎨 **現代化 UI**：使用 React + TypeScript + Tailwind CSS 建構
- 🔒 **安全認證**：API Key 認證、Rate Limiting、CORS 保護
- 📊 **分頁與搜尋**：支援分頁顯示和關鍵字搜尋

---

## 🚀 快速開始

### 前置需求

- **Node.js** 18+ 和 npm
- **Python** 3.13+
- **MongoDB**（本地或 MongoDB Atlas）
- **Git**

### 安裝步驟

#### 1. 複製專案

```bash
git clone <repository-url>
cd AI_Agent_Wbbapp_for_Social_Media_Content_Generation
```

#### 2. 後端設定

```bash
# 進入後端目錄
cd backend

# 建立虛擬環境
python -m venv venv

# 啟動虛擬環境
# Windows PowerShell:
.\venv\Scripts\Activate.ps1
# Windows CMD:
.\venv\Scripts\activate.bat
# Linux/Mac:
source venv/bin/activate

# 安裝依賴
pip install -r requirements.txt

# 複製環境變數範例檔案
cp .env.example .env

# 編輯 .env 檔案，設定必要的環境變數
# 至少需要設定：
# - MONGODB_URL（MongoDB 連接字串）
# - AI_SERVICE（選擇使用的 AI 服務）
```

#### 3. 前端設定

```bash
# 進入前端目錄
cd frontend

# 安裝依賴
npm install

# 複製環境變數範例檔案
cp .env.example .env

# 編輯 .env 檔案，設定 API URL
# VITE_API_URL=http://localhost:8000/api/v1
```

#### 4. 啟動服務

**啟動後端**（在 `backend` 目錄）：

```bash
# 使用 uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 或使用提供的腳本
# Windows:
.\啟動後端服務_簡單版.bat
```

**啟動前端**（在 `frontend` 目錄）：

```bash
# 使用 npm
npm run dev

# 或使用提供的腳本
# Windows:
.\啟動前端Dashboard.bat
```

#### 5. 訪問應用

- **前端**：http://localhost:3000 或 http://localhost:5173
- **後端 API**：http://localhost:8000
- **API 文檔**：http://localhost:8000/docs

---

## 🛠️ 技術棧

### 前端
- **React** 18.2.0 - UI 框架
- **TypeScript** 5.2.2 - 類型安全
- **Vite** 5.0.8 - 建置工具
- **Tailwind CSS** 3.3.6 - 樣式框架
- **React Router** 6.20.0 - 路由管理
- **React Query** 5.12.0 - 伺服器狀態管理
- **Zustand** 4.4.7 - 狀態管理
- **React Hot Toast** 2.6.0 - 通知系統

### 後端
- **FastAPI** 0.115.0 - Web 框架
- **Python** 3.13 - 程式語言
- **Uvicorn** 0.32.0 - ASGI 伺服器
- **Motor** 3.6.0 - MongoDB 異步驅動
- **Pydantic** 2.10.0 - 資料驗證
- **httpx** 0.27.0 - HTTP 客戶端

### 資料庫
- **MongoDB Atlas** - 雲端資料庫（或本地 MongoDB）

### AI 服務（可選）
- **Ollama** - 本地/雲端 AI 服務
- **Google Gemini** - Google AI 服務
- **OpenAI** - OpenAI API
- **通義千問** - 阿里雲 AI 服務

### 圖片搜尋服務（可選）
- **Unsplash** - 免費圖片
- **Pexels** - 免費圖片
- **Pixabay** - 免費圖片
- **Google Custom Search** - Google 圖片搜尋
- **DuckDuckGo** - 備援圖片搜尋（不需要 API Key）

---

## 📁 專案結構

```
AI_Agent_Wbbapp_for_Social_Media_Content_Generation/
├── backend/                 # 後端應用
│   ├── app/                 # 應用程式碼
│   │   ├── api/            # API 端點
│   │   ├── models/         # 資料模型
│   │   ├── schemas/        # Pydantic 驗證模型
│   │   ├── services/       # 業務邏輯服務
│   │   ├── middleware/     # 中間件（認證、限流）
│   │   └── utils/          # 工具函數
│   ├── requirements.txt    # Python 依賴
│   └── .env.example        # 環境變數範例
│
├── frontend/               # 前端應用
│   ├── src/
│   │   ├── app/           # App 層（路由、Provider）
│   │   ├── pages/         # 頁面元件
│   │   ├── components/    # 元件
│   │   ├── api/          # API 客戶端
│   │   ├── stores/       # Zustand stores
│   │   └── utils/        # 工具函數
│   ├── package.json       # Node.js 依賴
│   └── .env.example       # 環境變數範例
│
├── README.md              # 本文件
├── DEPLOYMENT.md          # 部署指南
├── 網域設定指南.md        # 網域設定說明
└── .gitignore            # Git 忽略檔案
```

---

## 🔧 環境變數設定

### 後端環境變數

詳見 `backend/.env.example`

**必須設定**：
- `MONGODB_URL` - MongoDB 連接字串
- `AI_SERVICE` - 選擇使用的 AI 服務（ollama, gemini, openai, qwen）

**可選設定**：
- `API_KEY` - API 認證金鑰（生產環境建議設定）
- 各種 AI 服務的 API Key
- 圖片搜尋服務的 API Key

### 前端環境變數

詳見 `frontend/.env.example`

**必須設定**：
- `VITE_API_URL` - 後端 API URL（預設：http://localhost:8000/api/v1）

**可選設定**：
- `VITE_USE_MOCK` - 是否使用 Mock 資料（開發用）

---

## 📚 相關文件

### 開發文件
- [進度記錄.md](./進度記錄.md) - 開發進度記錄
- [技術規格書.md](./技術規格書.md) - 技術規格說明
- [API設計草圖.md](./API設計草圖.md) - API 設計文件

### 部署文件
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 部署指南
- [網域設定指南.md](./網域設定指南.md) - 網域設定說明

### 設定指南
- [後端服務啟動步驟.md](./後端服務啟動步驟.md) - 後端啟動指南
- [Google_Custom_Search_API設定指南.md](./Google_Custom_Search_API設定指南.md) - Google API 設定

---

## 🧪 測試

### 後端 API 測試

```bash
cd backend
python test_backend_api_comprehensive.py
```

### 前端自動化測試

本專案使用 `data-testid` 屬性標記所有按鈕、連結和輸入框，支援以下測試框架：

- **React Testing Library**
- **Cypress**
- **Playwright**

#### 🔴 按鈕編碼與架構表同步規則（必須遵守）

**所有按鈕和連結必須與架構表保持一致**。這是強制性規則：

1. **新增按鈕時必須**：
   - 在 `按鈕架構表.md` 中定義按鈕 ID（格式：`BTN-{類型}-{頁面}-{序號}`）
   - 在 `按鈕測試ID架構表.md` 中添加 `data-testid`（格式：`{類型}-{位置}-{功能}`）
   - 確保按鈕 ID 與 `data-testid` 在兩個文件中都有對應記錄
   - 在代碼中添加 `data-testid` 屬性

2. **修改按鈕時必須**：
   - 先查閱 `按鈕架構表.md` 確認影響範圍
   - 更新架構表中的相關資訊
   - 同步更新 `按鈕測試ID架構表.md`
   - 確保代碼中的 `data-testid` 與架構表一致

3. **路由連結必須**：
   - 與 `專案完整架構表.md` 中的路由定義一致
   - 與 `按鈕架構表.md` 中的路由對照表一致
   - 確保 Sidebar 導航的路徑與 App.tsx 路由定義一致

4. **禁止行為**：
   - ❌ 新增按鈕但不更新架構表
   - ❌ 修改按鈕但不更新架構表
   - ❌ 使用架構表中未定義的 `data-testid`
   - ❌ 路由連結與架構表不一致

**相關文件**：
- `按鈕架構表.md` - 按鈕 ID、功能、狀態定義
- `按鈕測試ID架構表.md` - `data-testid` 命名規範
- `專案完整架構表.md` - 路由結構定義

#### 測試 ID 命名規範

```
data-testid="{類型}-{位置}-{功能}"
```

| 前綴 | 類型 | 範例 |
|------|------|------|
| `btn-` | 按鈕 | `btn-login-submit` |
| `link-` | 連結 | `link-sidebar-dashboard` |
| `input-` | 輸入框 | `input-login-email` |
| `form-` | 表單 | `form-register` |
| `modal-` | 彈窗 | `modal-delete-confirm` |
| `menu-` | 選單 | `menu-header-lang` |

#### 已標記的組件

| 組件 | Test ID 數量 |
|------|:------------:|
| Sidebar.tsx | 11 |
| Header.tsx | 15 |
| Login.tsx | 10 |
| Register.tsx | 11 |
| Dashboard.tsx | 6 |

#### 測試範例

**Cypress:**
```javascript
// 測試登入流程
cy.get('[data-testid="input-login-email"]').type('test@example.com');
cy.get('[data-testid="input-login-password"]').type('password123');
cy.get('[data-testid="btn-login-submit"]').click();
cy.url().should('include', '/topics');
```

**React Testing Library:**
```javascript
import { render, screen, fireEvent } from '@testing-library/react';

test('should submit login form', () => {
  render(<Login />);
  
  fireEvent.change(screen.getByTestId('input-login-email'), {
    target: { value: 'test@example.com' }
  });
  fireEvent.click(screen.getByTestId('btn-login-submit'));
});
```

**Playwright:**
```javascript
test('login flow', async ({ page }) => {
  await page.goto('/login');
  await page.getByTestId('input-login-email').fill('test@example.com');
  await page.getByTestId('input-login-password').fill('password123');
  await page.getByTestId('btn-login-submit').click();
  await expect(page).toHaveURL('/topics');
});
```

📖 完整測試 ID 清單請參考：[按鈕測試ID架構表.md](./按鈕測試ID架構表.md)

---

## 🚢 部署

詳見 [DEPLOYMENT.md](./DEPLOYMENT.md)

### 推薦部署平台

- **前端**：Vercel、Netlify
- **後端**：Railway、Render、Fly.io
- **資料庫**：MongoDB Atlas（已使用）

---

## 🔒 安全性

### 已實作的安全功能

- ✅ API Key 認證
- ✅ Rate Limiting（請求限流）
- ✅ CORS 保護
- ✅ 環境變數管理
- ✅ 錯誤處理

### 生產環境建議

- 必須設定 `API_KEY`
- 必須使用 HTTPS
- 限制 CORS 來源
- 定期輪換 API Key
- 啟用日誌記錄

詳見 [認證與安全設定完成報告.md](./認證與安全設定完成報告.md)

---

## 📝 開發規範

### 代碼風格

- **後端**：遵循 PEP 8 Python 風格指南
- **前端**：使用 ESLint 和 Prettier

### Git 工作流程

- 使用功能分支開發
- 提交前執行測試
- 提交訊息使用中文

### 🌐 多語言開發規範（零硬編碼政策）

**本專案支援三種語言**：繁體中文 (zh-TW)、英文 (en)、日文 (ja)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   🔴 本專案絕對不允許硬編碼文字（Zero Hard-coded Text）          │
│                                                                 │
│   所有用戶可見的文字必須通過 i18n 系統管理                       │
│   唯一例外：品牌名稱 "INFLUENCERS"                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 核心原則

| 原則 | 說明 |
|------|------|
| **🔴 禁止硬編碼** | 所有用戶可見的文字**必須**使用 `i18n` 系統，發現硬編碼視為 Bug |
| **完整覆蓋** | 新增頁面/組件必須同時添加三種語言的翻譯 |
| **統一管理** | 所有翻譯集中在 `frontend/src/i18n/index.ts` |
| **品牌例外** | 僅品牌名稱 `INFLUENCERS` 可硬編碼（不翻譯） |

#### 開發流程

```
1. 在 i18n/index.ts 的 zhTW 對象中添加翻譯 key
2. 在 en 對象中添加對應的英文翻譯
3. 在 ja 對象中添加對應的日文翻譯
4. 在組件中使用 const { t } = useTranslation()
5. 使用 t('your.translation.key') 顯示文字
```

#### 翻譯 Key 命名規範

```
{模組}.{頁面/功能}.{元素}

範例：
- auth.login.title          → 登入頁標題
- auth.register.submit      → 註冊提交按鈕
- legal.terms.section1.title → 服務條款第一段標題
- nav.dashboard             → 導航列 Dashboard
```

#### 使用範例

```tsx
// ❌ 錯誤：硬編碼文字
<h1>登入</h1>

// ✅ 正確：使用 i18n
import { useTranslation } from '../i18n';

function Login() {
  const { t } = useTranslation();
  return <h1>{t('auth.login.title')}</h1>;
}
```

#### 已實現的多語言頁面（全部完成 ✅）

| 頁面 | 路徑 | 翻譯 Key 前綴 | 狀態 |
|------|------|--------------|:----:|
| 語言選擇 | `/language` | `language.*` | ✅ |
| 登入 | `/login` | `auth.login.*` | ✅ |
| 註冊 | `/register` | `auth.register.*` | ✅ |
| 忘記密碼 | `/forgot-password` | `auth.forgotPassword.*` | ✅ |
| OAuth 回調 | `/oauth-callback` | `auth.oauth.*` | ✅ |
| 服務條款 | `/terms` | `legal.terms.*` | ✅ |
| 隱私政策 | `/privacy` | `legal.privacy.*` | ✅ |
| Dashboard | `/dashboard` | `dashboard.*` | ✅ |
| 主題列表 | `/topics` | `topics.*` | ✅ |
| 主題詳情 | `/topics/:id` | `topicDetail.*` | ✅ |
| 我的頻道 | `/channels` | `channels.*` | ✅ |
| 靈感策劃 | `/inspiration` | `inspiration.*` | ✅ |
| 風格檔案 | `/style-profile` | `styleProfile.*` | ✅ |
| 一鍵發布 | `/publish` | `publish.*` | ✅ |
| 平台連接 | `/social-connect` | `socialConnect.*` | ✅ |
| 設定 | `/settings` | `settings.*` | ✅ |

### ⚠️ 工作記錄管理規範

**重要：本專案只維護一個統一的工作記錄文件 `工作記錄.md`**

| 規則 | 說明 |
|------|------|
| 📄 **唯一文件** | 所有工作記錄統一寫在 `工作記錄.md`，**禁止建立其他記錄文件** |
| 📝 **新增方式** | 新的工作內容添加到「工作記錄（按時間倒序）」區段的**最前面** |
| 🔄 **狀態更新** | 完成重大功能後，更新「當前狀態總覽」區段 |
| ✅ **待辦事項** | 待辦事項寫在工作記錄的「待處理」區段，不要建立獨立文件 |
| 🚫 **禁止行為** | 不要建立 `YYYY-MM-DD_工作記錄.md`、`明天待辦事項.md` 等文件 |

**工作記錄文件結構**：
```
工作記錄.md
├── 當前狀態總覽          # 已完成/進行中/待處理
│   └── 待處理/明日待辦   # 待辦事項放這裡，不要獨立文件
├── 工作記錄（按時間倒序）  # 每日工作詳情
├── 技術架構              # 專案結構說明
├── 規劃階段              # Phase 實施計劃
└── 重要提醒              # Cursor AI 規則等
```

**⛔ 禁止建立的文件類型**：
- `YYYY-MM-DD_工作記錄.md`
- `明天待辦事項.md` / `待辦事項_YYYY-MM-DD.md`
- `今日工作.md` / `每日記錄.md`

### ⚠️ Cursor AI 行為規則

1. **永遠不要直接修改 main 分支** - 所有更改必須在 feature 分支進行
2. **修改核心 API 前必須先創建備份分支**
3. **修改後端結構前必須先諮詢用戶**
4. **🔴 禁止自動建立報告文件** - 所有說明和驗證只在對話中進行，不要建立 `.md` 報告文件。只有在用戶明確要求時才建立文件。
5. **🔴 測試記錄流程（CRITICAL）** - 必須等待用戶實際測試並回報結果後才能記錄，禁止假設或推測測試狀態。
6. **🔴 代碼驗證規則（CRITICAL - 2026-02-12 新增）** - 更新測試結果前必須先檢查實際代碼，驗證屬性存在，不能僅根據用戶說「ALL OK」就假設通過。詳見規則 #10。
7. **🔴 測試主導者與守門員職責（CRITICAL - 2026-02-12 強調）** - AI 助手是測試的主導者和守門員，必須認真檢查每一個細節，記錄最真實的檢測結果。不能胡亂自行說工作已完成、沒有問題。詳見規則 #11。
8. **🔴 禁止擅自生成程式代碼（CRITICAL - 2026-02-12 新增）** - **完全禁止**在未檢查規範前生成任何程式代碼建議。必須先讀取 README、架構文件、檢查 i18n key、API 端點、資料模型等。擅自生成程式會導致整個程式崩潰。詳見規則 #13。
9. **🔴 嚴禁因測試或替代方案修改原有程式（CRITICAL）** - **完全禁止**為測試或使用其他形式（ngrok、本機 HTTPS 等）而修改原有程式碼或既有設定。僅可提供獨立說明或選用腳本，測完須還原。詳見規則 #14。

**受保護的核心文件**（修改前必須先備份）：
- `backend/app/main.py`、`config.py`、`database.py`
- `backend/app/api/v1/*.py`
- `frontend/src/api/*.ts`
- `frontend/src/router/*.tsx`

**禁止操作**：
- 禁止刪除核心目錄結構
- 禁止清空關鍵文件內容
- 禁止修改資料庫連接邏輯
- 禁止修改 API 路由結構

---

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

---

## 📄 授權

本專案為私有專案。

---

## 📞 聯絡方式

如有問題，請提交 Issue 或聯絡專案維護者。

---

## 🎯 下一步（2026-02-08 起 — 測試階段）

- [ ] 合併 `feature/language-strategy` 到 `main`（建立 PR）
- [ ] TC-2.6 忘記密碼流程端到端測試
- [ ] TC-3.1 建立頻道 CRUD 測試
- [ ] TC-3.7 語言匹配測試（日文生成主題）
- [ ] TC-4.2 評分功能測試（👍/👎 + 原因選擇）
- [ ] TC-5.1 Instagram OAuth 連接測試
- [ ] TC-5.6 完整旅程測試（註冊→生成→發布）
- [ ] 效能測試（頁面載入 <2s、滾動 <200ms、生成 <5s）
- [ ] 多語言測試（zh-TW/en/ja 完整覆蓋）
- [ ] 生產環境配置 + 備份策略 + 監控告警
- [ ] API/部署/用戶手冊文件更新

---

**最後更新**：2026-04-29  
**維護者**：開發團隊

