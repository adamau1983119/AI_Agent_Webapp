# 搜尋邏輯範本 - 第三方開發指南

## 📋 概述

本文檔提供系統搜尋邏輯的完整範本，讓第三方開發者能夠理解、修改和擴展搜尋功能。

系統包含兩類搜尋：
1. **主題搜尋**：從各種來源收集主題
2. **圖片搜尋**：根據關鍵字搜尋相關圖片

---

## 🔍 主題搜尋邏輯範本

### 1. 核心接口定義

```python
class TopicCollector:
    """主題收集器基類"""
    
    async def collect_topics(
        self,
        category: Category,      # 主題分類：FASHION, FOOD, TREND
        count: int = 3,         # 需要收集的主題數量
        use_fallback: bool = True  # 是否使用備用方案
    ) -> List[Dict[str, Any]]:
        """
        收集主題的主入口
        
        流程：
        1. 嘗試從主要來源收集（如 RSS Feed）
        2. 如果數量不足，使用備用方案（如關鍵字生成）
        3. 返回主題列表
        """
        pass
```

### 2. 搜尋流程圖

```
┌─────────────────────────────────────────────────────────┐
│  collect_topics(category, count, use_fallback)          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
        ┌─────────────────────┐
        │ 步驟 1: RSS 收集     │
        │ _collect_from_rss()  │
        └──────────┬──────────┘
                   │
                   ▼
        ┌─────────────────────┐
        │ 檢查數量是否足夠？    │
        │ len(topics) >= count │
        └──────────┬──────────┘
                   │
        ┌──────────┴──────────┐
        │                      │
      是│                      │否
        │                      │
        ▼                      ▼
   ┌─────────┐      ┌─────────────────────┐
   │ 返回結果│      │ 步驟 2: 備用方案      │
   └─────────┘      │ _generate_from_keywords()│
                    └──────────┬──────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ 合併結果並返回       │
                    │ topics[:count]       │
                    └─────────────────────┘
```

### 3. RSS 收集邏輯範本

```python
async def _collect_from_rss(
    self,
    category: Category,
    count: int
) -> List[Dict[str, Any]]:
    """
    從 RSS Feed 收集主題
    
    範本結構：
    1. 定義 RSS Feed 列表（按分類）
    2. 遍歷每個 Feed URL
    3. 解析 RSS 內容
    4. 提取標題和關鍵字
    5. 翻譯標題（如果需要）
    6. 構建主題對象
    """
    topics = []
    feeds = self.rss_feeds.get(category, [])  # 獲取該分類的 Feed 列表
    
    async with httpx.AsyncClient(timeout=10.0) as client:
        for feed_url in feeds:
            try:
                # 1. 獲取 RSS Feed
                response = await client.get(feed_url)
                feed = feedparser.parse(response.text)
                
                # 2. 解析每個條目
                for entry in feed.entries[:count]:
                    title = entry.get("title", "")
                    link = entry.get("link", "")
                    
                    if title:
                        # 3. 提取關鍵字
                        keywords = self._extract_keywords(title, category)
                        
                        # 4. 翻譯標題（如果需要）
                        chinese_title = await self._translate_title_to_chinese(
                            title, category
                        )
                        
                        # 5. 構建主題對象
                        topic = {
                            "title": chinese_title,
                            "category": category.value,
                            "source": feed.feed.get("title", "RSS Feed"),
                            "sources": [{
                                "type": "rss",
                                "name": feed.feed.get("title", "RSS Feed"),
                                "url": link,
                                "title": chinese_title,
                                "original_title": title,
                                "fetched_at": datetime.utcnow(),
                                "verified": True,
                                "keywords": keywords,
                            }],
                        }
                        topics.append(topic)
                        
                        if len(topics) >= count:
                            break
                            
            except Exception as e:
                logger.warning(f"無法從 RSS {feed_url} 收集主題: {e}")
                continue
    
    return topics
```

### 4. RSS Feed 配置範本

```python
# 在 __init__ 方法中定義
self.rss_feeds = {
    Category.FASHION: [
        "https://www.vogue.com/feed/rss",
        "https://www.elle.com/rss/all.xml",
        "https://www.harpersbazaar.com/feed/",
        # 可以添加更多 Feed URL
    ],
    Category.FOOD: [
        "https://www.foodnetwork.com/feeds/all.rss",
        "https://www.bonappetit.com/feed/rss",
        "https://www.seriouseats.com/feeds/all",
        # 可以添加更多 Feed URL
    ],
    Category.TREND: [
        "https://feeds.feedburner.com/oreilly/radar",
        "https://techcrunch.com/feed/",
        "https://www.theverge.com/rss/index.xml",
        # 可以添加更多 Feed URL
    ],
}
```

### 5. 備用關鍵字生成範本

```python
async def _generate_from_keywords(
    self,
    category: Category,
    count: int
) -> List[Dict[str, Any]]:
    """
    從備用關鍵字生成主題
    
    範本結構：
    1. 獲取該分類的備用關鍵字列表
    2. 使用 AI 生成中文標題（可選）
    3. 構建主題對象
    """
    topics = []
    keywords = self.fallback_keywords.get(category, [])
    
    # 嘗試使用 AI 生成中文標題
    try:
        ai_service = AIServiceFactory.get_service(settings.AI_SERVICE)
        
        for keyword in keywords[:count]:
            try:
                # 使用 AI 生成標題
                prompt = build_title_prompt(category, keyword=keyword)
                chinese_title = await ai_service._call_api(prompt)
                chinese_title = chinese_title.strip().strip('"').strip("'").strip()
                chinese_title = chinese_title.split('\n')[0].strip()
                
                topic = {
                    "title": chinese_title,
                    "category": category.value,
                    "source": "AI Generated",
                    "sources": [{
                        "type": "ai",
                        "name": "AI Generated Topic",
                        "url": "",
                        "title": chinese_title,
                        "fetched_at": datetime.utcnow(),
                        "verified": False,
                        "keywords": [keyword],
                    }],
                }
                topics.append(topic)
            except Exception as e:
                # AI 生成失敗，使用關鍵字作為標題
                topic = {
                    "title": keyword,
                    "category": category.value,
                    "source": "AI Generated",
                    "sources": [{
                        "type": "ai",
                        "name": "AI Generated Topic",
                        "url": "",
                        "title": keyword,
                        "fetched_at": datetime.utcnow(),
                        "verified": False,
                        "keywords": [keyword],
                    }],
                }
                topics.append(topic)
    except Exception as e:
        # 完全無法使用 AI，直接使用關鍵字
        for keyword in keywords[:count]:
            topic = {
                "title": keyword,
                "category": category.value,
                "source": "AI Generated",
                "sources": [{
                    "type": "ai",
                    "name": "AI Generated Topic",
                    "url": "",
                    "title": keyword,
                    "fetched_at": datetime.utcnow(),
                    "verified": False,
                    "keywords": [keyword],
                }],
            }
            topics.append(topic)
    
    return topics
```

### 6. 備用關鍵字配置範本

```python
# 在 __init__ 方法中定義
self.fallback_keywords = {
    Category.FASHION: [
        "2025春夏時尚趨勢",
        "可持續時尚",
        "復古風格回歸",
        "街頭時尚",
        "時尚科技",
        "環保時尚",
        # 可以添加更多關鍵字
    ],
    Category.FOOD: [
        "香港美食推薦",
        "街頭小吃",
        "傳統美食",
        "新興餐廳",
        "美食趨勢",
        "健康飲食",
        # 可以添加更多關鍵字
    ],
    Category.TREND: [
        "AI技術發展",
        "可持續發展",
        "社會趨勢",
        "科技創新",
        "文化現象",
        "生活方式",
        # 可以添加更多關鍵字
    ],
}
```

### 7. 關鍵字提取範本

```python
def _extract_keywords(
    self,
    text: str,
    category: Category
) -> List[str]:
    """
    從文本中提取關鍵字
    
    範本結構：
    1. 根據分類定義相關關鍵字
    2. 從文本中匹配關鍵字
    3. 去重並限制數量
    """
    keywords = []
    words = text.split()
    
    # 根據分類添加相關關鍵字
    if category == Category.FASHION:
        fashion_keywords = ["時尚", "潮流", "風格", "設計", "服裝"]
        keywords.extend([w for w in words if any(kw in w for kw in fashion_keywords)])
    elif category == Category.FOOD:
        food_keywords = ["美食", "餐廳", "料理", "小吃", "料理"]
        keywords.extend([w for w in words if any(kw in w for kw in food_keywords)])
    elif category == Category.TREND:
        trend_keywords = ["趨勢", "發展", "創新", "技術", "社會"]
        keywords.extend([w for w in words if any(kw in w for kw in trend_keywords)])
    
    # 去重並限制數量
    return list(set(keywords))[:5]
```

---

## 🖼️ 圖片搜尋邏輯範本

### 1. 核心接口定義

```python
class ImageService:
    """圖片服務管理器"""
    
    async def search_images(
        self,
        keywords: str,              # 搜尋關鍵字
        source: Optional[ImageSource] = None,  # 指定來源（可選）
        page: int = 1,             # 頁碼
        limit: int = 20,           # 每頁數量
        use_fallback: bool = True   # 是否使用備援服務
    ) -> List[Dict[str, Any]]:
        """
        搜尋圖片的主入口
        
        流程：
        1. 如果指定來源，使用該來源
        2. 否則按優先順序嘗試各個服務
        3. 如果所有服務都失敗，使用備援服務（如 DuckDuckGo）
        """
        pass
```

### 2. 搜尋流程圖

```
┌─────────────────────────────────────────────────────────┐
│  search_images(keywords, source, page, limit, fallback)│
└─────────────────┬───────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                    │
    指定來源？            未指定來源
        │                    │
        ▼                    ▼
┌───────────────┐    ┌──────────────────┐
│ 使用指定來源   │    │ 按優先順序嘗試     │
│ service.search │    │ 1. Unsplash       │
└───────┬───────┘    │ 2. Pexels         │
        │            │ 3. Pixabay         │
        │            └──────┬─────────────┘
        │                   │
        │            ┌──────┴──────┐
        │            │              │
        │         成功│              │失敗
        │            │              │
        │            ▼              ▼
        │    ┌──────────────┐  ┌──────────────┐
        │    │ 返回結果      │  │ 嘗試備援服務   │
        │    └──────────────┘  │ DuckDuckGo    │
        │                      └──────┬───────┘
        │                             │
        └─────────────────────────────┘
                      │
                      ▼
              ┌──────────────┐
              │ 返回圖片列表  │
              └──────────────┘
```

### 3. 圖片搜尋邏輯範本

```python
async def search_images(
    self,
    keywords: str,
    source: Optional[ImageSource] = None,
    page: int = 1,
    limit: int = 20,
    use_fallback: bool = True
) -> List[Dict[str, Any]]:
    """
    搜尋圖片（支援備援機制）
    
    範本結構：
    1. 如果指定來源，使用該來源
    2. 否則按優先順序嘗試各個服務
    3. 如果所有服務都失敗，使用備援服務
    """
    if source:
        # 使用指定來源
        service_map = {
            ImageSource.UNSPLASH: self.unsplash,
            ImageSource.PEXELS: self.pexels,
            ImageSource.PIXABAY: self.pixabay,
            ImageSource.DUCKDUCKGO: self.duckduckgo,
        }
        
        service = service_map.get(source)
        if not service:
            raise ValueError(f"不支援的圖片來源: {source}")
        
        try:
            return await service.search_images(keywords, page, limit)
        except Exception as e:
            logger.error(f"{source.value} 搜尋失敗: {e}")
            if not use_fallback:
                raise
            # 繼續嘗試備援服務
    
    # 按優先順序嘗試各個服務
    last_error = None
    for service, service_source in self.services:
        try:
            images = await service.search_images(keywords, page, limit)
            logger.info(f"使用 {service_source.value} 成功搜尋圖片")
            return images
        except ValueError as e:
            # API Key 未設定，跳過此服務
            logger.warning(f"{service_source.value} API Key 未設定，跳過")
            continue
        except Exception as e:
            logger.warning(f"{service_source.value} 搜尋失敗: {e}")
            last_error = e
            continue
    
    # 所有 API 服務都失敗，嘗試使用備援服務
    if use_fallback:
        logger.info("所有 API 服務都失敗，嘗試使用備援服務...")
        try:
            images = await self.duckduckgo.search_images(keywords, page, limit)
            logger.info(f"✅ 備援服務搜尋成功，找到 {len(images)} 張圖片")
            return images
        except Exception as e:
            logger.error(f"備援服務搜尋也失敗: {e}")
            if last_error:
                raise last_error
            raise ValueError("所有圖片服務都失敗")
    
    # 如果 use_fallback=False，則拋出錯誤
    if last_error:
        raise last_error
    raise ValueError("沒有可用的圖片服務")
```

### 4. 服務優先順序配置範本

```python
# 在 __init__ 方法中定義
def __init__(self):
    self.unsplash = UnsplashService()
    self.pexels = PexelsService()
    self.pixabay = PixabayService()
    self.duckduckgo = DuckDuckGoService()  # 備援服務（無需 API Key）
    
    # 服務優先順序（從高到低）
    self.services = [
        (self.unsplash, ImageSource.UNSPLASH),
        (self.pexels, ImageSource.PEXELS),
        (self.pixabay, ImageSource.PIXABAY),
    ]
```

### 5. 單個圖片服務接口範本

```python
class ImageServiceBase:
    """圖片服務基類"""
    
    async def search_images(
        self,
        keywords: str,
        page: int = 1,
        limit: int = 20
    ) -> List[Dict[str, Any]]:
        """
        搜尋圖片
        
        返回格式：
        [
            {
                "id": "image_id",
                "url": "https://example.com/image.jpg",
                "width": 1920,
                "height": 1080,
                "source": ImageSource.UNSPLASH,
                "photographer": "Photographer Name",
                "photographer_url": "https://...",
                "license": "Free",
            },
            ...
        ]
        """
        pass
```

---

## 🔧 如何修改和擴展

### 1. 添加新的 RSS Feed

**位置：** `backend/app/services/automation/topic_collector.py`

```python
# 在 __init__ 方法中修改
self.rss_feeds = {
    Category.FASHION: [
        "https://www.vogue.com/feed/rss",
        "https://www.elle.com/rss/all.xml",
        "https://www.harpersbazaar.com/feed/",
        "https://新網站.com/feed/rss",  # 添加新的 Feed
    ],
    # ...
}
```

### 2. 添加新的備用關鍵字

**位置：** `backend/app/services/automation/topic_collector.py`

```python
# 在 __init__ 方法中修改
self.fallback_keywords = {
    Category.FASHION: [
        "2025春夏時尚趨勢",
        "可持續時尚",
        "新關鍵字1",  # 添加新的關鍵字
        "新關鍵字2",
        # ...
    ],
    # ...
}
```

### 3. 添加新的圖片服務

**步驟 1：** 創建新的服務類

```python
# backend/app/services/images/new_service.py
from app.services.images.base import ImageServiceBase

class NewImageService(ImageServiceBase):
    """新的圖片服務"""
    
    def __init__(self):
        self.api_key = settings.NEW_SERVICE_API_KEY
    
    async def search_images(
        self,
        keywords: str,
        page: int = 1,
        limit: int = 20
    ) -> List[Dict[str, Any]]:
        # 實現搜尋邏輯
        pass
```

**步驟 2：** 在 ImageService 中註冊

```python
# backend/app/services/images/image_service.py
from app.services.images.new_service import NewImageService

class ImageService:
    def __init__(self):
        # ...
        self.new_service = NewImageService()
        
        # 添加到服務列表（按優先順序）
        self.services = [
            (self.unsplash, ImageSource.UNSPLASH),
            (self.new_service, ImageSource.NEW_SERVICE),  # 添加新服務
            (self.pexels, ImageSource.PEXELS),
            # ...
        ]
```

### 4. 修改關鍵字提取邏輯

**位置：** `backend/app/services/automation/topic_collector.py`

```python
def _extract_keywords(
    self,
    text: str,
    category: Category
) -> List[str]:
    """
    可以改進為使用 NLP 庫（如 jieba）進行更精確的關鍵字提取
    """
    # 使用 jieba 分詞（範例）
    import jieba
    
    words = jieba.cut(text)
    
    # 根據分類過濾關鍵字
    if category == Category.FASHION:
        fashion_keywords = ["時尚", "潮流", "風格", "設計", "服裝"]
        keywords = [w for w in words if w in fashion_keywords]
    # ...
    
    return list(set(keywords))[:5]
```

### 5. 添加新的搜尋來源

**範例：** 添加 Twitter/X API 作為主題來源

```python
async def _collect_from_twitter(
    self,
    category: Category,
    count: int
) -> List[Dict[str, Any]]:
    """
    從 Twitter/X API 收集主題
    
    範本結構：
    1. 調用 Twitter API
    2. 解析推文
    3. 提取熱門話題
    4. 構建主題對象
    """
    topics = []
    
    # 調用 Twitter API
    tweets = await twitter_api.search_tweets(
        query=self._build_twitter_query(category),
        count=count
    )
    
    for tweet in tweets:
        topic = {
            "title": tweet.text,
            "category": category.value,
            "source": "Twitter",
            "sources": [{
                "type": "twitter",
                "name": "Twitter",
                "url": tweet.url,
                "title": tweet.text,
                "fetched_at": datetime.utcnow(),
                "verified": True,
                "keywords": self._extract_keywords(tweet.text, category),
            }],
        }
        topics.append(topic)
    
    return topics
```

---

## 📊 數據結構範本

### 主題對象結構

```python
topic = {
    "title": str,              # 主題標題（中文）
    "category": str,           # 分類：fashion, food, trend
    "source": str,             # 來源名稱
    "sources": [               # 來源詳情列表
        {
            "type": str,        # 來源類型：rss, ai, twitter, etc.
            "name": str,        # 來源名稱
            "url": str,         # 來源 URL（如果有）
            "title": str,       # 標題
            "original_title": str,  # 原始標題（如果是翻譯的）
            "fetched_at": datetime,  # 獲取時間
            "verified": bool,   # 是否驗證
            "keywords": List[str],  # 關鍵字列表
        }
    ],
}
```

### 圖片對象結構

```python
image = {
    "id": str,                 # 圖片 ID
    "url": str,                # 圖片 URL
    "width": int,              # 寬度
    "height": int,             # 高度
    "source": ImageSource,     # 來源：UNSPLASH, PEXELS, etc.
    "photographer": str,       # 攝影師名稱（如果有）
    "photographer_url": str,   # 攝影師 URL（如果有）
    "license": str,            # 授權類型
    "keywords": List[str],     # 關鍵字列表
}
```

---

## 🎯 最佳實踐

### 1. 錯誤處理

```python
try:
    # 搜尋邏輯
    result = await service.search(...)
except ValueError as e:
    # API Key 未設定，跳過此服務
    logger.warning(f"服務不可用: {e}")
    continue
except Exception as e:
    # 其他錯誤，記錄並繼續
    logger.error(f"搜尋失敗: {e}")
    last_error = e
    continue
```

### 2. 超時設置

```python
async with httpx.AsyncClient(timeout=10.0) as client:
    response = await client.get(url)
```

### 3. 日誌記錄

```python
logger.info(f"開始搜尋: {keywords}")
logger.warning(f"服務 {service} 失敗，嘗試下一個")
logger.error(f"所有服務都失敗: {error}")
```

### 4. 備援機制

```python
# 優先使用主要服務
for service in primary_services:
    try:
        return await service.search(...)
    except:
        continue

# 如果所有主要服務都失敗，使用備援服務
if use_fallback:
    return await fallback_service.search(...)
```

---

## 📝 總結

### 主題搜尋邏輯

1. **主要來源：** RSS Feed（可配置）
2. **備用方案：** 關鍵字 + AI 生成（可配置）
3. **擴展方式：** 添加新的 Feed URL 或關鍵字

### 圖片搜尋邏輯

1. **主要服務：** Unsplash, Pexels, Pixabay（按優先順序）
2. **備援服務：** DuckDuckGo（無需 API Key）
3. **擴展方式：** 實現 `ImageServiceBase` 接口並註冊

### 修改建議

1. **配置優先：** 優先修改配置（Feed URL、關鍵字），而非代碼
2. **接口一致：** 新增服務需遵循現有接口規範
3. **錯誤處理：** 確保有完善的錯誤處理和備援機制
4. **日誌記錄：** 記錄關鍵操作，便於調試

---

## 🔗 相關文件

- **主題收集器：** `backend/app/services/automation/topic_collector.py`
- **圖片服務：** `backend/app/services/images/image_service.py`
- **圖片服務基類：** `backend/app/services/images/base.py`
- **主題模型：** `backend/app/models/topic.py`
- **圖片模型：** `backend/app/models/image.py`

