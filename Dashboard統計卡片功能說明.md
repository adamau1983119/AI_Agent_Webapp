# Dashboard 統計卡片功能說明

**位置：** Dashboard 頁面頂部的四個進度卡片  
**更新日期：** 2026-01-13

---

## 📊 四個統計卡片詳細功能

### 1. 待審核 (Pending)

**功能：**
- **顯示內容：** `待審核數量/總主題數` 和百分比
- **計算方式：** 統計所有狀態為 `pending` 的主題數量
- **狀態含義：**
  - `pending` = 主題已生成，但尚未被用戶確認
  - 這些主題可能已經有內容和圖片，但需要用戶審核確認

**使用場景：**
- 查看有多少主題等待審核
- 了解工作進度
- 提醒需要處理的主題數量

**操作流程：**
1. 後台自動生成主題 → 狀態為 `pending`
2. 用戶查看主題詳情
3. 用戶點擊「確認」按鈕 → 狀態變為 `confirmed`

**相關功能：**
- 在主題詳情頁面可以點擊「確認」按鈕將主題狀態改為 `confirmed`
- 在主題列表頁面可以篩選 `pending` 狀態的主題

---

### 2. 已確認 (Confirmed)

**功能：**
- **顯示內容：** `已確認數量/總主題數` 和百分比
- **計算方式：** 統計所有狀態為 `confirmed` 的主題數量
- **狀態含義：**
  - `confirmed` = 主題已被用戶確認，可以正式使用
  - 這些主題已經過審核，內容和圖片都已確認

**使用場景：**
- 查看已完成審核的主題數量
- 了解工作完成度
- 追蹤已確認的主題比例

**操作流程：**
1. 用戶在主題詳情頁面點擊「確認」按鈕
2. 主題狀態從 `pending` 變為 `confirmed`
3. 卡片中的「已確認」數量增加

**相關功能：**
- 在主題列表頁面可以篩選 `confirmed` 狀態的主題
- 已確認的主題可以正式發布或使用

---

### 3. 內容評分 (Content Score)

**功能：**
- **顯示內容：** `平均字數/100` 和百分比
- **計算方式：** 
  - 計算所有主題的平均字數（`wordCount`）
  - 公式：`所有主題的總字數 / 主題數量`
  - 顯示為 `平均字數/100`，百分比上限為 100%

**狀態含義：**
- 反映內容的豐富程度
- 字數越多，表示內容越完整
- 目標是達到 100 字以上（100%）

**使用場景：**
- 評估內容質量
- 追蹤內容完整度
- 了解是否需要補充內容

**數據來源：**
- 每個主題的 `wordCount` 欄位
- 來自主題的內容（article）字數統計

**相關功能：**
- 在主題詳情頁面可以看到具體的字數
- 生成內容時可以設定目標字數（如 500 字）

---

### 4. 今日主題 (Today's Topics)

**功能：**
- **顯示內容：** `今日生成數量/9` 和百分比
- **計算方式：** 
  - 統計今天（當前日期）生成的主題數量
  - 目標是每天生成 9 個主題
  - 百分比 = `(今日主題數 / 9) * 100%`

**狀態含義：**
- 追蹤每日主題生成進度
- 目標：每天 9 個主題（3 個時段 × 3 個主題）

**使用場景：**
- 查看今日工作進度
- 了解是否達到每日目標
- 決定是否需要生成更多主題

**操作功能：**
- 如果今日主題 < 9，卡片上會顯示「立即生成」按鈕
- 點擊按鈕可以觸發後台生成今日主題
- 生成過程會顯示「生成中...」狀態

**生成時段：**
根據排程設定，每天應該在以下時段生成主題：
- 07:00 HKT - 時尚趨勢（3 個主題）
- 12:00 HKT - 美食推薦（3 個主題）
- 18:00 HKT - 社會趨勢（3 個主題）
- **總計：9 個主題**

**相關功能：**
- 排程服務會自動在指定時間生成主題
- 也可以手動點擊「立即生成」按鈕
- 在「排程」頁面可以查看詳細的排程設定

---

## 📋 卡片數據更新機制

### 自動更新：
- 當主題狀態改變時（pending → confirmed）
- 當新主題生成時
- 當主題內容更新時（字數變化）

### 手動刷新：
- 點擊頁面的「重試」按鈕
- 重新載入頁面
- 切換到其他頁面再返回

---

## 🔄 主題狀態流程

```
後台自動生成主題
    ↓
狀態：pending（待審核）
    ↓
用戶查看並審核
    ↓
用戶點擊「確認」按鈕
    ↓
狀態：confirmed（已確認）
    ↓
可以正式使用
```

---

## 📊 數據計算公式

### 待審核：
```javascript
pendingCount = topics.filter(t => t.status === 'pending').length
percentage = (pendingCount / totalTopics) * 100
```

### 已確認：
```javascript
confirmedCount = topics.filter(t => t.status === 'confirmed').length
percentage = (confirmedCount / totalTopics) * 100
```

### 內容評分：
```javascript
averageWordCount = topics.reduce((sum, t) => sum + t.wordCount, 0) / topics.length
percentage = Math.min(100, averageWordCount)
```

### 今日主題：
```javascript
todayTopics = topics.filter(t => t.generatedAt.startsWith(today)).length
percentage = (todayTopics / 9) * 100
```

---

## 🎯 使用建議

### 待審核卡片：
- **目標：** 保持較低的待審核數量
- **行動：** 定期審核並確認主題
- **理想狀態：** 0 個待審核（所有主題都已確認）

### 已確認卡片：
- **目標：** 提高已確認比例
- **行動：** 積極確認優質主題
- **理想狀態：** 100% 已確認（所有主題都已審核）

### 內容評分卡片：
- **目標：** 達到 100 字以上（100%）
- **行動：** 確保每個主題都有完整的內容
- **理想狀態：** 平均字數 ≥ 100 字

### 今日主題卡片：
- **目標：** 每天生成 9 個主題
- **行動：** 如果不足 9 個，點擊「立即生成」
- **理想狀態：** 9/9（100%）

---

## 🔍 相關頁面和功能

### 主題列表頁面：
- 可以查看所有主題
- 可以篩選 `pending` 或 `confirmed` 狀態
- 可以查看每個主題的詳細信息

### 主題詳情頁面：
- 可以查看主題的完整內容
- 可以點擊「確認」按鈕改變狀態
- 可以查看字數統計

### 排程頁面：
- 可以查看排程設定
- 可以手動觸發主題生成
- 可以查看排程歷史

---

## 📝 總結

| 卡片 | 功能 | 數據來源 | 目標 |
|------|------|----------|------|
| **待審核** | 顯示等待審核的主題數量 | `status === 'pending'` | 0 個（全部已審核） |
| **已確認** | 顯示已確認的主題數量 | `status === 'confirmed'` | 100%（全部已確認） |
| **內容評分** | 顯示平均內容字數 | `wordCount` 平均值 | ≥ 100 字（100%） |
| **今日主題** | 顯示今日生成的主題數量 | `generatedAt` 為今天 | 9/9（100%） |

---

**這些卡片幫助您快速了解專案的整體狀態和進度！** 📊

