# 新搜尋邏輯需求文檔

## 📋 概述

本需求文檔描述新的主題搜尋 API 系統，整合以下四個核心功能：
1. **中文全文搜尋**（支援 Elasticsearch 或 MongoDB $regex）
2. **快取策略**（Redis 快取熱門查詢）
3. **查詢限制**（限制結果數量，避免過大結果集）
4. **權限控制**（依用戶角色過濾結果）

---

## 🎯 核心需求

### 1. 中文全文搜尋

#### 需求描述
- 支援中文關鍵字搜尋主題標題、摘要、內容
- 優先使用 Elasticsearch（支援中文斷詞）
- 如果 Elasticsearch 不可用，回退到 MongoDB $regex
- 支援分類篩選（fashion, food, trend）

#### 技術方案

**方案 A：Elasticsearch（推薦）**
- 使用 IK Analyzer 進行中文斷詞
- 支援相關度排序
- 支援同義詞、拼音搜尋

**方案 B：MongoDB $regex（備援）**
- 當 Elasticsearch 不可用時自動切換
- 使用正則表達式匹配
- 效能較差，但可作為備援方案

#### 配置要求
- Elasticsearch 索引名稱：`topics`
- 中文分析器：`ik_max_word`
- 索引欄位：`title`, `summary`, `content`

---

### 2. 快取策略

#### 需求描述
- 使用 Redis 快取熱門查詢結果
- 快取 TTL：5 分鐘
- 支援快取失效策略
- 記錄熱門查詢統計

#### Redis Key 結構
```
search:{query_hash}:{category}:{page}:{limit}:{role} → JSON 結果（TTL: 5分鐘）
hot:queries → Sorted Set（分數 = 查詢次數，TTL: 1小時）
```

#### 快取邏輯
1. 查詢前先檢查 Redis 快取
2. 如果快取命中，直接返回結果
3. 如果快取未命中，查詢資料庫
4. 查詢結果寫入快取（TTL: 5分鐘）
5. 更新熱門查詢統計

#### 快取失效策略
- **時間失效**：TTL 自動過期
- **事件失效**：新增/更新主題時清除相關快取
- **手動失效**：管理員可手動清除快取

---

### 3. 查詢限制

#### 需求描述
- 限制單次查詢返回的最大結果數：50 筆
- 支援分頁功能（page, limit）
- 限制查詢字串長度：最多 100 字元
- 限制分頁頁數：最多 100 頁

#### API 參數限制
```python
query: str = Query(..., min_length=2, max_length=100)
page: int = Query(1, ge=1, le=100)
limit: int = Query(10, ge=1, le=50)
```

#### 輸入驗證
- 清理查詢字串（移除危險字符）
- 驗證查詢字串格式
- 防止 SQL/NoSQL 注入

---

### 4. 權限控制

#### 需求描述
- 根據用戶角色過濾查詢結果
- 不同角色可查看的欄位不同
- 支援訪客、普通用戶、付費用戶、管理員四種角色

#### 角色權限對照表

| 角色 | 可查看欄位 | 說明 |
|------|-----------|------|
| **guest** | title, summary | 訪客只能看標題和摘要 |
| **user** | title, summary, source_url, preview_image | 普通用戶可看完整文章 |
| **premium** | 所有欄位 | 付費用戶可看所有資料 |
| **admin** | 所有欄位 + metadata | 管理員可看所有資料（包括 metadata） |

#### 權限檢查流程
1. 從 JWT Token 獲取用戶角色
2. 執行資料庫查詢
3. 根據角色過濾結果欄位
4. 返回過濾後的結果

---

## 📊 資料庫設計

### MongoDB Schema

#### Collection: `topics`

```javascript
{
  "_id": "ObjectId",
  "title": "Valentino 發表2026春夏時尚系列",
  "summary": "Valentino 在巴黎展示2026春夏系列，融合浪漫與現代風格。",
  "content": "完整的中文文章內容（500字）...",
  
  "source": {
    "url": "https://www.vogue.com/article/valentino-2026-spring-summer",
    "images": [
      "https://vogue.com/images/valentino-2026.jpg"
    ],
    "language": "en",
    "style": {
      "tone": "新聞報導",
      "structure": "短段落",
      "vocabulary": "專業術語"
    }
  },

  "generated": {
    "language": "zh",
    "summary_30": "Valentino 在巴黎展示2026春夏系列，融合浪漫與現代風格。",
    "article_500": "翻譯並改寫的中文文章（保持原文風格）...",
    "script_30s": "30秒中文播報腳本..."
  },

  "images": {
    "preview": [
      "https://vogue.com/images/valentino-2026.jpg"
    ],
    "matched": [
      "https://images.unsplash.com/valentino-show.jpg",
      "https://pexels.com/photo/runway.jpg"
    ]
  },

  "metadata": {
    "created_at": "2026-01-20T09:00:00Z",
    "updated_at": "2026-01-20T09:30:00Z",
    "categories": ["fashion", "trend"],
    "tags": ["Valentino", "巴黎時裝週", "春夏系列"],
    "is_public": true,
    "is_active": true
  }
}
```

### MongoDB 索引設計

```javascript
// 1. 原文連結唯一索引
db.topics.createIndex({ "source.url": 1 }, { unique: true })

// 2. 分類索引
db.topics.createIndex({ "metadata.categories": 1 })

// 3. 標籤索引
db.topics.createIndex({ "metadata.tags": 1 })

// 4. 建立時間索引
db.topics.createIndex({ "metadata.created_at": -1 })

// 5. 複合索引：分類 + 時間
db.topics.createIndex({ 
  "metadata.categories": 1, 
  "metadata.created_at": -1 
})

// 6. 複合索引：標籤 + 時間
db.topics.createIndex({ 
  "metadata.tags": 1, 
  "metadata.created_at": -1 
})

// 7. 全文索引（可選）
db.topics.createIndex(
  { title: "text", summary: "text", content: "text" },
  { default_language: "none" }
)
```

### Elasticsearch Mapping

```json
{
  "settings": {
    "analysis": {
      "analyzer": {
        "chinese_analyzer": {
          "type": "custom",
          "tokenizer": "ik_max_word",
          "filter": ["lowercase", "stop"]
        }
      }
    }
  },
  "mappings": {
    "properties": {
      "title": {
        "type": "text",
        "analyzer": "chinese_analyzer",
        "fields": {
          "keyword": { "type": "keyword" }
        }
      },
      "summary": {
        "type": "text",
        "analyzer": "chinese_analyzer"
      },
      "content": {
        "type": "text",
        "analyzer": "chinese_analyzer"
      },
      "metadata": {
        "properties": {
          "categories": { "type": "keyword" },
          "tags": { "type": "keyword" },
          "created_at": { "type": "date" }
        }
      }
    }
  }
}
```

---

## 🔌 API 設計

### 端點：`GET /api/v1/topics/search`

#### 請求參數

| 參數 | 類型 | 必填 | 說明 | 預設值 | 限制 |
|------|------|------|------|--------|------|
| `query` | string | 是 | 搜尋關鍵字 | - | 2-100 字元 |
| `category` | string | 否 | 分類篩選（fashion/food/trend） | - | - |
| `page` | integer | 否 | 頁碼 | 1 | 1-100 |
| `limit` | integer | 否 | 每頁數量 | 10 | 1-50 |

#### 請求範例

```bash
GET /api/v1/topics/search?query=巴黎時裝週&category=fashion&page=1&limit=10
Authorization: Bearer <JWT_TOKEN>
```

#### 回應格式

```json
{
  "source": "db",
  "results": [
    {
      "title": "Valentino 發表2026春夏時尚系列",
      "summary": "Valentino 在巴黎展示2026春夏系列，融合浪漫與現代風格。",
      "content": "完整的中文文章內容...",
      "source_url": "https://www.vogue.com/article/valentino-2026-spring-summer",
      "preview_image": "https://vogue.com/images/valentino-2026.jpg"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 25,
    "pages": 3
  }
}
```

#### 錯誤回應

```json
{
  "detail": "查詢字串無效"
}
```

```json
{
  "detail": "無權限查詢"
}
```

---

### 端點：`GET /api/v1/topics/search/check`

#### 功能
檢查某篇原文是否已收錄

#### 請求參數

| 參數 | 類型 | 必填 | 說明 |
|------|------|------|------|
| `url` | string | 是 | 原文 URL |

#### 請求範例

```bash
GET /api/v1/topics/search/check?url=https://www.vogue.com/article/valentino-2026-spring-summer
```

#### 回應格式

```json
{
  "exists": true,
  "topic": {
    "title": "Valentino 發表2026春夏時尚系列",
    "summary": "Valentino 在巴黎展示2026春夏系列...",
    "source_url": "https://www.vogue.com/article/valentino-2026-spring-summer",
    "preview_image": "https://vogue.com/images/valentino-2026.jpg"
  }
}
```

---

### 端點：`GET /api/v1/topics/search/hot-queries`

#### 功能
取得熱門查詢列表

#### 請求參數

| 參數 | 類型 | 必填 | 說明 | 預設值 | 限制 |
|------|------|------|------|--------|------|
| `limit` | integer | 否 | 返回數量 | 10 | 1-50 |

#### 回應格式

```json
{
  "queries": [
    {
      "query": "巴黎時裝週",
      "count": 125
    },
    {
      "query": "時尚趨勢",
      "count": 98
    }
  ]
}
```

---

### 端點：`DELETE /api/v1/topics/search/cache`（管理員）

#### 功能
清除快取（僅管理員）

#### 請求參數

| 參數 | 類型 | 必填 | 說明 |
|------|------|------|------|
| `pattern` | string | 是 | 快取 key 模式 |

#### 回應格式

```json
{
  "deleted": 15,
  "message": "已清除 15 個快取項目"
}
```

---

### 端點：`GET /api/v1/topics/search/health`

#### 功能
檢查搜尋服務健康狀態

#### 回應格式

```json
{
  "elasticsearch": "ok",
  "redis": "ok",
  "mongodb": "ok"
}
```

---

## 🔧 實作細節

### 1. 連接池配置

#### Elasticsearch
```python
from elasticsearch import AsyncElasticsearch

es = AsyncElasticsearch(
    ["http://localhost:9200"],
    maxsize=10,  # 連接池大小
    timeout=5
)
```

#### Redis
```python
from redis.asyncio import Redis

redis_client = Redis(
    host="localhost",
    port=6379,
    db=0,
    max_connections=10,
    decode_responses=True
)
```

---

### 2. 查詢流程

```
用戶請求
  ↓
API 層（驗證、限制、權限）
  ↓
快取層（Redis）- 檢查是否有快取
  ↓
搜尋層（Elasticsearch / MongoDB）
  ↓
結果處理（權限過濾、分頁）
  ↓
寫入快取
  ↓
返回結果
```

---

### 3. 錯誤處理

#### 錯誤類型
1. **輸入驗證錯誤**：400 Bad Request
2. **權限不足**：403 Forbidden
3. **服務不可用**：503 Service Unavailable
4. **內部錯誤**：500 Internal Server Error

#### 錯誤處理策略
- Elasticsearch 失敗 → 回退到 MongoDB
- Redis 失敗 → 繼續查詢，但不使用快取
- 所有服務失敗 → 返回 503 錯誤

---

### 4. 日誌記錄

#### 記錄內容
- 查詢關鍵字
- 用戶 ID 和角色
- 查詢結果數量
- 快取命中/未命中
- 錯誤訊息

#### 日誌格式
```
[INFO] Search: '巴黎時裝週' by user user_123, found 25 results, cache: miss
[ERROR] Elasticsearch error: Connection timeout, falling back to MongoDB
[WARNING] Redis cache error: Connection refused, skipping cache
```

---

## 📝 實作檢查清單

### Phase 1：基礎功能（MVP）
- [ ] MongoDB 查詢實作
- [ ] 基本分頁（limit/skip）
- [ ] API 層查詢限制
- [ ] 簡單權限控制（公開/私有）

### Phase 2：效能優化
- [ ] Redis 快取實作
- [ ] 游標分頁（取代 skip）
- [ ] 查詢結果投影優化
- [ ] 索引優化

### Phase 3：進階功能
- [ ] Elasticsearch 整合
- [ ] 完整權限系統
- [ ] 熱門查詢統計
- [ ] 搜尋建議/自動完成

### Phase 4：監控與維護
- [ ] 查詢效能監控
- [ ] 快取命中率監控
- [ ] 錯誤日誌與告警
- [ ] 慢查詢分析

---

## 🚀 未來改進建議

1. **搜尋建議**：根據用戶輸入提供搜尋建議
2. **自動完成**：實作搜尋框自動完成功能
3. **相關搜尋**：顯示相關搜尋關鍵字
4. **搜尋歷史**：記錄用戶搜尋歷史
5. **個人化搜尋**：根據用戶偏好調整搜尋結果排序

---

## 📌 注意事項

1. **安全性**：使用 `json.loads/dumps` 取代 `eval()`，防止代碼注入
2. **效能**：使用連接池管理 Elasticsearch 和 Redis 連接
3. **錯誤處理**：完善異常處理與日誌記錄
4. **輸入驗證**：清理查詢字串，防止注入攻擊
5. **快取策略**：合理設定 TTL，平衡資料新鮮度與效能

---

## 📚 參考資料

- [Elasticsearch IK Analyzer 文檔](https://github.com/medcl/elasticsearch-analysis-ik)
- [Redis 快取最佳實踐](https://redis.io/docs/manual/patterns/cache/)
- [MongoDB 索引最佳實踐](https://www.mongodb.com/docs/manual/applications/indexes/)
- [FastAPI 文檔](https://fastapi.tiangolo.com/)

