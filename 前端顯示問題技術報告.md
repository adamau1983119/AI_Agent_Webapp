# 前端顯示問題技術報告

## 📋 問題概述

**問題描述：** 後端 API 可以正常生成主題，但前端應用（運行在 `localhost:3000`）無法顯示數據，出現 CORS 錯誤和 429 速率限制錯誤。

**影響範圍：** 前端 Dashboard 頁面無法載入主題列表和排程數據

**嚴重程度：** 🔴 高（影響核心功能）

---

## 🔍 問題詳情

### 1. 錯誤現象

#### 瀏覽器控制台錯誤：
```
Access to fetch at 'http://localhost:8000/api/v1/schedules' from origin 'http://localhost:3000' 
has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.

GET http://localhost:8000/api/v1/topics?page=1&limit=12 net::ERR_FAILED 429 (Too Many Requests)
GET http://localhost:8000/api/v1/schedules net::ERR_FAILED 429 (Too Many Requests)
```

#### Dashboard 顯示狀態：
- 所有進度卡片顯示 "0/0" 或 "0/9"
- 主題列表為空
- 顯示 "等待數據..." 狀態

### 2. 後端狀態

#### ✅ 正常運行的功能：
- MongoDB 連接成功
- API 端點正常響應（200 狀態碼）
- 主題生成功能正常（成功生成 9 個主題）
- CORS 設定包含 `localhost:3000`

#### ⚠️ 後端日誌顯示：
```
設定 CORS，允許的來源: ['http://localhost:5173', 'http://localhost:3000', 'https://ai-agent-webapp-ten.vercel.app']
解析後的 CORS_ORIGINS: ['http://localhost:5173', 'http://localhost:3000', 'https://ai-agent-webapp-ten.vercel.app']
```

---

## 🛠️ 已嘗試的修復

### 1. 修復 Database 真值測試錯誤 ✅
**問題：** `Database objects do not implement truth value testing or bool()`
**修復：** 將所有 `if db:` 改為 `if db is not None:`
**狀態：** 已修復

### 2. 修復 404 錯誤在控制台顯示 ✅
**問題：** 內容不存在時返回 404，錯誤被記錄到控制台
**修復：** 在 `client.ts` 中靜默處理 404 錯誤
**狀態：** 已修復

### 3. 修復日期處理錯誤 ✅
**問題：** `RangeError: Invalid time value` 在 Dashboard.tsx
**修復：** 添加日期驗證和錯誤處理
**狀態：** 已修復

### 4. 修復速率限制中間件的 CORS 問題 ✅
**問題：** 速率限制中間件返回 429 時沒有 CORS header
**修復：** 在 `rate_limit.py` 中添加 CORS header 到 429 響應
**狀態：** 已修復

### 5. 調整前端輪詢頻率 ✅
**問題：** 前端每 1 秒輪詢 2 個 API，超過速率限制（60次/分鐘）
**修復：** 將輪詢間隔從 1 秒調整為 2 秒
**狀態：** 已修復

---

## 🔧 技術環境

### 前端環境
- **框架：** React 18.2.0 + TypeScript
- **構建工具：** Vite 5.0.8
- **運行端口：** `localhost:3000`（在 `vite.config.ts` 中設定）
- **API Base URL：** `http://localhost:8000/api/v1`
- **狀態管理：** React Query (@tanstack/react-query)
- **HTTP 客戶端：** Fetch API

### 後端環境
- **框架：** FastAPI
- **運行端口：** `localhost:8000`
- **資料庫：** MongoDB (Motor)
- **CORS 設定：** 
  ```python
  CORS_ORIGINS = [
      "http://localhost:5173",
      "http://localhost:3000",
      "https://ai-agent-webapp-ten.vercel.app"
  ]
  ```
- **速率限制：** 60 次/分鐘，1000 次/小時

### 中間件順序
1. CORSMiddleware (FastAPI 內建)
2. CustomCORSMiddleware (自定義備份)
3. RateLimitMiddleware (速率限制)
4. APIKeyMiddleware (API Key 認證，如果啟用)

---

## 📊 問題分析

### 可能的原因

#### 1. CORS Header 未正確設定
- **症狀：** 瀏覽器控制台顯示 CORS 錯誤
- **可能原因：**
  - 中間件執行順序問題
  - 速率限制中間件在 CORS 中間件之前執行，返回 429 時跳過了 CORS header 設定
  - 預檢請求（OPTIONS）未正確處理

#### 2. 速率限制過於嚴格
- **症狀：** 頻繁出現 429 錯誤
- **當前設定：** 60 次/分鐘
- **前端行為：** 
  - 初始載入：2 個 API 請求（topics + schedules）
  - 輪詢期間：每 2 秒 2 個 API 請求（30次/分鐘）
  - 總計：可能超過 60 次/分鐘

#### 3. 中間件執行順序問題
- **FastAPI 中間件執行順序：** 後添加的中間件先執行
- **當前順序：**
  1. APIKeyMiddleware（最後添加，最先執行）
  2. RateLimitMiddleware（第二個添加）
  3. CustomCORSMiddleware（第三個添加）
  4. CORSMiddleware（最先添加，最後執行）

#### 4. 前端請求頻率問題
- **Dashboard 組件：** 使用 React Query 自動重試
- **輪詢機制：** 每 2 秒發送 2 個請求
- **可能觸發速率限制：** 特別是在多個組件同時載入時

---

## 🔍 診斷步驟

### 1. 檢查 CORS Header
```bash
# 使用 curl 檢查 CORS header
curl -H "Origin: http://localhost:3000" \
     -H "Access-Control-Request-Method: GET" \
     -H "Access-Control-Request-Headers: Content-Type" \
     -X OPTIONS \
     http://localhost:8000/api/v1/topics \
     -v
```

**預期結果：**
```
Access-Control-Allow-Origin: http://localhost:3000
Access-Control-Allow-Methods: GET, POST, PUT, PATCH, DELETE, OPTIONS
Access-Control-Allow-Headers: Content-Type, Authorization, X-API-Key, Accept
```

### 2. 檢查速率限制狀態
```bash
# 檢查響應頭中的速率限制資訊
curl http://localhost:8000/api/v1/topics -v
```

**預期結果：**
```
X-RateLimit-Limit: 60
X-RateLimit-Remaining: 59
X-RateLimit-Reset: <timestamp>
```

### 3. 檢查後端日誌
查看後端日誌中是否有：
- CORS 相關的警告或錯誤
- 速率限制觸發的記錄
- 429 響應的詳細資訊

### 4. 檢查前端網路請求
在瀏覽器開發者工具的 Network 標籤中檢查：
- 請求的 Origin header
- 響應的 CORS header
- 請求頻率和 429 錯誤的觸發時機

---

## 💡 建議的解決方案

### 方案 1：調整中間件順序（推薦）
**問題：** 速率限制中間件在 CORS 中間件之前執行
**解決：** 確保 CORS 中間件在速率限制之前執行

```python
# 建議的中間件順序（從後到前）：
# 1. CORSMiddleware（最後添加，最後執行）
# 2. CustomCORSMiddleware（第三個添加）
# 3. RateLimitMiddleware（第二個添加）
# 4. APIKeyMiddleware（最先添加，最先執行）
```

### 方案 2：增加速率限制額度
**問題：** 60 次/分鐘可能不夠
**解決：** 針對開發環境增加速率限制

```python
# 開發環境：增加速率限制
if settings.ENVIRONMENT == "development":
    RATE_LIMIT_PER_MINUTE = 120  # 增加到 120 次/分鐘
```

### 方案 3：優化前端請求策略
**問題：** 前端輪詢頻率過高
**解決：** 
- 使用 WebSocket 或 Server-Sent Events 替代輪詢
- 增加輪詢間隔（從 2 秒改為 3-5 秒）
- 使用 React Query 的 `refetchInterval` 替代手動輪詢

### 方案 4：排除健康檢查和預檢請求
**問題：** OPTIONS 預檢請求也被計入速率限制
**解決：** 在速率限制中間件中排除 OPTIONS 請求

```python
exclude_paths = [
    "/health",
    "/api/v1/health",
    "/docs",
    "/openapi.json",
    "/redoc",
]

# 排除 OPTIONS 請求
if request.method == "OPTIONS":
    return await call_next(request)
```

### 方案 5：統一使用 FastAPI CORSMiddleware
**問題：** 同時使用 FastAPI CORSMiddleware 和自定義 CORS 中間件可能導致衝突
**解決：** 移除自定義 CORS 中間件，只使用 FastAPI 內建的 CORSMiddleware

---

## 📝 相關文件

### 關鍵文件位置：
- **後端 CORS 設定：** `backend/app/main.py` (第 300-333 行)
- **速率限制中間件：** `backend/app/middleware/rate_limit.py`
- **前端 API 客戶端：** `frontend/src/api/client.ts`
- **Dashboard 組件：** `frontend/src/pages/Dashboard.tsx`
- **配置檔案：** `backend/app/config_module.py`

### 相關代碼片段：

#### 後端 CORS 設定：
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=cors_origins_list if cors_origins_list else ["*"],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
    allow_headers=["Content-Type", "Authorization", "X-API-Key", "Accept"],
    expose_headers=["X-RateLimit-Limit", "X-RateLimit-Remaining", "X-RateLimit-Reset"],
)
```

#### 前端 API 請求：
```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8000/api/v1'

async function fetchAPI<T>(endpoint: string, options?: RequestConfig): Promise<T> {
  const url = `${API_BASE_URL}${endpoint}`
  // ... 請求邏輯
}
```

---

## 🎯 預期結果

修復後應該能夠：
1. ✅ 前端成功載入主題列表
2. ✅ 前端成功載入排程數據
3. ✅ Dashboard 顯示正確的統計數據
4. ✅ 沒有 CORS 錯誤
5. ✅ 沒有 429 速率限制錯誤（或錯誤被正確處理）

---

## 📅 問題時間線

- **2026-01-20 13:32** - 首次發現問題
- **2026-01-20 13:33** - 修復 Database 真值測試錯誤
- **2026-01-20 13:34** - 修復 404 錯誤顯示
- **2026-01-20 13:35** - 修復日期處理錯誤
- **2026-01-20 13:36** - 修復速率限制 CORS 問題
- **2026-01-20 13:37** - 調整前端輪詢頻率
- **2026-01-20 13:38** - 問題仍然存在，創建此報告

---

## 🔗 相關資源

- [FastAPI CORS 文檔](https://fastapi.tiangolo.com/tutorial/cors/)
- [React Query 文檔](https://tanstack.com/query/latest)
- [MongoDB Motor 文檔](https://motor.readthedocs.io/)

---

## 📌 待第三方審查的問題

1. **中間件執行順序是否正確？**
   - FastAPI 中間件的執行順序是否會影響 CORS header 的設定？
   - 速率限制中間件在 CORS 中間件之前執行是否會導致問題？

2. **速率限制設定是否合理？**
   - 60 次/分鐘對於開發環境是否足夠？
   - 是否需要針對不同端點設定不同的速率限制？
   - OPTIONS 預檢請求是否應該被計入速率限制？

3. **CORS 設定是否有遺漏？**
   - 是否所有必要的 header 都已設定？
   - 預檢請求（OPTIONS）是否正確處理？
   - 429 響應是否需要 CORS header？（已修復，但需要確認）

4. **前端請求策略是否最佳？**
   - 輪詢是否是最佳方案？
   - 是否有更好的實時更新方案（WebSocket、SSE）？
   - React Query 的自動重試機制是否會加劇速率限制問題？

5. **環境變數配置問題？**
   - 前端是否正確讀取 `VITE_API_URL`？
   - 是否存在環境變數未正確載入的情況？

---

## 🧪 測試建議

### 快速診斷腳本
已提供 PowerShell 診斷腳本 `診斷腳本.ps1`，可自動執行以下檢查：
- 後端服務狀態
- CORS Header 驗證
- 速率限制測試
- 前端服務檢查

**使用方法：**
```powershell
.\診斷腳本.ps1
```

### 測試 1：驗證 CORS Header（PowerShell）
```powershell
# 測試 OPTIONS 預檢請求
Invoke-WebRequest -Uri "http://localhost:8000/api/v1/topics" `
    -Method OPTIONS `
    -Headers @{
        "Origin" = "http://localhost:3000"
        "Access-Control-Request-Method" = "GET"
        "Access-Control-Request-Headers" = "Content-Type"
    } `
    -UseBasicParsing | Select-Object -ExpandProperty Headers

# 預期應該看到：
# Access-Control-Allow-Origin: http://localhost:3000
# Access-Control-Allow-Methods: GET, POST, PUT, PATCH, DELETE, OPTIONS
```

### 測試 2：驗證速率限制（PowerShell）
```powershell
# 快速發送多個請求，測試速率限制
1..70 | ForEach-Object {
    $response = Invoke-WebRequest -Uri "http://localhost:8000/api/v1/topics" `
        -UseBasicParsing `
        -ErrorAction SilentlyContinue
    Write-Host "Request $_: Status $($response.StatusCode)"
    Start-Sleep -Milliseconds 100
}

# 應該在第 61 個請求時看到 429 錯誤
```

### 測試 3：檢查健康檢查端點
```powershell
# 檢查後端健康狀態
Invoke-WebRequest -Uri "http://localhost:8000/health" -UseBasicParsing
Invoke-WebRequest -Uri "http://localhost:8000/api/v1/health" -UseBasicParsing
```

### 測試 3：檢查前端環境變數
在瀏覽器控制台執行：
```javascript
console.log('VITE_API_URL:', import.meta.env.VITE_API_URL)
console.log('API Base URL:', import.meta.env.VITE_API_URL || 'http://localhost:8000/api/v1')
```

### 測試 4：檢查網路請求
在瀏覽器開發者工具的 Network 標籤中：
1. 檢查請求的 `Origin` header 是否為 `http://localhost:3000`
2. 檢查響應的 `Access-Control-Allow-Origin` header
3. 檢查是否有 429 錯誤，以及錯誤響應是否包含 CORS header

---

## 📋 關鍵代碼位置

### 後端
- **CORS 設定：** `backend/app/main.py` (第 314-333 行)
- **速率限制中間件：** `backend/app/middleware/rate_limit.py`
- **配置檔案：** `backend/app/config_module.py` (第 75-88 行)

### 前端
- **API 客戶端：** `frontend/src/api/client.ts`
- **Dashboard 組件：** `frontend/src/pages/Dashboard.tsx`
- **Vite 配置：** `frontend/vite.config.ts` (端口設定為 3000)

---

## 🔍 額外診斷資訊

### 後端日誌關鍵片段
```
設定 CORS，允許的來源: ['http://localhost:5173', 'http://localhost:3000', 'https://ai-agent-webapp-ten.vercel.app']
解析後的 CORS_ORIGINS: ['http://localhost:5173', 'http://localhost:3000', 'https://ai-agent-webapp-ten.vercel.app']
✅ MongoDB 連接成功: ai_agent_webapp
```

### 前端請求示例
```typescript
// Dashboard.tsx 中的請求
const { data: topicsResponse } = useQuery({
  queryKey: ['topics'],
  queryFn: () => topicsAPI.getTopics(),
  // ...
})

const { data: schedules } = useQuery({
  queryKey: ['schedules'],
  queryFn: () => api.getSchedules(),
  // ...
})
```

### 中間件執行流程
```
請求 → APIKeyMiddleware → RateLimitMiddleware → CustomCORSMiddleware → CORSMiddleware → 路由處理器
```

**注意：** FastAPI 中間件是反向執行的，最後添加的中間件最先執行。

---

## 💡 可能的根本原因

1. **中間件執行順序問題**
   - 速率限制中間件在 CORS 中間件之前執行
   - 當速率限制觸發時，直接返回 429，跳過了 CORS header 設定
   - **已修復：** 在速率限制中間件中添加了 CORS header

2. **預檢請求被計入速率限制**
   - OPTIONS 請求也被計入速率限制計數
   - 導致預檢請求失敗，無法通過 CORS 檢查

3. **前端輪詢頻率過高**
   - 每 2 秒發送 2 個請求 = 60 次/分鐘
   - 加上初始載入和其他組件的請求，可能超過限制
   - **已調整：** 從 1 秒改為 2 秒

4. **React Query 自動重試**
   - React Query 的默認重試機制可能導致額外的請求
   - 429 錯誤可能觸發重試，進一步加劇問題

---

**報告生成時間：** 2026-01-20 13:40
**報告版本：** 1.1
**狀態：** 🔴 待解決
**優先級：** 高

