# generate-today API 診斷腳本使用說明

## 📋 快速開始

### 1. 檢查 requests 套件

診斷腳本需要 `requests` 套件。如果尚未安裝，請執行：

```bash
# 使用後端虛擬環境
cd backend
.\venv\Scripts\pip.exe install requests

# 或使用系統 Python
pip install requests
```

### 2. 執行診斷腳本

```bash
# 在專案根目錄執行
python diagnose_generate_today.py
```

或使用後端虛擬環境的 Python：

```bash
cd backend
.\venv\Scripts\python.exe ..\diagnose_generate_today.py
```

## 🎯 診斷結果說明

### ✅ 狀態碼 200
- API 正常運作
- 會顯示返回的 JSON 內容

### ⚠️ 狀態碼 400
- **通常表示：資料庫未連接或請求參數錯誤**
- 會顯示錯誤訊息和修復建議
- **解決方法：**
  - 檢查 MongoDB 是否運行
  - 檢查 `.env` 中的 `MONGODB_URL` 配置
  - 確認 MongoDB 連接字串正確

### ❌ 狀態碼 500
- **伺服器內部錯誤**
- 請查看後端日誌獲取詳細錯誤資訊
- **解決方法：**
  - 查看後端 console 的完整錯誤堆疊
  - 檢查後端日誌文件（如果有的話）

### ❌ 連接失敗
- **後端服務未啟動**
- **解決方法：**
  ```bash
  cd backend
  .\venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
  ```

## 📝 注意事項

1. **確保後端服務正在運行**：診斷前請確認後端服務在 `http://localhost:8000` 運行
2. **檢查後端日誌**：如果返回 500 錯誤，請同時查看後端 console 輸出
3. **網路連接**：確保沒有防火牆或代理阻擋本地連接

## 🔧 進階使用

### 測試強制生成

修改腳本中的請求數據：

```python
response = requests.post(
    API_URL,
    json={"force": True},  # 強制重新生成
    headers={"Content-Type": "application/json"},
    timeout=10
)
```

### 查看詳細錯誤

如果返回 500 錯誤，後端日誌會包含完整的錯誤堆疊追蹤，幫助定位問題。

