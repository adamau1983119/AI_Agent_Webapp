# 🔴 開發人員必讀規則（每日開發前檢查）

> **版本**: v1.0.0  
> **更新日期**: 2026-02-06  
> **適用對象**: 所有開發人員（包括 AI 助手）  
> **強制性**: ⚠️ **違反以下任何規則視為嚴重錯誤**

---

## 📋 快速檢查清單（每日開發前）

在開始任何代碼修改前，請確認：

- [ ] ✅ 已閱讀本文件的所有規則
- [ ] ✅ 已確認當前分支（不是 `main` 或 `develop`）
- [ ] ✅ 已確認文檔更新日期（不超過 7 天）
- [ ] ✅ 已了解今日任務（查看 `工作記錄.md`）
- [ ] ✅ 已了解專案架構（查看 `專案完整架構表.md`）
- [ ] 🔴 **已檢查所有規範和架構要求（規則 0）** - 生成代碼前必須完成

**⚠️ 未完成上述檢查，禁止進行任何代碼修改！**

**🔴 特別提醒**：違反規則 0（擅自生成程式）會導致整個程式崩壞，必須嚴格遵守！

### v7 附加（CTO 鎖定 · 2026-06-05）

> 詳 [`docs/v7_dev_monitoring_discipline.md`](./docs/v7_dev_monitoring_discipline.md)

- **M1**：**禁止** pino／pino-pretty；本機後端僅 **uvicorn :8000**；成本日誌僅 **`log_cost_event`**（`backend/app/utils/logger.py`）。
- **M2**：**禁止**檔案頂部 `// CRITICAL…150` 類註解；**v7 新建** `.py` **≤150 行**，將超必須拆檔。
- **M3**：checklist 結案以 **:8000 / :3000 瀏覽器截圖** 為主；終端 Loguru 僅輔助。

---

## 🔴 核心規則（必須遵守）

### 規則 0：🔴 修改前必須完整了解程式架構（CRITICAL - 2026-02-13 新增）

**AI 助手在修改任何程式碼前必須先詳細了解整個要修正內容的程式，與其他介面的程式，之後再修正，避免整個程式崩壞**

#### 核心原則

- **完全禁止**在未了解程式架構前修改任何程式碼
- **完全禁止**在未檢查依賴關係前修改程式
- **完全禁止**在未確認影響範圍前修改程式
- **未了解程式就修改會導致整個程式崩壞**

#### 必須遵守的檢查流程（修改代碼前）

1. ✅ **必須先讀取相關的程式文件**：
   - 使用 `read_file` 讀取要修改的文件
   - 使用 `codebase_search` 搜尋相關的介面和依賴
   - 使用 `grep` 查找相關的函數、類別、變數使用位置
2. ✅ **必須了解程式架構和依賴關係**：
   - 確認要修改的程式與哪些其他程式有依賴關係
   - 確認修改是否會影響其他介面或功能
   - 確認 API 端點、資料模型、i18n key 等的使用情況
3. ✅ **必須檢查影響範圍**：
   - 使用 `grep` 查找所有使用該函數/類別/變數的地方
   - 確認修改是否會破壞現有功能
   - 確認是否需要同步修改其他文件
4. ✅ **必須驗證修改的正確性**：
   - 確認修改符合專案架構和設計規範
   - 確認修改不會導致循環依賴
   - 確認修改不會破壞現有測試

#### 禁止行為

- ❌ 未讀取相關程式就開始修改
- ❌ 未了解依賴關係就修改程式
- ❌ 未檢查影響範圍就修改程式
- ❌ 未驗證修改正確性就提交
- ❌ 修改後導致整個程式崩壞

#### 正確流程（修改代碼前必須完成）

1. 用戶提出修改需求
2. **AI 必須先詳細了解**：
   - [ ] 已讀取要修改的文件
   - [ ] 已搜尋相關的介面和依賴
   - [ ] 已查找所有使用該程式的地方
   - [ ] 已了解程式架構和依賴關係
   - [ ] 已檢查影響範圍
   - [ ] 已確認修改不會破壞現有功能
3. **如果發現問題**：
   - 必須先說明發現的問題和影響範圍
   - 必須提供完整的修改計劃
   - 不能直接修改導致程式崩壞
4. **執行修改**：
   - 必須確保修改符合所有規範
   - 必須確保修改不會破壞現有功能
   - 必須確保修改後程式能正常運作

#### 違反後果

- 🔴 未了解程式就修改會導致整個程式崩壞
- 🔴 未檢查影響範圍就修改會破壞現有功能
- 🔴 未驗證修改正確性會導致系統錯誤
- 🔴 違反此規則視為嚴重錯誤

#### 檢查清單（每次修改代碼前必須完成）

- [ ] 已讀取要修改的文件
- [ ] 已搜尋相關的介面和依賴
- [ ] 已查找所有使用該程式的地方
- [ ] 已了解程式架構和依賴關係
- [ ] 已檢查影響範圍
- [ ] 已確認修改不會破壞現有功能
- [ ] 已確認修改符合專案架構和設計規範
- [ ] 已確認修改不會導致循環依賴
- [ ] 已確認修改不會破壞現有測試

---

### 規則 0.5：🔴 禁止擅自生成程式代碼（CRITICAL - 2026-02-12 新增）

**AI 助手絕對禁止擅自生成或建議程式代碼，除非已完整檢查所有規範和架構要求**

#### 核心原則

- **完全禁止**在未檢查規範前生成任何程式代碼建議
- **完全禁止**使用不存在的 i18n key、API 端點、函數名稱
- **完全禁止**違反專案架構和設計規範的代碼
- **擅自生成程式會導致整個程式崩潰**

#### 必須遵守的檢查流程（生成代碼前）

1. ✅ **必須先讀取 README.md** 的所有規則（特別是規則 1-12）
2. ✅ **必須先讀取架構文件**：
   - `專案完整架構表.md` - 確認路由、組件結構
   - `按鈕架構表.md` - 確認按鈕 ID 和功能
   - `按鈕測試ID架構表.md` - 確認 data-testid 命名
3. ✅ **必須檢查 i18n key 是否存在**：
   - 使用 `grep` 或 `read_file` 檢查 `frontend/src/i18n/index.ts`
   - 確認所有使用的 `t('xxx')` key 都存在
   - 如果不存在，必須先添加翻譯（三種語言）
4. ✅ **必須檢查 API 端點是否存在**：
   - 確認後端 API 路由是否已實現
   - 確認參數格式是否正確
5. ✅ **必須檢查資料模型**：
   - 確認使用的欄位名稱是否正確
   - 確認資料結構是否符合模型定義
6. ✅ **必須遵守所有規則**：
   - 規則 3：按鈕必須有 data-testid
   - 規則 4：按鈕編碼與架構表同步
   - 規則 6：禁止硬編碼文字
   - 規則 7：禁止使用靜態模板
   - 規則 8：Git 分支規則

#### 禁止行為

- ❌ 未讀取 README 就生成代碼
- ❌ 未檢查 i18n key 就使用 `t('xxx')`
- ❌ 未檢查架構表就新增按鈕或路由
- ❌ 未檢查 API 就調用端點
- ❌ 未檢查資料模型就使用欄位
- ❌ 違反任何既定規範的代碼建議

#### 正確流程（生成代碼前必須完成）

1. 用戶提出需求或問題
2. **AI 必須先讀取並檢查**：
   - [ ] README.md 的所有規則
   - [ ] 相關架構文件
   - [ ] i18n key 是否存在
   - [ ] API 端點是否存在
   - [ ] 資料模型是否匹配
3. **如果發現缺失**：
   - 必須先說明需要添加的內容（例如：i18n key、API 端點）
   - 必須提供完整的添加步驟
   - 不能直接使用不存在的資源
4. **生成代碼建議**：
   - 必須標註所有依賴項（例如：「需先添加 i18n key」）
   - 必須說明執行順序
   - 必須確保符合所有規範

#### 違反後果

- 🔴 擅自生成程式會導致整個程式崩潰
- 🔴 使用不存在的 i18n key 會導致 UI 顯示錯誤
- 🔴 違反架構規範會導致功能無法運作
- 🔴 未檢查就生成代碼視為嚴重錯誤

#### 檢查清單（每次生成代碼建議前必須完成）

- [ ] 已讀取 README.md 的所有規則
- [ ] 已讀取相關架構文件
- [ ] 已檢查所有使用的 i18n key 是否存在
- [ ] 已檢查所有使用的 API 端點是否存在
- [ ] 已檢查所有使用的資料模型欄位是否正確
- [ ] 已確認符合規則 3（按鈕 data-testid）
- [ ] 已確認符合規則 4（架構表同步）
- [ ] 已確認符合規則 6（禁止硬編碼）
- [ ] 已確認符合規則 7（禁止靜態模板）
- [ ] 已確認符合規則 8（Git 分支）
- [ ] 已標註所有依賴項和執行順序

#### 錯誤範例（2026-02-12）

- AI 未檢查 i18n key 就建議使用 `t('dashboard.finance')`
- 實際檢查後發現：該 key 不存在於 `frontend/src/i18n/index.ts`
- 結果：如果直接使用會導致 UI 顯示錯誤或崩潰

#### 正確做法

1. 先使用 `grep` 檢查 i18n key 是否存在
2. 如果不存在，先說明需要添加的翻譯（三種語言）
3. 提供添加步驟
4. 然後再提供使用該 key 的代碼建議

---

### 規則 1：查看真實日期

**每次查看 README 必須確認「更新日期」**

- 如果 README 更新日期超過 7 天，請先查看 `工作記錄.md` 了解最新狀態
- 不要使用過時的文檔進行開發

---

### 規則 2：遵循架構文件

**修改前先閱讀所有架構文件**

必須閱讀的文件（按順序）：
1. `README.md` - 核心價值、規則、禁止操作
2. `專案完整架構表.md` - 路由、組件結構、技術架構
3. `Git分支策略與版本管理.md` - 分支規則、合併流程
4. `工作記錄.md` - 當前狀態、今日任務、進度
5. `品牌設計規範.md` - Lane Crawford 風格
6. `按鈕測試ID架構表.md` - data-testid 命名

**禁止在未閱讀架構文件的情況下修改代碼！**

---

### 規則 3：按鈕必須標記

**所有新增按鈕必須添加 `data-testid` 屬性**

- 參考 `按鈕測試ID架構表.md` 的命名規範
- 格式：`data-testid="{類型}-{位置}-{功能}"`
- 範例：`data-testid="btn-login-submit"`

**禁止新增沒有 `data-testid` 的按鈕！**

---

### 規則 4：🔴 按鈕編碼與架構表同步（強制性）

**所有按鈕和連結必須與架構表保持一致**

#### 新增按鈕時必須：

1. ✅ 在 `按鈕架構表.md` 中定義按鈕 ID（格式：`BTN-{類型}-{頁面}-{序號}`）
2. ✅ 在 `按鈕測試ID架構表.md` 中添加 `data-testid`（格式：`{類型}-{位置}-{功能}`）
3. ✅ 確保按鈕 ID 與 `data-testid` 在兩個文件中都有對應記錄
4. ✅ 在代碼中添加 `data-testid` 屬性

#### 修改按鈕時必須：

1. ✅ 先查閱 `按鈕架構表.md` 確認影響範圍
2. ✅ 更新架構表中的相關資訊
3. ✅ 同步更新 `按鈕測試ID架構表.md`
4. ✅ 確保代碼中的 `data-testid` 與架構表一致

#### 路由連結必須：

1. ✅ 與 `專案完整架構表.md` 中的路由定義一致
2. ✅ 與 `按鈕架構表.md` 中的路由對照表一致
3. ✅ 確保 Sidebar 導航的路徑與 App.tsx 路由定義一致

#### 禁止行為：

- ❌ 新增按鈕但不更新架構表
- ❌ 修改按鈕但不更新架構表
- ❌ 使用架構表中未定義的 `data-testid`
- ❌ 路由連結與架構表不一致

**相關文件**：
- `按鈕架構表.md` - 按鈕 ID、功能、狀態定義
- `按鈕測試ID架構表.md` - `data-testid` 命名規範
- `專案完整架構表.md` - 路由結構定義

---

### 規則 5：🔴 禁止模擬測試（強制性）

**測試必須使用真正的後台和 API**

- ✅ 必須使用真實的後台服務
- ✅ 必須使用真實的 API 端點
- ✅ 所有測試結果必須來自真實服務回應

**禁止行為**：
- ❌ 使用 Mock 數據
- ❌ 使用模板輸入輸出
- ❌ 使用假造的測試數據
- ❌ 跳過真實 API 調用

---

### 規則 6：🔴 禁止硬編碼文字（強制性）

**本專案絕對不允許硬編碼文字（Zero Hard-coded Text）**

#### 核心原則：

- ✅ 所有用戶可見的文字**必須**使用 `i18n` 系統
- ✅ 支援三種語言：繁體中文 (zh-TW)、英文 (en)、日文 (ja)
- ✅ 所有翻譯集中在 `frontend/src/i18n/index.ts`
- ✅ 唯一例外：品牌名稱 `INFLUENCERS` 可硬編碼（不翻譯）

#### 開發流程：

```
1. 在 i18n/index.ts 的 zhTW 對象中添加翻譯 key
2. 在 en 對象中添加對應的英文翻譯
3. 在 ja 對象中添加對應的日文翻譯
4. 在組件中使用 const { t } = useTranslation()
5. 使用 t('your.translation.key') 顯示文字
```

#### 翻譯 Key 命名規範：

```
{模組}.{頁面/功能}.{元素}

範例：
- auth.login.title          → 登入頁標題
- auth.register.submit      → 註冊提交按鈕
- legal.terms.section1.title → 服務條款第一段標題
- nav.dashboard             → 導航列 Dashboard
```

#### 使用範例：

```tsx
// ❌ 錯誤：硬編碼文字
<h1>登入</h1>

// ✅ 正確：使用 i18n
import { useTranslation } from '../i18n';

function Login() {
  const { t } = useTranslation();
  return <h1>{t('auth.login.title')}</h1>;
}
```

**發現硬編碼文字視為 Bug，必須立即修復！**

---

### 規則 7：🔴 禁止使用靜態模板（強制性）

**本專案的核心原則：禁止使用靜態模板**

#### 禁止的做法：

- ❌ 儲存文章模板在資料庫
- ❌ 預設 Caption 範本
- ❌ 固定 Hashtag 列表
- ❌ 靜態風格範本

#### 正確做法：

- ✅ 所有內容必須通過 AI API 即時生成
- ✅ AI 根據內容動態生成 Caption
- ✅ AI 分析內容後即時推薦 Hashtag
- ✅ 從用戶評分學習動態風格檔案

**原因**：避免輸出重複的模板內容，確保每位用戶獲得專屬的個人化內容

---

### 規則 8：Git 分支規則

**永遠不要直接修改 main 分支**

#### 必須遵守：

- ✅ 所有更改必須在 feature 分支進行
- ✅ 修改核心 API 前必須先創建備份分支
- ✅ 修改後端結構前必須先諮詢用戶
- ✅ 測試通過後建立 Tag

#### 受保護的核心文件（修改前必須先備份）：

- `backend/app/main.py`、`config.py`、`database.py`
- `backend/app/api/v1/*.py`
- `frontend/src/api/*.ts`
- `frontend/src/router/*.tsx`

#### 禁止操作：

- ❌ 直接在 `main` 分支上修改
- ❌ 直接在 `develop` 分支上修改
- ❌ 刪除核心目錄結構
- ❌ 清空關鍵文件內容
- ❌ 修改資料庫連接邏輯
- ❌ 修改 API 路由結構

---

### 規則 9：工作記錄管理規範

**本專案只維護一個統一的工作記錄文件**

#### 必須遵守：

- ✅ 所有工作記錄統一寫在 `工作記錄.md`
- ✅ 新的工作內容添加到「工作記錄（按時間倒序）」區段的**最前面**
- ✅ 完成重大功能後，更新「當前狀態總覽」區段
- ✅ 待辦事項寫在工作記錄的「待處理」區段

#### 禁止行為：

- ❌ 建立 `YYYY-MM-DD_工作記錄.md`
- ❌ 建立 `明天待辦事項.md` / `待辦事項_YYYY-MM-DD.md`
- ❌ 建立 `今日工作.md` / `每日記錄.md`
- ❌ 建立其他獨立的記錄文件

---

### 規則 17：🔴 全端多語言自動轉換防護準則（CRITICAL - 2026-08-20 新增）

**AI 助手與開發者在編寫或修改任何生成、搜尋、顯示及 i18n 相關代碼時，必須嚴格遵循多語言生態系四層防護鐵律，嚴禁任何破壞語言轉換的修改。**

#### 四層多語言不可破壞約束（Invariants）

1. **靜態 UI 字典層（Static UI Invariant）**：
   - `frontend/src/i18n/index.ts` 中的 `zh-TW`、`en`、`ja` 三個字典鍵值必須 **100% 同步對齊**，嚴禁單一語言缺漏。
   - 前端 JSX 視圖、按鈕、輸入框 placeholder、toast 提示等**嚴禁出現任何硬編碼文字**，一律調用 `t('xxx')`。
   - `en` 字典不得含中文字元；`ja` 字典不得含未翻譯純繁中漢字。

2. **動態顯示與覆蓋層（Dynamic Overlay Invariant）**：
   - 語言腳本判定（`title_matches_display_language` / `titleMatchesScript`）必須嚴格：**日語標題必須包含假名或短品牌詞；純 CJK 漢字且 0 假名者嚴禁判定為日語**。
   - 快取驗證（`usable_cached_title` / `_pack_ok`）必須傳入目標語系校驗腳本，**語言不符的快取必須自動失效並觸發重新翻譯**。
   - `locale_resolved` 標記只有在標題腳本確實符合目標語言時才允許為 `true`。

3. **AI 生成模組層（AI Generation Invariant - 重點）**：
   - 所有調用 LLM 生成內容的函數（文章、腳本、靈感、分身、頻道助手等），**必須接收並傳遞 `language` 參數**，並在 Prompt 中明確標註輸出語言。
   - AI 回應解析正則**禁止使用單一中文關鍵字**（例如不可寫死 `r'靈感\d+:'`，必須支援 `靈感|Inspiration|Idea|アイディア|アイデア`），並具備寬鬆段落降級容錯，防止外語生成時解析失敗噴空。
   - 生成完畢後必須使用腳本檢驗器確認產物語言正確。

4. **搜尋與查詢層（Multilingual Search Invariant）**：
   - 主題庫與內容查詢時，**禁止僅查詢原生 `title` 欄位**，必須同時對 `titles_i18n.ja`、`titles_i18n.en`、`titles_i18n.zh-TW` 與 `original_title` 進行聯集搜尋，確保任何語系關鍵字均能精準命中。

#### 驗收門禁（原子化 Python Checking 驅動）

- **原子化檢驗流程**：
  1. 靜態字典修改後 ➔ 執行 `python scripts/audit_project_i18n.py`（模組 1）
  2. AI 生成/正則修改後 ➔ 執行 `python -m unittest backend/tests/test_i18n_language_conversion.py`
  3. 資料庫搜尋修改後 ➔ 執行 `python scripts/audit_project_i18n.py`（模組 3）
  4. 提交前總體驗收 ➔ 執行 `python scripts/audit_project_i18n.py --strict`
- 只有當審計與測試 100% 通過（Exit 0）且前端 `npm run build` 無錯誤時，Git Pre-Commit Hook 才會放行。

---

### 規則 18：🔴 控制人員 Keep All 防呆與 3 秒一鍵還原 SOP（2026-08-20 新增）

**在 Cursor IDE 開發中，控制人員若誤按「Keep All」，請遵循以下防呆原則，無須恐慌：**

1. **Keep All 只是「本地暫存」**：
   - 按下 Keep All 只會將變更存入本地工作區（Working Tree），**不會**存入 Git Commit 歷史，更**不會**自動部署至生產站（Railway）。
2. **Git Pre-Commit 物理硬門禁攔截**：
   - 本地已配置 `.git/hooks/pre-commit`。若 AI 產生的代碼破壞了多語言規範或未通過單元測試，控制人員即使執行 `git commit` 也會被 Git 物理攔截並強制中斷。
3. **誤按 Keep All 的 3 秒一鍵還原指令（Undo All）**：
   - 若發現 AI 改錯、或 Python Checking 噴紅字，控制人員只需在專案根目錄終端執行：
     ```powershell
     .\scripts\undo_all.ps1
     ```
     （或直接執行：`git restore . ; git clean -fd`）
   - **效果**：0.5 秒內 100% 清除所有暫存修改，乾淨回到改動前的原始狀態，系統零污染。

---

### 規則 19：🔴 主題卡事實錨定、按需生成與風格解耦防線鐵律（CRITICAL - 2026-08-20 新增）

**主題卡是整個 WebApp 內容生產鏈最核心的資料基石。AI 助手與開發者必須嚴格遵守以下四層生成管線約束：**

#### 1. 公眾主題卡管線約束（Public Topic Generation Invariant）
- **每日生成僅提供多角度資訊卡片**：公眾分類（fashion / food / trend）由排程於 04:00 HKT 自動採集產出，為全體用戶提供客觀多角度的趨勢卡片。
- **嚴禁自動生成全文短文**：ackend/config/topic_generation.yaml 中所有分類之 generate_content 必須為 alse，嚴禁自動浪費 Token 生成不需要的長文。

#### 2. 用戶按需觸發原則（Strict JIT Content Generation Invariant）
- **所有文章與社群貼文生成必須由用戶主動觸發**：嚴禁任何前端組件（如 PostKitPanel 等）在 useEffect 或背景無感知自動調用 LLM 產生文章或仿文。
- **只有當用戶在前端點擊「生成短文」或「生成此平台專屬文案」時，方可發送 API 請求**。

#### 3. 事實上下文錨定（Fact Grounding Invariant）
- 所有後續短文生成、改寫與社群分發（Soul/Shell Layer），**必須以主題的 summary_flash（客觀事實摘要）或 rticle 原文為唯一事實依據**。
- 嚴禁在 Prompt 中僅傳送一句模糊標題而忽略事實上下文，杜絕 LLM 憑空捏造情節。

#### 4. 風格 DNA 與語義解耦防污染（Disentangled Style & Anti-Pollution Invariant）
- **風格 DNA 僅提供語氣、修辭節奏與人設視角**（Tone & Voice only）。
- **嚴禁名詞跨領域污染**：嚴禁將個人 DNA 詞庫中的專用詞彙（如美食探店名詞「串豬大腸、脆皮」）強行套入非相關領域（如財經、政治、科技貿易協定）。
- **Hashtag 相關性**：社群標籤必須優先自主題內容與關鍵字動態提煉，禁止無腦塞入不相關的個人詞庫。

#### 5. 驗收門禁
- 凡修改產卡、Prompt、Alter Ego 或 Post Kit 相關邏輯，提交前必須執行：
  `ash
  python scripts/audit_topic_generation_pipeline.py
  python -m unittest backend/tests/test_ops_alert_card_fix.py
  `
- 只有兩項 100% ALL PASS 方可提交。

---

## 📚 必讀文件清單

在開始開發前，必須按順序閱讀：

| 順序 | 文件 | 目的 | 必須閱讀 |
|:----:|------|------|:--------:|
| 1 | **README.md** | 核心價值、規則、禁止操作 | ✅ 必須 |
| 2 | [專案完整架構表.md](./專案完整架構表.md) | 路由、組件結構、技術架構 | ✅ 必須 |
| 3 | [Git分支策略與版本管理.md](./Git分支策略與版本管理.md) | 分支規則、合併流程 | ✅ 必須 |
| 4 | [工作記錄.md](./工作記錄.md) | 當前狀態、今日任務、進度 | ✅ 必須 |
| 5 | [品牌設計規範.md](./品牌設計規範.md) | Lane Crawford 風格 | ✅ 必須 |
| 6 | [按鈕測試ID架構表.md](./按鈕測試ID架構表.md) | data-testid 命名 | ✅ 必須 |
| 7 | [按鈕架構表.md](./按鈕架構表.md) | 按鈕 ID、功能、狀態定義 | ✅ 必須 |

---

## 🎯 每日開發流程

### 開始開發前：

1. ✅ 閱讀本文件的所有規則
2. ✅ 確認當前分支（不是 `main`/`develop`）
3. ✅ 查看 `工作記錄.md` 了解今日任務
4. ✅ 閱讀相關的架構文件

### 開發過程中：

1. ✅ 新增按鈕時：更新兩個架構表文件
2. ✅ 新增文字時：使用 i18n，添加三語言翻譯
3. ✅ 修改路由時：確認與架構表一致
4. ✅ 測試時：使用真實後台和 API

### 提交代碼前：

1. ✅ 確認所有按鈕都有 `data-testid`
2. ✅ 確認沒有硬編碼文字
3. ✅ 確認架構表已更新
4. ✅ 確認路由與架構表一致
5. ✅ 確認測試使用真實 API

---

## ⚠️ 違規後果

違反以下規則視為嚴重錯誤：

- 🔴 **規則 0：修改前必須完整了解程式架構** - **最嚴重，會導致整個程式崩壞**
- 🔴 **規則 0.5：禁止擅自生成程式代碼** - **最嚴重，會導致整個程式崩潰**
- 🔴 規則 4：按鈕編碼與架構表同步
- 🔴 規則 5：禁止模擬測試
- 🔴 規則 6：禁止硬編碼文字
- 🔴 規則 7：禁止使用靜態模板
- 🔴 規則 8：Git 分支規則

**後果**：
- **規則 0 違反**：擅自生成程式會導致整個程式崩潰，必須立即停止並重新檢查
- 代碼審查不通過
- 必須重新修改
- 可能影響專案進度

---

## 📞 需要幫助？

如有疑問，請：
1. 先查閱相關架構文件
2. 查看 `工作記錄.md` 中的類似案例
3. 詢問專案維護者

---

## 📋 每日開發檢查清單（複製使用）

在開始開發前，複製以下清單並逐項檢查：

```
□ 已閱讀本文件的所有規則
□ 已確認當前分支（不是 main/develop）
□ 已確認文檔更新日期（不超過 7 天）
□ 已了解今日任務（查看 工作記錄.md）
□ 已了解專案架構（查看 專案完整架構表.md）
□ 🔴 已檢查所有規範和架構要求（規則 0）

生成代碼前（規則 0）：
□ 已讀取 README.md 的所有規則
□ 已讀取相關架構文件
□ 已檢查所有使用的 i18n key 是否存在
□ 已檢查所有使用的 API 端點是否存在
□ 已檢查所有使用的資料模型欄位是否正確
□ 已確認符合所有既定規範

開發過程中：
□ 新增按鈕時：更新兩個架構表文件
□ 新增文字時：使用 i18n，添加三語言翻譯
□ 修改路由時：確認與架構表一致
□ 測試時：使用真實後台和 API

提交代碼前：
□ 確認所有按鈕都有 data-testid
□ 確認沒有硬編碼文字
□ 確認架構表已更新
□ 確認路由與架構表一致
□ 確認測試使用真實 API
```

---

## 🔄 版本歷史

| 版本 | 日期 | 變更說明 |
|------|------|----------|
| v1.0.0 | 2026-02-06 | 初始版本 - 從 README 提取所有必須遵守的規則 |

---

**最後更新**：2026-02-06  
**維護者**：開發團隊

