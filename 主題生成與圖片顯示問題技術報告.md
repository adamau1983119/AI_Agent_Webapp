# 主題生成與圖片顯示問題技術報告

## 📋 執行摘要

**核心問題：** 系統的三個基本功能無法正常工作：
1. ❌ **生成主題** - 點擊「立即生成」後，主題無法正常生成或顯示
2. ❌ **按照主題搜尋圖片** - 圖片搜尋功能無法正常工作  
3. ❌ **顯示圖片** - 前端無法正確顯示主題的預覽圖片

**根本原因（已確認）：**
- 🔴 **關鍵問題：** `workflow._search_and_add_images()` 將圖片存儲到 `images` 集合，但**沒有更新主題文檔的 `preview_images` 字段**
- 主題文檔的 `preview_images` 在創建時初始化為空數組 `[]`，之後從未被更新
- 前端無法從主題文檔獲取圖片 URL，只能顯示佔位圖

**建議解決方案：**
- ✅ **方案 A（推薦）：** 在 `_search_and_add_images()` 方法中，圖片搜尋完成後更新主題文檔的 `preview_images` 字段
- ⚠️ **方案 B（替代）：** 前端從 `images` 集合查詢圖片（需要額外的 API 調用）

**影響範圍：** 整個 Dashboard 的核心功能無法使用

**嚴重程度：** 🔴 極高（影響核心業務流程）

**報告時間：** 2026-01-20

---

## 📋 問題概述

**問題描述：** 系統無法正常完成三個核心功能：
1. **生成主題** - 點擊「立即生成」後，主題無法正常生成或顯示
2. **按照主題搜尋圖片** - 圖片搜尋功能無法正常工作
3. **顯示圖片** - 前端無法正確顯示主題的預覽圖片

**影響範圍：** 整個 Dashboard 的核心功能無法使用

**嚴重程度：** 🔴 極高（影響核心業務流程）

**報告時間：** 2026-01-20

---

## 🔍 問題詳情

### 1. 主題生成問題

#### 症狀：
- 點擊「立即生成」按鈕後，後端日誌顯示任務已提交
- 但前端顯示「尚未生成主題」或「0 個主題」
- 進度卡片顯示「0/30」

#### 預期行為：
- 點擊「立即生成」後，應該生成 30 個主題（3 個分類 × 10 個主題）
- 前端應該能立即或稍後顯示生成的主題

#### 實際行為：
- 後端日誌顯示任務已提交，但沒有看到實際生成的主題
- 前端無法顯示主題列表

### 2. 圖片搜尋問題

#### 症狀：
- 主題生成後，圖片搜尋功能無法正常工作
- 主題的 `preview_images` 字段為空數組 `[]`
- 前端顯示佔位圖而非實際圖片

#### 預期行為：
- 主題生成後，應該自動搜尋相關圖片
- 圖片 URL 應該存儲在主題的 `preview_images` 字段中
- 前端應該能顯示實際的圖片

#### 實際行為：
- `preview_images` 字段始終為空
- 前端只能顯示佔位圖

### 3. 圖片顯示問題

#### 症狀：
- 即使後端有圖片數據，前端也無法正確顯示
- 圖片 URL 可能格式不正確或無法訪問
- 圖片載入失敗時沒有正確的錯誤處理

#### 預期行為：
- 前端應該能正確讀取 `preview_images` 或 `preview_images` 字段
- 圖片應該能正常載入和顯示
- 圖片載入失敗時應該顯示佔位圖

#### 實際行為：
- 前端無法讀取圖片數據
- 圖片無法顯示

---

## 🛠️ 已嘗試的修復

### 1. 修復前端日期過濾邏輯 ✅
**問題：** 前端無法正確過濾今日主題
**修復：** 添加了對 `generated_at` 字段的檢查
**狀態：** 已修復，但問題仍然存在

### 2. 優化 React Query 配置 ✅
**問題：** 前端輪詢頻率過高，導致 429 錯誤
**修復：** 調整輪詢間隔為 5 秒，關閉自動重試
**狀態：** 已修復，但主題仍然無法顯示

### 3. 修復 CORS 和速率限制問題 ✅
**問題：** CORS 錯誤和 429 速率限制錯誤
**修復：** 調整中間件順序，排除 OPTIONS 請求，放寬開發環境速率限制
**狀態：** 已修復，但核心功能仍然無法使用

### 4. 優化前端圖片顯示邏輯 ✅
**問題：** 前端無法正確讀取和顯示圖片
**修復：** 添加了對 `previewImages` 和 `preview_images` 的檢查
**狀態：** 已修復，但圖片數據仍然為空

### 5. 添加調試日誌 ✅
**問題：** 無法追蹤問題根源
**修復：** 添加了詳細的調試日誌
**狀態：** 已添加，但問題仍然存在

---

## 🔧 技術環境

### 後端環境
- **框架：** FastAPI
- **資料庫：** MongoDB (Motor)
- **圖片搜尋服務：** DuckDuckGo（預設，不需要 API Key）
- **AI 服務：** DeepSeek（需要 API Key，但可能未配置）

### 前端環境
- **框架：** React 18.2.0 + TypeScript
- **構建工具：** Vite 5.0.8
- **狀態管理：** React Query (@tanstack/react-query)
- **HTTP 客戶端：** Fetch API

---

## 📊 代碼流程分析

### 主題生成流程

```
1. 用戶點擊「立即生成」
   ↓
2. 前端調用 POST /api/v1/schedules/generate-today
   ↓
3. 後端提交背景任務 generate_all_task()
   ↓
4. 背景任務調用 scheduler_service.trigger_manual_generation()
   ↓
5. trigger_manual_generation() 調用 topic_collector.collect_topics()
   ↓
6. 為每個主題創建資料庫記錄
   ↓
7. 調用 workflow.process_topic() 處理主題
   ↓
8. process_topic() 調用 _search_and_add_images() 搜尋圖片
   ↓
9. _search_and_add_images() 調用 image_service.search_images()
   ↓
10. 圖片搜尋結果存儲到 images 集合
   ↓
11. 圖片 URL 應該更新到主題的 preview_images 字段（❓ 問題點）
```

### 圖片搜尋流程

```
1. workflow._search_and_add_images() 被調用
   ↓
2. 從主題內容或標題提取關鍵字
   ↓
3. 調用 image_service.search_images(keywords, count)
   ↓
4. image_service 嘗試多個圖片服務（Unsplash, Pexels, Pixabay, Google, DuckDuckGo）
   ↓
5. 圖片搜尋結果返回
   ↓
6. 圖片數據存儲到 images 集合
   ↓
7. 圖片 URL 應該更新到主題的 preview_images 字段（❓ 問題點）
```

### 前端顯示流程

```
1. Dashboard 組件載入
   ↓
2. 使用 React Query 調用 topicsAPI.getTopics()
   ↓
3. 獲取主題列表
   ↓
4. 過濾今日主題（使用 generated_at 或 created_at）
   ↓
5. TodayTopics 組件接收 topics 數據
   ↓
6. 按分類分組主題
   ↓
7. 嘗試讀取 previewImages 或 preview_images 字段
   ↓
8. 顯示圖片和標題（❓ 問題點：圖片數據為空）
```

---

## 🔍 關鍵代碼位置

### 後端

#### 1. 主題生成 (`backend/app/api/v1/schedules.py`)
```python
# 第 336-370 行：背景任務生成主題
async def generate_all_task():
    for category in [Category.FASHION, Category.FOOD, Category.TREND]:
        topics = await scheduler_service.trigger_manual_generation(
            category=category,
            count=10
        )
```

#### 2. 主題處理 (`backend/app/services/automation/scheduler.py`)
```python
# 第 238-258 行：創建主題並處理
created_topic = await self.topic_repo.create_topic(topic_data)
await self.workflow.process_topic(
    topic_id=topic_id,
    auto_generate_content=True,
    auto_search_images=True,
    image_count=8
)
```

#### 3. 圖片搜尋 (`backend/app/services/automation/workflow.py`)
```python
# 第 224-307 行：搜尋並添加圖片
async def _search_and_add_images(self, topic, count):
    images = await self._search_images_with_retry(search_keywords, count)
    # 圖片存儲到 images 集合
    await self.image_repo.create_image(image_data)
    # ❓ 問題：沒有更新主題的 preview_images 字段
```

#### 4. 主題模型 (`backend/app/models/topic.py`)
```python
# 第 67 行：preview_images 字段定義
preview_images: Optional[List[str]] = Field(default_factory=list, ...)
```

### 前端

#### 1. Dashboard 組件 (`frontend/src/pages/Dashboard.tsx`)
```typescript
// 第 29-43 行：獲取主題列表
const { data: topicsResponse } = useQuery({
  queryKey: ['topics'],
  queryFn: () => topicsAPI.getTopics(),
  refetchInterval: 5000,
})
```

#### 2. TodayTopics 組件 (`frontend/src/components/features/TodayTopics.tsx`)
```typescript
// 第 101-109 行：獲取主題圖片
const getTopicImage = (topic: Topic) => {
  const previewImages = (topic as any).previewImages || (topic as any).preview_images || []
  if (previewImages && previewImages.length > 0) {
    return previewImages[0]
  }
  // 返回佔位圖
}
```

---

## 💡 可能的根本原因

### 1. 圖片 URL 未更新到主題文檔 ⚠️ **已確認的根本原因**

**問題：** 
- `workflow._search_and_add_images()` 將圖片存儲到 `images` 集合（第 304 行）
- **但沒有更新主題文檔的 `preview_images` 字段**
- 主題文檔的 `preview_images` 在創建時初始化為空數組 `[]`（scheduler.py 第 139 行）
- 之後從未被更新

**證據：**
- 代碼檢查確認：`workflow.py` 的 `_search_and_add_images()` 方法只調用 `image_repo.create_image()`
- 沒有調用 `topic_repo.update_topic()` 來更新 `preview_images` 字段
- `grep` 搜索顯示：整個代碼庫中只有初始化 `preview_images = []` 的地方，沒有更新邏輯

**影響：**
- 前端無法從主題文檔獲取圖片 URL
- 前端只能顯示佔位圖
- 圖片數據存在 `images` 集合中，但前端無法直接訪問

**代碼位置：**
- `backend/app/services/automation/workflow.py` 第 224-316 行：`_search_and_add_images()` 方法
- `backend/app/services/automation/scheduler.py` 第 139 行：初始化 `preview_images = []`

### 2. 主題生成失敗但沒有錯誤提示 ⚠️

**問題：**
- 背景任務可能失敗，但錯誤沒有正確記錄或傳遞
- 前端無法知道生成是否成功

**證據：**
- 後端日誌顯示任務已提交，但沒有看到實際生成的主題
- 前端顯示「0 個主題」

**影響：**
- 用戶無法知道生成是否成功
- 無法診斷問題

### 3. 圖片搜尋服務配置問題 ⚠️

**問題：**
- 圖片搜尋服務（Unsplash, Pexels, Pixabay, Google）需要 API Key
- 如果所有服務都失敗，會回退到 DuckDuckGo
- DuckDuckGo 搜尋可能失敗或返回空結果

**證據：**
- 環境變數驗證顯示「所有圖片服務的 API Key 都未設定」
- 系統回退到 DuckDuckGo

**影響：**
- 圖片搜尋可能失敗
- 無法獲取圖片 URL

### 4. 日期字段不匹配 ⚠️

**問題：**
- 後端使用 `generated_at`（下劃線格式）
- 前端可能期望 `generatedAt`（駝峰格式）
- API 響應可能沒有正確轉換字段名稱

**證據：**
- 前端添加了對多種字段名稱的檢查
- 調試日誌顯示日期字段可能為空

**影響：**
- 前端無法正確過濾今日主題
- 主題無法顯示

### 5. 背景任務執行時序問題 ⚠️

**問題：**
- 背景任務是異步執行的
- 前端可能在任務完成前就查詢數據
- React Query 的輪詢可能不夠頻繁

**證據：**
- 後端日誌顯示任務已提交，但沒有看到完成日誌
- 前端輪詢間隔為 5 秒，可能不夠

**影響：**
- 主題生成後無法立即顯示
- 需要手動刷新頁面

---

## 🧪 診斷步驟

### 1. 檢查後端日誌

查看後端日誌中是否有以下信息：
- `🚀 背景任務開始：生成今日所有主題`
- `📝 開始生成 {category} 主題（目標：10 個）...`
- `✅ 生成 {category} 主題完成，共 {count} 個`
- `📊 今日主題生成完成！`

如果沒有看到這些日誌，說明背景任務沒有執行或失敗了。

### 2. 檢查資料庫

直接查詢 MongoDB，確認：
- `topics` 集合中是否有今日生成的主題
- 主題的 `preview_images` 字段是否為空
- `images` 集合中是否有對應的圖片記錄

```javascript
// MongoDB 查詢示例
db.topics.find({
  generated_at: {
    $gte: ISODate("2026-01-20T00:00:00Z"),
    $lte: ISODate("2026-01-20T23:59:59Z")
  }
}).pretty()

// 檢查 preview_images 字段
db.topics.find({
  generated_at: {
    $gte: ISODate("2026-01-20T00:00:00Z"),
    $lte: ISODate("2026-01-20T23:59:59Z")
  }
}, {
  id: 1,
  title: 1,
  preview_images: 1,
  generated_at: 1
}).pretty()
```

### 3. 檢查 API 響應

在瀏覽器開發者工具的 Network 標籤中：
- 查看 `GET /api/v1/topics` 的響應數據
- 確認主題數據的結構
- 確認 `preview_images` 或 `preview_images` 字段是否存在

### 4. 檢查圖片搜尋服務

查看後端日誌中是否有圖片搜尋相關的錯誤：
- `圖片搜尋失敗`
- `ImageSearchError`
- `DuckDuckGo 搜尋失敗`

### 5. 檢查前端控制台

查看瀏覽器控制台中的調試日誌：
- `📊 主題數據調試：`
- `📊 TodayTopics 調試：`
- 日期比較日誌

---

## 📝 相關文件

### 關鍵文件位置：

#### 後端
- **主題生成 API：** `backend/app/api/v1/schedules.py` (第 259-410 行)
- **主題處理服務：** `backend/app/services/automation/scheduler.py` (第 210-271 行)
- **工作流服務：** `backend/app/services/automation/workflow.py` (第 54-307 行)
- **圖片搜尋服務：** `backend/app/services/images/image_service.py`
- **主題模型：** `backend/app/models/topic.py` (第 54-76 行)

#### 前端
- **Dashboard 組件：** `frontend/src/pages/Dashboard.tsx`
- **TodayTopics 組件：** `frontend/src/components/features/TodayTopics.tsx`
- **Topics API：** `frontend/src/api/topics.ts`
- **Topic 類型定義：** `frontend/src/types/index.ts` (第 1-15 行)

---

## 📌 待第三方審查的問題

### 1. 圖片 URL 更新邏輯缺失 ❓ **已確認的關鍵問題**

**問題：** 
`workflow._search_and_add_images()` 將圖片存儲到 `images` 集合，但**沒有更新主題文檔的 `preview_images` 字段**。

**確認：**
- ✅ 代碼檢查確認：`_search_and_add_images()` 方法（workflow.py 第 224-316 行）只調用 `image_repo.create_image()`
- ✅ 沒有調用 `topic_repo.update_topic()` 來更新 `preview_images` 字段
- ✅ 整個代碼庫中沒有更新 `preview_images` 的邏輯

**建議解決方案：**

**方案 A：在圖片搜尋完成後更新主題文檔（推薦）**
```python
# 在 workflow._search_and_add_images() 方法中，圖片搜尋完成後：
# 獲取所有圖片 URL
image_urls = [img.get("url") for img in images[:count] if img.get("url")]

# 更新主題文檔的 preview_images 字段
if image_urls:
    await self.topic_repo.update_topic(
        topic_id,
        {"$set": {"preview_images": image_urls}}
    )
```

**方案 B：前端從 images 集合查詢圖片（替代方案）**
- 前端調用 `/api/v1/images?topic_id={topic_id}` 獲取圖片列表
- 需要添加新的 API 端點或修改現有端點

**推薦：** 方案 A，因為：
- 更符合數據模型設計（主題文檔應該包含預覽圖片）
- 前端查詢更簡單（不需要額外的 API 調用）
- 性能更好（減少數據庫查詢）

### 2. 背景任務錯誤處理不完善 ❓

**問題：**
- 背景任務失敗時，錯誤沒有正確記錄或傳遞給前端
- 前端無法知道生成是否成功

**建議：**
- 添加更詳細的錯誤日誌
- 考慮使用 WebSocket 或 Server-Sent Events 來實時通知前端生成進度
- 或者，添加一個 API 端點來查詢生成任務的狀態

### 3. 圖片搜尋服務配置問題 ❓

**問題：**
- 所有圖片服務的 API Key 都未設定
- 系統回退到 DuckDuckGo，但 DuckDuckGo 搜尋可能失敗

**建議：**
- 確認 DuckDuckGo 搜尋是否正常工作
- 如果 DuckDuckGo 搜尋失敗，應該有明確的錯誤提示
- 考慮添加圖片搜尋的測試端點

### 4. 日期字段轉換問題 ❓

**問題：**
- 後端使用 `generated_at`（下劃線格式）
- 前端可能期望 `generatedAt`（駝峰格式）
- API 響應可能沒有正確轉換字段名稱

**建議：**
- 確認 API 響應的字段名稱格式
- 如果需要，添加字段名稱轉換邏輯
- 或者，統一使用一種命名格式

### 5. 前端數據同步問題 ❓

**問題：**
- 背景任務是異步執行的
- 前端可能在任務完成前就查詢數據
- React Query 的輪詢可能不夠頻繁

**建議：**
- 增加輪詢頻率（從 5 秒改為 2 秒）
- 或者，使用 WebSocket 或 Server-Sent Events 來實時更新
- 或者，在生成任務完成後立即通知前端

---

## 🎯 預期修復後的效果

修復後應該能夠：
1. ✅ 點擊「立即生成」後，後端成功生成 30 個主題
2. ✅ 主題生成後，自動搜尋相關圖片
3. ✅ 圖片 URL 正確存儲在主題的 `preview_images` 字段中
4. ✅ 前端能正確讀取和顯示圖片
5. ✅ 前端能正確顯示主題列表（按分類分組）
6. ✅ 圖片能正常載入和顯示（不是佔位圖）

---

## 📅 問題時間線

- **2026-01-20 13:32** - 首次發現主題無法顯示問題
- **2026-01-20 13:33-13:40** - 修復 CORS、速率限制、日期過濾等問題
- **2026-01-20 14:00** - 修改 UI，要求顯示圖片和標題
- **2026-01-20 14:30** - 發現圖片無法顯示
- **2026-01-20 15:00** - 創建此問題報告

---

## 🔗 相關資源

- [FastAPI 背景任務文檔](https://fastapi.tiangolo.com/tutorial/background-tasks/)
- [React Query 文檔](https://tanstack.com/query/latest)
- [MongoDB Motor 文檔](https://motor.readthedocs.io/)

---

---

## 🔧 快速修復建議

### 修復方案 A：更新主題文檔的 preview_images 字段（推薦）

**文件：** `backend/app/services/automation/workflow.py`

**位置：** `_search_and_add_images()` 方法，第 310-312 行之後

**修改：**
```python
# 在現有代碼之後添加：
logger.info(f"主題 {topic_id} 添加了 {added_count} 張圖片")

# ✅ 新增：更新主題文檔的 preview_images 字段
if added_count > 0:
    try:
        # 獲取所有已添加的圖片 URL
        added_images = await self.image_repo.get_images_by_topic_id(topic_id)
        image_urls = [img.get("url") for img in added_images if img.get("url")]
        
        if image_urls:
            # 更新主題文檔的 preview_images 字段
            await self.topic_repo.update_topic(
                topic_id,
                {"$set": {"preview_images": image_urls}}
            )
            logger.info(f"✅ 已更新主題 {topic_id} 的 preview_images 字段，共 {len(image_urls)} 張圖片")
    except Exception as e:
        logger.error(f"❌ 更新主題 preview_images 失敗: {e}")
        # 不影響主流程，只記錄錯誤

return added_count
```

**優點：**
- 修復簡單，只需添加幾行代碼
- 符合數據模型設計
- 前端無需修改

### 修復方案 B：前端從 images API 獲取圖片（替代方案）

**文件：** `frontend/src/components/features/TodayTopics.tsx`

**修改：**
- 添加新的 API 調用來獲取圖片列表
- 需要後端提供 `/api/v1/images?topic_id={topic_id}` 端點

**缺點：**
- 需要修改前端和後端
- 增加 API 調用次數
- 性能較差

---

**報告生成時間：** 2026-01-20 15:00
**報告版本：** 1.1
**狀態：** 🔴 待解決
**優先級：** 極高

