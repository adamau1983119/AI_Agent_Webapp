# 品牌設計規範 (Brand Design Guidelines)

> **版本**: v1.0.0  
> **更新日期**: 2026-02-03  
> **風格**: Lane Crawford 高端極簡風格

---

## 📋 目錄

1. [品牌識別](#品牌識別)
2. [字體系統](#字體系統)
3. [色彩系統](#色彩系統)
4. [間距與排版](#間距與排版)
5. [組件樣式](#組件樣式)
6. [頁面佈局](#頁面佈局)
7. [程式碼參考](#程式碼參考)

---

## 品牌識別

### 品牌名稱

| 項目 | 內容 | 使用場景 |
|------|------|----------|
| **主標題** | `INFLUENCERS` | 左側黑色區域、移動端標題 |
| **副標題** | - | 已移除，簡化設計 |
| **標語 Slogan** | `AI-POWERED CONTENT CREATION` | 品牌名稱下方 |

### 品牌常數（程式碼）

```typescript
// 統一品牌設定 - 所有頁面必須使用此常數
const BRAND = {
  name: 'INFLUENCERS',
  slogan: 'AI-POWERED CONTENT CREATION',
};
```

### 使用位置

| 頁面 | 檔案 | 品牌顯示 |
|------|------|----------|
| 語言選擇 | `LanguageSelection.tsx` | ✅ BRAND.name + BRAND.slogan |
| 登入 | `Login.tsx` | ✅ BRAND.name + BRAND.slogan |
| 註冊 | `Register.tsx` | ✅ BRAND.name + BRAND.slogan |
| 忘記密碼 | `ForgotPassword.tsx` | ✅ BRAND.name + BRAND.slogan |

---

## 字體系統

### 字體來源

```html
<!-- index.html -->
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500&family=Montserrat:wght@200;300;400;500&display=swap" rel="stylesheet">
```

### Tailwind 配置

```javascript
// tailwind.config.js
fontFamily: {
  'display': ['"Cormorant Garamond"', 'Georgia', 'serif'],
  'sans': ['"Montserrat"', 'system-ui', 'sans-serif'],
}
```

### 字體使用規範

| 類型 | 字體 | Tailwind Class | 用途 |
|------|------|----------------|------|
| **標題字體** | Cormorant Garamond | `font-display` | 品牌名稱、頁面標題 |
| **內文字體** | Montserrat | `font-sans` | 標籤、按鈕、段落文字 |

### 字重規範

| 字重 | Tailwind Class | 用途 |
|------|----------------|------|
| 200 (Extra Light) | `font-extralight` | 特殊強調 |
| 300 (Light) | `font-light` | 主要標題、內文 |
| 400 (Regular) | `font-normal` | 一般文字 |
| 500 (Medium) | `font-medium` | 按鈕、連結 |

---

## 字體樣式表

### 品牌名稱 (INFLUENCERS)

```
桌面版左側：
├── 字體: font-display (Cormorant Garamond)
├── 大小: text-6xl
├── 字重: font-light
├── 字距: tracking-[0.4em]
├── 大小寫: uppercase
└── 顏色: text-white

移動端：
├── 字體: font-display
├── 大小: text-3xl ~ text-4xl
├── 字重: font-light
├── 字距: tracking-[0.3em]
├── 大小寫: uppercase
└── 顏色: text-black
```

### 標語 (AI-POWERED CONTENT CREATION)

```
桌面版左側：
├── 字體: font-sans (Montserrat)
├── 大小: text-xs
├── 字重: font-light
├── 字距: tracking-[0.25em]
├── 大小寫: uppercase
└── 顏色: text-white/60

移動端：
├── 字體: font-sans
├── 大小: text-[10px]
├── 字重: font-light
├── 字距: tracking-[0.2em]
├── 大小寫: uppercase
└── 顏色: text-gray-400
```

### 頁面標題

```
├── 字體: font-display
├── 大小: text-2xl
├── 字重: font-light
├── 字距: tracking-[0.1em]
└── 顏色: text-black
```

### 副標題/說明文字

```
├── 字體: font-sans (預設)
├── 大小: text-xs
├── 字重: font-light
├── 字距: tracking-[0.1em]
├── 大小寫: uppercase
└── 顏色: text-gray-400
```

### 表單標籤

```
├── 字體: font-sans (預設)
├── 大小: text-[10px]
├── 字重: normal
├── 字距: tracking-[0.15em]
├── 大小寫: uppercase
└── 顏色: text-gray-500
```

### 按鈕文字

```
├── 字體: font-sans (預設)
├── 大小: text-[11px]
├── 字重: normal
├── 字距: tracking-[0.2em]
├── 大小寫: uppercase
└── 顏色: text-white (黑底) / text-black (白底)
```

### 輔助文字

```
├── 字體: font-sans (預設)
├── 大小: text-[10px]
├── 字重: font-light
├── 字距: tracking-wide 或 tracking-[0.1em]
└── 顏色: text-gray-400
```

---

## 色彩系統

### 主要色彩

| 名稱 | 色碼 | Tailwind | 用途 |
|------|------|----------|------|
| **背景色** | `#FAF9F7` | `bg-[#FAF9F7]` | 頁面主背景（奶白色） |
| **主要黑** | `#000000` | `bg-black` | 按鈕、文字、左側區域 |
| **純白** | `#FFFFFF` | `text-white` | 白色文字、按鈕文字 |

### 灰階

| 名稱 | Tailwind | 用途 |
|------|----------|------|
| **深灰文字** | `text-gray-500` | 表單標籤 |
| **中灰文字** | `text-gray-400` | 說明文字、副標題 |
| **淺灰文字** | `text-gray-300` | placeholder |
| **邊框** | `border-gray-200` | 輸入框邊框 |
| **分隔線** | `bg-gray-200` | 裝飾分隔線 |

### 狀態色彩

| 狀態 | 色碼 | 用途 |
|------|------|------|
| **錯誤** | `text-red-500`, `border-red-300` | 錯誤訊息、驗證失敗 |
| **成功** | `text-green-600` | 驗證通過指示 |
| **禁用** | `bg-gray-300` | 禁用按鈕 |

---

## 間距與排版

### 間距規範

| 元素 | 間距 |
|------|------|
| 頁面內邊距 | `px-6` (移動端) / `px-8` (桌面端) |
| 標題與內容間距 | `mb-10` ~ `mb-12` |
| 表單欄位間距 | `space-y-6` |
| 標籤與輸入框間距 | `mb-3` |

### 分隔線

```
裝飾分隔線：
├── 寬度: w-16 ~ w-24
├── 高度: h-px
├── 顏色: bg-gray-200 (淺) / bg-black (深)
└── 位置: mx-auto (置中)
```

---

## 組件樣式

### 主要按鈕（黑底）

```css
w-full py-4 bg-black text-white text-[11px] tracking-[0.2em] uppercase 
hover:bg-gray-900 disabled:bg-gray-300 disabled:cursor-not-allowed 
transition-colors duration-300
```

### 次要按鈕（白底黑框）

```css
w-full px-6 py-4 border border-gray-200 hover:border-black 
text-black text-[11px] tracking-[0.15em] uppercase 
transition-all duration-300
```

### 輸入框（底線樣式）

```css
w-full px-0 py-3 bg-transparent border-0 border-b border-gray-200 
text-black placeholder-gray-300 
focus:outline-none focus:border-black 
transition-colors duration-300 text-sm tracking-wide
```

### Checkbox（自訂樣式）

```html
<div className="w-4 h-4 border border-gray-300 
  peer-checked:border-black peer-checked:bg-black 
  transition-all duration-200">
</div>
```

---

## 頁面佈局

### 桌面版分屏佈局

```
┌─────────────────────┬─────────────────────┐
│                     │                     │
│    左側黑色區域      │    右側表單區域      │
│    lg:w-1/2         │    lg:w-1/2         │
│                     │                     │
│    bg-black         │    bg-[#FAF9F7]     │
│                     │                     │
│    品牌名稱          │    表單內容          │
│    標語             │    max-w-sm         │
│    幾何裝飾          │                     │
│                     │                     │
└─────────────────────┴─────────────────────┘
```

### 移動端單欄佈局

```
┌─────────────────────────────────────────────┐
│  Header (返回、語言切換)                      │
├─────────────────────────────────────────────┤
│                                             │
│  品牌名稱 (置中)                              │
│  ─────────                                  │
│  標語                                        │
│                                             │
│  頁面標題                                    │
│  說明文字                                    │
│                                             │
│  表單內容                                    │
│                                             │
├─────────────────────────────────────────────┤
│  Footer (版權)                               │
└─────────────────────────────────────────────┘
```

### 幾何裝飾

```css
/* 45度旋轉方形 */
.absolute.top-1/2.left-1/2.transform.-translate-x-1/2.-translate-y-1/2
.w-[500px].h-[500px].border.border-white.rotate-45.opacity-[0.08]
```

---

## 程式碼參考

### 品牌常數模板

```typescript
// 在每個認證頁面頂部定義
const BRAND = {
  name: 'INFLUENCERS',
  slogan: 'AI-POWERED CONTENT CREATION',
};
```

### 左側裝飾區模板

```tsx
{/* 左側裝飾區 - 桌面版顯示 */}
<div className="hidden lg:flex lg:w-1/2 bg-black items-center justify-center relative overflow-hidden">
  {/* 幾何裝飾 */}
  <div className="absolute inset-0 opacity-[0.08]">
    <div className="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] border border-white rotate-45"></div>
  </div>
  
  <div className="relative z-10 text-center px-12">
    <h1 className="text-white font-display text-6xl font-light tracking-[0.4em] uppercase mb-8">
      {BRAND.name}
    </h1>
    <div className="w-24 h-px bg-white/50 mx-auto mb-8"></div>
    <p className="text-white/60 font-sans text-xs tracking-[0.25em] uppercase font-light">
      {BRAND.slogan}
    </p>
  </div>
</div>
```

### 移動端標題模板

```tsx
{/* 移動端 Logo */}
<div className="lg:hidden text-center mb-12">
  <h1 className="font-display text-3xl font-light tracking-[0.3em] uppercase text-black">
    {BRAND.name}
  </h1>
  <div className="w-16 h-px bg-black mx-auto mt-4 mb-4"></div>
  <p className="text-gray-400 text-[10px] tracking-[0.2em] uppercase font-light">
    {BRAND.slogan}
  </p>
</div>
```

---

## 維護指南

### 新增頁面時

1. 引入 `BRAND` 常數
2. 使用標準佈局模板
3. 遵循字體樣式表
4. 保持色彩一致性

### 修改品牌時

1. 更新此文件的品牌識別章節
2. 搜尋所有使用 `BRAND` 常數的檔案
3. 確保所有頁面同步更新

### 版本紀錄

| 版本 | 日期 | 變更說明 |
|------|------|----------|
| v1.0.0 | 2026-02-03 | 初始版本 - Lane Crawford 風格統一 |

---

> **注意**: 此規範適用於所有認證相關頁面（語言選擇、登入、註冊、忘記密碼）。  
> 其他頁面（Dashboard、Topics 等）可參考此規範但不強制套用。

