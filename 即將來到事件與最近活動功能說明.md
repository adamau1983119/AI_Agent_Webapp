# 即將來到事件與最近活動功能說明

## 📋 概述

這兩個功能是 Dashboard 右側資訊欄的組件，用於顯示系統的重要時間節點和用戶操作歷史。

---

## 🗓️ 即將來到事件 (UpcomingEvents)

### 設計用途

**「即將來到事件」** 用於顯示**未來即將發生的系統事件**，幫助用戶提前了解需要關注的時間節點。

### 預期功能

1. **排程任務提醒**
   - 顯示即將執行的自動排程任務
   - 例如：明天早上 7:00 的「時尚趨勢」主題生成任務
   - 例如：後天中午 12:00 的「美食推薦」主題生成任務

2. **截止日期提醒**
   - 顯示即將到期的內容審核截止時間
   - 顯示即將發布的主題時間

3. **系統維護通知**
   - 顯示預定的系統維護時間
   - 顯示 API 配額重置時間

### 顯示格式

- **標題**：事件名稱（例如：「時尚趨勢主題生成」）
- **日期**：事件發生的日期（例如：「2026-01-14」）
- **時間**：事件發生的具體時間（例如：「07:00」）
- **視覺設計**：左側紫色邊框，突出重要性

### 當前狀態

⚠️ **尚未連接後端 API**

- 組件 UI 已實現
- 數據結構已定義
- 等待後端提供事件數據 API

### 可能的數據來源

1. **排程服務** (`backend/app/services/automation/scheduler.py`)
   - 從排程任務中提取即將執行的任務
   - 顯示未來 7 天內的排程事件

2. **主題模型** (`backend/app/models/topic.py`)
   - 從主題的 `scheduledAt` 字段提取即將發布的主題
   - 顯示待發布的主題時間

---

## 📊 最近活動 (RecentActivities)

### 設計用途

**「最近活動」** 用於顯示**用戶或系統最近執行的操作記錄**，幫助用戶了解系統的運作狀態和歷史操作。

### 預期功能

1. **內容生成記錄**
   - 顯示最近生成的主題
   - 顯示最近生成的文章內容
   - 顯示最近匹配的照片

2. **審核操作記錄**
   - 顯示最近確認的主題
   - 顯示最近拒絕的主題
   - 顯示最近修改的內容

3. **系統操作記錄**
   - 顯示最近執行的排程任務
   - 顯示最近完成的批量操作
   - 顯示最近發生的錯誤或警告

### 顯示格式

- **圖標**：根據活動類型顯示不同圖標
  - 📄 `document`：文檔/內容相關操作
  - 🖼️ `image`：圖片相關操作
  - ✅ `check`：審核/確認相關操作
- **標題**：活動描述（例如：「已生成主題：時尚趨勢」）
- **時間**：活動發生的時間（例如：「2 小時前」）
- **顏色標記**：根據活動類型使用不同顏色
  - 紫色：內容生成類
  - 藍色：圖片操作類
  - 綠色：審核確認類

### 當前狀態

⚠️ **尚未連接後端 API**

- 組件 UI 已實現
- 圖標系統已實現
- 等待後端提供活動日誌 API

### 可能的數據來源

1. **互動記錄** (`backend/app/api/v1/interactions.py`)
   - 從用戶互動記錄中提取最近的操作
   - 顯示用戶的點擊、確認、拒絕等操作

2. **主題歷史** (`backend/app/models/topic.py`)
   - 從主題的 `updatedAt`、`confirmedAt` 等時間戳提取
   - 顯示最近更新的主題

3. **活動日誌表**（可能需要新建）
   - 創建專門的活動日誌集合
   - 記錄所有重要的系統操作和用戶操作

---

## 🔄 與現有功能的關聯

### 與排程服務的關聯

- **即將來到事件** 可以從排程服務 (`scheduler.py`) 中提取即將執行的任務
- 排程服務已經定義了三個時間段的任務：
  - 07:00 HKT - 時尚趨勢
  - 12:00 HKT - 美食推薦
  - 18:00 HKT - 社會趨勢

### 與主題管理的關聯

- **最近活動** 可以從主題的狀態變更中提取
- 當主題從 `pending` 變為 `confirmed` 時，可以記錄為一項活動
- 當主題生成內容或匹配照片時，可以記錄為一項活動

---

## 💡 建議的實現方案

### 方案 1：使用現有數據

**即將來到事件：**
- 從 `schedules` API 中提取未來 7 天內的排程任務
- 從 `topics` API 中提取 `scheduledAt` 在未來的主题

**最近活動：**
- 從 `topics` API 中提取最近更新的主題（按 `updatedAt` 排序）
- 從 `interactions` API 中提取最近的用戶互動記錄

### 方案 2：創建專門的 API

**即將來到事件 API：**
```typescript
GET /api/v1/events/upcoming
Response: {
  events: [
    {
      title: string,
      date: string,
      time: string,
      type: 'schedule' | 'deadline' | 'maintenance'
    }
  ]
}
```

**最近活動 API：**
```typescript
GET /api/v1/activities/recent
Response: {
  activities: [
    {
      icon: 'document' | 'image' | 'check',
      title: string,
      time: string,
      color: 'purple' | 'blue' | 'green'
    }
  ]
}
```

---

## 📍 在 Dashboard 中的位置

這兩個組件位於 Dashboard 頁面的**右側資訊欄**（`lg:col-span-4`），顯示順序為：

1. **為您推薦**（推薦主題）
2. **即將到來的事件** ← 這裡
3. **最近活動** ← 這裡

---

## ✅ 總結

### 即將來到事件
- **用途**：預告未來事件，幫助用戶提前規劃
- **數據來源**：排程任務、主題發布時間
- **狀態**：UI 已實現，等待 API 連接

### 最近活動
- **用途**：記錄操作歷史，幫助用戶了解系統運作
- **數據來源**：主題更新記錄、用戶互動記錄
- **狀態**：UI 已實現，等待 API 連接

---

## 🚀 下一步行動

1. **確定數據來源**：決定是使用現有 API 還是創建新 API
2. **實現後端邏輯**：提取和格式化事件/活動數據
3. **連接前端組件**：使用 `useQuery` 從 API 獲取數據
4. **測試和優化**：確保數據顯示正確，時間格式化合理

