# i18n 硬編碼影響評估報告

## 📋 執行摘要

本報告評估剩餘 7 個硬編碼問題對**下周測試**和**後續修改**的影響程度。

**關鍵結論**：
- ✅ **不影響下周測試**：5 個問題（71%）
- ⚠️ **可能影響下周測試**：2 個問題（29%）
- 🔴 **會影響後續修改**：3 個問題（43%）

---

## 🔍 詳細問題分析

### 問題 1: `frontend/src/api/channels.ts` - categoryLabels

**硬編碼文字**：`"其他（自定義）"` (第 180 行)

**使用場景**：
- 在 `CreateChannel.tsx` 中顯示頻道類別選項
- 用戶在建立頻道時會看到此文字

**影響評估**：

| 評估項目 | 影響程度 | 說明 |
|---------|---------|------|
| **下周測試** | 🟢 **低** | 僅在建立頻道時顯示，已有翻譯鍵 `channels.other` |
| **用戶體驗** | 🟡 **中** | 如果用戶切換語言，此文字不會改變 |
| **後續修改** | 🟡 **中** | 需要重構 `categoryLabels` 為函數，接受語言參數 |

**建議**：
- ✅ **短期（下周測試前）**：可接受，不影響核心功能
- ⚠️ **中期（測試後）**：將 `categoryLabels` 改為函數 `getCategoryLabels(language)`
- 📝 **修復優先級**：中（P2）

**修復方案**：
```typescript
// 當前（硬編碼）
export const categoryLabels: Record<ChannelCategory, string> = {
  other: '其他（自定義）',
};

// 建議（i18n）
export const getCategoryLabels = (t: (key: string) => string): Record<ChannelCategory, string> => ({
  other: t('channels.other'),
});
```

---

### 問題 2-3: `frontend/src/api/topics.ts` - 搜尋驗證錯誤

**硬編碼文字**：
1. `"搜尋關鍵字至少需要 2 個字元"` (第 218 行)
2. `"搜尋關鍵字最多 100 個字元"` (第 221 行)

**使用場景**：
- 用戶在主題搜尋頁面輸入關鍵字時觸發驗證
- 如果輸入不符合規則，會拋出錯誤並顯示給用戶

**影響評估**：

| 評估項目 | 影響程度 | 說明 |
|---------|---------|------|
| **下周測試** | 🔴 **高** | **會直接影響測試**：錯誤訊息只顯示中文，無法測試多語言 |
| **用戶體驗** | 🔴 **高** | 用戶切換語言後，錯誤訊息仍為中文 |
| **後續修改** | 🔴 **高** | 需要將驗證邏輯移到組件層，或創建非 hook 版本的 i18n |

**建議**：
- ⚠️ **短期（下周測試前）**：**建議修復**，會影響多語言測試
- ✅ **中期（測試後）**：必須修復，影響用戶體驗
- 📝 **修復優先級**：高（P1）

**修復方案**：
```typescript
// 方案 1：在組件層驗證（推薦）
// Topics.tsx
const { t } = useTranslation();
if (!query || query.trim().length < 2) {
  throw new Error(t('topics.search.minLength'));
}

// 方案 2：將 i18n 作為參數傳入
export const searchTopics = async (params: SearchParams, t?: (key: string) => string) => {
  const minLengthMsg = t ? t('topics.search.minLength') : '搜尋關鍵字至少需要 2 個字元';
  if (!query || query.trim().length < 2) {
    throw new Error(minLengthMsg);
  }
};
```

---

### 問題 4-5: `frontend/src/components/layout/Header.tsx` - 日期格式模板

**硬編碼文字**：
1. `"yyyy年M月d日 EEEE"` (第 18 行)
2. `"yyyy年M月d日(EEEE)"` (第 20 行)

**使用場景**：
- 用於 `date-fns` 庫的日期格式化
- 這些是**格式模板**，不是用戶直接看到的文字

**影響評估**：

| 評估項目 | 影響程度 | 說明 |
|---------|---------|------|
| **下周測試** | 🟢 **無** | 格式模板不影響功能，日期顯示正常 |
| **用戶體驗** | 🟢 **無** | 用戶看到的是格式化後的日期，不是模板本身 |
| **後續修改** | 🟢 **無** | 這是技術實現細節，不需要 i18n |

**建議**：
- ✅ **短期（下周測試前）**：**可忽略**，不影響測試
- ✅ **中期（測試後）**：**可忽略**，這是格式模板，不是用戶可見文字
- 📝 **修復優先級**：無（P0 - 不需要修復）

**說明**：
- 這些字符串是 `date-fns` 庫的格式模板
- 用戶看到的是格式化後的日期（如 "2026年2月6日 星期五"），不是模板本身
- 可以排除在 i18n 檢查之外

---

### 問題 6: `frontend/src/stores/authStore.ts` - OAuth 認證失敗

**硬編碼文字**：`"OAuth 認證失敗"` (第 183 行)

**使用場景**：
- 當 OAuth 回調失敗時，作為後備錯誤訊息
- 通常後端會返回已翻譯的錯誤訊息（`error.response?.data?.detail`）

**影響評估**：

| 評估項目 | 影響程度 | 說明 |
|---------|---------|------|
| **下周測試** | 🟡 **中** | 只有在後端未返回錯誤訊息時才會顯示，發生機率低 |
| **用戶體驗** | 🟡 **中** | 如果後端錯誤，用戶會看到中文錯誤訊息 |
| **後續修改** | 🟡 **中** | 需要創建非 hook 版本的 i18n，或從全局獲取 |

**建議**：
- ✅ **短期（下周測試前）**：可接受，後端已使用 i18n，此為後備訊息
- ⚠️ **中期（測試後）**：建議修復，完善錯誤處理
- 📝 **修復優先級**：中（P2）

**修復方案**：
```typescript
// 方案 1：從全局 i18n 獲取（需要創建非 hook 版本）
import { getTranslation } from '@/i18n';
const message = error.response?.data?.detail || getTranslation('auth.oauthFailed');

// 方案 2：確保後端總是返回錯誤訊息（推薦）
// 後端已實現，此為額外保障
```

---

### 問題 7: `frontend/src/utils/toast.ts` - showLoading 默認訊息

**硬編碼文字**：`"處理中..."` (第 80 行)

**使用場景**：
- 當調用 `showLoading()` 時，如果未提供訊息，使用此默認值
- 在多個地方使用，如內容生成、圖片搜尋等

**影響評估**：

| 評估項目 | 影響程度 | 說明 |
|---------|---------|------|
| **下周測試** | 🟡 **中** | 如果測試時不傳入訊息，會顯示中文 |
| **用戶體驗** | 🟡 **中** | 用戶切換語言後，默認訊息仍為中文 |
| **後續修改** | 🟡 **中** | 需要修改所有調用處，傳入已翻譯的訊息 |

**建議**：
- ✅ **短期（下周測試前）**：可接受，大部分調用處已傳入訊息
- ⚠️ **中期（測試後）**：建議修復，統一處理
- 📝 **修復優先級**：中（P2）

**修復方案**：
```typescript
// 方案 1：修改所有調用處（推薦）
showLoading(t('common.processing'));

// 方案 2：創建包裝函數
export const showLoadingI18n = (t: (key: string) => string) => {
  return showLoading(t('common.processing'));
};
```

---

## 📊 影響總結表

| 問題 | 文件 | 影響下周測試 | 影響用戶體驗 | 影響後續修改 | 優先級 |
|------|------|------------|------------|------------|--------|
| 1. categoryLabels | channels.ts | 🟢 低 | 🟡 中 | 🟡 中 | P2 |
| 2. 搜尋最小長度 | topics.ts | 🔴 **高** | 🔴 **高** | 🔴 **高** | **P1** |
| 3. 搜尋最大長度 | topics.ts | 🔴 **高** | 🔴 **高** | 🔴 **高** | **P1** |
| 4. 日期格式模板 | Header.tsx | 🟢 無 | 🟢 無 | 🟢 無 | P0 |
| 5. 日期格式模板 | Header.tsx | 🟢 無 | 🟢 無 | 🟢 無 | P0 |
| 6. OAuth 錯誤 | authStore.ts | 🟡 中 | 🟡 中 | 🟡 中 | P2 |
| 7. Loading 訊息 | toast.ts | 🟡 中 | 🟡 中 | 🟡 中 | P2 |

**優先級說明**：
- **P0**: 不需要修復（格式模板）
- **P1**: 高優先級（會影響測試）
- **P2**: 中優先級（建議修復，但不緊急）

---

## 🎯 下周測試影響分析

### ✅ 不會影響測試的問題（5 個）

1. **Header.tsx 日期格式** (2 個)
   - 原因：格式模板，不影響功能
   - 建議：可忽略

2. **channels.ts categoryLabels** (1 個)
   - 原因：僅在建立頻道時顯示，已有翻譯鍵
   - 建議：測試時可接受

3. **authStore.ts OAuth 錯誤** (1 個)
   - 原因：後端已使用 i18n，此為後備訊息
   - 建議：測試時可接受

4. **toast.ts Loading 訊息** (1 個)
   - 原因：大部分調用處已傳入訊息
   - 建議：測試時可接受

### ⚠️ 可能影響測試的問題（2 個）

1. **topics.ts 搜尋驗證錯誤** (2 個)
   - **影響**：如果測試多語言搜尋功能，錯誤訊息只顯示中文
   - **測試場景**：
     - 用戶切換到英文/日文
     - 輸入少於 2 個字元的關鍵字
     - 錯誤訊息仍為中文 ❌
   - **建議**：**測試前修復**，或測試時跳過多語言驗證測試

---

## 🔧 後續修改影響分析

### 🔴 會影響後續修改的問題（3 個）

1. **topics.ts 搜尋驗證** (2 個)
   - **影響**：如果後續要支持更多語言，需要重構驗證邏輯
   - **修改成本**：中（需要將驗證移到組件層）

2. **channels.ts categoryLabels** (1 個)
   - **影響**：如果後續要動態切換語言，需要重構為函數
   - **修改成本**：低（只需改為函數）

### 🟡 可能影響後續修改的問題（2 個）

1. **authStore.ts OAuth 錯誤**
   - **影響**：如果後續要完善錯誤處理，需要支持 i18n
   - **修改成本**：中（需要創建非 hook 版本的 i18n）

2. **toast.ts Loading 訊息**
   - **影響**：如果後續要統一所有默認訊息，需要修改所有調用處
   - **修改成本**：低（只需在調用處傳入翻譯）

---

## 📋 修復建議與時間表

### 下周測試前（緊急）

**必須修復**（P1）：
- ✅ `topics.ts` 搜尋驗證錯誤（2 個問題）
  - **時間**：1-2 小時
  - **方法**：將驗證邏輯移到組件層（Topics.tsx）
  - **影響**：確保多語言測試正常

### 測試後（建議）

**建議修復**（P2）：
- `channels.ts` categoryLabels（1 個問題）
  - **時間**：30 分鐘
  - **方法**：改為函數，接受語言參數

- `authStore.ts` OAuth 錯誤（1 個問題）
  - **時間**：1 小時
  - **方法**：創建非 hook 版本的 i18n，或確保後端總是返回錯誤訊息

- `toast.ts` Loading 訊息（1 個問題）
  - **時間**：1 小時
  - **方法**：修改所有調用處，傳入已翻譯的訊息

### 不需要修復（P0）

- `Header.tsx` 日期格式模板（2 個問題）
  - **原因**：格式模板，不是用戶可見文字
  - **建議**：更新檢查腳本，排除此類問題

---

## 🎯 結論與建議

### 對下周測試的影響

**總體評估**：🟡 **輕微影響**

- ✅ **5 個問題**（71%）不會影響測試
- ⚠️ **2 個問題**（29%）可能影響多語言測試
  - 建議在測試前修復 `topics.ts` 的搜尋驗證錯誤
  - 或測試時跳過多語言驗證測試場景

### 對後續修改的影響

**總體評估**：🟡 **中等影響**

- 🔴 **3 個問題**（43%）會影響後續修改
  - `topics.ts` 搜尋驗證：需要重構驗證邏輯
  - `channels.ts` categoryLabels：需要改為函數
  - 其他問題影響較小

### 最終建議

1. **立即修復**（測試前）：
   - `topics.ts` 搜尋驗證錯誤（2 個問題）

2. **測試後修復**（建議）：
   - `channels.ts` categoryLabels
   - `authStore.ts` OAuth 錯誤
   - `toast.ts` Loading 訊息

3. **不需要修復**：
   - `Header.tsx` 日期格式模板（格式模板，非用戶可見文字）

---

**報告生成時間**：2026-02-06  
**評估基準**：下周測試計劃 + 後續功能擴展需求

