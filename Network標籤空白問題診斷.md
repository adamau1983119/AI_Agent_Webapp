# Network 標籤空白問題診斷

## 🔍 問題描述

Chrome DevTools Network 標籤顯示為空白，沒有看到任何網絡請求。

---

## ✅ 立即檢查步驟

### 步驟 1: 確認 Network 記錄已開啟

1. **檢查記錄按鈕**
   - 在 Network 標籤左上角，應該有一個**紅色圓圈**圖標
   - 如果顯示為**灰色**，點擊它開啟記錄

2. **清除並重新載入**
   - 點擊清除按鈕（圓圈帶斜線圖標）
   - 按 `Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac）硬重新載入
   - 觀察 Network 標籤是否有請求出現

---

### 步驟 2: 檢查過濾器設定

1. **確認過濾器按鈕**
   - 確認 "All" 按鈕已選中（藍色背景）
   - 如果選中了其他類型（如 "Img"、"JS"），可能過濾掉了 API 請求

2. **檢查 Filter 輸入框**
   - 確認 "Filter" 輸入框為空
   - 如果有文字，會過濾掉不符合的請求

3. **檢查 Invert 選項**
   - 確認 "Invert" 複選框未勾選

---

### 步驟 3: 檢查 Console 標籤

1. **切換到 Console 標籤**
   - 查看是否有錯誤訊息
   - 查看是否有 API 請求的日誌

2. **應該看到的日誌**（如果 API 請求失敗）：
   ```
   ❌ API request failed: /topics
   💡 診斷建議：
     1. 檢查後端服務是否運行：...
     2. 檢查 VITE_API_URL 環境變數：...
   ```

---

### 步驟 4: 檢查頁面是否完全載入

1. **確認頁面狀態**
   - 查看頁面是否顯示「載入中...」
   - 查看頁面是否顯示錯誤訊息
   - 查看頁面是否顯示「無法連接到後端服務」

2. **如果顯示錯誤**
   - 按照錯誤訊息的建議檢查
   - 查看 `ConnectionErrorDisplay` 組件顯示的診斷步驟

---

### 步驟 5: 手動觸發 API 請求

**在 Console 中輸入**：
```javascript
// 測試 API 連接
fetch('https://your-backend-domain.railway.app/api/v1/topics?page=1&limit=1')
  .then(r => r.json())
  .then(data => console.log('✅ API 連接成功:', data))
  .catch(err => console.error('❌ API 連接失敗:', err))
```

**將 `your-backend-domain.railway.app` 替換為實際的後端網域**

---

## 🎯 預期應該看到的請求

**正常情況下，Network 標籤應該顯示**：

1. **頁面載入時**：
   ```
   GET /api/v1/topics?page=1&limit=12
   GET /api/v1/schedules
   GET /api/v1/recommendations/user_default?limit=5
   ```

2. **每個請求的詳情**：
   - **Name**: `/api/v1/topics?...`
   - **Status**: `200`（成功）或 `404`/`500`（錯誤）
   - **Type**: `fetch` 或 `xhr`
   - **Size**: 響應大小
   - **Time**: 請求耗時

---

## 🚨 常見問題和解決方案

### 問題 1: Network 記錄未開啟

**症狀**: Network 標籤完全空白

**解決**:
1. 點擊 Network 標籤左上角的紅色圓圈圖標
2. 確認圖標變為紅色（表示記錄已開啟）
3. 重新載入頁面

---

### 問題 2: 請求被過濾掉

**症狀**: 看到其他請求，但沒有 API 請求

**解決**:
1. 點擊 "All" 按鈕（確保所有類型都顯示）
2. 清除 "Filter" 輸入框
3. 確認 "Invert" 未勾選

---

### 問題 3: 後端服務未運行

**症狀**: Console 顯示 "Failed to fetch" 錯誤

**解決**:
1. 檢查後端健康檢查：`https://your-backend-domain.railway.app/health`
2. 檢查 Railway Dashboard 確認服務運行中
3. 檢查 Railway 日誌查看錯誤

---

### 問題 4: API URL 設定錯誤

**症狀**: Console 顯示 404 或 CORS 錯誤

**解決**:
1. 在 Console 輸入：`console.log(import.meta.env.VITE_API_URL)`
2. 確認顯示的 URL 正確
3. 在 Vercel Dashboard 檢查環境變數

---

### 問題 5: React Query 緩存

**症狀**: 第一次載入有請求，但後續沒有

**解決**:
1. 這是正常行為（React Query 會緩存結果）
2. 要強制重新請求，在 Console 輸入：
   ```javascript
   // 清除 React Query 緩存
   window.location.reload()
   ```

---

## 📋 快速診斷清單

- [ ] Network 記錄已開啟（紅色圓圈）
- [ ] 已清除 Network 日誌
- [ ] 已硬重新載入頁面（Ctrl+Shift+R）
- [ ] "All" 過濾器已選中
- [ ] "Filter" 輸入框為空
- [ ] Console 沒有 JavaScript 錯誤
- [ ] 後端健康檢查正常
- [ ] `VITE_API_URL` 環境變數正確

---

## 🔧 如果仍然空白

### 方法 1: 檢查 React Query 是否正常

在 Console 輸入：
```javascript
// 檢查 React Query 是否初始化
console.log('React Query:', window.__REACT_QUERY_STATE__)
```

### 方法 2: 手動觸發請求

在 Console 輸入：
```javascript
// 手動觸發 API 請求
fetch('https://your-backend-domain.railway.app/api/v1/topics?page=1&limit=1')
  .then(r => {
    console.log('Status:', r.status)
    return r.json()
  })
  .then(data => console.log('Data:', data))
  .catch(err => console.error('Error:', err))
```

### 方法 3: 檢查 Network 標籤設定

1. 點擊 Network 標籤右上角的齒輪圖標
2. 確認以下選項：
   - ✅ "Preserve log"（可選，保留日誌）
   - ✅ "Disable cache"（可選，禁用緩存）

---

## 📊 正常 Network 標籤應該顯示

**頁面載入時應該看到**：

1. **HTML 請求**
   ```
   GET /index.html - 200
   ```

2. **JavaScript 請求**
   ```
   GET /assets/index-xxx.js - 200
   GET /assets/vendor-xxx.js - 200
   ```

3. **API 請求**（最重要）
   ```
   GET /api/v1/topics?page=1&limit=12 - 200
   GET /api/v1/schedules - 200
   GET /api/v1/recommendations/user_default?limit=5 - 200 或 404
   ```

---

**最後更新**: 2025-12-30

