# 📋 明天待辦事項 - 2026-01-30（星期五）

> **專案**：Influencers AI Agents v4.0.0  
> **分支**：v4.0.0-development  
> **目標**：完成 API 鑰匙申請，準備 Phase 1 開發

---

## 🎯 主要目標

**完成所有 P0 優先級的 API 鑰匙申請和配置**

---

## ✅ 待辦事項清單

### 🌅 早上（優先處理）

| # | 任務 | 預估時間 | 狀態 |
|:-:|------|:--------:|:----:|
| 1 | **Google OAuth 憑證** | 30 分鐘 | ☐ |
|   | └── 在現有 GCP 專案新增 OAuth 2.0 憑證 | | |
|   | └── 設定 OAuth 同意畫面 | | |
|   | └── 記錄 `GOOGLE_CLIENT_ID` 和 `GOOGLE_CLIENT_SECRET` | | |
| 2 | **Gmail App Password** | 15 分鐘 | ☐ |
|   | └── 啟用兩步驟驗證（如尚未啟用） | | |
|   | └── 建立應用程式密碼 | | |
|   | └── 記錄 `SMTP_PASSWORD` | | |
| 3 | **生成 JWT Secret** | 5 分鐘 | ☐ |
|   | └── `python -c "import secrets; print(secrets.token_hex(32))"` | | |
|   | └── 記錄 `JWT_SECRET` | | |

### 🌞 下午（驗證現有 API）

| # | 任務 | 預估時間 | 狀態 |
|:-:|------|:--------:|:----:|
| 4 | **驗證 DeepSeek API** | 10 分鐘 | ☐ |
|   | └── 確認 API Key 有效 | | |
|   | └── 測試三語生成（繁中/英文/日文） | | |
| 5 | **驗證 Google Custom Search** | 10 分鐘 | ☐ |
|   | └── 確認 API Key 和 CSE ID 有效 | | |
|   | └── 測試搜尋功能 | | |
| 6 | **驗證圖片服務** | 10 分鐘 | ☐ |
|   | └── 確認 Unsplash/Pexels/Pixabay 正常 | | |

### 🌆 傍晚（如有時間）

| # | 任務 | 預估時間 | 狀態 |
|:-:|------|:--------:|:----:|
| 7 | **建立 Meta Developer 帳號** | 30 分鐘 | ☐ |
|   | └── 註冊開發者帳號 | | |
|   | └── 建立應用程式 | | |
| 8 | **建立 TikTok Developer 帳號** | 30 分鐘 | ☐ |
|   | └── 註冊開發者帳號 | | |
|   | └── 建立應用程式 | | |

---

## 📝 詳細步驟指南

### 1️⃣ Google OAuth 憑證（在現有 GCP 專案）

```
步驟：
1. 前往 https://console.cloud.google.com/
2. 選擇現有專案（應該已有 Custom Search）
3. APIs & Services → Library → 啟用 "Google+ API"
4. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
5. 應用程式類型：Web application
6. 名稱：Influencers AI Agents
7. 已授權的 JavaScript 來源：
   - http://localhost:3000
   - http://localhost:5173
8. 已授權的重新導向 URI：
   - http://localhost:8000/api/v1/auth/google/callback
9. 取得 Client ID 和 Client Secret
10. APIs & Services → OAuth consent screen → 設定同意畫面
```

### 2️⃣ Gmail App Password

```
步驟：
1. 前往 https://myaccount.google.com/security
2. 確認已啟用兩步驟驗證
3. 安全性 → 應用程式密碼
4. 選擇應用程式：郵件
5. 選擇裝置：其他（自訂名稱）
6. 輸入名稱：Influencers AI Agents
7. 複製 16 位密碼（記得去除空格）
```

### 3️⃣ 生成 JWT Secret

```powershell
# 方法 1：使用 Python
python -c "import secrets; print(secrets.token_hex(32))"

# 方法 2：使用 OpenSSL
openssl rand -hex 32
```

---

## 📋 完成後需要更新的文件

1. **`backend/.env`** - 新增以下環境變數：
   ```env
   # JWT
   JWT_SECRET=<生成的密鑰>
   JWT_EXPIRE_HOURS=24
   
   # Google OAuth
   GOOGLE_CLIENT_ID=<OAuth Client ID>
   GOOGLE_CLIENT_SECRET=<OAuth Client Secret>
   
   # Gmail SMTP
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=<你的 Gmail>
   SMTP_PASSWORD=<App Password>
   ```

2. **`v4.0.0_API鑰匙申請清單.md`** - 更新狀態追蹤表

---

## ⚠️ 注意事項

1. **Gmail App Password** 需要先啟用兩步驟驗證
2. **OAuth 同意畫面** 需要填寫應用程式名稱和支援郵箱
3. **不要將 API 鑰匙提交到 Git**，確保 `.env` 在 `.gitignore` 中
4. **Meta 和 TikTok** 只是建立帳號，實際開發在 Phase 4-5

---

## 🎯 明天結束時的預期成果

- [ ] Google OAuth 憑證取得並測試
- [ ] Gmail SMTP 配置完成並測試
- [ ] JWT Secret 生成並配置
- [ ] 所有 v3 已有 API 確認正常運作
- [ ] `.env` 文件更新完成
- [ ] `v4.0.0_API鑰匙申請清單.md` 狀態更新

---

## 📅 後續計劃

完成 API 鑰匙配置後，即可開始 **Phase 1: 基礎架構重構**：

| 任務 | 說明 |
|------|------|
| 1.1 | 多語言 i18n 架構設計 |
| 1.2 | 用戶模型擴展（付費欄位、風格檔案） |
| 1.3 | 無限滾動 API 重構 |
| 1.4 | 4 小時排程調整 |
| 1.5 | 15 天資料清理機制 |

---

**加油！明天完成這些，就可以正式開始 v4.0.0 開發了！** 🚀

