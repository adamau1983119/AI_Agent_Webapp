# 問題報告：API 錯誤與資料庫連接問題

## 📋 問題概述

後端 API 在處理主題生成請求時返回 500 內部服務器錯誤，主要與資料庫連接狀態和錯誤處理邏輯相關。

## 🔴 當前錯誤

### 1. generate-today API 返回 500 錯誤

**錯誤端點：**
```
POST http://localhost:8000/api/v1/schedules/generate-today
```

**錯誤訊息：**
```
伺服器內部錯誤 (Internal Server Error)
狀態碼: 500
```

**發生場景：**
- 前端 Dashboard 點擊「立即生成」按鈕
- 調用 `generate-today` API 生成今日所有主題
- 資料庫未連接時（開發環境）

### 2. MongoDB 連接狀態

**當前狀態：**
- 資料庫連接：`disconnected`
- 環境：`development`
- 連接字串：`mongodb://localhost:27017`（本地 MongoDB）

**問題：**
- 本地 MongoDB 服務可能未啟動
- 或 MongoDB Atlas 認證失敗（如果使用雲端）

## 📊 系統架構

### 技術棧
- **後端：** FastAPI (Python 3.13)
- **資料庫：** MongoDB (Motor/AsyncIO)
- **前端：** React + Vite
- **AI 服務：** DeepSeek API

### 相關文件
- `backend/app/api/v1/schedules.py` - 排程 API 端點
- `backend/app/database.py` - 資料庫連接管理（已改良）
- `backend/app/services/automation/scheduler.py` - 排程服務
- `backend/.env` - 環境變數配置

## 🔍 已實施的修復

### 1. MongoDB 連接改良 ✅
- 添加重試機制（最多 3 次，每次間隔 2 秒）
- 環境區分（開發環境允許連接失敗）
- 詳細錯誤處理和日誌記錄
- 連接字串驗證和安全處理

### 2. API 錯誤處理改進 ✅
- `topics` API：資料庫未連接時返回空列表（200 OK）
- `schedules` API：資料庫未連接時返回預設排程數據（200 OK）
- `generate-today` API：已添加資料庫連接檢查和錯誤處理

### 3. 配置完成 ✅
- MongoDB 配置：已設置 `MONGODB_URL=mongodb://localhost:27017`
- DeepSeek API Key：已配置 `DEEPSEEK_API_KEY=sk-9995dc68272e4e2b8406af2caa557cb0`

## 🐛 當前問題詳情

### 問題 1：generate-today API 仍返回 500 錯誤

**錯誤堆疊：**
```
POST /api/v1/schedules/generate-today
→ 500 Internal Server Error
→ 伺服器內部錯誤
```

**可能原因：**
1. 後端服務器未重新載入修復後的代碼
2. `trigger_manual_generation` 函數中仍有未處理的異常
3. 資料庫操作在連接失敗時拋出異常但未被正確捕獲

**當前代碼邏輯：**
```python
# backend/app/api/v1/schedules.py
async def generate_today_all_topics(...):
    try:
        # 檢查資料庫連接
        is_connected = await check_connection()
        if not is_connected:
            # 返回友好錯誤訊息（開發環境）
            return {...}
        
        # 檢查現有主題
        existing_topics, _ = await topic_repo.list_topics(...)
        
        # 在背景任務中生成主題
        async def generate_all_task():
            topics = await scheduler_service.trigger_manual_generation(...)
        
        background_tasks.add_task(generate_all_task)
        return {...}
    except Exception as e:
        # 捕獲所有異常
        raise HTTPException(status_code=500, detail=str(e))
```

### 問題 2：資料庫連接失敗

**錯誤訊息：**
```
MongoDB 連接失敗: bad auth : authentication failed
```

**可能原因：**
1. MongoDB Atlas 認證資訊錯誤
2. IP 白名單未包含當前 IP
3. 本地 MongoDB 服務未啟動
4. 連接字串格式不正確

## 🔧 已嘗試的解決方案

### 方案 1：改良資料庫連接邏輯 ✅
- 添加重試機制
- 環境區分處理
- 詳細錯誤日誌

### 方案 2：改進 API 錯誤處理 ✅
- 添加資料庫連接檢查
- 開發環境友好錯誤訊息
- 捕獲 ConnectionFailure 異常

### 方案 3：配置環境變數 ✅
- 創建 `.env` 文件
- 配置 MongoDB 連接字串
- 配置 DeepSeek API Key

## 📝 需要第三方協助的資訊

### 1. 代碼審查
請檢查以下文件，確認錯誤處理邏輯是否正確：

**文件：** `backend/app/api/v1/schedules.py`
- `generate_today_all_topics` 函數（第 233-350 行）
- 錯誤處理邏輯是否完整
- 變數作用域是否正確

**文件：** `backend/app/services/automation/scheduler.py`
- `trigger_manual_generation` 函數（第 195-256 行）
- 資料庫操作錯誤處理
- 異常傳播機制

### 2. 資料庫連接建議
- 本地 MongoDB：如何確保服務正常運行？
- MongoDB Atlas：如何正確配置連接字串和認證？
- 開發環境：是否應該允許在資料庫未連接時繼續運行？

### 3. 錯誤處理最佳實踐
- 背景任務中的異常應該如何處理？
- 如何確保用戶收到友好的錯誤訊息？
- 開發環境 vs 生產環境的錯誤處理策略

## 🎯 具體問題

### 問題 A：為什麼 generate-today 仍返回 500？

**觀察：**
- 已添加資料庫連接檢查
- 已添加 ConnectionFailure 異常捕獲
- 但 API 仍返回 500 錯誤

**可能原因：**
1. 後端服務器未重新載入代碼（uvicorn --reload 可能未檢測到更改）
2. 異常發生在背景任務中，未被正確處理
3. 其他未預期的異常類型

### 問題 B：如何優雅處理資料庫未連接的情況？

**當前行為：**
- 開發環境：允許系統啟動，但某些功能無法使用
- 生成主題：需要資料庫連接才能保存

**問題：**
- 是否應該在資料庫未連接時完全阻止生成任務？
- 還是應該返回友好的錯誤訊息，說明需要連接資料庫？

### 問題 C：背景任務中的異常處理

**當前實現：**
```python
async def generate_all_task():
    try:
        # 生成主題
        topics = await scheduler_service.trigger_manual_generation(...)
    except Exception as e:
        logger.error(f"生成失敗: {e}")
        # 只記錄錯誤，不影響 API 回應
```

**問題：**
- 背景任務中的異常是否應該影響 API 回應？
- 如何讓用戶知道背景任務執行失敗？

## 💡 建議的解決方案

### 方案 1：確保代碼重新載入
- 手動重啟後端服務器
- 或檢查 uvicorn --reload 是否正常工作

### 方案 2：改進錯誤處理
- 在 `trigger_manual_generation` 中添加更詳細的錯誤處理
- 確保所有資料庫操作都有異常捕獲
- 在開發環境中提供更友好的錯誤訊息

### 方案 3：連接 MongoDB
- 啟動本地 MongoDB 服務
- 或配置正確的 MongoDB Atlas 連接字串

## 📚 相關文件

- `問題報告_MongoDB認證失敗.md` - MongoDB 連接問題詳細報告
- `MongoDB連接改良說明.md` - 已實施的改良說明
- `配置完成說明.md` - 環境變數配置指南

## 🔍 診斷步驟

### 步驟 1：檢查後端日誌
查看後端服務器終端輸出，尋找：
- 錯誤堆疊追蹤
- 異常訊息
- 資料庫連接狀態

### 步驟 2：測試 API
```bash
curl -X POST http://localhost:8000/api/v1/schedules/generate-today \
  -H "Content-Type: application/json" \
  -d '{"force":false}'
```

### 步驟 3：檢查資料庫連接
```python
from app.database import check_connection
is_connected = await check_connection()
print(f"資料庫連接狀態: {is_connected}")
```

## 📌 需要第三方協助的具體問題

1. **代碼邏輯檢查：**
   - `generate_today_all_topics` 函數的錯誤處理是否正確？
   - 是否有遺漏的異常類型？

2. **架構建議：**
   - 背景任務中的異常應該如何處理？
   - 如何讓用戶知道任務執行狀態？

3. **最佳實踐：**
   - 開發環境中，資料庫未連接時應該如何處理？
   - 如何平衡開發便利性和錯誤處理？

4. **調試建議：**
   - 如何快速定位 500 錯誤的根本原因？
   - 應該添加哪些日誌來幫助診斷？

## 🎯 預期結果

修復後應該：
1. ✅ `generate-today` API 不再返回 500 錯誤
2. ✅ 資料庫未連接時返回友好的錯誤訊息（200 或 400 狀態碼）
3. ✅ 前端可以正常顯示錯誤訊息
4. ✅ 用戶了解需要連接資料庫才能生成主題

---

**報告生成時間：** 2026-01-16  
**問題狀態：** 🔴 待解決  
**影響範圍：** 阻止主題生成功能  
**優先級：** 高

