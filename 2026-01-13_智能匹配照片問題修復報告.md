# 2026-01-13 智能匹配照片問題修復報告

**日期：** 2026年1月13日  
**問題：** 智能匹配照片功能無法顯示照片  
**狀態：** ✅ 已修復

---

## 🔍 問題分析

### 問題描述

用戶點擊「智能匹配照片（8張）」按鈕後：
- 控制台顯示 404 錯誤：`/contents/topic_food_20260113032927_0`
- 控制台顯示請求超時：`/v1/contents/topic_food_20260113032927_0/generate`
- 圖片無法顯示，顯示「圖片(0張)」
- 部分圖片顯示「圖片無法載入」（特別是 TikTok 來源的圖片）

### 根本原因

1. **智能匹配照片需要先有內容**
   - 後端 API `/api/v1/images/{topic_id}/match` 需要先獲取主題的內容（article）
   - 如果內容不存在，後端返回 404 錯誤
   - 前端沒有正確處理這個錯誤，導致用戶不知道需要先生成內容

2. **按鈕禁用邏輯不夠嚴格**
   - 前端只檢查 `!content`，但沒有檢查 `content.article` 是否為空
   - 如果內容存在但 article 為空，按鈕仍然可以點擊，導致錯誤

3. **錯誤訊息不夠友好**
   - 後端返回的錯誤訊息不夠明確
   - 前端沒有針對 404 錯誤提供特殊處理和提示

4. **圖片代理問題（次要）**
   - 部分圖片（特別是 TikTok 來源）無法載入
   - 這可能是 URL 驗證或代理端點的問題，但不影響智能匹配功能本身

---

## ✅ 修復方案

### 1. 改進前端錯誤處理

**文件：** `frontend/src/pages/TopicDetail.tsx`

**改進內容：**
- 在 `matchPhotosMutation` 的 `onError` 中檢查 404 錯誤
- 當檢測到 404 錯誤時，顯示更友好的提示訊息：「請先生成內容才能匹配照片。請先點擊「生成內容」按鈕。」
- 其他錯誤仍然顯示原始錯誤訊息

**代碼變更：**
```typescript
onError: (error: any) => {
  // 檢查是否為 404 錯誤（內容不存在）
  const status = error?.status || error?.response?.status
  if (status === 404) {
    const errorDetail = error?.response?.data?.detail || error?.message || ''
    if (errorDetail.includes('主題內容不存在') || errorDetail.includes('內容不存在')) {
      showError('請先生成內容才能匹配照片。請先點擊「生成內容」按鈕。')
    } else {
      showError('主題內容不存在，請先生成內容')
    }
  } else {
    const errorMessage = error?.response?.data?.detail || error?.message || '匹配照片失敗'
    showError(errorMessage)
  }
  console.error('匹配照片失敗:', error)
},
```

### 2. 改進按鈕禁用邏輯

**文件：** `frontend/src/pages/TopicDetail.tsx`

**改進內容：**
- 按鈕禁用條件從 `!content` 改為 `!content || !content?.article`
- 改進按鈕的 `title` 提示文字，更清楚地說明為什麼按鈕被禁用

**代碼變更：**
```typescript
<button
  onClick={() => matchPhotosMutation.mutate(8)}
  disabled={matchPhotosMutation.isPending || !content || !content?.article}
  className="..."
  title={!content || !content?.article ? '請先生成內容才能匹配照片。智能匹配需要根據文章內容來匹配相關圖片。' : '根據文章內容智能匹配相關照片'}
>
  {matchPhotosMutation.isPending ? '匹配中...' : '智能匹配照片（8張）'}
</button>
```

### 3. 改進後端錯誤訊息

**文件：** `backend/app/api/v1/images.py`

**改進內容：**
- 當內容不存在時，返回更友好的錯誤訊息
- 檢查 `article` 是否為空，如果為空也返回明確的錯誤訊息

**代碼變更：**
```python
# 取得文章內容
content = await content_repo.get_content_by_topic_id(topic_id)
if not content:
    raise HTTPException(
        status_code=404,
        detail=f"主題內容不存在，請先生成內容才能匹配照片。主題 ID: {topic_id}"
    )

article_text = content.get("article", "")
if not article_text or not article_text.strip():
    raise HTTPException(
        status_code=400,
        detail="文章內容為空，無法進行智能匹配。請先生成完整的文章內容。"
    )
```

---

## 📋 使用流程

### 正確的使用順序

1. **創建主題** → 主題狀態為 `pending`
2. **生成內容** → 點擊「生成內容（500字文章 + 30秒腳本）」按鈕
3. **等待內容生成完成** → 內容生成可能需要一些時間
4. **智能匹配照片** → 內容生成後，點擊「智能匹配照片（8張）」按鈕
5. **查看匹配結果** → 圖片會自動顯示在圖片區塊

### 錯誤處理

- 如果用戶在沒有內容時點擊「智能匹配照片」按鈕：
  - 按鈕會被禁用（如果前端邏輯正確）
  - 如果按鈕沒有被禁用，會顯示友好的錯誤訊息，提示用戶先生成內容

---

## 🧪 測試建議

### 測試場景 1：正常流程
1. 創建一個新主題
2. 點擊「生成內容」按鈕
3. 等待內容生成完成
4. 點擊「智能匹配照片（8張）」按鈕
5. 確認圖片成功顯示

### 測試場景 2：錯誤處理
1. 創建一個新主題（不生成內容）
2. 嘗試點擊「智能匹配照片（8張）」按鈕
3. 確認按鈕被禁用，或顯示友好的錯誤訊息

### 測試場景 3：內容為空
1. 創建一個新主題
2. 生成內容但內容為空（理論上不應該發生）
3. 嘗試點擊「智能匹配照片（8張）」按鈕
4. 確認顯示適當的錯誤訊息

---

## 📝 相關文件

- `frontend/src/pages/TopicDetail.tsx` - 主題詳情頁面
- `frontend/src/api/images.ts` - 圖片 API 客戶端
- `backend/app/api/v1/images.py` - 圖片 API 端點
- `backend/app/services/images/enhanced_photo_matcher.py` - 智能匹配服務

---

## ⚠️ 已知問題

1. **圖片代理問題**（次要）
   - 部分圖片（特別是 TikTok 來源）無法載入
   - 這可能是 URL 驗證或代理端點的問題
   - 不影響智能匹配功能本身，但會影響圖片顯示

2. **內容生成超時**
   - 控制台顯示內容生成請求超時（10秒）
   - 可能需要增加超時時間或改進後端處理速度

---

## 🎯 下一步行動

1. ✅ 已修復：前端錯誤處理和按鈕禁用邏輯
2. ✅ 已修復：後端錯誤訊息
3. ⏳ 待測試：部署後驗證修復效果
4. ⏳ 待處理：圖片代理問題（如果仍然存在）
5. ⏳ 待處理：內容生成超時問題（如果需要）

---

**修復完成時間：** 2026-01-13  
**修復者：** AI Assistant  
**狀態：** ✅ 已完成，等待部署和測試

