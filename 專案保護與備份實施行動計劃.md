# 專案保護與備份實施行動計劃

**建立日期：** 2026-01-13  
**專案：** AI Agent Webapp for Social Media Content Generation  
**目標：** 完全杜絕 Cursor 亂改專案結構，建立完善的備份與還原機制

---

## 📋 總體目標

1. ✅ 保護核心專案結構，防止意外破壞
2. ✅ 建立多層次備份機制
3. ✅ 實現快速災難恢復
4. ✅ 建立自動化監控和預警

---

## 🎯 階段一：立即實施（今天完成）

### 1.1 設置 Git 分支保護

**目標：** 防止直接修改 main 分支

**步驟：**
1. 訪問 GitHub 倉庫設置
2. 進入 Settings → Branches
3. 添加分支保護規則：
   - Branch name pattern: `main`
   - ✅ Require a pull request before merging
   - ✅ Require approvals: 1
   - ✅ Require status checks to pass before merging
   - ✅ Require branches to be up to date before merging
   - ✅ Do not allow bypassing the above settings

**驗證：**
```bash
# 嘗試直接 push 到 main（應該失敗）
git push origin main
```

**預期結果：** 必須通過 PR 才能合併到 main

---

### 1.2 創建 `.cursorrules` 文件

**目標：** 定義 Cursor AI 的行為規則

**文件位置：** 專案根目錄 `.cursorrules`

**內容：**
```
# Cursor AI 行為規則

## 核心原則
- 永遠不要直接修改 main 分支
- 所有更改必須在 feature 分支進行
- 修改核心 API 前必須先創建備份分支
- 修改後端結構前必須先諮詢用戶

## 受保護的核心文件
以下文件修改前必須先備份：
- backend/app/main.py
- backend/app/config.py
- backend/app/database.py
- backend/app/api/v1/*.py
- frontend/src/api/*.ts
- frontend/src/router/*.tsx

## 禁止操作
- 禁止刪除核心目錄結構
- 禁止清空關鍵文件內容
- 禁止修改資料庫連接邏輯
- 禁止修改 API 路由結構

## 建議工作流
1. 創建 feature 分支
2. 進行修改
3. 創建 PR
4. 等待審查和測試通過
5. 合併到 main
```

---

### 1.3 創建基礎結構驗證腳本

**目標：** 在提交前驗證專案結構

**文件位置：** `scripts/validate_structure.py`

**內容：**
```python
#!/usr/bin/env python3
"""
專案結構驗證腳本
用於檢查核心目錄和文件是否存在
"""
import os
import sys
from pathlib import Path

# 定義專案核心目錄結構
REQUIRED_DIRS = [
    "backend/app/api/v1",
    "backend/app/services",
    "backend/app/models",
    "frontend/src/api",
    "frontend/src/components",
    "frontend/src/pages",
    "frontend/src/types",
]

# 關鍵文件（必須存在且非空）
REQUIRED_FILES = [
    "backend/app/main.py",
    "backend/app/config.py",
    "backend/app/database.py",
    "frontend/src/api/client.ts",
]

def main():
    """驗證專案結構"""
    errors = []
    warnings = []
    
    # 檢查核心目錄
    for dir_path in REQUIRED_DIRS:
        if not os.path.exists(dir_path):
            errors.append(f"❌ 缺少核心目錄: {dir_path}")
        elif not os.path.isdir(dir_path):
            errors.append(f"❌ 路徑不是目錄: {dir_path}")
    
    # 檢查關鍵文件
    for file_path in REQUIRED_FILES:
        if not os.path.exists(file_path):
            errors.append(f"❌ 缺少關鍵文件: {file_path}")
        elif os.path.getsize(file_path) == 0:
            warnings.append(f"⚠️ 關鍵文件為空: {file_path}")
    
    # 輸出結果
    if errors:
        print("❌ 專案結構驗證失敗！")
        for error in errors:
            print(f"  {error}")
        print("\n請確保所有核心目錄和文件都存在。")
        sys.exit(1)
    
    if warnings:
        print("⚠️ 專案結構驗證通過，但有警告：")
        for warning in warnings:
            print(f"  {warning}")
    else:
        print("✅ 專案結構驗證通過，所有核心目錄和文件存在。")
    
    sys.exit(0)

if __name__ == "__main__":
    main()
```

**設置執行權限：**
```bash
chmod +x scripts/validate_structure.py
```

---

### 1.4 設置 Pre-commit Hook

**目標：** 在提交前自動驗證結構

**文件位置：** `.git/hooks/pre-commit`

**內容：**
```bash
#!/bin/sh
# Pre-commit hook: 驗證專案結構

echo "🔍 驗證專案結構..."

# 執行結構驗證腳本
python scripts/validate_structure.py

if [ $? -ne 0 ]; then
    echo "❌ 結構驗證失敗，提交已取消。"
    exit 1
fi

echo "✅ 結構驗證通過"
exit 0
```

**設置執行權限：**
```bash
chmod +x .git/hooks/pre-commit
```

---

## 🎯 階段二：本週完成（1-3 天內）

### 2.1 增強結構驗證腳本

**目標：** 添加 Git 變更檢查和文件大小驗證

**改進內容：**
- 檢查被刪除的文件是否為關鍵文件
- 檢查文件大小異常變化
- 添加白名單機制

**文件：** `scripts/validate_structure_enhanced.py`

---

### 2.2 創建自動備份腳本

**目標：** 在修改關鍵文件前自動備份

**文件位置：** `scripts/auto_backup.ps1` (Windows)

**功能：**
- 檢測關鍵文件變更
- 自動創建備份目錄
- 創建 Git 標籤
- 記錄備份日誌

---

### 2.3 設置 GitHub Actions CI

**目標：** 在 CI 中自動驗證結構

**文件位置：** `.github/workflows/validate-structure.yml`

**功能：**
- 每次 PR 自動驗證結構
- 運行結構驗證腳本
- 報告驗證結果

---

### 2.4 創建備份目錄結構

**目標：** 建立標準化的備份目錄

**結構：**
```
backups/
├── daily/          # 每日備份
├── weekly/         # 每週備份
├── milestones/     # 重要里程碑
└── emergency/      # 緊急備份
```

---

## 🎯 階段三：本月完成（1-2 週內）

### 3.1 設置 Staging 環境

**目標：** 在部署前測試所有更改

**Railway Staging：**
- 創建 `develop` 分支
- 設置自動部署到 staging 環境
- 測試通過後才合併到 main

**Vercel Preview：**
- 每個 PR 自動創建預覽環境
- 測試前端功能

---

### 3.2 實施 E2E 測試

**目標：** 自動驗證關鍵功能

**測試覆蓋：**
- 主題自動生成
- 內容生成
- 智能匹配照片
- 圖片顯示
- 資料庫連接

**工具：** Playwright 或 Cypress

---

### 3.3 設置 MongoDB 自動備份

**目標：** 定期備份資料庫

**設置：**
- MongoDB Atlas 自動快照（每日）
- 保留 30 天快照
- 設置手動導出腳本

---

### 3.4 創建災難恢復文檔

**目標：** 記錄完整的還原流程

**文檔內容：**
- Git 還原步驟
- 資料庫還原步驟
- 部署環境還原步驟
- 緊急聯繫方式

---

## 🎯 階段四：持續改進（長期）

### 4.1 監控和預警

**目標：** 實時監控專案健康狀態

**工具：**
- GitHub Actions 監控關鍵文件變更
- Sentry 監控生產環境錯誤
- Slack/Email 通知

---

### 4.2 定期演練

**目標：** 確保備份和還原流程有效

**頻率：**
- 每季度測試還原流程
- 每年進行完整災難恢復演練

---

### 4.3 優化保護規則

**目標：** 根據實際使用情況調整規則

**方法：**
- 收集誤報數據
- 優化驗證邏輯
- 更新受保護文件清單

---

## 📊 實施時間表

| 階段 | 任務 | 預計時間 | 優先級 |
|------|------|----------|--------|
| 階段一 | Git 分支保護 | 30 分鐘 | 🔴 高 |
| 階段一 | .cursorrules | 15 分鐘 | 🔴 高 |
| 階段一 | 結構驗證腳本 | 1 小時 | 🔴 高 |
| 階段一 | Pre-commit hook | 30 分鐘 | 🔴 高 |
| 階段二 | 增強驗證腳本 | 2 小時 | 🟡 中 |
| 階段二 | 自動備份腳本 | 2 小時 | 🟡 中 |
| 階段二 | GitHub Actions CI | 1 小時 | 🟡 中 |
| 階段三 | Staging 環境 | 3 小時 | 🟢 低 |
| 階段三 | E2E 測試 | 4 小時 | 🟢 低 |
| 階段三 | MongoDB 備份 | 1 小時 | 🟡 中 |
| 階段三 | 災難恢復文檔 | 2 小時 | 🟡 中 |

**總計：** 約 17.5 小時（2-3 個工作日）

---

## ✅ 驗證清單

### 階段一完成後檢查：
- [ ] Git 分支保護已設置
- [ ] `.cursorrules` 文件已創建
- [ ] 結構驗證腳本可以運行
- [ ] Pre-commit hook 可以阻止無效提交
- [ ] 測試：嘗試刪除核心目錄，應該被阻止

### 階段二完成後檢查：
- [ ] 增強驗證腳本可以檢測文件大小變化
- [ ] 自動備份腳本可以正常運行
- [ ] GitHub Actions CI 可以自動驗證
- [ ] 備份目錄結構已建立

### 階段三完成後檢查：
- [ ] Staging 環境可以正常部署
- [ ] E2E 測試可以通過
- [ ] MongoDB 備份已設置
- [ ] 災難恢復文檔已完成

---

## 🚨 緊急情況處理

### 如果專案結構被破壞：

1. **立即停止所有更改**
   ```bash
   git stash
   ```

2. **檢查 Git 歷史**
   ```bash
   git log --oneline -10
   ```

3. **還原到最後一個穩定版本**
   ```bash
   git checkout <stable-commit-hash>
   ```

4. **創建緊急備份**
   ```bash
   git tag emergency-backup-$(date +%Y%m%d-%H%M%S)
   ```

5. **檢查備份目錄**
   ```bash
   ls -la backups/emergency/
   ```

---

## 📝 注意事項

1. **不要跳過階段一**：這是基礎，必須先完成
2. **逐步實施**：不要一次性實施所有功能
3. **測試每個步驟**：確保每個功能都正常工作
4. **文檔化**：記錄所有配置和變更
5. **定期檢查**：每週檢查備份和驗證腳本是否正常

---

## 🎯 成功標準

專案保護機制被認為成功，當：
- ✅ 無法直接修改 main 分支
- ✅ 結構驗證可以阻止無效提交
- ✅ 備份可以成功還原
- ✅ 災難恢復時間 < 1 小時
- ✅ 零誤報率（或 < 5%）

---

**最後更新：** 2026-01-13  
**負責人：** AI Assistant  
**狀態：** 📋 待實施

