# 圖片顯示問題 - 第三方專家審查報告

**報告日期：** 2026年1月12日  
**報告目的：** 請求第三方專家提供專業意見，改善圖片未能顯示的問題  
**報告狀態：** 待專家審查

---

## 📋 執行摘要

### 問題概述

在 AI Agent Webapp 中，圖片搜尋功能能夠成功從 Google Custom Search API 獲取圖片數據，但圖片無法在前端 Dashboard 正確顯示。API 返回的圖片 URL 存在，但瀏覽器無法載入這些圖片。

### 核心問題

1. **API 響應正常：** 後端成功返回圖片數據（包含 URL、尺寸、來源等）
2. **前端接收正常：** 前端成功接收並解析 API 響應
3. **圖片無法顯示：** 瀏覽器無法載入圖片，顯示灰色占位符或錯誤訊息

---

## 🔍 問題詳細描述

### 1. 問題現象

#### 用戶操作流程
1. 用戶在 Dashboard 點擊「搜尋圖片」按鈕
2. 輸入關鍵字（例如：「✨金球獎紅毯直擊！明星最美造型一次看個夠👗」）
3. 選擇圖片來源（例如：「Google Custom Search」）
4. 點擊「搜尋」按鈕

#### 預期結果
- 顯示 10 張圖片縮圖
- 用戶可以點擊圖片選擇並添加到主題

#### 實際結果
- API 成功返回 10 張圖片的數據
- 前端顯示 10 個灰色占位符
- 圖片無法載入，顯示「⚠️ 圖片無法載入」錯誤訊息
- 用戶仍然可以選擇圖片（即使無法顯示）

### 2. API 響應示例

**成功的 API 響應：**
```json
{
    "data": [
        {
            "id": "google_416027",
            "topic_id": "",
            "url": "https://www.tiktok.com/api/img/?itemId=7589087192719297812&location=0&aid=1988",
            "source": "Google Custom Search",
            "photographer": "www.tiktok.com",
            "photographer_url": "https://www.tiktok.com/@girlstyle.my/video/7589087192719297812",
            "license": "Unknown",
            "keywords": ["✨金球獎紅毯直擊！明星最造型一次看個夠👗"],
            "order": 0,
            "width": 1080,
            "height": 1920,
            "fetched_at": "2026-01-12T05:25:15.667473"
        },
        {
            "id": "google_103757",
            "url": "https://lookaside.instagram.com/seo/google_widget/crawler/?media_id=3620065956484150658",
            "source": "Google Custom Search",
            "photographer": "www.instagram.com",
            ...
        }
    ],
    "pagination": {
        "page": 1,
        "limit": 20,
        "total": 10,
        "total_pages": 1
    },
    "source": "Google Custom Search",
    "attempts": [...],
    "trace_id": "7b4bd797"
}
```

**關鍵觀察：**
- API 返回的 URL 類型：
  - TikTok: `https://www.tiktok.com/api/img/?itemId=...`
  - Instagram: `https://lookaside.instagram.com/seo/google_widget/crawler/?media_id=...`
- 這些 URL 不是直接的圖片文件 URL
- 這些 URL 可能需要特殊的請求頭或認證

---

## 🏗️ 技術架構

### 系統架構

```
前端 (Vercel)
  ↓ HTTP Request
後端 API (Railway)
  ↓ API Call
Google Custom Search API
  ↓ Response
後端處理並返回
  ↓ JSON Response
前端接收並顯示
  ↓ <img src={url} />
瀏覽器嘗試載入圖片
  ❌ 失敗（CORS 或 URL 無效）
```

### 技術棧

**前端：**
- React 18.2.0
- TypeScript 5.2.2
- Vite 5.0.8
- React Query 5.12.0

**後端：**
- FastAPI 0.115.0
- Python 3.13
- httpx 0.27.0

**圖片服務：**
- Google Custom Search API
- DuckDuckGo（後備服務）

---

## 🔬 問題分析

### 1. CORS（跨域資源共享）問題

#### 問題描述
TikTok 和 Instagram 的圖片 URL 可能不允許跨域訪問。

#### 證據
- 圖片 URL 來自第三方服務（TikTok、Instagram）
- 這些服務可能有 CORS 限制
- 瀏覽器控制台可能顯示 CORS 錯誤

#### 相關代碼
```typescript
// frontend/src/components/features/ImageSearch.tsx
<img
  src={image.url}
  alt={image.id}
  className="w-full h-full object-cover"
  crossOrigin="anonymous"  // 嘗試使用 CORS
  onError={(e) => {
    setImageError(true)
    // 錯誤處理
  }}
/>
```

### 2. URL 類型問題

#### 問題描述
Google Custom Search 返回的 URL 不是直接的圖片文件 URL，而是：
- API 端點 URL（需要參數）
- 動態生成的 URL（可能過期）
- 需要認證的 URL

#### URL 類型分析

**TikTok URL 格式：**
```
https://www.tiktok.com/api/img/?itemId=7589087192719297812&location=0&aid=1988
```
- 這是 API 端點，不是直接的圖片文件
- 可能需要特定的請求頭
- 可能需要認證

**Instagram URL 格式：**
```
https://lookaside.instagram.com/seo/google_widget/crawler/?media_id=3620065956484150658
```
- 這是 SEO widget 的 crawler URL
- 可能不是直接的圖片 URL
- 可能需要特定的用戶代理或請求頭

### 3. 後端處理邏輯

#### 當前實現
```python
# backend/app/services/images/google_custom_search.py
result.append({
    "id": f"google_{hash(link) % 1000000}",
    "url": link,  # 直接使用 Google 返回的 link
    "thumbnail_url": item.get("image", {}).get("thumbnailLink", link),
    "width": item.get("image", {}).get("width", 0),
    "height": item.get("image", {}).get("height", 0),
    ...
})
```

**問題：**
- 直接使用 `item.get("link")` 作為圖片 URL
- 沒有驗證 URL 是否可以直接訪問
- 沒有處理需要特殊請求頭的 URL

### 4. 前端錯誤處理

#### 當前實現
```typescript
// frontend/src/components/features/ImageSearch.tsx
onError={(e) => {
  setImageLoading(false)
  setImageError(true)
  if (diagnosticMode) {
    console.error('圖片載入失敗:', {
      id: image.id,
      url: image.url,
      source: image.source
    })
  }
}}
```

**改進：**
- 已添加錯誤處理
- 顯示錯誤訊息
- 允許用戶選擇無法載入的圖片

---

## 📊 測試結果

### 1. API 測試結果

**測試腳本：** `backend/test_google_cse_comprehensive.py`

**測試結果：**
- ✅ 所有查詢（cat, dog, sunset, technology）都返回 200 狀態碼
- ✅ 每個查詢都返回 10 張圖片
- ✅ 成功率：100%

**結論：** Google Custom Search API 工作正常

### 2. 前端顯示測試

**測試結果：**
- ✅ API 響應成功接收
- ✅ 數據正確解析
- ❌ 圖片無法載入
- ⚠️ 顯示錯誤訊息：「⚠️ 圖片無法載入」

**結論：** 問題在於圖片 URL 無法在瀏覽器中直接訪問

### 3. 瀏覽器控制台錯誤

**預期錯誤類型：**
- CORS 錯誤：`Access to image at '...' from origin '...' has been blocked by CORS policy`
- 404 錯誤：`Failed to load resource: the server responded with a status of 404`
- 403 錯誤：`Failed to load resource: the server responded with a status of 403`

---

## 🎯 需要專家意見的問題

### 問題 1：CORS 限制

**問題：**
如何處理第三方服務（TikTok、Instagram）的 CORS 限制？

**可能的解決方案：**
1. **後端圖片代理**
   - 後端獲取圖片後轉發給前端
   - 避免 CORS 問題
   - 需要額外的帶寬和存儲

2. **使用 thumbnail_url**
   - Google Custom Search 提供 `thumbnailLink`
   - 可能比原始 URL 更容易訪問
   - 但可能質量較低

3. **過濾 URL**
   - 只返回可以直接訪問的圖片 URL
   - 過濾掉 TikTok、Instagram 等需要認證的 URL
   - 可能減少可用圖片數量

**專家問題：**
- 哪種方案最適合？
- 是否有其他更好的解決方案？
- 如何平衡圖片質量和可訪問性？

### 問題 2：URL 類型處理

**問題：**
如何處理不同類型的圖片 URL（API 端點、動態 URL、需要認證的 URL）？

**可能的解決方案：**
1. **URL 驗證**
   - 在後端驗證 URL 是否可以直接訪問
   - 只返回可訪問的 URL
   - 需要額外的 HTTP 請求

2. **URL 轉換**
   - 嘗試將 API 端點 URL 轉換為直接的圖片 URL
   - 可能需要解析 HTML 或使用其他 API
   - 複雜度較高

3. **使用不同的圖片來源**
   - 優先使用支援直接訪問的來源（Unsplash、Pexels、Pixabay）
   - Google Custom Search 作為備援
   - 需要配置多個 API Keys

**專家問題：**
- 如何識別和處理不同類型的 URL？
- 是否有通用的 URL 驗證方法？
- 如何優化圖片來源的優先順序？

### 問題 3：Google Custom Search 配置

**問題：**
Google Custom Search 的配置是否正確？是否需要調整以獲取更適合的圖片 URL？

**當前配置：**
```python
params = {
    "key": self.api_key,
    "cx": self.search_engine_id,
    "q": keywords,
    "searchType": "image",  # 圖片搜尋
    "num": limit,
    "start": (page - 1) * limit + 1,
    "safe": "active",  # 安全搜尋
}
```

**可能的改進：**
1. **使用專用圖片 CSE**
   - 建立專門用於圖片搜尋的 Custom Search Engine
   - 可能返回更適合的圖片 URL

2. **調整搜尋參數**
   - 使用不同的參數組合
   - 可能影響返回的 URL 類型

3. **使用 Google Images API**
   - 如果可用，使用專門的圖片 API
   - 可能提供更好的圖片 URL

**專家問題：**
- Google Custom Search 的配置是否最優？
- 是否有更好的 Google 圖片搜尋 API？
- 如何配置以獲取最適合的圖片 URL？

### 問題 4：前端顯示策略

**問題：**
在前端，如何處理無法載入的圖片？

**當前實現：**
- 顯示錯誤訊息
- 允許用戶選擇無法載入的圖片
- 使用占位符

**可能的改進：**
1. **後備圖片**
   - 使用 thumbnail_url 作為後備
   - 如果原始 URL 失敗，嘗試 thumbnail_url

2. **圖片預檢**
   - 在顯示前檢查圖片是否可訪問
   - 只顯示可訪問的圖片
   - 需要額外的 HTTP 請求

3. **漸進式載入**
   - 先顯示低質量圖片
   - 然後載入高質量圖片
   - 改善用戶體驗

**專家問題：**
- 哪種策略最適合？
- 如何平衡性能和用戶體驗？
- 是否有其他最佳實踐？

---

## 📝 相關代碼位置

### 後端代碼

1. **Google Custom Search 服務**
   - 文件：`backend/app/services/images/google_custom_search.py`
   - 行數：1-222
   - 關鍵函數：`search_images()`

2. **圖片服務管理器**
   - 文件：`backend/app/services/images/image_service_manager.py`
   - 行數：1-198
   - 關鍵函數：`search_images()`, `_try_provider()`

3. **API 端點**
   - 文件：`backend/app/api/v1/images.py`
   - 行數：68-210
   - 關鍵函數：`search_images()`

### 前端代碼

1. **圖片搜尋組件**
   - 文件：`frontend/src/components/features/ImageSearch.tsx`
   - 行數：26-104（ImageItem 組件）
   - 關鍵函數：圖片載入和錯誤處理

2. **圖片 API 客戶端**
   - 文件：`frontend/src/api/images.ts`
   - 行數：117-178
   - 關鍵函數：`searchImages()`

3. **主題詳情頁面**
   - 文件：`frontend/src/pages/TopicDetail.tsx`
   - 行數：105-113, 293-327
   - 關鍵函數：圖片列表顯示

---

## 🔧 已實施的修復

### 1. 改進錯誤處理

**實施內容：**
- 添加 `ImageItem` 組件處理單個圖片的加載狀態
- 顯示加載指示器
- 顯示錯誤訊息
- 允許用戶選擇無法載入的圖片

**文件：** `frontend/src/components/features/ImageSearch.tsx`

### 2. 添加診斷模式

**實施內容：**
- 診斷模式下顯示詳細錯誤信息
- 在控制台記錄錯誤詳情
- 幫助開發人員診斷問題

**文件：** `frontend/src/components/features/ImageSearch.tsx`

### 3. 改進智能匹配按鈕

**實施內容：**
- 使用 `useMutation` 管理狀態
- 添加加載狀態
- 改進錯誤處理

**文件：** `frontend/src/pages/TopicDetail.tsx`

---

## 📋 問題清單

### 高優先級問題

1. **❓ CORS 限制**
   - 問題：TikTok 和 Instagram 的 URL 無法直接訪問
   - 影響：圖片無法顯示
   - 需要：專家建議解決方案

2. **❓ URL 類型處理**
   - 問題：API 端點 URL 不是直接的圖片文件
   - 影響：瀏覽器無法載入
   - 需要：專家建議處理方法

3. **❓ Google Custom Search 配置**
   - 問題：返回的 URL 類型不適合直接顯示
   - 影響：圖片無法顯示
   - 需要：專家建議配置優化

### 中優先級問題

4. **❓ 圖片來源優先順序**
   - 問題：如何優化圖片來源的選擇
   - 影響：圖片可用性
   - 需要：專家建議策略

5. **❓ 前端顯示策略**
   - 問題：如何處理無法載入的圖片
   - 影響：用戶體驗
   - 需要：專家建議最佳實踐

### 低優先級問題

6. **❓ 性能優化**
   - 問題：如何優化圖片載入性能
   - 影響：用戶體驗
   - 需要：專家建議優化方案

---

## 🎯 期望的專家意見

### 1. 技術建議

- **CORS 問題解決方案**
  - 推薦的技術方案
  - 實施難度和成本
  - 性能和可擴展性考慮

- **URL 處理策略**
  - 如何識別和處理不同類型的 URL
  - 推薦的驗證方法
  - 最佳實踐

### 2. 架構建議

- **系統架構優化**
  - 是否需要後端圖片代理
  - 如何平衡性能和成本
  - 可擴展性考慮

- **圖片來源策略**
  - 如何優化圖片來源的選擇
  - 優先順序建議
  - 備援機制

### 3. 配置建議

- **Google Custom Search 配置**
  - 是否需要調整配置
  - 是否需要專用 CSE
  - 參數優化建議

### 4. 最佳實踐

- **前端圖片處理**
  - 推薦的顯示策略
  - 錯誤處理最佳實踐
  - 用戶體驗優化

---

## 📊 環境信息

### 部署環境

**前端：**
- 平台：Vercel
- URL：https://ai-agent-webapp-ten.vercel.app
- 框架：React + Vite

**後端：**
- 平台：Railway
- URL：https://gentle-enchantment-production-1865.up.railway.app
- 框架：FastAPI

### 配置信息

**Google Custom Search：**
- API Key：已配置 ✅
- Search Engine ID：已配置 ✅
- 測試結果：100% 成功率 ✅

**其他圖片服務：**
- Unsplash：未配置
- Pexels：未配置
- Pixabay：未配置
- DuckDuckGo：已配置（後備服務）

---

## 📎 附件

### 相關文件

1. **測試報告**
   - `backend/google_cse_test_report_20260112_131228.txt`
   - Google Custom Search API 測試結果

2. **修復記錄**
   - `2026-01-08_圖片顯示問題修復報告.md`
   - `2026-01-12_智能匹配按鈕修復報告.md`

3. **代碼文件**
   - `backend/app/services/images/google_custom_search.py`
   - `frontend/src/components/features/ImageSearch.tsx`
   - `frontend/src/api/images.ts`

### 測試數據

**API 響應示例：**
- 包含在報告的「API 響應示例」部分

**錯誤日誌：**
- 需要從瀏覽器控制台獲取
- 建議在診斷模式下測試

---

## ✅ 下一步行動

### 等待專家意見

1. **技術方案評估**
   - 評估專家建議的解決方案
   - 選擇最適合的方案

2. **實施計劃**
   - 制定詳細的實施計劃
   - 分配資源和時間

3. **測試和驗證**
   - 實施後進行測試
   - 驗證解決方案的有效性

---

## 📞 聯繫信息

**項目信息：**
- 項目名稱：AI Agent Webapp for Social Media Content Generation
- GitHub：https://github.com/adamau1983119/AI_Agent_Webapp
- 前端：https://ai-agent-webapp-ten.vercel.app
- 後端：https://gentle-enchantment-production-1865.up.railway.app

**報告生成時間：** 2026年1月12日  
**報告版本：** 1.0  
**報告狀態：** 待專家審查

---

**感謝您花時間審查此報告。我們期待您的專業意見和建議！**

