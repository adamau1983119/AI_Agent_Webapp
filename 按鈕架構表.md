# 按鈕架構表 (Button Architecture Table)

> **版本**: v1.0.0  
> **更新日期**: 2026-02-03  
> **專案**: Influencers AI Agents

---

## 📋 目錄

1. [架構概述](#架構概述)
2. [按鈕分類系統](#按鈕分類系統)
3. [導航類按鈕](#1-導航類按鈕-navigation)
4. [認證類按鈕](#2-認證類按鈕-authentication)
5. [表單提交按鈕](#3-表單提交按鈕-form-submission)
6. [操作類按鈕](#4-操作類按鈕-actions)
7. [輔助類按鈕](#5-輔助類按鈕-utility)
8. [狀態與回饋規範](#狀態與回饋規範)
9. [測試檢查清單](#測試檢查清單)

---

## 架構概述

### 按鈕命名規則

```
BTN-{類型}-{頁面/組件}-{序號}

類型代碼:
- NAV: 導航類
- AUTH: 認證類
- FORM: 表單提交
- ACT: 操作類
- UTL: 輔助類
```

### 路由對照表

| 路由 | 頁面 | 說明 |
|------|------|------|
| `/` | RootRedirect | 根路徑重定向 |
| `/language` | LanguageSelection | 語言選擇（無Layout） |
| `/login` | Login | 登入頁（無Layout） |
| `/register` | Register | 註冊頁（無Layout） |
| `/forgot-password` | ForgotPassword | 忘記密碼（無Layout） |
| `/oauth-callback` | OAuthCallback | OAuth 回調（無Layout） |
| `/dashboard` | Dashboard | 控制面板 |
| `/topics` | Topics | 主題列表 |
| `/topics/:id` | TopicDetail | 主題詳情 |
| `/channels` | Channels | 頻道管理 |
| `/channels/create` | CreateChannel | 建立頻道 |
| `/inspiration` | Inspiration | 靈感探索 |
| `/style-profile` | StyleProfile | 風格檔案 |
| `/social-connect` | SocialConnect | 社群連結 |
| `/publish` | Publish | 發布內容 |
| `/preferences` | Preferences | 偏好設定 |
| `/schedule` | Schedule | 排程管理 |
| `/settings` | Settings | 帳號設定 |

---

## 按鈕分類系統

```
按鈕
├── 導航類 (Navigation)
│   ├── Sidebar 選單項目
│   ├── Header 連結
│   └── 頁面內跳轉
│
├── 認證類 (Authentication)
│   ├── 登入/登出
│   ├── 註冊
│   └── OAuth
│
├── 表單提交 (Form Submission)
│   ├── 登入表單
│   ├── 註冊表單
│   └── 編輯表單
│
├── 操作類 (Actions)
│   ├── CRUD 操作
│   ├── 內容生成
│   └── 圖片管理
│
└── 輔助類 (Utility)
    ├── 顯示/隱藏
    ├── 篩選/搜尋
    └── 分頁控制
```

---

## 1. 導航類按鈕 (Navigation)

### 1.1 Sidebar 選單

| 按鈕 ID | 名稱 | 路由 | i18n Key | 組件 | 權限 |
|---------|------|------|----------|------|------|
| BTN-NAV-SIDE-001 | Logo/首頁 | `/` | - | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-002 | 控制面板 | `/dashboard` | `nav.dashboard` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-003 | 主題列表 | `/topics` | `nav.topics` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-004 | 頻道管理 | `/channels` | `nav.channels` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-005 | 靈感探索 | `/inspiration` | `nav.inspiration` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-006 | 風格檔案 | `/style-profile` | `nav.styleProfile` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-007 | 發布內容 | `/publish` | `nav.publish` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-008 | 社群連結 | `/social-connect` | `nav.socialConnect` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-009 | 偏好設定 | `/preferences` | `nav.settings` | `Sidebar.tsx` | 全部 |
| BTN-NAV-SIDE-010 | 排程管理 | `/schedule` | `nav.schedule` | `Sidebar.tsx` | 全部 |

### 1.2 Header 連結

| 按鈕 ID | 名稱 | 路由/功能 | i18n Key | 組件 | 權限 |
|---------|------|-----------|----------|------|------|
| BTN-NAV-HDR-001 | 漢堡選單 | `toggleSidebar()` | - | `Header.tsx` | 全部 |
| BTN-NAV-HDR-002 | 設定 | `/settings` | `nav.settings` | `Header.tsx` | 會員 |
| BTN-NAV-HDR-003 | 登入 | `/login` | `nav.login` | `Header.tsx` | 訪客 |
| BTN-NAV-HDR-004 | 註冊 | `/register` | `nav.register` | `Header.tsx` | 訪客 |

### 1.3 頁面內跳轉

| 按鈕 ID | 名稱 | 路由 | 組件 | 說明 |
|---------|------|------|------|------|
| BTN-NAV-PAGE-001 | 返回頭條 | `/dashboard`（或 `navigate(-1)`） | `TopicDetail.tsx` | 從詳情頁回頭條，保留分類 Tab |
| BTN-NAV-PAGE-002 | 返回登入 | `/login` | `ForgotPassword.tsx` | 忘記密碼頁返回 |
| BTN-NAV-PAGE-003 | 返回登入(設定) | `/login` | `Settings.tsx` | 未登入提示跳轉 |

---

## 2. 認證類按鈕 (Authentication)

### 2.1 登入頁面 (`Login.tsx`)

| 按鈕 ID | 名稱 | 功能 | i18n Key | 狀態 | 回饋 |
|---------|------|------|----------|------|------|
| BTN-AUTH-LOGIN-001 | 顯示/隱藏密碼 | `setShowPassword()` | `common.show/hide` | 正常 | 切換圖示 |
| BTN-AUTH-LOGIN-002 | 登入 | `handleSubmit()` → API | `auth.login.submit` | 正常/Loading/禁用 | 成功→Dashboard / 錯誤提示 |
| BTN-AUTH-LOGIN-003 | Google 登入 | `handleGoogleLogin()` → OAuth | `auth.login.google` | 正常/Loading | 跳轉 Google → OAuth Callback |
| BTN-AUTH-LOGIN-004 | 訪客瀏覽 | `navigate('/topics')` | `auth.login.guestMode` | 正常 | 跳轉主題列表 |

### 2.2 註冊頁面 (`Register.tsx`)

| 按鈕 ID | 名稱 | 功能 | i18n Key | 狀態 | 回饋 |
|---------|------|------|----------|------|------|
| BTN-AUTH-REG-001 | Google 註冊 | `handleGoogleRegister()` → OAuth | - | 正常/Loading | 跳轉 Google |
| BTN-AUTH-REG-002 | 提交註冊 | `handleSubmit()` → API | `auth.register.submit` | 正常/Loading/禁用 | 成功提示 / 錯誤提示 |

### 2.3 語言選擇頁面 (`LanguageSelection.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-AUTH-LANG-001 | 繁體中文 | `handleLanguageSelect('zh-TW')` | 正常/選中 | 設定語言 → `/login` |
| BTN-AUTH-LANG-002 | English | `handleLanguageSelect('en')` | 正常/選中 | 設定語言 → `/login` |
| BTN-AUTH-LANG-003 | 日本語 | `handleLanguageSelect('ja')` | 正常/選中 | 設定語言 → `/login` |
| BTN-AUTH-LANG-004 | 重置語言 | `localStorage.clear()` | 正常 | 重新載入頁面 |

### 2.4 登出按鈕

| 按鈕 ID | 名稱 | 功能 | i18n Key | 組件 | 回饋 |
|---------|------|------|----------|------|------|
| BTN-AUTH-LOGOUT-001 | 登出 (Sidebar) | `logout()` → `/login` | `nav.logout` | `Sidebar.tsx` | 跳轉登入頁 |
| BTN-AUTH-LOGOUT-002 | 登出 (Header) | `handleLogout()` | `nav.logout` | `Header.tsx` | 跳轉登入頁 |
| BTN-AUTH-LOGOUT-003 | 登出 (Settings) | `handleLogout()` | `nav.logout` | `Settings.tsx` | 跳轉登入頁 |

### 2.5 忘記密碼 (`ForgotPassword.tsx`)

| 按鈕 ID | 名稱 | 功能 | i18n Key | 狀態 | 回饋 |
|---------|------|------|----------|------|------|
| BTN-AUTH-FORGOT-001 | 返回 | `navigate('/login')` | `common.back` | 正常 | 跳轉登入頁 |
| BTN-AUTH-FORGOT-002 | 切換語言 | `setLanguage()` | - | 正常 | 切換語言 |
| BTN-AUTH-FORGOT-003 | 提交重設 | `handleSubmit()` | `auth.resetPassword` | 正常/Loading/禁用 | 成功/錯誤提示 |

---

## 3. 表單提交按鈕 (Form Submission)

### 3.1 搜尋相關

| 按鈕 ID | 名稱 | 功能 | 組件 | 狀態 |
|---------|------|------|------|------|
| BTN-FORM-SEARCH-001 | 清除搜尋 | `setSearchQuery('')` | `Header.tsx` | 正常 |
| BTN-FORM-SEARCH-002 | 圖片搜尋提交 | `handleSearch()` | `ImageSearch.tsx` | 正常/禁用(空關鍵字) |

### 3.2 編輯相關

| 按鈕 ID | 名稱 | 功能 | 組件 | 狀態 | 回饋 |
|---------|------|------|------|------|------|
| BTN-FORM-EDIT-001 | 取消編輯 | `onClose()` | `TopicEditor.tsx` | 正常 | 關閉彈窗 |
| BTN-FORM-EDIT-002 | 儲存編輯 | `updateMutation.mutate()` | `TopicEditor.tsx` | 正常/Loading/禁用 | 成功/錯誤 Toast |

### 3.3 設定相關

| 按鈕 ID | 名稱 | 功能 | 組件 | i18n Key | 狀態 |
|---------|------|------|------|----------|------|
| BTN-FORM-SET-001 | 儲存個人資料 | `handleSaveProfile()` | `Settings.tsx` | - | 正常/Loading |
| BTN-FORM-SET-002 | 更換頭像 | (未實作) | `Settings.tsx` | `profile.changeAvatar` | 正常 |
| BTN-FORM-SET-003 | 儲存密碼 | (未實作) | `Settings.tsx` | `common.save` | 正常 |

---

## 4. 操作類按鈕 (Actions)

### 4.1 主題操作 (`TopicDetail.tsx`)

| 按鈕 ID | 名稱 | 功能 | 權限 | 狀態 | 回饋 |
|---------|------|------|------|------|------|
| BTN-ACT-TOPIC-001 | 編輯主題 | `setShowEditor(true)` | 會員 | 正常 | 開啟編輯器 |
| BTN-ACT-TOPIC-002 | 確認主題 | `confirmMutation.mutate()` | 會員 | 正常/Loading/禁用 | 成功 Toast |
| BTN-ACT-TOPIC-003 | 刪除主題 | `setShowDeleteConfirm(true)` | 會員 | 正常 | 開啟確認彈窗 |
| BTN-ACT-TOPIC-004 | 確認刪除 | `deleteMutation.mutate()` | 會員 | 正常/Loading | 成功 → 返回列表 |
| BTN-ACT-TOPIC-005 | 取消刪除 | `setShowDeleteConfirm(false)` | 會員 | 正常 | 關閉彈窗 |

### 4.2 內容生成 (`TopicDetail.tsx`)

| 按鈕 ID | 名稱 | 功能 | 權限 | 狀態 | 回饋 |
|---------|------|------|------|------|------|
| BTN-ACT-CONTENT-001 | 生成內容 | `generateContentMutation.mutate()` | 會員 | 正常/Loading/禁用 | 生成中 → 顯示內容 |
| BTN-ACT-CONTENT-002 | 重新生成 | `regenerateContentMutation.mutate()` | 會員 | 正常/Loading/禁用 | 重新生成 |

### 4.3 圖片操作

| 按鈕 ID | 名稱 | 功能 | 組件 | 狀態 | 回饋 |
|---------|------|------|------|------|------|
| BTN-ACT-IMG-001 | 新增圖片 | `setShowImageSearch(true)` | `TopicDetail.tsx` | 正常 | 開啟搜尋 |
| BTN-ACT-IMG-002 | 智能匹配 | `matchPhotosMutation.mutate()` | `TopicDetail.tsx` | 正常/Loading/禁用(無內容) | 匹配圖片 |
| BTN-ACT-IMG-003 | 手動搜尋 | `setShowImageSearch(true)` | `TopicDetail.tsx` | 正常 | 開啟搜尋 |
| BTN-ACT-IMG-004 | 選擇圖片 | `onSelect()` | `ImageSearch.tsx` | 正常/Loading | 添加圖片 |
| BTN-ACT-IMG-005 | 關閉搜尋 | `onClose()` | `ImageSearch.tsx` | 正常 | 關閉彈窗 |
| BTN-ACT-IMG-006 | 建議關鍵字 | `setKeywords(keyword)` | `ImageSearch.tsx` | 正常 | 填入關鍵字 |
| BTN-ACT-IMG-007 | 驗證圖片 | `validateMutation.mutate()` | `ImageGallery.tsx` | 正常/Loading | 驗證結果 |
| BTN-ACT-IMG-008 | 批量匹配 | `matchMutation.mutate()` | `ImageGallery.tsx` | 正常/Loading | 匹配結果 |
| BTN-ACT-IMG-009 | 開始排序 | `handleStartReorder()` | `ImageGallery.tsx` | 正常 | 進入排序模式 |
| BTN-ACT-IMG-010 | 取消排序 | `handleCancelReorder()` | `ImageGallery.tsx` | 正常 | 退出排序 |
| BTN-ACT-IMG-011 | 儲存排序 | `handleSaveReorder()` | `ImageGallery.tsx` | 正常/Loading | 儲存成功 |
| BTN-ACT-IMG-012 | 上移圖片 | `onMoveUp(index)` | `ImageGallery.tsx` | 正常 | 位置交換 |
| BTN-ACT-IMG-013 | 下移圖片 | `onMoveDown(index)` | `ImageGallery.tsx` | 正常 | 位置交換 |
| BTN-ACT-IMG-014 | 預覽圖片 | `onPreview()` | `ImageGallery.tsx` | 正常 | 開啟預覽 |
| BTN-ACT-IMG-015 | 刪除圖片 | `onDelete(image.id)` | `ImageGallery.tsx` | 正常 | 移除圖片 |
| BTN-ACT-IMG-016 | 關閉預覽 | `onClose()` | `ImagePreview.tsx` | 正常 | 關閉彈窗 |

### 4.4 互動按鈕 (`InteractionButtons.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-INT-001 | 喜歡 | `handleLike()` | 正常/Loading/已按 | 高亮顯示 |
| BTN-ACT-INT-002 | 不喜歡 | `handleDislike()` | 正常/Loading/已按 | 高亮顯示 |
| BTN-ACT-INT-003 | 編輯 | `onEdit()` | 正常 | 開啟編輯器 |
| BTN-ACT-INT-004 | 更換圖片 | `onReplace()` | 正常 | 開啟圖片搜尋 |
| BTN-ACT-INT-005 | 查看歷史 | `onViewHistory()` | 正常 | 開啟歷史記錄 |

### 4.5 評分面板 (`RatingPanel.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-RATE-001 | 喜歡 | `handleRatingClick('like')` | 正常/已評 | 高亮 + 顯示原因選項 |
| BTN-ACT-RATE-002 | 不喜歡 | `handleRatingClick('dislike')` | 正常/已評 | 高亮 + 顯示原因選項 |
| BTN-ACT-RATE-003 | 原因選項 | `toggleReason(reason)` | 正常/選中 | 切換選中狀態 |
| BTN-ACT-RATE-004 | 提交評分 | `handleSubmit()` | 正常/Loading | 成功 Toast |

### 4.6 Dashboard 操作 (`Dashboard.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-DASH-001 | 重試載入 | `handleRetry()` | 正常 | 重新載入數據 |
| BTN-ACT-DASH-002 | 刪除今日主題 | `handleDeleteToday()` | 正常/Loading | 確認後刪除 |
| BTN-ACT-DASH-003 | 生成今日主題 | `handleGenerateToday()` | 正常/Loading | 生成新主題 |
| BTN-ACT-DASH-004 | 頭條 Tab 全部 | `selectTab('all')` | 正常/選中 | `/dashboard` 今日三區 |
| BTN-ACT-DASH-005 | 頭條 Tab 時尚 | `selectTab('fashion')` | 正常/選中 | `/dashboard?tab=fashion` 直接歷史 |
| BTN-ACT-DASH-006 | 頭條 Tab 美食 | `selectTab('food')` | 正常/選中 | `/dashboard?tab=food` 直接歷史 |
| BTN-ACT-DASH-007 | 頭條 Tab 趨勢 | `selectTab('trend')` | 正常/選中 | `/dashboard?tab=trend` 直接歷史 |
| BTN-ACT-DASH-008 | 更多時尚 | `selectTab('fashion')` | 正常 | 切到時尚 Tab（不進主題庫） |
| BTN-ACT-DASH-009 | 更多美食 | `selectTab('food')` | 正常 | 切到美食 Tab |
| BTN-ACT-DASH-010 | 更多趨勢 | `selectTab('trend')` | 正常 | 切到趨勢 Tab |
| BTN-ACT-DASH-011 | 展開歷史 | **隱藏**（進分類 Tab 即列表） | 隱藏 | 列保留不刪 |

### 4.7 頻道操作 (`CreateChannel.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-CHANNEL-001 | AI 助手 | `setShowAssist(true)` | 正常 | 開啟 AI 助手對話框 |
| BTN-ACT-CHANNEL-002 | 關閉助手 | `handleAssistClose()` | 正常 | 關閉對話框 |
| BTN-ACT-CHANNEL-003 | 提交助手輸入 | `handleAssistSubmit()` → API | 正常/Loading/禁用(空輸入) | 顯示解析結果 |
| BTN-ACT-CHANNEL-004 | 確認應用 | `handleAssistConfirm()` | 正常 | 應用結果到表單 |
| BTN-ACT-CHANNEL-005 | 修改輸入 | `handleAssistReset()` | 正常 | 清除結果，重新輸入 |
| BTN-ACT-CHANNEL-006 | 快捷類別按鈕 | `handleQuickCategoryClick(cat)` | 正常/禁用(處理中) | 自動填入類別和輸入框 |
| BTN-ACT-CHANNEL-007 | 快捷地區按鈕 | `handleQuickRegionClick(reg)` | 正常/禁用(處理中) | 自動填入地區和輸入框 |

### 4.8 頻道詳情操作 (`ChannelDetail.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-CHANNEL-008 | 返回列表 | `navigate('/channels')` | 正常 | 返回頻道列表 |
| BTN-ACT-CHANNEL-009 | 手動收集 | `handleTriggerCollection()` → API | 正常/Loading/禁用(收集中) | 觸發收集任務 |
| BTN-ACT-CHANNEL-010 | 編輯頻道 | `navigate('/channels/:id/edit')` | 正常 | 跳轉到設定頁 |

### 4.9 頻道設定操作 (`ChannelEdit.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-CHANNEL-011 | 返回詳情 | `navigate('/channels/:id')` | 正常 | 返回頻道詳情 |
| BTN-ACT-CHANNEL-012 | 添加關鍵字 | `handleAddKeyword()` | 正常/禁用(空輸入) | 添加關鍵字標籤 |
| BTN-ACT-CHANNEL-013 | 移除關鍵字 | `handleRemoveKeyword(keyword)` | 正常 | 移除關鍵字標籤 |
| BTN-ACT-CHANNEL-014 | 儲存變更 | `handleSubmit()` → API | 正常/Loading/禁用(空名稱) | 更新頻道資訊 |
| BTN-ACT-CHANNEL-015 | 刪除頻道 | `setShowDeleteConfirm(true)` | 正常 | 開啟刪除確認對話框 |
| BTN-ACT-CHANNEL-016 | 確認刪除 | `handleDelete()` → API | 正常/Loading | 刪除頻道並返回列表 |
| BTN-ACT-CHANNEL-017 | 取消刪除 | `setShowDeleteConfirm(false)` | 正常 | 關閉確認對話框 |

### 4.10 主題列表操作 (`Topics.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-TOPIC-006 | 無限滾動模式 | `setViewMode('infinite')` | 正常/選中 | 切換到無限滾動 |
| BTN-ACT-TOPIC-007 | 分頁模式 | `setViewMode('pagination')` | 正常/選中 | 切換到分頁模式 |
| BTN-ACT-TOPIC-008 | 主題卡片 | `handleTopicClick(topicId)` | 正常 | 跳轉到主題詳情 |

### 4.11 主題詳情操作 (`TopicDetail.tsx` - 擴展)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-TOPIC-009 | 返回頭條 | `goBackToHeadlines()` | 正常 | 上一頁或 `/dashboard` |
| BTN-ACT-TOPIC-010 | 新增圖片 | `setShowImageSearch(true)` | 正常 | 開啟圖片搜尋 |
| BTN-ACT-TOPIC-011 | 搜尋圖片 | `setShowImageSearch(true)` | 正常 | 開啟圖片搜尋 |
| BTN-ACT-TOPIC-012 | 快捷轉貼文章 | `postKitRef.current?.scrollIntoView()` | 正常 | 平滑滾動至發文套件區 |
| BTN-ACT-TOPIC-013 | 查看翻譯報道 | `setSourceViewMode('translated')` | 正常/選中 | 切換呈現完整翻譯新聞內容 |
| BTN-ACT-TOPIC-014 | 查看英文原文 | `setSourceViewMode('original')` | 正常/選中 | 切換呈現完整英文原始報道 |

### 4.12 發文套件操作 (`PostKitPanel.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 | 回饋 |
|---------|------|------|------|------|
| BTN-ACT-POSTKIT-001 | 生成專屬文案 | `handleGeneratePlatformPreview()` | 正常/Loading | 按需生成該平台文案 (JIT) |
| BTN-ACT-POSTKIT-002 | 複製文案 | `navigator.clipboard.writeText(...)` | 正常 | 成功 Toast 提示 |

---

## 5. 輔助類按鈕 (Utility)

### 5.1 語言切換

| 按鈕 ID | 名稱 | 功能 | 組件 |
|---------|------|------|------|
| BTN-UTL-LANG-001 | 語言選單觸發 | `setShowLangMenu()` | `Header.tsx` |
| BTN-UTL-LANG-002 | 選擇語言 | `handleLanguageChange()` | `Header.tsx` |

### 5.2 用戶選單

| 按鈕 ID | 名稱 | 功能 | 組件 |
|---------|------|------|------|
| BTN-UTL-USER-001 | 用戶選單觸發 | `setShowUserMenu()` | `Header.tsx` |

### 5.3 通知

| 按鈕 ID | 名稱 | 功能 | 組件 | 狀態 |
|---------|------|------|------|------|
| BTN-UTL-NOTIF-001 | 通知按鈕 | (未實作) | `Header.tsx` | 正常 |

### 5.4 分頁控制 (`Pagination.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 |
|---------|------|------|------|
| BTN-UTL-PAGE-001 | 上一頁 | `handlePageChange(currentPage - 1)` | 正常/禁用(第一頁) |
| BTN-UTL-PAGE-002 | 頁碼 | `handlePageChange(pageNum)` | 正常/選中 |
| BTN-UTL-PAGE-003 | 下一頁 | `handlePageChange(currentPage + 1)` | 正常/禁用(最後頁) |
| BTN-UTL-PAGE-004 | 跳轉頁面 | `handleSubmit()` | 正常 |

### 5.5 篩選與視圖 (`Topics.tsx`, `TopicFilters.tsx`)

| 按鈕 ID | 名稱 | 功能 | 組件 | 狀態 |
|---------|------|------|------|------|
| BTN-UTL-VIEW-001 | 無限滾動視圖 | `setViewMode('infinite')` | `Topics.tsx` | 正常/選中 |
| BTN-UTL-VIEW-002 | 分頁視圖 | `setViewMode('pagination')` | `Topics.tsx` | 正常/選中 |
| BTN-UTL-FILTER-001 | 重置篩選 | `handleReset()` | `TopicFilters.tsx` | 正常 |

### 5.6 日曆控制 (`Calendar.tsx`)

| 按鈕 ID | 名稱 | 功能 | 狀態 |
|---------|------|------|------|
| BTN-UTL-CAL-001 | 上個月 | `prevMonth()` | 正常 |
| BTN-UTL-CAL-002 | 下個月 | `nextMonth()` | 正常 |

### 5.7 錯誤重試

| 按鈕 ID | 名稱 | 功能 | 組件 |
|---------|------|------|------|
| BTN-UTL-ERR-001 | 重試 (連線錯誤) | `onRetry()` | `ConnectionErrorDisplay.tsx` |
| BTN-UTL-ERR-002 | 重試 (一般錯誤) | `onRetry()` | `ErrorDisplay.tsx` |

### 5.8 設定頁標籤 (`Settings.tsx`)

| 按鈕 ID | 名稱 | 功能 | i18n Key |
|---------|------|------|----------|
| BTN-UTL-TAB-001 | 個人檔案標籤 | `setActiveTab('profile')` | `settings.profile` |
| BTN-UTL-TAB-002 | 帳號標籤 | `setActiveTab('account')` | `settings.account` |
| BTN-UTL-TAB-003 | 外觀標籤 | `setActiveTab('appearance')` | `settings.appearance` |

### 5.9 帳號操作 (`Settings.tsx`)

| 按鈕 ID | 名稱 | 功能 | i18n Key | 狀態 |
|---------|------|------|----------|------|
| BTN-UTL-ACC-001 | 取消連結 Google | (未實作) | `account.unlinkGoogle` | 正常(已連結) |
| BTN-UTL-ACC-002 | 連結 Google | (未實作) | `account.linkGoogle` | 正常(未連結) |
| BTN-UTL-ACC-003 | 刪除帳號 | (未實作) | `account.deleteAccount` | 正常 |
| BTN-UTL-ACC-004 | 深色模式 | 已選中 | - | 選中 |
| BTN-UTL-ACC-005 | 淺色模式 | (未實作) | `feature.comingSoon` | 禁用 |

### 5.10 登入提示彈窗 (`TopicDetail.tsx`)

| 按鈕 ID | 名稱 | 功能 | i18n Key |
|---------|------|------|----------|
| BTN-UTL-PROMPT-001 | 取消 | `setShowLoginPrompt(false)` | `common.cancel` |
| BTN-UTL-PROMPT-002 | 登入 | `navigate('/login')` | `auth.login.title` |
| BTN-UTL-PROMPT-003 | 註冊 | `navigate('/register')` | `auth.register.title` |

---

## 狀態與回饋規範

### 按鈕狀態定義

| 狀態 | CSS 樣式 | 說明 |
|------|----------|------|
| **正常 (Normal)** | `hover:bg-xxx` | 可點擊，有 hover 效果 |
| **Loading** | `disabled:opacity-50` + Spinner | 處理中，顯示載入指示 |
| **禁用 (Disabled)** | `disabled:cursor-not-allowed` | 不可點擊，灰色顯示 |
| **選中 (Active)** | `bg-primary text-white` | 當前選中狀態 |
| **成功 (Success)** | 綠色高亮 | 操作成功回饋 |
| **錯誤 (Error)** | 紅色高亮 | 操作失敗回饋 |

### 回饋機制

| 類型 | 實作方式 | 使用場景 |
|------|----------|----------|
| **Toast 通知** | `react-hot-toast` | 操作成功/失敗 |
| **彈窗確認** | Modal Dialog | 刪除確認、登入提示 |
| **狀態切換** | CSS 樣式變化 | 喜歡/不喜歡、選中狀態 |
| **頁面跳轉** | `navigate()` | 登入成功、表單提交 |
| **內容更新** | React Query refetch | 數據變更後刷新 |

---

## 測試檢查清單

### ✅ 導航測試

- [ ] BTN-NAV-SIDE-002: 點擊「控制面板」→ 跳轉到 `/dashboard`
- [ ] BTN-NAV-SIDE-003: 點擊「主題列表」→ 跳轉到 `/topics`
- [ ] BTN-NAV-HDR-003: 訪客點擊「登入」→ 跳轉到 `/login`
- [ ] BTN-NAV-HDR-004: 訪客點擊「註冊」→ 跳轉到 `/register`

### ✅ 認證測試

- [ ] BTN-AUTH-LOGIN-002: 正確帳密登入 → 成功跳轉
- [ ] BTN-AUTH-LOGIN-002: 錯誤帳密登入 → 顯示錯誤訊息
- [ ] BTN-AUTH-LOGIN-003: Google 登入 → 跳轉 OAuth
- [ ] BTN-AUTH-LOGOUT-001: 登出 → 清除 token，跳轉登入頁

### ✅ 操作測試

- [ ] BTN-ACT-TOPIC-001: 會員可編輯主題
- [ ] BTN-ACT-TOPIC-001: 訪客點擊 → 顯示登入提示
- [ ] BTN-ACT-CONTENT-001: 生成內容 → 顯示 Loading → 顯示內容
- [ ] BTN-ACT-IMG-002: 智能匹配 → 需要先有內容

### ✅ 狀態測試

- [ ] Loading 狀態：按鈕顯示載入動畫
- [ ] Disabled 狀態：按鈕不可點擊
- [ ] 錯誤處理：API 失敗顯示錯誤訊息

---

## 維護指南

### 新增按鈕時

1. **分配 ID**: 根據命名規則分配唯一 ID
2. **更新架構表**: 在對應分類中新增記錄
3. **定義狀態**: 明確按鈕的所有可能狀態
4. **設定回饋**: 定義成功/失敗的用戶回饋
5. **添加 i18n**: 如需要，新增翻譯 key
6. **測試清單**: 新增對應測試項目

### 修改按鈕時

1. 先查閱架構表確認影響範圍
2. 更新架構表中的相關資訊
3. 同步更新測試清單
4. 執行相關測試驗證

### 版本紀錄

| 版本 | 日期 | 變更說明 |
|------|------|----------|
| v1.0.0 | 2026-02-03 | 初始版本，包含完整按鈕清單 |

---

> **注意**: 此文件應與程式碼保持同步，每次修改按鈕功能後請更新此架構表。

