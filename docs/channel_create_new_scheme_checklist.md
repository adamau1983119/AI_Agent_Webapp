# 新方案檢查清單 — 建立頻道「助手主導」（對齊完整願景一至六與 §E）

> **用途**：產品／前端／後端／測試開會與收尾時逐項勾選；**不表示須單次迭代全部完成**。  
> **核心（一句）**：願景、§E 與 `AGENTS` 工作天已定案後，本檔作為「建立頻道／助手主導」的 **交付對照**，請依 **完成狀態總覽** 與 **A～I** 自上而下複核；僅 **`[x]`** 且已有文件或程式證據者視為結案（規則 #11：勾選前須查實）。  
> **依據**：[channel_create_full_vision_一至四.md](./channel_create_full_vision_一至四.md)（**一至六節**；檔名仍為 `一至四` 者為歷史路徑）、[channel_create_ai_guided_spec.md](./channel_create_ai_guided_spec.md)（**§E**、Phase B～D）、[專案完整架構表.md](../專案完整架構表.md)（建立頻道 §⑤）、[AGENTS.md](../AGENTS.md)（工作天與 Phase B～D 對應）。  
> **全專案（非僅建立頁）測試覆蓋**：見 **[architecture_test_matrix.md](./architecture_test_matrix.md)**（路由、靈感、社交、主題詳情等）。  
> **測試週架構缺口收口（12 項排程）**：見 **[test_week_gap_closure_plan.md](./test_week_gap_closure_plan.md)**（與下方 **I.1／I.2**、AGENTS 第 10～14 天對齊）。  
> **測試週每日 Checklist（含 I.1 建議日）**：[test_week_daily_checklist.md](./test_week_daily_checklist.md)。  
> **計劃（第五、六章）**：清單 **H、I** 對應完整願景 **第五節（資料與生命週期）**、**第六節（測試、發布、營運）**；**預設於測試階段**（AGENTS **第 10～14 工作天**；**自 2026-05-12（二）起僅測試**，見 `AGENTS.md` 日曆修訂）再逐項勾選與補證據；MVP／Phase B～D 開發階段可標 **N/A** 並註日期。  
> **建立／更新**：2026-05-09（**第 3～5 工作天合併衝線**；**第 9 天凍結宣告**；**2026-05-12 起測試段**；**test_week_gap_closure_plan** 架構缺口收口）／2026-05-08（初版）  
> **最後修訂（清單維護）**：2026-05-09 — **節 B～D**「對齊 AGENTS」日期與合併驗收 **2026-05-09** 一致；**I.0**；**I.1** 補 **RSS／URL 邊界**（第二輪複核）。  
>
> **衝線註記（2026-05-09 六）**：若日曆上第 3～5 天仍分列日期，以本檔與 `AGENTS.md` 勾選為準—**功能交付**以今日為開發段截止；**瀏覽器 E2E、無障礙全流、429 壓測**等改於 **AGENTS 第 10～14 天**（**2026-05-12 起**）逐項執行 **I 節**。
>
> **圖例**：`[x]`＝已有**文件或程式**證據、本檢核點**已結案**（規則 #11：不得虛報）。此為 Markdown **待辦「已勾選」**，**不是**錯誤／刪除用的「打叉」。`[ ]`＝尚未完成；**➖**＝本輪不適用／延後並註日期（見該列文字）。

---

## AGENTS 工作天 ↔ 本清單（對照表）

> 日期以 **`AGENTS.md` 日曆總表**為準（含 **2026-05-08 不計入** 之順延）。若您改日曆，請同步改此表。

| AGENTS 工作天 | 日期（目前） | 本清單節次 | 主旨 |
|:-------------:|-------------|------------|------|
| — | （無獨占日） | **A** | 決策／IA；宜在第 3 天開 Phase B **前**已對齊 |
| **第 3** | 2026-05-07（四）；**合併驗收 2026-05-09（六）** | **B** | §E Phase B |
| **第 4** | **合併驗收 2026-05-09（六）**（曆史表曾列 2026-05-12） | **C** | §E Phase C |
| **第 5** | **合併驗收 2026-05-09（六）**（曆史表曾列 2026-05-13） | **D** | §E Phase D |
| 第 6～8 | … | **E** 等 | 已結案；見 `AGENTS.md` |
| **第 9** | **2026-05-09（六）** 宣告凍結 | — | MVP 凍結；**2026-05-12 起僅測試** |
| 穿插 | … | **F** | 文件／架構表與實作同步 |
| 第 10～14 | **2026-05-12～05-21**（見 `AGENTS.md`；第 14 天避開與第 8 天同日） | **H**、**I** | 資料生命週期、測試／發布／營運（測試段） |
| 穿插 | … | **G** | 開發階段簡查；完整見 **I** |

---

## 完成狀態總覽（一目了然）

| 節次 | 狀態 | 證據／備註（截至 2026-05-09） |
|:----:|:----:|-------------------------------|
| **A** | 🟢 已落地（文件） | §E Phase A 凍結、`channel_create_full_vision` 一至六章；細項見下「A」勾選 |
| **B** | 🟢 開發結案 | 預設助手、lg 並排、摘要、表單收合／展開；見 `CreateChannel.tsx` |
| **C** | 🟢 開發結案 | 敘事／429／橫幅／RWD／抽屜／Tab 循環；**I.1** 補 375px／WCAG 簽核 |
| **D** | 🟢 開發結案 | testid、**D.1**、`npm run build`；**I.1** 補瀏覽器登入煙霧簽核 |
| **E** | 🟡 部分／視輪次 | 後端 feeds／assist 等已存在；「未破壞契約」持續維護；草稿 API＝願景 **➖** |
| **F** | 🟢 已同步 | 架構表 **⑤**、`channel_create_ai_guided_spec.md` **實作狀態** 2026-05-09 已更新 |
| **G** | 🟢 開發段已註 | 見下 **G**；釋出矩陣 **I.2** |
| **H** | 🟢 程式盤點結案 | 見下 **H**；行為 E2E **I.1** |
| **I** | ⏳ **測試週執行** | AGENTS 第 10～14 天；下方 **I** 逐項執行後勾選 |

---

## A — 決策與對齊（核心體驗目標＋資訊架構，對應「一＋二」）

**對齊 AGENTS**：無單獨工作日；應於 **第 3 天 Phase B** 開工前已具備（文件已寫則勾）。

- [x] **核心體驗目標**（第一節 1.1～1.2）與利害關係人讀後 **無歧義**（誰主畫面、是否零點擊開助手、表單退哪一層）。 — *證據：`channel_create_full_vision_一至四.md` §一、`channel_create_ai_guided_spec.md` §E1*
- [x] **L0～L4**（第二節）責任邊界已定：**L2** 採「頂條／僅助手內／混合」之一並寫入規格或本檔備註。 — *證據：§E2（L2 二選一或混合已寫死）；實作選項待 Phase B*
- [x] **L5（草稿／延續）**：已決定 **本階段做／不做**；若不做，已註明「延後原因」。 — *證據：完整願景 §五、`§E6` Phase B～D；**本階段 UI 不以草稿為門檻**，L5 列願景／測試段*
- [x] **與現有三步驟**：Step1～3 **業務規則**仍成立，僅 **畫面主從** 改變—已書面確認。 — *證據：`channel_create_ai_guided_spec.md` A1、§E4*
- [x] **§E Phase A** 與本清單 **無衝突**（§E＝已凍結版面原則；本清單＝落地勾選）。 — *證據：§E Phase A 凍結日期與 DoD*

---

## B — Phase B：前端版面與主流程（§E、對應「三」）

**對齊 AGENTS**：**第 3 工作天**（**2026-05-07（四）**；**Phase B 合併驗收 2026-05-09（六）** — 見上表「對照表」）。

**程式現況（證據）**：`frontend/src/pages/CreateChannel.tsx` — `showAssist` 預設 **`useState(true)`**（2026-05-09）；大螢幕助手欄與表單欄並排、**建立前摘要** `panel-channels-create-summary`；小螢幕 **底欄抽屜**（`btn-channels-mobile-summary-drawer` 等）補步驟與摘要。

- [x] 進入 `/channels/create` 時 **助手預設可見**（無需使用者先點「開啟助手」才能完成主路）。 — *2026-05-09*
- [x] **`showAssist`（或等價）預設為 true** 或等價行為（`useState(true)`，進頁即助手主欄）。 — *2026-05-09*
- [x] **主視覺**：助手區為 **主舞台**；傳統表單 **收斂** 至摘要／進階（符合 §E L1／L3／L4）。 — *2026-05-09：**lg＋助手開啟**時三步表單預設 **收合**（`panel-channels-form-collapsed-hint`），一鍵 **展開**；小螢幕維持表單可捲動可見；收起／關閉助手仍為 L4*
- [x] **Step 指示**（若保留）：與助手敘事 **不打架**（同一套「尚缺什麼」邏輯）。 — *2026-05-09：`CreateChannelStepNav`（`nav`／`ol`／`aria-current`）+ `channels.phaseC.stepHint` 與 `channels.guided.intro*` 同序對齊*
- [x] **建立前摘要（L3）**：至少可見 **名稱、描述、RSS 選取狀態** 之一組合；**建立** 與 **返回修改** 可及。 — *右欄摘要卡（助手開啟時）；「建立」仍在步驟 3 表單*（2026-05-09）
- [x] **進階／手動（L4）**：仍可到達；不形成 **唯一** 主路。 — *2026-05-09：標題列「收起助手」、助手內「關閉助手，改用表單」、✕ 關閉；表單三步仍完整可操作*
- [x] **元件**：若拆分子元件，**檔案／命名** 已記於工作記錄或 PR 說明（可選）。 — *2026-05-09：同檔 `CreateChannelSummaryPanel`、`CreateChannelStepNav`（可選再拆檔）*

---

## C — Phase C：互動與 RWD（§E）

**對齊 AGENTS**：**第 4 工作天**（**合併驗收 2026-05-09（六）**；`AGENTS` 日曆曾列 **2026-05-12（二）** — 以本檔與上表為準）。

- [x] **開場首句＋捷徑** 與 **guided step** 文案 **同一敘事**（無重複矛盾）。 — *2026-05-09：`channels.guided.intro`／`introStep2`／`introStep3` 與 `channels.phaseC.stepHint` 對齊頁面步驟圓圈*
- [x] **429／assist／validate 失敗**：助手區內或全域 **可理解** 的降級或重試提示（含 `detail.code` 對應 i18n）。 — *2026-05-09：`assist`／`wizard-options` 使用 `skipErrorHandler`；分析失敗 toast＋助手內橫幅；Step2 白名單／validate／RSS 池載入／`POST /channels` 在助手開啟時同步 **助手內紅色橫幅**（`feedActionBanner`）+ toast；429 沿用 `rateLimitToastI18nKey`；精靈載入失敗可「重新載入精靈」*
- [x] **RWD**：小螢幕下 **主舞台仍為助手**（摘要可改底抽屜或全幅，已自測至少一種寬度）。 — *2026-05-09：直向助手在上；底欄抽屜；**375px 截圖／簽名**排入 **I.1 無障礙**／多 viewport*
- [x] **焦點／鍵盤**：主路徑可 **鍵盤操作**（無障礙最小集，依團隊定義勾選）。 — *2026-05-09：`aria-*`、skip link、抽屜 **Tab 循環**；**全流 WCAG 對照**排入 **I.1***

---

## D — Phase D：測試與驗收（§E）

**對齊 AGENTS**：**第 5 工作天**（**合併驗收 2026-05-09（六）**；`AGENTS` 日曆曾列 **2026-05-13（三）** — 以本檔與上表為準）。

- [x] **`data-testid`**：新增／變更之按鈕、主 CTA、進階抽屜 — 對照 [按鈕測試ID架構表.md](../按鈕測試ID架構表.md)。 — *2026-05-09：`CreateChannel.tsx` 補 Step1～3 主流程、`panel-channels-create-form`、`link-channels-phasec-skip-to-form`、`btn-channels-create-back`；§4.5 表已列*
- [x] **手測腳本**：存在「**未先點開助手**」即可完成建立（或記錄 **BLOCK** 與原因）。 — *見下方 **D.1**；預設進頁助手開啟時，先點 **`btn-channels-assist-minimize`** 或 **`btn-channels-assist-close`** 再只走表單*
- [x] **煙霧（開發可驗）**：前端 **`npm run build`** 通過、無編譯期 throw。 — *2026-05-09*  
  **瀏覽器端到端**：登入 → `/channels/create` → 不白屏 — *排入 **測試週** 執行 **D.1 步驟 0** 並存證（規則 #11）*
- [x] **三語**：本輪建立頁可見字維持 **zh-TW／en／ja**（`channels.phaseC.*` 等）；無新增硬編碼。 — *2026-05-09 複核*
- [x] **工作記錄**：本輪 Phase D 起點、證據、明日第一步 **已寫**。 — *2026-05-09 頂部補列*

### D.1 手測腳本 —「未先依賴助手」完成建立（表單主路）

**前置**：已登入；後端可連；Mongo 正常。進入 `/channels/create`。

0. **（可選）煙霧**：頁面有標題與表單區 `panel-channels-create-form`，Console 無紅色未捕捉錯誤。
1. 若預設已開助手：點 **`btn-channels-assist-minimize`**（收起）或 **`btn-channels-assist-close`**（關閉並清空助手狀態亦可）；**勿**使用精靈快捷完成三步。
2. **Step1**：（**lg 寬度且助手仍開**時，表單可能收合）先點 **`btn-channels-expand-wizard-form`**；再點 **`btn-channels-step1-category-*`** → **`btn-channels-step1-next`**。
3. **Step2**：點 **`btn-channels-step2-region-*`**；候選池可選擇性使用 `btn-channels-step2-pool-add-*`（或略過 RSS）→ **`btn-channels-step2-next`**。若類別為 `other`，須先 **`input-channels-step2-keyword`** + **`btn-channels-step2-keyword-add`**。
4. **Step3**：進入步驟 3 時 **lg＋助手**會 **自動展開表單**（可送出）；**`input-channels-step3-name`** 填名稱 → **`btn-channels-step3-submit`**。
5. **期望**：成功 toast、導向 `/channels` 或列表可見新頻道；**BLOCK** 時記錄 Network status 與 Console。

**已知**：進頁預設 `showAssist === true` 屬產品設定；本腳本驗證「關閉／收起助手後表單仍可獨立完成」，符合 Phase D「未先點開助手」之**實質**主路（不依賴分析／精靈）。

---

## E — 後端與安全（對應「四」；本方案若僅動 UI 可標 ➖）

**對齊 AGENTS**：無固定日；動後端契約時於 **第 6～9 天**或 PR 內自檢。

> **與總覽「E 🟡」**：前三項 `[x]`＝**已盤點／證據成立**（契約、限流、SSRF 路徑）；**非**表示「後端已無後續工作」。頂部總覽 **🟡** 是因 **契約須持續迴歸**＋**草稿 API 本輪未交付**（見下列第四點 **➖**）。

- [x] **契約**：`assist`／`wizard-options`／`feeds/*`／`POST /channels` **未破壞**既有行為；若有改版已更新 spec／Swagger 說明。 — *截至 2026-05-08 現行端點已存在於程式庫；日後變更須再跑迴歸*
- [x] **限流**：validate／search 仍符合設計；若上多實例，**Redis** 策略已檢視（見工作記錄 Redis 列案）。 — *已落地 `feed_validate_rate_limit.py`；Redis 為選用*
- [x] **SSRF／probe**：貼 URL 路徑 **未放寬**於未經審查之變更。 — *維持既有 probe 路徑；若有 PR 須重檢*
- **➖ 草稿 API（L5）** — **N/A（2026-05-08）**：本輪不以草稿 API 為交付項；若未來實作須補 **權限與清理策略**；見完整願景 §五。（*此列不用 `[x]`，避免與「功能已上線」混淆。*）

---

## F — 文件與架構表同步

- [x] `channel_create_ai_guided_spec.md` **實作狀態** 表：Phase B／C／D 與 **實際進度** 一致。 — *2026-05-09：§E6／實作狀態表已改 **✅**；見該檔*
- [x] `專案完整架構表.md` **⑤**：若版面目標已上線，**狀態列**已更新。 — *2026-05-09：`CreateChannel` 助手優先 UI 已落地敘述同步*
- [x] `channel_create_full_vision_一至四.md`：若有 **範圍裁切**，附錄或結尾 **註明「已採用／暫緩」**。 — *結尾／§六說明全集與切版關係*

---

## G — 上線與營運（開發階段簡查；**完整見 I**）

- [x] **輕量**：本迭代若涉及釋出，**開關／回滾** 是否已 **口頭或 PR 註記**（可選）。 — *2026-05-09：本輪 **未**新增後端 feature flag；`showAssist` 預設於 `CreateChannel.tsx`。若合併 PR，建議說明「建立頻道 UI 行為變更、無 env 開關」*
- [x] **完整收口**：功能開關、漸進放量、回滾、稽核與 **ADR／SoT** — **請於測試階段改勾 I 節**。 — *2026-05-09：**開發段**收口＝本檔 B～H 與 `工作記錄`；**營運完整矩陣**改執行 **I.2／I.3***

---

## H — 資料與生命週期（完整願景 **第五章**）

**對齊 AGENTS**：**第 10～14 工作天** 補 **行為／E2E** 證據；**2026-05-09** 已完成 **程式／契約盤點**如下（規則 #11：盤點可勾；壓測與列表同步見 **I**）。

- [x] **Mongo `channels`**：正式頻道為權威來源；欄位與現行 `POST /channels`／讀取 API **一致**（測試階段對照 schema／範例文件）。 — *盤點：`ChannelCreate`／`ChannelResponse`（`backend/app/models/channel.py`）與 `channels.py` 建立／讀取路由對齊；**E2E 寫入讀回**見 **I.1***
- [x] **草稿**：已決定 **`channel_drafts` 集合** 或 **`users.drafts`**（或兩者皆否與理由）；若實作，具 **建立／讀取／更新／刪除** 與 **權限、過期清理** 策略。 — *➖ 與 **E**、願景 §五 一致：**本階段不做**草稿 API；無集合*
- [x] **`selected_feeds`**：**正規化**（trim、https、長度）、**去重**、**≤10** 上限之前後端 **單一規則**；邊界案例有測案。 — *盤點：後端 `ChannelCreate.selected_feeds` **`max_length=10`**、`ChannelFeedEntry.url` **http(s)** 與長度上限；`channel_service` 使用 **`raw_selected[:10]`**；前端 `MAX_STEP2_SELECTED_FEEDS=10`。**邊界自動測**見 **I.1***
- [x] **建立後與 collector**：`get_rss_sources_for_channel`／Layer1／Layer2 與使用者選取 **行為一致**；已擴寫或附 **策略文件** 連結（架構表 ①② 延伸）。 — *盤點：`channel_service.py` 註解與 **Layer1 僅用 selected_feeds** 分支；`專案完整架構表.md` 建立頻道節*
- [x] **刪除／改名／再編輯**：與「建立」對稱之 **CRUD** 與 **列表／詳情同步** 已測；無幽靈資料或快取錯位。 — *盤點：`channels.py` 具 **`update_channel`**、**`delete_channel`**；**UI 同步／幽靈資料**見 **I.1 迴歸***

---

## I — 測試、發布、營運（完整願景 **第六章**；**測試階段執行**）

**對齊 AGENTS**：**第 10～14 工作天**。

> **2026-05-09**：**B～D 與 H 盤點**已結案；下列 **I.1～I.3** 為 **測試週工單**—執行後逐項改 `[x]` 並附證據（截圖／報告連結／PR）。  
> **排程與優先級（P0／P1）**：見 **[test_week_gap_closure_plan.md](./test_week_gap_closure_plan.md)** 表 **#1**（本節 **I.1／I.2** 與第 12～14 天）。

### I.0 測試週前（文件／建置可驗；**非**瀏覽器 E2E）

> 下列項目不取代 **I.1**；僅記錄 **2026-05-09** 已具備之基線，避免測試週重複問「備份／build 做了沒」。

- [x] **文件快照**：`docs/backups/2026-05-09_test_week_docs_snapshot`（`AGENTS.md`、`README.md`、`工作記錄.md`、`專案完整架構表.md`、`architecture_test_matrix.md`、`test_week_gap_closure_plan.md`、`test_week_daily_checklist.md`、**本清單**）。 — *`工作記錄.md` **2026-05-09 補***
- [x] **前端建置煙霧**：於 `frontend` 執行 **`npm run build`** 通過（Vite 5.4.x）；chunk 體積提示非錯誤。 — *`工作記錄.md` **2026-05-09 補**；**I.1** 之瀏覽器 E2E／登入煙霧仍待測試週*

### I.1 測試（完整）

- [ ] **E2E**：**僅助手主路**完成建立（含送出前確認）；證據（錄影／步驟截圖／報告連結）。
- [ ] **無障礙**：全建立流鍵盤／焦點／語意（可對照 WCAG 或團隊自訂門檻）。
- [ ] **多語**：zh-TW／en／ja **全流** 無硬編碼、無缺 key。
- [ ] **429**：validate／search（及相關）限流與 **`detail.code`** 與前端 toast **一致**。
- [ ] **離線／逾時**：斷網與長請求下 **可理解** UI 與重試；無未捕捉錯誤。
- [ ] **大列表效能**：候選池／長訊息列表之捲動與互動 **達標**（門檻寫入測試計劃）。
- [ ] **RSS／URL 邊界**：Step2（或助手）貼 **非 http(s)**、**過長**、**明顯內網／file** 等案例時，**validate**／錯誤提示符合預期、**不**未捕捉 500；與清單 **E**（SSRF／probe）及後端行為一致或記 **BLOCK**。

### I.2 發布（完整）

- [ ] **功能開關**：例如 **`assistant_first_create_channel`**（或實作採用之環境變數名）已文件化、預設值與各環境矩陣已表列。
- [ ] **漸進放量**：百分比／allowlist 策略與 **負責人／回滾觸發條件** 已寫。
- [ ] **回滾策略**：關閉開關或回退版本之 **步驟** 演練至少一次並記錄。

### I.3 文件（單一 Source of Truth）

- [x] **規格＋架構表**：`channel_create_ai_guided_spec.md`、`channel_create_full_vision_一至四.md`（一至六）、`專案完整架構表.md` §⑤ **互相引用無漂移**（測試階段做一次全文對照）。 — *2026-05-09：三檔互链與 **⑤** 已更新；**測試週**可做一次性全文 diff 簽核*
- [x] **ADR**：至少 **「為何表單弱化／改助手主導」**、**「為何檢索邊界如此定」** 已建檔（路徑建議 `docs/adr/`，檔名依專案慣例）。 — *2026-05-09：[docs/adr/0001](./adr/0001-assistant-first-channel-create-ui.md)、[0002](./adr/0002-rss-retrieval-mvp-whitelist.md)、[索引](./adr/README.md)*

---

**使用方式**：複製本檔到當次 PR／工作記錄附錄，或於會議中共享螢幕逐項勾選；**規則 #11/#12**：勾選前**須**對照實際程式與測試結果。**H、I** 預設在 **測試階段** 轉為 **必勾**（開發階段可標 N/A 並註日期）。
