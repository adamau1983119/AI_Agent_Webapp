# 重啟後測試步驟

## ✅ 緩存已清除

Python 緩存已成功清除，現在需要重新啟動後端服務器。

---

## 🔄 重新啟動後端服務器

### 方法 1：使用快速啟動腳本（推薦）
```powershell
.\快速啟動後端.bat
```

### 方法 2：手動啟動
```powershell
cd backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 方法 3：使用 PowerShell 腳本
```powershell
.\啟動後端服務器.ps1
```

---

## ⏱️ 等待服務器啟動

啟動後，請等待 **5-10 秒** 讓服務器完全啟動。

你可以看到類似以下的輸出：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

---

## 🧪 測試 API

### 1. 使用診斷腳本測試
```powershell
python diagnose_generate_today.py
```

### 2. 預期結果

#### ✅ 成功情況（資料庫未連接）
```
[警告] 客戶端錯誤 (400)
通常表示：資料庫未連接或請求參數錯誤
💡 訊息: 資料庫未連接，無法生成主題
💡 建議: 請配置 MONGODB_URL 並確保 MongoDB 服務正在運行
```

#### ✅ 成功情況（資料庫已連接）
```
[成功] 返回結果
{
  "status": "accepted",
  "message": "生成任務已提交，請稍後查看結果",
  ...
}
```

#### ❌ 如果仍然出現 500 錯誤
如果仍然出現 `name 'ConnectionFailure' is not defined` 錯誤：
1. 確認後端服務器已完全重啟
2. 檢查後端 console 的完整錯誤堆疊
3. 可能需要檢查其他文件是否也需要導入 `ConnectionFailure`

---

## 📋 檢查清單

- [ ] 緩存已清除（✅ 已完成）
- [ ] 後端服務器已停止
- [ ] 後端服務器已重新啟動
- [ ] 等待服務器完全啟動（5-10 秒）
- [ ] 執行診斷腳本測試 API
- [ ] 檢查結果是否符合預期

---

## 🎯 下一步

1. **重新啟動後端服務器**
2. **測試 API**：`python diagnose_generate_today.py`
3. **檢查結果**：確認錯誤是否已解決

