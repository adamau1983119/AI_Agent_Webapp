# i18n 修復優先級分析

> **分析日期**: 2026-02-06  
> **目標**: 區分哪些問題需要修復，哪些不需要

---

## ✅ 不需要修復（開發者可見 - 約 80%）

### 1. API 文檔描述 (`description=`)

**範例**:
```python
category: Optional[str] = Query(None, description="分類篩選")
preset_style: PresetStyle = Query(..., description="預設風格")
```

**原因**: 
- 用於 Swagger/OpenAPI 文檔
- 僅開發者可見
- 保持中文有助於開發者理解

**文件**:
- `backend/app/api/v1/articles.py` - 12 個問題（全部是 description）
- `backend/app/api/v1/discover.py` - 26 個問題（大部分是 description）
- `backend/app/api/v1/style_profile.py` - 8 個問題（大部分是 description）

---

### 2. 日誌訊息 (`logger.info/error/warning`)

**範例**:
```python
logger.info(f"用戶註冊成功: {user['email']}")
logger.info(f"用戶 {current_user['email']} 建立頻道: {channel['name']}")
logger.warning(f"驗證 URL 失敗: {url}, 錯誤: {e}")
```

**原因**:
- 用於系統日誌和調試
- 僅開發者可見
- 保持中文有助於開發者理解

**文件**:
- `backend/app/api/v1/auth.py` - 2 個問題（logger 訊息）
- `backend/app/api/v1/channels.py` - 27 個問題（大部分是 logger）
- `backend/app/api/v1/images.py` - 51 個問題（大部分是 logger）

---

### 3. 文檔字符串 (`"""..."""`)

**範例**:
```python
"""從 MongoDB 文檔轉換為 ContentResponse"""
"""Feature Flag 端點"""
"""健康檢查端點"""
```

**原因**:
- 用於代碼文檔
- 僅開發者可見
- 保持中文有助於開發者理解

**文件**:
- `backend/app/api/v1/contents.py` - 19 個問題（大部分是 docstring）
- `backend/app/api/v1/feature_flags.py` - 7 個問題（docstring）
- `backend/app/api/v1/health.py` - 1 個問題（docstring）

---

### 4. 配置文件和內部數據

**範例**:
```python
# backend/app/config/brands.py
"時尚", "潮流", "設計", "穿搭", "服飾"
```

**原因**:
- 內部數據結構
- 不直接返回給用戶
- 如果需要顯示給用戶，應該在 API 層轉換

---

## 🔴 需要修復（用戶可見 - 約 20%）

### 1. API 錯誤訊息 (`HTTPException(detail=)`)

**範例**:
```python
# ❌ 錯誤 - 用戶可見
detail="資料庫未連接，請檢查 MongoDB 設置"
detail="Email 或密碼錯誤"
detail="註冊失敗，請稍後再試"
```

**修復方式**:
```python
# ✅ 正確 - 使用 i18n
detail=self._get_error_message("database.not_connected", language)
detail=self._get_error_message("auth.invalid_credentials", language)
detail=self._get_error_message("auth.register_failed", language)
```

**需要修復的文件**:
- `backend/app/database.py` - 第 69 行：`detail="資料庫未連接，請檢查 MongoDB 設置"`
- `backend/app/api/v1/auth.py` - 需要檢查是否有 `detail=` 的錯誤訊息
- 其他 API 文件中的 `HTTPException(detail=...)`

---

### 2. API 響應訊息 (`message=`)

**範例**:
```python
# ❌ 錯誤 - 用戶可見
message="Email 驗證成功"
message="驗證郵件已發送"
```

**修復方式**:
```python
# ✅ 正確 - 使用 i18n
message=self._get_success_message("auth.email_verified", language)
message=self._get_success_message("auth.email_sent", language)
```

**需要修復的文件**:
- `backend/app/api/v1/auth.py` - 檢查所有 `message=` 的響應

---

### 3. 錯誤建議訊息（返回給用戶的）

**範例**:
```python
# ❌ 錯誤 - 用戶可見
"suggestion": f"請在後端環境變數中設定{settings.AI_SERVICE.upper()}_API_KEY\n"
f"1. 如果使用 DeepSeek，請設置 DEEPSEEK_API_KEY\n"
```

**修復方式**:
```python
# ✅ 正確 - 使用 i18n
"suggestion": self._get_error_message("ai.api_key_missing", language, {
    "service": settings.AI_SERVICE.upper()
})
```

**需要修復的文件**:
- `backend/app/api/v1/contents.py` - 第 28 行和 272 行

---

### 4. 前端硬編碼文字

**範例**:
```typescript
// ❌ 錯誤 - 用戶可見
fashion: '時尚',
food: '美食',
trend: '趨勢'
```

**修復方式**:
```typescript
// ✅ 正確 - 使用 i18n
fashion: t('channels.category.fashion'),
food: t('channels.category.food'),
trend: t('channels.category.trend')
```

**需要修復的文件**:
- `frontend/src/api/channels.ts`
- `frontend/src/api/errors.ts`
- `frontend/src/api/ratings.ts`
- 其他前端文件

---

## 📊 問題分類統計

### 不需要修復（開發者可見）

| 類型 | 數量 | 範例文件 |
|------|------|---------|
| API 文檔描述 | ~800 | `articles.py`, `discover.py` |
| 日誌訊息 | ~1000 | `channels.py`, `images.py` |
| 文檔字符串 | ~600 | `contents.py`, `health.py` |
| 配置文件 | ~200 | `brands.py`, `config_loader.py` |
| **小計** | **~2600** | |

### 需要修復（用戶可見）

| 類型 | 數量 | 優先級 | 範例文件 |
|------|------|--------|---------|
| API 錯誤訊息 | ~30 | P0 | `database.py`, `auth.py` |
| API 響應訊息 | ~10 | P0 | `auth.py` |
| 錯誤建議訊息 | ~5 | P1 | `contents.py` |
| 前端硬編碼 | ~50 | P0 | `channels.ts`, `errors.ts` |
| **小計** | **~95** | | |

---

## 🎯 修復優先級

### P0（必須修復 - 用戶直接看到）

1. **後端 API 錯誤訊息** (`HTTPException(detail=)`)
   - `backend/app/database.py` - 1 個
   - `backend/app/api/v1/auth.py` - 需要檢查
   - 其他 API 文件

2. **前端硬編碼文字**
   - `frontend/src/api/channels.ts` - 16 個
   - `frontend/src/api/errors.ts` - 11 個
   - 其他前端文件

### P1（建議修復 - 部分用戶可見）

1. **API 響應訊息** (`message=`)
2. **錯誤建議訊息** (`suggestion=`)

### P2（不需要修復 - 開發者可見）

1. API 文檔描述
2. 日誌訊息
3. 文檔字符串
4. 配置文件

---

## 💡 修復建議

### 1. 改進檢查腳本

建議修改 `check_i18n_compliance.py`，排除：
- `description=` 參數（API 文檔）
- `logger.*()` 調用（日誌）
- `"""..."""` 文檔字符串
- 配置文件中的內部數據

### 2. 專注修復用戶可見的文字

只修復：
- `HTTPException(detail=...)`
- `message=...`（返回給用戶的）
- `suggestion=...`（返回給用戶的）
- 前端所有硬編碼文字

---

## 📝 下一步行動

1. ✅ **已完成**: 分析問題分類
2. ⬜ **待完成**: 改進檢查腳本，只檢查用戶可見的文字
3. ⬜ **待完成**: 修復 P0 問題（API 錯誤訊息和前端硬編碼）
4. ⬜ **待完成**: 修復 P1 問題（API 響應訊息）

---

**結論**: 真正需要修復的問題約 95 個（約 3.6%），而不是 2650 個。


