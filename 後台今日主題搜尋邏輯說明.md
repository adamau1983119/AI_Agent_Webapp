# 後台今日主題搜尋邏輯說明

## 📋 概述

後台系統會根據三個固定時間段自動生成不同分類的主題：
- **07:00** - 時尚趨勢 (FASHION)
- **12:00** - 美食推薦 (FOOD)
- **18:00** - 社會趨勢 (TREND)

每個時間段會生成 **3 個主題**，每日共 **9 個主題**。

---

## 🕐 排程時間設定

### 時間對應關係

系統使用 **UTC 時間**進行排程，但顯示的是 **香港時間 (HKT = UTC+8)**：

| 香港時間 (HKT) | UTC 時間 | 分類 | 說明 |
|---------------|---------|------|------|
| 07:00 | 23:00 (前一天) | 時尚趨勢 | 早上時尚資訊 |
| 12:00 | 04:00 | 美食推薦 | 中午美食資訊 |
| 18:00 | 10:00 | 社會趨勢 | 晚上社會趨勢 |

### 排程設定位置

**文件：** `backend/app/services/automation/scheduler.py`

```python
# 07:00 香港時間 = 23:00 UTC（前一天）
self.scheduler.add_job(
    self._generate_topics_for_timeslot,
    CronTrigger(hour=23, minute=0, timezone='UTC'),  # 香港時間 07:00
    id="fashion_topics_07:00",
    args=[Category.FASHION, "07:00"],
    replace_existing=True
)

# 12:00 香港時間 = 04:00 UTC
self.scheduler.add_job(
    self._generate_topics_for_timeslot,
    CronTrigger(hour=4, minute=0, timezone='UTC'),  # 香港時間 12:00
    id="food_topics_12:00",
    args=[Category.FOOD, "12:00"],
    replace_existing=True
)

# 18:00 香港時間 = 10:00 UTC
self.scheduler.add_job(
    self._generate_topics_for_timeslot,
    CronTrigger(hour=10, minute=0, timezone='UTC'),  # 香港時間 18:00
    id="trend_topics_18:00",
    args=[Category.TREND, "18:00"],
    replace_existing=True
)
```

---

## 🔍 主題搜尋邏輯

### 1. 排程觸發流程

當排程時間到達時，系統會執行 `_generate_topics_for_timeslot` 方法：

```python
async def _generate_topics_for_timeslot(
    self,
    category: Category,  # FASHION, FOOD, 或 TREND
    time_slot: str       # "07:00", "12:00", 或 "18:00"
):
    # 1. 收集主題（每個時間段收集 3 個）
    topics_data = await self.topic_collector.collect_topics(
        category=category,
        count=3,
        use_fallback=True
    )
    
    # 2. 為每個主題建立資料庫記錄
    for topic_data in topics_data:
        # 生成唯一 ID
        topic_id = f"topic_{category.value}_{datetime.utcnow().strftime('%Y%m%d%H%M%S')}_{len(created_topics)}"
        
        # 建立主題記錄
        created_topic = await self.topic_repo.create_topic(topic_data)
        
        # 自動處理主題（生成內容和圖片）
        await self.workflow.process_topic(
            topic_id=topic_id,
            auto_generate_content=True,
            auto_search_images=True,
            image_count=8
        )
```

### 2. 主題收集邏輯

**文件：** `backend/app/services/automation/topic_collector.py`

#### 步驟 1：嘗試從 RSS 收集

系統會先嘗試從 RSS Feed 收集主題：

```python
# RSS Feed 來源
rss_feeds = {
    Category.FASHION: [
        "https://www.vogue.com/feed/rss",
        "https://www.elle.com/rss/all.xml",
        "https://www.harpersbazaar.com/feed/",
    ],
    Category.FOOD: [
        "https://www.foodnetwork.com/feeds/all.rss",
        "https://www.bonappetit.com/feed/rss",
        "https://www.seriouseats.com/feeds/all",
    ],
    Category.TREND: [
        "https://feeds.feedburner.com/oreilly/radar",
        "https://techcrunch.com/feed/",
        "https://www.theverge.com/rss/index.xml",
    ],
}
```

#### 步驟 2：如果 RSS 失敗，使用備用關鍵字

如果 RSS 無法取得或數量不足，系統會使用備用關鍵字並通過 AI 生成主題：

```python
# 備用關鍵字
fallback_keywords = {
    Category.FASHION: [
        "2025春夏時尚趨勢", "可持續時尚", "復古風格回歸",
        "街頭時尚", "時尚科技", "環保時尚"
    ],
    Category.FOOD: [
        "香港美食推薦", "街頭小吃", "傳統美食",
        "新興餐廳", "美食趨勢", "健康飲食"
    ],
    Category.TREND: [
        "AI技術發展", "可持續發展", "社會趨勢",
        "科技創新", "文化現象", "生活方式"
    ],
}
```

#### 步驟 3：AI 生成中文標題

使用 AI 服務（DeepSeek）根據關鍵字生成中文標題：

```python
# 使用 AI 生成中文標題
prompt = build_title_prompt(category, keyword=keyword)
chinese_title = await ai_service._call_api(prompt)
```

---

## 📊 前端顯示邏輯

### 時間段分類邏輯

**文件：** `backend/app/api/v1/schedules.py`

前端通過 `GET /api/v1/schedules` API 獲取排程數據，後端會根據主題的 `generated_at` 時間進行分類：

```python
# 按時間段分組
time_slots = {
    "07:00": {"category": Category.FASHION, "topics": []},
    "12:00": {"category": Category.FOOD, "topics": []},
    "18:00": {"category": Category.TREND, "topics": []},
}

# 根據生成時間分類
for topic in topics:
    generated_at = topic.get("generated_at")
    hour = generated_at.hour
    
    if 6 <= hour < 10:  # 07:00 時間段 (6-9點)
        time_slots["07:00"]["topics"].append(topic)
    elif 11 <= hour < 14:  # 12:00 時間段 (11-13點)
        time_slots["12:00"]["topics"].append(topic)
    elif 17 <= hour < 20:  # 18:00 時間段 (17-19點)
        time_slots["18:00"]["topics"].append(topic)
```

### 前端顯示

**文件：** `frontend/src/components/features/TodayTopics.tsx`

```typescript
const timeSlots = ['07:00', '12:00', '18:00']

// 顯示每個時間段的主題
{timeSlots.map((timeSlot) => {
  const schedule = schedules.find((s) => s.timeSlot === timeSlot)
  const topicsCount = schedule?.topicsCount || 0
  
  return (
    <div>
      <p>{timeSlot} {
        timeSlot === '07:00' ? '時尚趨勢' : 
        timeSlot === '12:00' ? '美食推薦' : 
        '社會趨勢'
      }</p>
      <p>{topicsCount}/3 主題完成</p>
    </div>
  )
})}
```

---

## 🔄 完整流程圖

```
┌─────────────────────────────────────────────────────────┐
│  排程觸發 (CronTrigger)                                  │
│  - 07:00 HKT (23:00 UTC) → 時尚趨勢                      │
│  - 12:00 HKT (04:00 UTC) → 美食推薦                      │
│  - 18:00 HKT (10:00 UTC) → 社會趨勢                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  _generate_topics_for_timeslot()                         │
│  - 接收 category 和 time_slot                            │
│  - 調用 topic_collector.collect_topics(count=3)         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  TopicCollector.collect_topics()                         │
│                                                           │
│  步驟 1: 嘗試從 RSS Feed 收集                            │
│    ├─ FASHION: Vogue, Elle, Harper's Bazaar            │
│    ├─ FOOD: Food Network, Bon Appétit, Serious Eats   │
│    └─ TREND: O'Reilly Radar, TechCrunch, The Verge     │
│                                                           │
│  步驟 2: 如果 RSS 失敗或不足，使用備用關鍵字            │
│    ├─ FASHION: "2025春夏時尚趨勢", "可持續時尚"...     │
│    ├─ FOOD: "香港美食推薦", "街頭小吃"...              │
│    └─ TREND: "AI技術發展", "可持續發展"...             │
│                                                           │
│  步驟 3: 使用 AI 生成中文標題                           │
│    └─ 調用 DeepSeek API 生成標題                       │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  建立主題記錄                                             │
│  - 生成唯一 ID                                            │
│  - 設定 status = PENDING                                  │
│  - 儲存到 MongoDB                                         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  自動處理主題 (AutomationWorkflow)                       │
│  - 生成文章內容 (auto_generate_content=True)            │
│  - 搜尋並匹配圖片 (auto_search_images=True)             │
│  - 圖片數量: 8 張                                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  前端顯示                                                 │
│  - GET /api/v1/schedules                                 │
│  - 根據 generated_at 時間分類到時間段                    │
│  - 顯示 "0/3 主題完成" 或 "3/3 主題完成"                 │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 關鍵代碼位置

### 後端核心文件

1. **排程服務：** `backend/app/services/automation/scheduler.py`
   - 定義排程時間和觸發邏輯
   - `_generate_topics_for_timeslot()` 方法

2. **主題收集器：** `backend/app/services/automation/topic_collector.py`
   - RSS Feed 收集邏輯
   - 備用關鍵字生成邏輯
   - AI 標題生成邏輯

3. **排程 API：** `backend/app/api/v1/schedules.py`
   - `GET /api/v1/schedules` - 獲取排程列表
   - 時間段分類邏輯（根據 `generated_at` 時間）

### 前端核心文件

1. **今日主題組件：** `frontend/src/components/features/TodayTopics.tsx`
   - 顯示三個時間段的主題進度
   - 顯示 "0/3 主題完成" 狀態

2. **Dashboard 頁面：** `frontend/src/pages/Dashboard.tsx`
   - 調用 `schedulesAPI.getSchedules()` 獲取數據
   - 傳遞給 `TodayTopics` 組件

---

## 🎯 關鍵邏輯總結

### 1. 時間段與分類對應

- **07:00** → `Category.FASHION` → "時尚趨勢"
- **12:00** → `Category.FOOD` → "美食推薦"
- **18:00** → `Category.TREND` → "社會趨勢"

### 2. 主題數量

- 每個時間段生成 **3 個主題**
- 每日共 **9 個主題**

### 3. 搜尋優先順序

1. **優先：** RSS Feed（從時尚/美食/科技網站收集）
2. **備用：** 備用關鍵字 + AI 生成標題
3. **最後：** 直接使用關鍵字作為標題

### 4. 時間分類邏輯

根據主題的 `generated_at` 時間（小時）分類：
- **6-9 點** → 07:00 時間段（時尚趨勢）
- **11-13 點** → 12:00 時間段（美食推薦）
- **17-19 點** → 18:00 時間段（社會趨勢）

### 5. 自動處理

生成主題後，系統會自動：
- 生成文章內容
- 搜尋並匹配 8 張相關圖片
- 儲存到資料庫

---

## 🔧 手動觸發生成

如果排程未執行或需要補齊主題，可以手動觸發：

### API 端點

```bash
# 生成今日所有主題（3個分類 × 3個主題 = 9個主題）
POST /api/v1/schedules/generate-today
Body: { "force": false }

# 手動生成指定分類的主題
POST /api/v1/schedules/generate
Body: {
  "category": "fashion",  # 或 "food", "trend"
  "count": 3
}
```

### 前端觸發

Dashboard 頁面的「立即生成」按鈕會調用：
```typescript
schedulesAPI.generateTodayAllTopics(force: boolean)
```

---

## 📌 注意事項

1. **時區問題：** 排程使用 UTC 時間，但顯示的是香港時間
2. **RSS 可用性：** 如果 RSS Feed 無法訪問，會自動切換到備用關鍵字
3. **AI 服務：** 需要配置 `DEEPSEEK_API_KEY` 才能使用 AI 生成標題
4. **資料庫時間：** 主題的 `generated_at` 時間用於前端分類顯示

---

## 🚀 未來改進建議

1. **更多數據來源：** 可以整合更多 RSS Feed 或 API
2. **智能關鍵字：** 使用 NLP 技術從新聞中提取關鍵字
3. **用戶偏好：** 根據用戶偏好調整主題分類和內容
4. **時間段自定義：** 允許用戶自定義時間段和分類對應關係

