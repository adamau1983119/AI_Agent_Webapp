# 部署完成 - 下一步指引

> **前端網址**：https://ai-agent-webapp-ten.vercel.app/  
> **狀態**：✅ 前端已成功部署到 Vercel

---

## ✅ 已完成

- [x] GitHub 倉庫已建立：https://github.com/adamau1983119/AI_Agent_Webapp
- [x] 代碼已推送到 GitHub
- [x] 前端已部署到 Vercel：https://ai-agent-webapp-ten.vercel.app/

---

## 🚀 下一步：部署後端到 Railway

### 步驟 1：訪問 Railway

1. **訪問 Railway**：https://railway.app
2. **登入**：
   - 點擊 "Login"
   - 選擇 "Continue with GitHub"
   - 授權 Railway 存取您的 GitHub 帳號

### 步驟 2：建立新專案

1. **點擊 "New Project"**
2. **選擇 "Deploy from GitHub repo"**
3. **選擇倉庫**：`adamau1983119/AI_Agent_Webapp`
4. **等待 Railway 偵測專案**

### 步驟 3：設定 Root Directory

1. **點擊專案設定**（齒輪圖標）
2. **找到 "Root Directory"**
3. **設定為**：`backend`
4. **保存**

### 步驟 4：設定環境變數

在 Railway 專案設定中，點擊 "Variables" 標籤，添加以下環境變數：

#### **必須設定的環境變數**：

```
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=ai_agent_webapp
PORT=8000
AI_SERVICE=ollama
ENVIRONMENT=production
DEBUG=false
```

#### **強烈建議設定**：

```
API_KEY=your_secure_api_key_here
CORS_ORIGINS=["https://ai-agent-webapp-ten.vercel.app"]
```

**注意**：
- `MONGODB_URL`：替換為您的 MongoDB Atlas 連接字串
- `CORS_ORIGINS`：使用您的前端 Vercel 網域

### 步驟 5：生成公開網域

1. **點擊 "Settings"** 標籤
2. **找到 "Networking"** 區塊
3. **點擊 "Generate Domain"**
4. **記下網域**（例如：`ai-agent-webapp-production.up.railway.app`）

---

## 🔄 步驟 6：更新前端環境變數

後端部署完成後：

1. **回到 Vercel**：https://vercel.com
2. **選擇專案**：`ai-agent-webapp`
3. **點擊 "Settings"** → **"Environment Variables"**
4. **更新 `VITE_API_URL`**：
   - 點擊編輯按鈕
   - 更新值為：`https://your-railway-domain.railway.app/api/v1`
   - 將 `your-railway-domain.railway.app` 替換為實際的 Railway 網域
5. **保存**：Vercel 會自動重新部署

---

## 🔄 步驟 7：更新後端 CORS（如果還沒設定）

1. **回到 Railway 專案設定**
2. **點擊 "Variables"** 標籤
3. **添加或更新環境變數**：
   ```
   CORS_ORIGINS=["https://ai-agent-webapp-ten.vercel.app"]
   ```
4. **保存**：Railway 會自動重新部署

---

## ✅ 驗證部署

### 檢查後端健康狀態

訪問後端健康檢查端點：
```
https://your-railway-domain.railway.app/health
```

應該返回：
```json
{
  "status": "healthy",
  "environment": "production",
  ...
}
```

### 檢查 API 文檔

訪問後端 API 文檔：
```
https://your-railway-domain.railway.app/docs
```

### 檢查前端連接

1. **訪問前端**：https://ai-agent-webapp-ten.vercel.app/
2. **打開瀏覽器開發者工具**（F12）
3. **檢查 Console**：
   - 應該沒有 CORS 錯誤
   - 應該沒有 API 連接錯誤
4. **檢查 Network 標籤**：
   - API 請求應該返回 200 狀態碼

---

## 📋 完整檢查清單

### 部署前
- [x] GitHub 倉庫已建立
- [x] 代碼已推送到 GitHub
- [x] 前端已部署到 Vercel
- [ ] 後端已部署到 Railway
- [ ] 後端環境變數已設定
- [ ] 後端公開網域已生成

### 部署後
- [ ] 後端健康檢查正常（`/health`）
- [ ] 後端 API 文檔可訪問（`/docs`）
- [ ] 前端環境變數已更新（使用後端網域）
- [ ] 後端 CORS 已設定（允許前端網域）
- [ ] 前端可以成功連接後端 API
- [ ] 所有功能正常運作

---

## 🆘 常見問題

### 問題 1：前端顯示 API 連接錯誤

**原因**：
- 後端還沒部署
- `VITE_API_URL` 環境變數未正確設定
- CORS 設定錯誤

**解決**：
1. 確認後端已部署並可訪問
2. 檢查 `VITE_API_URL` 環境變數是否正確
3. 檢查後端 CORS 設定

### 問題 2：CORS 錯誤

**原因**：
- 後端 `CORS_ORIGINS` 未包含前端網域

**解決**：
1. 在 Railway 設定 `CORS_ORIGINS` 環境變數
2. 確保包含完整的前端網域（包括 `https://`）
3. 重新部署後端

### 問題 3：後端無法連接 MongoDB

**原因**：
- `MONGODB_URL` 環境變數錯誤
- MongoDB Atlas IP 白名單未設定

**解決**：
1. 檢查 `MONGODB_URL` 是否正確
2. 在 MongoDB Atlas 設定 IP 白名單（允許所有 IP：`0.0.0.0/0`）

---

## 📝 重要網址記錄

### 前端
- **Vercel 網址**：https://ai-agent-webapp-ten.vercel.app/
- **Vercel 專案**：https://vercel.com/dashboard

### 後端（部署後填入）
- **Railway 網址**：`https://your-railway-domain.railway.app`
- **健康檢查**：`https://your-railway-domain.railway.app/health`
- **API 文檔**：`https://your-railway-domain.railway.app/docs`
- **Railway 專案**：https://railway.app/dashboard

### GitHub
- **倉庫**：https://github.com/adamau1983119/AI_Agent_Webapp

---

## 🎯 下一步行動

1. **立即執行**：部署後端到 Railway
2. **部署後**：更新前端環境變數
3. **最後**：驗證所有功能正常運作

---

**最後更新**：2025-12-29  
**狀態**：✅ 前端已部署，等待後端部署

