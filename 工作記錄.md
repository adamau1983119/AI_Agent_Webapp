# 📋 工作記錄 - AI Agent Webapp for Social Media Content Generation

> **專案名稱**：AI Agent Webapp for Social Media Content Generation  
> **版本**：3.0.0  
> **當前分支**：v3.0.0-development  
> **建立日期**：2025-12-30  
> **最後更新**：2026-01-23  
> **狀態**：開發中

---

## 📌 當前狀態總覽

### 已完成功能
- ✅ DeepSeek API 整合
- ✅ Google Custom Search API 整合
- ✅ 72 個 RSS Feed 來源（時尚 23、美食 19、趨勢 30）
- ✅ Phase 5A - 預覽圖片功能
- ✅ 優惠券/折扣文章過濾器
- ✅ 前端日曆修復
- ✅ API 超時處理
- ✅ React 無限循環修復
- ✅ RSS Feed 問題診斷與方案確認

### 技術決策已確認（2026-01-23）
- ✅ 採用方案 A（按角色分配）解決單一來源壟斷
- ✅ 使用純 MongoDB 做健康監控
- ✅ 多樣性指標納入驗收標準

### 所有 Phase 已完成！🎉🎉🎉（2026-01-23）
- ✅ Phase 5A - 即時預覽圖片（2026-01-22）
- ✅ Phase 1.1 - RSS 多源收集（角色分配）
- ✅ Phase 1.2 - 文章評分服務
- ✅ Phase 1.3 - 多樣性指標
- ✅ Phase 5B - 智能圖片匹配（含來源多樣性）
- ✅ Phase 2 - 健康監控（純 MongoDB）
- ✅ Phase 3 - Health API
- ✅ Phase 4 - Config 外部化
- ✅ **Phase 6 - 圖片匹配系統重構（全部完成！）**

### Phase 6 完成總覽（91 個測試 100% 通過）
| 階段 | 說明 | 測試數 | 狀態 |
|------|------|--------|------|
| 6.1 | 數據模型設計 | 28 | ✅ |
| 6.2 | Repository 層 | 26 | ✅ |
| 6.3 | 雙寫機制 | 4 | ✅ |
| 6.4 | 原文照片提取 | 6 | ✅ |
| 6.5 | Hashtag 提取 | 6 | ✅ |
| 6.6 | 聚合查詢服務 | 3 | ✅ |
| 6.7 | API 端點 | 2 | ✅ |
| 6.8 | 整合到 TopicCollector | 13 | ✅ |
| **總計** | | **91** | **✅** |

### Phase 6 新增核心功能
1. **Article + Photo 雙 Collection 結構**
2. **OriginalImageExtractor** - 從 RSS entry 提取原文照片
3. **HashtagExtractor** - 正則 + 品牌匹配提取 hashtags
4. **ImageMatchingService** - MongoDB 聚合查詢匹配圖片
5. **DualWriteService** - 雙寫機制（向後兼容）
6. **Articles API** - 新增 `/api/v1/articles` 端點

### 待處理
- ⏳ 整合測試（實際環境測試）
- ⏳ 性能測試
- ⏳ 數據遷移（舊 topics → 新 articles）

---

## 📅 工作記錄（按時間倒序）

---

### 2026-01-23（星期四）

**Git 分支**: `v3.0.0-development`

#### ✅ 完成工作

0. **Phase 6 規劃完成**
   - 討論圖片搜尋三大痛點
   - 確認改進方案：原文照片 + hashtags + MongoDB 聚合
   - 建立詳細規劃文件：`docs/planning/06_Phase6_Image_Matching_Redesign.md`
   - 更新檢查表：`docs/planning/02_Implementation_Checklist.md`

1. **Phase 6.1 - 數據模型設計** ✅
   - 建立 `Article` 模型（包含 images.preview/matched）
   - 建立 `Photo` 模型（照片索引結構）
   - 建立 `ImagePreview` 和 `ImageMatched` 子結構
   - 28 個單元測試通過

2. **Phase 6.2 - Repository 層** ✅
   - 建立 `ArticleRepository`（CRUD + 聚合查詢）
   - 建立 `PhotoRepository`（關鍵字匹配查詢）
   - 26 個單元測試通過

3. **Phase 6.3 - 雙寫機制** ✅
   - 建立 `DualWriteService`
   - 實現 `write_article()`（同時寫入 articles + topics）
   - 實現 `migrate_topic()`（遷移舊資料）
   - 實現回滾機制

4. **Phase 6.4 - 原文照片提取** ✅
   - 建立 `OriginalImageExtractor`
   - 從 `media_content`、`media_thumbnail`、`enclosures` 提取
   - 從 HTML `<img>` 標籤提取
   - 自動過濾追蹤像素和廣告

5. **Phase 6.5 - Hashtag 提取** ✅
   - 建立 `HashtagExtractor`
   - 正則提取已有 `#hashtag`
   - 提取專有名詞（大寫開頭）
   - 匹配 200+ 品牌名稱（`config/brands.py`）
   - AI 生成補充（可選）

6. **Phase 6.6 - 聚合查詢服務** ✅
   - 建立 `ImageMatchingService`
   - MongoDB 聚合管道匹配圖片
   - 分數計算：`0.4K + 0.3O + 0.2Q + 0.1D`
   - 多樣性加分機制

7. **Phase 6.7 - API 端點** ✅
   - 建立 `articles.py` router
   - `GET /api/v1/articles` - 文章列表
   - `GET /api/v1/articles/{id}` - 單篇文章
   - `GET /api/v1/articles/{id}/matched-images` - 匹配圖片
   - `POST /api/v1/articles/{id}/refresh-images` - 重新匹配

8. **Phase 6.8 - 整合到 TopicCollector** ✅
   - 整合 `OriginalImageExtractor` 提取原文照片
   - 整合 `HashtagExtractor` 提取 hashtags
   - 整合 `DualWriteService` 雙寫機制
   - 13 個整合測試通過

9. **環境修復與測試** ✅
   - 修復 Python 3.9 兼容性問題：
     * uvicorn: 0.40.0 → 0.23.2
     * websockets: 16.0 → 10.4
     * motor: 3.7.1 → 3.3.2
     * pymongo: 4.16.0 → 4.6.1
     * dnspython: 2.8.0 → 2.4.2
   - 修復 RSS 收集失敗問題：
     * 修復 `scoring_service.py` 中 `time.struct_time` 處理邏輯
     * 所有 RSS 來源現在可以正常收集主題
   - 後端服務成功啟動並通過測試
   - 前端與後端連接正常

**總計：91 個測試 100% 通過**

#### 🐛 修復的問題

1. **RSS 收集失敗** - `'time.struct_time' object has no attribute 'tzinfo'`
   - **原因**：`ScoringService._compute_time_score()` 沒有處理 `feedparser` 返回的 `time.struct_time` 格式
   - **解決**：添加 `time.struct_time` 轉換為 `datetime` 的邏輯
   - **結果**：所有 RSS 來源現在可以正常收集主題，包含來源網站、原文照片和 hashtags

2. **Python 3.9 兼容性問題**
   - **原因**：新版庫使用 Python 3.10+ 的 `type | None` 語法
   - **解決**：降級所有不兼容的庫到支持 Python 3.9 的版本
   - **結果**：後端可以正常啟動和運行

#### 📊 測試結果

- ✅ 後端服務：http://localhost:8000 - **READY**
- ✅ 前端服務：http://localhost:3000 - **READY**
- ✅ RSS 收集測試：成功從 Vogue, Hypebeast, Popbee, Business of Fashion, Who What Wear 收集主題
- ✅ 多樣性分數：score=1.0, sources=5, status=excellent
   - 設計決策：
     - ✅ 數據遷移：要遷移到新的 articles 結構（雙寫過渡）
     - ✅ 優先級：方案 A（先完成 Phase 6，再處理 API Key）
     - ✅ hashtags 來源：方案 C（正則提取 + AI 生成結合）
   - Phase 6 包含 8 個子階段，54 個任務，68 個測試

1. **Phase 1.1 - RSS 多源收集（角色分配）實施完成**
   - 新增 `backend/app/config/feed_roles.py`
     - Fashion 角色：authority, streetwear, asian, industry, practical
     - Food 角色：mainstream, professional, cultural, healthy, casual
     - Trend 角色：tech, science, culture, innovation, lifestyle
   - 重構 `topic_collector.py` 使用 `_collect_by_roles()` 方法
   - 移除「先到先得」的 break 邏輯
   - 每個角色分配 2 個主題，共 10 個主題/分類

2. **Phase 1.2 - 文章評分服務實施完成**
   - 新增 `backend/app/services/scoring_service.py`
   - 評分公式：Score = 0.4T + 0.3S + 0.2C + 0.1R
   - 支援時效性、來源信任度、完整度、相關度評分

3. **Phase 1.3 - 多樣性指標實施完成**
   - 新增 `DiversityScorer` 類
   - 多樣性公式：diversity_score = 1 - (max_ratio - avg_ratio)
   - 驗收標準：diversity_score >= 0.6

4. **Phase 5B - 智能圖片匹配（含多樣性加權）實施完成**
   - 新增 `backend/app/services/images/image_matcher.py`
   - 新增 `KeywordExtractor` 類
   - 評分公式：ImageScore = 0.4K + 0.25T + 0.15Q + 0.2D
   - 多樣性加權：不同來源的圖片獲得 D=1.0 加分

5. **配置檔更新**
   - 更新 `backend/config/topic_generation.yaml` 至 v3.0
   - 加入角色分配配置
   - 加入智能匹配設定
   - 預設 count 從 3 改為 10

6. **整合工作記錄**
   - 將所有每日工作記錄整合到統一的 `工作記錄.md`
   - 刪除 19 個舊的工作記錄文件
   - 更新 README.md 加入工作記錄管理規範

2. **版本管理**
   - 將分支從 `feature/phase5a-preview-images` 重命名為 `v3.0.0-development`
   - 更新 README.md 版本號為 3.0.0

3. **RSS Feed 問題診斷**
   - 發現「單一來源壟斷」問題：所有 Fashion 主題都來自 Vogue
   - 分析根因：`_collect_from_rss` 的「先到先得」邏輯
   - 建立診斷報告：`docs/RSS_Feed多源邏輯問題診斷報告_請求第三方建議.md`

4. **第三方建議審查與技術決策**
   - 收到並分析第三方專家 5 點建議
   - 確認採用方案 A（按角色分配）
   - 確認使用純 MongoDB 做健康監控（暫不引入 Redis）
   - 更新規劃文件 `01_Planning_Report.md` 和 `02_Implementation_Checklist.md`

5. **Phase 2 - 健康監控（純 MongoDB）實施完成**
   - 新增 `backend/app/services/repositories/feed_health_repository.py`
   - 新增 `backend/app/services/feed_health_service.py`
   - 整合到 `topic_collector.py`：自動記錄成功/失敗，跳過暫停的來源

6. **Phase 3 - Health API 實施完成**
   - 新增 `backend/app/api/v1/feeds.py`
   - API 端點：
     - GET /feeds/health - 所有 Feed 健康狀態
     - GET /feeds/health/{category} - 分類健康狀態
     - GET /feeds/stats - 統計摘要
     - GET /feeds/diversity-report - 多樣性報告
     - POST /feeds/pause - 手動暫停
     - POST /feeds/resume - 恢復

7. **Phase 4 - Config 外部化實施完成**
   - 新增 `backend/app/config/config_loader.py`
   - 更新 `topic_generation.yaml` 啟用健康監控

8. **單元測試實施完成並執行成功**
   - 新增 `backend/tests/` 測試目錄
   - 測試文件：
     - `test_scoring_service.py` - 文章評分 + 多樣性測試（14 個）
     - `test_image_matcher.py` - 智能圖片匹配測試（16 個）
     - `test_feed_health.py` - 健康監控測試（16 個）
     - `test_feed_roles.py` - RSS 角色分配測試（19 個）
     - `test_feeds_api.py` - Health API 測試（13 個）
   - **總計 78 個測試案例，100% 通過** ✅
   - 修復環境問題：pydantic、starlette 版本兼容性
   - 修復多樣性分數計算邏輯

#### 🎯 技術決策確認

| 項目 | 決定 | 理由 |
|------|------|------|
| RSS 收集邏輯 | **方案 A（按角色分配）** | 較簡單，天然有速率限制 |
| 健康監控存儲 | **純 MongoDB** | 減少系統複雜度，暫不引入 Redis |
| 分配比例 | **可配置化（YAML）** | 方便根據客戶需求調整 |
| 多樣性指標 | **納入驗收標準** | diversity_score >= 0.6 |
| Phase 5B 圖片 | **加入多樣性加權** | D = 20% 權重 |

#### 📊 FeedHealthRepository 設計（純 MongoDB 版）

```python
class FeedHealthRepository:
    async def is_paused(self, feed_url: str) -> bool:
        """3+ failures in 1 hour → paused"""
        one_hour_ago = datetime.utcnow() - timedelta(hours=1)
        failures = await self.collection.count_documents({
            "feed_url": feed_url,
            "status": "failure",
            "timestamp": {"$gte": one_hour_ago}
        })
        return failures >= 3

    async def record_failure(self, feed_url: str, error: str):
        await self.collection.insert_one({...})

    async def record_success(self, feed_url: str):
        await self.collection.insert_one({...})
```

#### 🔍 發現的問題

| 問題 | 影響 | 狀態 |
|------|------|------|
| RSS Feed 單一來源壟斷 | 內容缺乏獨特性和多樣性 | ✅ 方案已確認 |
| 每個分類應生成 10 個主題 | 配置正確，但預設參數需修改 | ⏳ 待實施 |

#### 📄 新建/更新文件

- `docs/RSS_Feed多源邏輯問題診斷報告_請求第三方建議.md`（新建）
- `docs/planning/01_Planning_Report.md`（更新至 v3.0）
- `docs/planning/02_Implementation_Checklist.md`（更新至 v3.0）

---

### 2026-01-22（星期四）

**Git 分支**: `feature/phase5a-preview-images`

#### ✅ 完成工作

1. **TopicCollector 改良規劃文件**
   - 建立 `docs/planning/` 目錄，包含 4 份規劃文件
   - 01_Planning_Report.md（規劃報告）
   - 02_Implementation_Checklist.md（~221 項檢查清單）
   - 03_Technical_Specification.md（技術規格）
   - 04_Image_Matching_Logic.md（圖片匹配邏輯）

2. **Phase 5A 實作完成**
   - 修改 `scheduler.py`：從 `sources[].images` 提取預覽圖片
   - 主題卡片現在會立即顯示 RSS 文章圖片

3. **前端日曆修復**
   - 修正星期計算邏輯（週一開始）
   - 修改 `Calendar.tsx`

4. **刪除所有主題 API**
   - 新增端點：`DELETE /api/v1/topics/all`

5. **MongoDB SourceStyle 編碼修復**
   - 修復 Pydantic 物件未轉換為 dict 的問題

6. **趨勢類別優惠券過濾器**
   - 新增 `_is_deal_or_coupon_article()` 函數
   - 過濾 WIRED RSS Feed 中的優惠券文章

7. **標題生成 Prompt 改進**
   - 禁止生成折扣/優惠相關內容
   - 添加分類特定指引

**Git 提交**:
- `ba24e1d` - docs: add TopicCollector improvement planning documents
- `32efeeb` - feat: Phase 5A 實作 + 日曆修復 + 刪除 API
- `17fa50f` - fix: 過濾趨勢類別中的優惠券/折扣文章

---

### 2026-01-21（星期三）

**Git 分支**: `feature/backup-2026-01-20-core-fixes`

#### ✅ 完成工作

1. **RSS Feed 擴展（72 個）**
   - 時尚趨勢：23 個（Vogue, Elle, Hypebeast 等）
   - 美食推薦：19 個（Eater, Bon Appétit 等）
   - 社會趨勢：30 個（WIRED, TechCrunch, The Verge 等）

2. **API 超時問題修復**
   - 資料庫連接檢查：2 秒超時
   - 主題查詢：5 秒超時
   - 超時時返回預設排程數據

3. **文章提取器實現**
   - 新增 `ArticleExtractor` 類
   - 功能：原文圖片提取、內容提取、語言檢測、風格分析

4. **React 無限循環修復**
   - 使用 `useMemo` 計算今日主題數量
   - 使用 `useRef` 追蹤狀態
   - 優化 `useEffect` 依賴項

5. **資料模型擴展**
   - Topic: `images`, `language`, `style`, `original_content`, `original_title`
   - Content: `source_urls`, `source_images`
   - Image: `ImageType` 枚舉

6. **AI Prompt 改進**
   - 接受原始內容、來源 URL、語言、風格資訊
   - 要求基於來源內容改寫

**修改文件**:
- `backend/app/api/v1/schedules.py`
- `backend/app/utils/article_extractor.py`（新增）
- `backend/app/services/automation/topic_collector.py`
- `backend/app/models/topic.py`, `content.py`, `image.py`
- `frontend/src/pages/Dashboard.tsx`

---

### 2026-01-20（星期一）

**Git 分支**: `feature/backup-2026-01-20-core-fixes`

#### ✅ 完成工作

1. **API Key 配置完成**
   - DeepSeek API Key
   - Google Custom Search API Key
   - Google Search Engine ID

2. **智能匹配照片功能修復**
   - 改用 `ImageServiceManager`
   - 動態關鍵字提取（英文專有名詞、中文詞語）
   - 降低匹配度閾值（核心≥0.6，非核心≥0.5）
   - 動態閾值調整機制
   - 增加超時時間（60秒）

3. **主題卡片佈局調整**
   - 圖片改為左上角小圖標
   - 文字內容佔據主要版面

4. **關鍵字提取改進**
   - 提取英文專有名詞
   - 提取簡潔中文關鍵字（2-4字）
   - 移除停用詞

**修改文件**:
- `backend/app/services/images/enhanced_photo_matcher.py`
- `frontend/src/api/images.ts`, `client.ts`
- `frontend/src/components/ui/TopicCard.tsx`
- `frontend/src/components/features/ImageSearch.tsx`

---

### 2026-01-15（星期三）

#### ✅ 完成工作

1. **客戶新需求分析**
   - 主題生成邏輯調整：每天 07:00 生成 10 個標題 + 1 張預覽圖片
   - 延遲生成機制：點擊標題才生成文章與 8 張圖片
   - 圖片搜尋優化：preview 與 full 模式

2. **程式編寫邏輯與介面設計**
   - API 端點：`POST /api/v1/topics/{id}/expand`
   - 搜尋方向調整邏輯設計
   - 快取機制設計

3. **配置檔設計**
   - `backend/config/topic_generation.yaml`
   - 每日生成、圖片搜尋、快取設定

4. **第三方專業建議整合**
   - 輸入驗證與安全性
   - 搜尋策略可配置化
   - 快取與重複生成控制
   - 圖片搜尋容錯機制
   - 前端 UX 優化

---

### 2026-01-13（星期一）

#### ✅ 完成工作

1. **Dashboard UI 優化**
   - 進度卡片縮小 50%
   - 六個功能卡片並列顯示
   - 今日主題按鈕改為紫色

2. **功能說明文檔創建**
   - Dashboard 統計卡片功能說明
   - 即將來到事件與最近活動功能說明
   - 後台今日主題搜尋邏輯說明

3. **Token 使用量分析**
   - 每個主題約 2000 tokens
   - 每日成本約 $0.04-0.05

---

### 2026-01-12（星期五）

#### ✅ 完成工作

1. **專家建議實施計劃**
   - CORS 限制 → 後端圖片代理
   - URL 類型問題 → URL 驗證 + 過濾
   - Google CSE 配置 → 專用圖片 CSE + 參數優化

---

### 2026-01-08（星期三）

#### ✅ 完成工作

1. **任務執行報告**
   - 前端 source 類型定義檢查 ✅
   - 後端日誌完整性檢查 ✅
   - Google Custom Search API 測試：100% 成功率 ✅

---

### 2026-01-07（星期二）

#### ✅ 完成工作

1. **DeepSeek API 成功運作**

2. **圖片搜尋問題修復**
   - 根因：`responseInterceptor` 自動提取 `data` 欄位
   - 解決：使用直接 `fetch` 獲取完整響應

3. **問題修復**
   - 編碼錯誤（`image_repository.py`）
   - 前端載入問題（添加超時和錯誤處理）
   - 速率限制問題（改進重試策略）
   - 緩存清除問題（移除 `queryClient.clear()`）

#### 經驗教訓
- 編碼問題：必須使用 UTF-8 編碼
- 調試代碼：不要在組件主體中執行影響全局狀態的操作
- API 響應：需考慮不同 API 的響應結構差異

---

### 2026-01-06（星期一）

#### ✅ 完成工作

1. **Google API 圖片搜尋修復**
   - 改用 `ImageServiceManager`

2. **版本號管理**
   - v1.0.0：`036362c`（2025-12-29）
   - v2.0.0：`ff8f9dc`（2025-12-30）

---

### 2025-12-30（星期一）

#### ✅ 完成工作

1. **DeepSeek API 整合**
   - 創建 `deepseek.py` 服務
   - 更新配置和工廠

2. **InteractionRepository 修復**
   - 修復 MongoDB 異步操作

3. **前端推薦 API 臨時禁用**
   - 避免 500 錯誤影響 Dashboard

4. **Railway 部署成功**
   - 環境變數驗證通過
   - MongoDB 連接成功
   - 排程服務已啟動

---

## 🔧 技術架構

### 後端 (Python FastAPI)
```
backend/app/
├── api/v1/          # API 端點
├── models/          # MongoDB 資料模型
├── schemas/         # Pydantic 驗證
├── services/
│   ├── ai/          # AI 服務 (DeepSeek, Gemini 等)
│   ├── automation/  # 排程和主題收集
│   └── images/      # 圖片搜尋服務
├── prompts/         # AI Prompt 模板
└── middleware/      # 認證和限流
```

### 前端 (React TypeScript)
```
frontend/src/
├── api/             # API 客戶端
├── components/      # UI 組件
├── pages/           # 頁面
└── stores/          # Zustand 狀態管理
```

---

## 📊 規劃階段

### Phase 實施計劃（v3.0 - 全部完成）

| 順序 | Phase | 名稱 | 狀態 | 完成日期 |
|------|-------|------|------|----------|
| - | Phase 5A | 即時預覽圖片 | ✅ **完成** | 2026-01-22 |
| 1 | Phase 1.1 | RSS 多源收集（角色分配） | ✅ **完成** | 2026-01-23 |
| 2 | Phase 1.2 | 文章評分 | ✅ **完成** | 2026-01-23 |
| 3 | Phase 1.3 | 多樣性指標 | ✅ **完成** | 2026-01-23 |
| 4 | Phase 5B | 智能圖片匹配 | ✅ **完成** | 2026-01-23 |
| 5 | Phase 2 | 健康監控（純 MongoDB） | ✅ **完成** | 2026-01-23 |
| 6 | Phase 3 | Health API | ✅ **完成** | 2026-01-23 |
| 7 | Phase 4 | Config 外部化 | ✅ **完成** | 2026-01-23 |

**🎉 所有 Phase 已完成！**

### RSS 角色分配方案（方案 A）

```
Fashion (10 topics):
├── Authority (2): Vogue, Elle
├── Streetwear (2): Hypebeast, Highsnobiety
├── Asian (2): Hypebae, SCMP Style
├── Industry (2): BoF, WWD
└── Practical (2): Refinery29, Who What Wear

Food (10 topics):
├── Mainstream (2): Eater, Bon Appétit
├── Professional (2): Food & Wine, Serious Eats
├── Cultural (2): Saveur, Taste Atlas
├── Healthy (2): EatingWell, Cooking Light
└── Casual (2): Delish, Tasty

Trend (10 topics):
├── Tech (2): TechCrunch, The Verge
├── Science (2): Ars Technica, Quanta
├── Culture (2): Vox, The Atlantic
├── Innovation (2): WIRED, MIT Technology Review
└── Lifestyle (2): Fast Company, Curbed
```

### Phase 5B 演算法（更新）
```
ImageScore = 0.4K + 0.25T + 0.15Q + 0.2D

K = Keyword Match (關鍵字匹配) - 40%
T = Trust Score (來源信任度) - 25%
Q = Quality Score (圖片品質) - 15%
D = Diversity Bonus (來源多樣性) - 20% ← 新增
```

### 多樣性指標
```
diversity_score = 1 - (max_ratio - avg_ratio)

驗收標準：diversity_score >= 0.6
```

---

## ⚠️ 重要提醒

### Cursor AI 行為規則
1. **永遠不要直接修改 main 分支**
2. **受保護的核心文件修改前必須先備份**
3. **禁止刪除核心目錄結構**

### 受保護文件
- `backend/app/main.py`, `config.py`, `database.py`
- `backend/app/api/v1/*.py`
- `frontend/src/api/*.ts`
- `frontend/src/router/*.tsx`

---

## 🚀 啟動指令

**後端**:
```bash
cd backend
.\venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**前端**:
```bash
cd frontend
npm run dev
```

**訪問**:
- 前端：http://localhost:3000 或 http://localhost:5173
- 後端 API：http://localhost:8000
- API 文檔：http://localhost:8000/docs

---

## 📝 備註

- 此文件為統一工作記錄，請勿另建每日記錄文件
- 新的工作內容請添加到「工作記錄（按時間倒序）」區段的最前面
- 重大功能完成後請更新「當前狀態總覽」區段

---

**維護者**：開發團隊  
**最後更新**：2026-01-23

