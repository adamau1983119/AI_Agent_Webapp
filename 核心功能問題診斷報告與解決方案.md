# 核心功能問題診斷報告與解決方案

> **報告日期**：2026-01-20  
> **報告類型**：第三方專家技術診斷報告  
> **嚴重程度**：🔴 極高（影響所有核心業務功能）

---

## 📋 執行摘要

本報告針對 AI 代理 Web 應用程式的五個核心功能問題進行深入診斷，並提供專業的解決方案。所有問題都與系統的核心業務流程相關，需要立即修復。

**核心問題清單：**
1. ❌ **未能生成相關主題的照片** - 主題卡片顯示佔位圖而非實際圖片
2. ❌ **未能生成30字撮要內容** - 主題卡片缺少內容摘要
3. ❌ **未能生成主題500字內容** - 點擊生成內容按鈕後無法生成文章
4. ❌ **未能按照內容尋找八張相關照片** - 智能匹配照片功能無法正常工作
5. ❌ **整個專案的核心內容一直未能實現** - 系統無法完成完整的內容生成流程

---

## 🔍 問題 1：未能生成相關主題的照片

### 問題描述

**症狀：**
- 主題卡片顯示漸層背景佔位圖，而非實際照片
- 前端顯示「圖片數量：0張」
- 主題詳情頁面顯示「沒有圖片」

**預期行為：**
- 每個主題應該有至少 1 張預覽圖片
- 圖片應該顯示在主題卡片上
- 圖片應該與主題內容相關

### 根本原因分析

#### 原因 1.1：配置問題 - 圖片搜尋服務未配置 ⚠️ **關鍵問題**

**問題：**
- 所有圖片服務的 API Key 都未設定（UNSPLASH_ACCESS_KEY、PEXELS_API_KEY、PIXABAY_API_KEY、GOOGLE_API_KEY）
- 系統回退到 DuckDuckGo，但 DuckDuckGo 搜尋可能失敗或返回空結果

**證據：**
- 配置檔案 `backend/app/config_module.py` 顯示所有圖片服務 API Key 為空字符串
- 環境變數驗證日誌顯示「所有圖片服務的 API Key 都未設定」

**影響：**
- 圖片搜尋功能完全無法使用
- 即使有備援機制（DuckDuckGo），也可能因為其他錯誤而失敗

#### 原因 1.2：圖片搜尋邏輯問題

**問題：**
- `workflow._search_and_add_images()` 方法在搜尋圖片時可能因為 API Key 未設定而失敗
- 錯誤處理不完善，失敗時沒有明確的錯誤提示

**證據：**
- `backend/app/services/automation/workflow.py` 第 224-335 行
- 圖片搜尋失敗時只記錄警告，不影響主流程

#### 原因 1.3：圖片 URL 更新邏輯已修復 ✅

**狀態：** 已修復
- `workflow._search_and_add_images()` 方法已包含更新 `preview_images` 字段的邏輯（第 313-328 行）
- 圖片搜尋完成後會更新主題文檔的 `preview_images` 字段

### 解決方案

#### 方案 1.1：配置圖片搜尋服務 API Key（必須）🔴 **優先級最高**

**步驟：**

1. **選擇圖片服務（至少配置一個）：**

   **選項 A：Google Custom Search API（推薦）**
   - 訪問：https://console.cloud.google.com/apis/credentials
   - 創建 API Key
   - 創建 Custom Search Engine：https://programmablesearchengine.google.com/controlpanel/create
   - 獲取 Search Engine ID
   - 在 `.env` 或 Railway 環境變數中添加：
     ```env
     GOOGLE_API_KEY=your_google_api_key
     GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id
     ```

   **選項 B：Unsplash API（簡單）**
   - 訪問：https://unsplash.com/developers
   - 創建應用並獲取 Access Key
   - 在 `.env` 或 Railway 環境變數中添加：
     ```env
     UNSPLASH_ACCESS_KEY=your_unsplash_access_key
     ```

   **選項 C：Pexels API（免費）**
   - 訪問：https://www.pexels.com/api/
   - 創建帳號並獲取 API Key
   - 在 `.env` 或 Railway 環境變數中添加：
     ```env
     PEXELS_API_KEY=your_pexels_api_key
     ```

2. **驗證配置：**
   - 重啟後端服務
   - 檢查日誌，確認圖片服務已配置
   - 測試圖片搜尋功能

#### 方案 1.2：改進錯誤處理和日誌記錄

**修改文件：** `backend/app/services/automation/workflow.py`

**建議：**
- 在圖片搜尋失敗時記錄詳細錯誤訊息
- 如果所有圖片服務都失敗，返回明確的錯誤提示
- 在前端顯示友好的錯誤訊息

---

## 🔍 問題 2：未能生成30字撮要內容

### 問題描述

**症狀：**
- 主題卡片不顯示30字內容撮要
- 前端顯示「暫無內容撮要」佔位符
- 主題詳情頁面沒有顯示摘要

**預期行為：**
- 每個主題應該有約30字的中文摘要
- 摘要應該顯示在主題卡片上
- 摘要應該簡潔說明主題的核心內容

### 根本原因分析

#### 原因 2.1：RSS Feed 收集的主題未生成 description ✅ **已修復**

**問題：**
- `topic_collector._collect_from_rss()` 方法在收集主題時沒有生成 `description` 字段
- 只有從備用關鍵字生成的主題才有 `description`

**狀態：** 已修復
- `backend/app/services/automation/topic_collector.py` 已修改
- `_translate_title_to_chinese()` 方法現在返回標題和30字撮要
- `_collect_from_rss()` 方法現在保存 `description` 字段

#### 原因 2.2：前端顯示邏輯 ✅ **已修復**

**狀態：** 已修復
- `frontend/src/components/ui/TopicCard.tsx` 已修改
- 現在始終顯示內容撮要區域
- 如果沒有 `description`，顯示「暫無內容撮要」佔位符

### 解決方案

#### 方案 2.1：驗證修復是否生效

**步驟：**
1. 重新生成主題（刪除現有主題並重新生成）
2. 檢查新生成的主題是否有 `description` 字段
3. 檢查前端是否正確顯示摘要

#### 方案 2.2：確保 AI 服務正常運行

**要求：**
- DeepSeek API Key 必須正確配置（見問題 3）
- AI 服務必須能夠正常調用
- 翻譯標題時才能生成30字撮要

---

## 🔍 問題 3：未能生成主題500字內容

### 問題描述

**症狀：**
- 點擊「生成內容（500字文章 + 30秒腳本）」按鈕後無法生成內容
- 前端顯示錯誤訊息：「DeepSeek API Key 未設定」
- 主題詳情頁面顯示「尚未生成內容」

**預期行為：**
- 點擊生成內容按鈕後，應該生成500字文章和30秒腳本
- 內容應該保存到資料庫
- 前端應該顯示生成的內容

### 根本原因分析

#### 原因 3.1：DeepSeek API Key 未配置 🔴 **關鍵問題**

**問題：**
- `.env` 文件中的 `DEEPSEEK_API_KEY` 設置為 `your_api_key_here`（佔位符）
- 實際的 API Key 未設置
- AI 服務無法調用，導致內容生成失敗

**證據：**
- `backend/.env` 文件顯示：`DEEPSEEK_API_KEY=your_api_key_here`
- 後端日誌顯示：「DeepSeek API Key 未設定」
- `backend/app/services/ai/deepseek.py` 第 54-56 行檢查 API Key

**影響：**
- 所有 AI 生成功能都無法使用
- 包括：內容生成、標題翻譯、30字撮要生成

### 解決方案

#### 方案 3.1：配置 DeepSeek API Key（必須）🔴 **優先級最高**

**步驟：**

1. **獲取 DeepSeek API Key：**
   - 訪問：https://platform.deepseek.com/api_keys
   - 登入或註冊帳號
   - 創建新的 API Key（格式：`sk-...`）
   - 複製並保存 API Key

2. **配置 API Key：**

   **本地開發環境：**
   - 打開 `backend/.env` 文件
   - 找到：`DEEPSEEK_API_KEY=your_api_key_here`
   - 替換為：`DEEPSEEK_API_KEY=sk-你的實際API Key`
   - 保存文件

   **Railway 生產環境：**
   - 訪問 Railway Dashboard → Variables
   - 添加環境變數：
     - Key: `DEEPSEEK_API_KEY`
     - Value: `sk-你的實際API Key`
     - Environment: Production（或 All Environments）
   - 保存並等待重新部署

3. **驗證配置：**
   - 重啟後端服務（本地）或等待 Railway 重新部署
   - 檢查後端日誌，確認沒有「API Key 未設定」的錯誤
   - 測試生成內容功能

#### 方案 3.2：改進錯誤處理 ✅ **已修復**

**狀態：** 已修復
- `backend/app/api/v1/contents.py` 已改進錯誤處理
- `frontend/src/pages/TopicDetail.tsx` 已改進錯誤顯示
- 現在會顯示清晰的配置指導訊息

---

## 🔍 問題 4：未能按照內容尋找八張相關照片

### 問題描述

**症狀：**
- 點擊「智能匹配照片（8張）」按鈕後無法匹配照片
- 前端顯示錯誤：「請先生成內容才能匹配照片」
- 即使有內容，匹配功能也可能失敗

**預期行為：**
- 生成內容後，應該能夠根據文章內容智能匹配8張相關照片
- 照片應該與文章內容高度相關
- 照片應該保存到資料庫並顯示在主題詳情頁面

### 根本原因分析

#### 原因 4.1：依賴關係問題

**問題：**
- 智能匹配照片功能需要先有文章內容
- 如果內容未生成，匹配功能無法使用
- 這是一個正確的設計，但錯誤提示不夠清晰

**證據：**
- `backend/app/api/v1/images.py` 第 550-562 行檢查內容是否存在
- 如果內容不存在，返回 404 錯誤

#### 原因 4.2：圖片搜尋服務未配置 ⚠️ **與問題 1 相同**

**問題：**
- 所有圖片服務的 API Key 都未設定
- 即使有內容，圖片搜尋也可能失敗

**影響：**
- 智能匹配功能無法正常工作
- 即使內容已生成，也無法搜尋到相關圖片

#### 原因 4.3：匹配邏輯複雜度問題

**問題：**
- `EnhancedPhotoMatcher` 使用分層閾值檢查（核心≥0.85，非核心≥0.75）
- 如果匹配度要求過高，可能無法找到足夠的照片
- 關鍵字提取可能不夠準確

**證據：**
- `backend/app/services/images/enhanced_photo_matcher.py` 第 19-99 行
- 匹配邏輯較複雜，可能導致匹配失敗

### 解決方案

#### 方案 4.1：配置圖片搜尋服務 API Key（必須）🔴 **與問題 1 相同**

**要求：**
- 必須配置至少一個圖片服務的 API Key
- 推薦使用 Google Custom Search API 或 Unsplash API
- 詳細步驟見「問題 1 解決方案」

#### 方案 4.2：改進匹配邏輯

**建議：**
- 降低匹配度閾值（如果無法找到足夠照片）
- 改進關鍵字提取邏輯
- 添加更多備援搜尋策略

#### 方案 4.3：改進錯誤提示

**建議：**
- 如果內容未生成，顯示清晰的提示：「請先點擊「生成內容」按鈕生成文章內容」
- 如果圖片搜尋失敗，顯示具體的錯誤原因和解決建議

---

## 🔍 問題 5：整個專案的核心內容一直未能實現

### 問題描述

**症狀：**
- 系統無法完成完整的內容生成流程
- 用戶無法正常使用核心功能
- 所有功能都依賴於其他功能的成功執行

**預期行為：**
- 用戶應該能夠：
  1. 生成主題（帶標題和30字撮要）
  2. 為主題生成預覽圖片
  3. 生成500字文章和30秒腳本
  4. 根據文章內容匹配8張相關照片
  5. 查看完整的主題內容和圖片

### 根本原因分析

#### 原因 5.1：配置問題 - 所有 API Key 未配置 🔴 **根本原因**

**問題：**
- DeepSeek API Key 未配置 → 無法生成內容
- 圖片服務 API Key 未配置 → 無法搜尋圖片
- 導致整個系統無法正常工作

**影響：**
- 所有核心功能都無法使用
- 系統處於非功能狀態

#### 原因 5.2：依賴關係鏈問題

**問題：**
- 功能之間存在嚴格的依賴關係：
  - 生成內容 → 需要 DeepSeek API Key
  - 搜尋圖片 → 需要圖片服務 API Key
  - 智能匹配照片 → 需要內容 + 圖片服務 API Key
  - 顯示圖片 → 需要圖片已搜尋並保存

**影響：**
- 任何一個環節失敗，都會導致整個流程中斷
- 錯誤處理不夠完善，用戶無法知道具體問題

#### 原因 5.3：錯誤處理和用戶體驗問題

**問題：**
- 錯誤訊息不夠清晰
- 用戶不知道如何解決問題
- 缺少配置指導和驗證機制

### 解決方案

#### 方案 5.1：完整配置所有必需的 API Key（必須）🔴 **優先級最高**

**必需配置：**

1. **DeepSeek API Key（必須）**
   ```env
   DEEPSEEK_API_KEY=sk-你的實際API Key
   AI_SERVICE=deepseek
   ```

2. **圖片服務 API Key（至少一個，必須）**
   ```env
   # 選項 A：Google Custom Search（推薦）
   GOOGLE_API_KEY=your_google_api_key
   GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id
   
   # 或選項 B：Unsplash
   UNSPLASH_ACCESS_KEY=your_unsplash_access_key
   
   # 或選項 C：Pexels
   PEXELS_API_KEY=your_pexels_api_key
   ```

3. **MongoDB 配置（必須）**
   ```env
   MONGODB_URL=mongodb+srv://...
   MONGODB_DB_NAME=ai_agent_webapp
   ```

#### 方案 5.2：添加配置驗證和診斷工具

**建議：**
- 添加啟動時配置檢查
- 提供配置診斷 API 端點
- 在前端顯示配置狀態

#### 方案 5.3：改進錯誤處理和用戶指導

**建議：**
- 所有錯誤都應該提供清晰的解決建議
- 添加配置指南和故障排除文檔
- 提供配置檢查工具

---

## 📊 問題優先級和修復順序

### 🔴 優先級 1（必須立即修復）

1. **配置 DeepSeek API Key**
   - 影響：所有 AI 生成功能
   - 修復時間：5 分鐘
   - 難度：簡單

2. **配置圖片搜尋服務 API Key**
   - 影響：所有圖片搜尋功能
   - 修復時間：10-15 分鐘
   - 難度：簡單

### 🟡 優先級 2（已修復，需要驗證）

3. **30字撮要內容生成**
   - 狀態：已修復
   - 需要：重新生成主題以驗證

4. **圖片 URL 更新邏輯**
   - 狀態：已修復
   - 需要：配置圖片服務後驗證

### 🟢 優先級 3（改進建議）

5. **錯誤處理和用戶體驗**
   - 狀態：部分改進
   - 需要：持續優化

---

## 🛠️ 立即行動計劃

### 步驟 1：配置 DeepSeek API Key（5 分鐘）

1. 訪問 https://platform.deepseek.com/api_keys
2. 創建並複製 API Key
3. 更新 `backend/.env` 文件：
   ```env
   DEEPSEEK_API_KEY=sk-你的實際API Key
   AI_SERVICE=deepseek
   ```
4. 如果使用 Railway，在環境變數中添加相同配置
5. 重啟後端服務

### 步驟 2：配置圖片搜尋服務 API Key（10-15 分鐘）

**推薦：Google Custom Search API**

1. 訪問 https://console.cloud.google.com/apis/credentials
2. 創建 API Key
3. 訪問 https://programmablesearchengine.google.com/controlpanel/create
4. 創建 Custom Search Engine，獲取 Search Engine ID
5. 更新 `backend/.env` 文件：
   ```env
   GOOGLE_API_KEY=your_google_api_key
   GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id
   ```
6. 如果使用 Railway，在環境變數中添加相同配置
7. 重啟後端服務

### 步驟 3：驗證配置（5 分鐘）

1. 檢查後端日誌，確認沒有配置錯誤
2. 測試生成主題功能
3. 測試生成內容功能
4. 測試圖片搜尋功能

### 步驟 4：重新生成主題（可選）

1. 刪除現有主題（如果有的話）
2. 重新生成主題
3. 驗證新主題是否有：
   - ✅ 30字撮要內容
   - ✅ 預覽圖片
   - ✅ 可以生成500字內容
   - ✅ 可以匹配8張相關照片

---

## 📝 配置檢查清單

### 必需配置項

- [ ] `DEEPSEEK_API_KEY` - DeepSeek API Key（格式：`sk-...`）
- [ ] `AI_SERVICE` - 設置為 `deepseek`
- [ ] 至少一個圖片服務 API Key：
  - [ ] `GOOGLE_API_KEY` + `GOOGLE_SEARCH_ENGINE_ID`（推薦）
  - [ ] 或 `UNSPLASH_ACCESS_KEY`
  - [ ] 或 `PEXELS_API_KEY`
  - [ ] 或 `PIXABAY_API_KEY`
- [ ] `MONGODB_URL` - MongoDB 連接字符串
- [ ] `MONGODB_DB_NAME` - 資料庫名稱

### 可選配置項

- [ ] `DEEPSEEK_MODEL` - DeepSeek 模型（預設：`deepseek-chat`）
- [ ] `ENVIRONMENT` - 環境類型（`development` 或 `production`）

---

## 🎯 預期結果

配置完成後，系統應該能夠：

1. ✅ **生成主題** - 自動生成30個主題（3個分類 × 10個主題）
2. ✅ **顯示30字撮要** - 每個主題都有內容摘要
3. ✅ **顯示預覽圖片** - 每個主題都有至少1張預覽圖片
4. ✅ **生成500字內容** - 點擊生成內容按鈕後成功生成文章和腳本
5. ✅ **匹配8張照片** - 根據文章內容智能匹配相關照片
6. ✅ **完整流程** - 從主題生成到內容和圖片匹配的完整流程都能正常工作

---

## 📞 技術支援

如果按照上述步驟配置後仍有問題，請檢查：

1. **後端日誌** - 查看具體錯誤訊息
2. **API Key 格式** - 確認 API Key 格式正確
3. **環境變數** - 確認環境變數已正確設置並生效
4. **網路連接** - 確認後端能夠訪問外部 API
5. **資料庫連接** - 確認 MongoDB 連接正常

---

## 📅 報告總結

**報告生成時間：** 2026-01-20  
**問題數量：** 5 個核心問題  
**已修復：** 2 個（30字撮要、圖片 URL 更新）  
**需要配置：** 2 個（DeepSeek API Key、圖片服務 API Key）  
**需要改進：** 1 個（錯誤處理和用戶體驗）

**關鍵結論：**
- 🔴 **所有問題的根本原因都是配置問題**
- ✅ **代碼邏輯已經正確實現**
- ⚠️ **只需要正確配置 API Key 即可解決大部分問題**
- 📋 **按照本報告的步驟操作，應該能夠在 30 分鐘內解決所有問題**

---

**報告結束**

