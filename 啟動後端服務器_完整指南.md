# 啟動後端服務器完整指南

## 🚀 快速啟動

### 方法 1：使用啟動腳本（推薦）

**Windows CMD：**
```cmd
雙擊：快速啟動後端.bat
```

**PowerShell：**
```powershell
.\啟動後端服務器.ps1
```

### 方法 2：手動啟動

**在新的終端視窗中執行：**

```powershell
# 1. 進入後端目錄
cd "F:\Adam 2025\Myproject\AI_Agent_Wbbapp_for_Social_Media_Content_Generation\backend"

# 2. 直接使用虛擬環境的 Python（不需要激活）
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## ✅ 成功啟動的標誌

您應該會看到類似以下的輸出：

```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx] using WatchFiles
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

## 🔍 檢查服務器狀態

### 1. 檢查端口是否被占用

```powershell
netstat -ano | Select-String ":8000"
```

如果看到端口 8000 被占用，說明服務器正在運行。

### 2. 測試健康檢查端點

在瀏覽器中訪問：
```
http://localhost:8000/api/v1/health
```

應該會看到 JSON 回應。

### 3. 查看 API 文檔

在瀏覽器中訪問：
```
http://localhost:8000/docs
```

應該會看到 Swagger API 文檔界面。

## 🔄 連接到 Dashboard

後端啟動成功後：

1. **回到 Dashboard**：`http://localhost:3000`
2. **點擊「重試」按鈕**
3. **或刷新頁面**（F5）

應該就能連接到 API 並顯示數據了！

## ⚠️ 常見問題

### 問題 1：端口已被占用

**解決方案：**
```powershell
# 找到占用端口的進程
netstat -ano | Select-String ":8000"

# 終止進程（替換 PID 為實際進程 ID）
taskkill /PID <PID> /F
```

### 問題 2：依賴未安裝

**解決方案：**
```powershell
cd backend
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 問題 3：MongoDB 連接失敗

**檢查：**
- MongoDB 是否運行（如果使用本地 MongoDB）
- MongoDB 連接字串是否正確（`.env` 文件中的 `MONGODB_URL`）

### 問題 4：導入錯誤

**已修復的問題：**
- ✅ `Dict` 和 `Any` 導入已添加
- ✅ `__init__.py` 已添加到 `app/config` 目錄
- ✅ `PyYAML` 和 `pytz` 依賴已安裝

## 📝 當前配置

- **後端端口：** 8000
- **API 基礎 URL：** `http://localhost:8000/api/v1`
- **健康檢查：** `http://localhost:8000/api/v1/health`
- **API 文檔：** `http://localhost:8000/docs`

## 🎯 下一步

1. ✅ 啟動後端服務器
2. ✅ 檢查健康檢查端點
3. ✅ 刷新 Dashboard
4. ✅ 測試 API 連接

---

**如果仍有問題，請提供具體錯誤訊息！**

