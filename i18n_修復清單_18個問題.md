# i18n 修復清單 - 18 個需要修復的問題

## 📊 統計
- **總問題數**: 18
- **涉及文件數**: 10
- **前端問題**: 8 個
- **後端問題**: 10 個

---

## 🔴 後端問題 (10 個)

### 1. `backend/app/api/v1/schedules.py` (2 個問題)

#### 問題 1: 第 310 行
- **類型**: Error suggestion
- **文字**: `請檢查 MONGODB_URL 並確保 MongoDB 服務正在運行`
- **上下文**: `"suggestion": "請檢查 MONGODB_URL 並確保 MongoDB 服務正在運行"`
- **修復方案**: 使用 i18n 工具函數返回多語言錯誤建議

#### 問題 2: 第 338 行
- **類型**: Error suggestion
- **文字**: `請檢查 MONGODB_URL 配置和 MongoDB 服務狀態`
- **上下文**: `"suggestion": "請檢查 MONGODB_URL 配置和 MongoDB 服務狀態"`
- **修復方案**: 使用 i18n 工具函數返回多語言錯誤建議

---

### 2. `backend/app/services/automation/workflow.py` (1 個問題)

#### 問題 3: 第 126 行
- **類型**: API response message
- **文字**: `所有圖片服務的 API Key 都未設定，圖片搜尋失敗`
- **上下文**: `message="所有圖片服務的 API Key 都未設定，圖片搜尋失敗",`
- **修復方案**: 使用 `_get_error_message()` 函數或錯誤訊息字典

---

### 3. `backend/app/services/distribution_service.py` (5 個問題)

#### 問題 4: 第 56 行
- **類型**: Service error message
- **文字**: `斷開連接失敗`
- **上下文**: `return False, "斷開連接失敗"`
- **修復方案**: 使用 `_get_error_message()` 函數

#### 問題 5: 第 101 行
- **類型**: Service error message
- **文字**: `Token 交換失敗`
- **上下文**: `return None, "Token 交換失敗"`
- **修復方案**: 使用 `_get_error_message()` 函數

#### 問題 6: 第 110 行
- **類型**: Service error message
- **文字**: `無法取得用戶資訊`
- **上下文**: `return None, "無法取得用戶資訊"`
- **修復方案**: 使用 `_get_error_message()` 函數

#### 問題 7: 第 268 行
- **類型**: Service error message
- **文字**: `TikTok API 整合尚未完成`
- **上下文**: `return None, "TikTok API 整合尚未完成"`
- **修復方案**: 使用 `_get_error_message()` 函數

#### 問題 8: 第 299 行
- **類型**: Service error message
- **文字**: `沒有有效的平台連接`
- **上下文**: `return None, "沒有有效的平台連接"`
- **修復方案**: 使用 `_get_error_message()` 函數

---

### 4. `backend/app/services/style_learning_service.py` (2 個問題)

#### 問題 9: 第 51 行
- **類型**: Service error message
- **文字**: `更新風格失敗`
- **上下文**: `return None, "更新風格失敗"`
- **修復方案**: 使用 `_get_error_message()` 函數

#### 問題 10: 第 60 行
- **類型**: Service error message
- **文字**: `重置失敗`
- **上下文**: `return False, "重置失敗"`
- **修復方案**: 使用 `_get_error_message()` 函數

---

## 🔴 前端問題 (8 個)

### 5. `frontend/src/api/channels.ts` (1 個問題)

#### 問題 11: 第 178 行
- **類型**: Frontend hardcoded text
- **文字**: `其他（自定義）`
- **上下文**: `other: '其他（自定義）',`
- **修復方案**: 使用 `t('channels.other')` 或類似的翻譯鍵

---

### 6. `frontend/src/api/topics.ts` (2 個問題)

#### 問題 12: 第 218 行
- **類型**: Frontend hardcoded text
- **文字**: `搜尋關鍵字至少需要 2 個字元`
- **上下文**: `throw new Error('搜尋關鍵字至少需要 2 個字元')`
- **修復方案**: 使用 `t('topics.search.minLength')` 或類似的翻譯鍵

#### 問題 13: 第 221 行
- **類型**: Frontend hardcoded text
- **文字**: `搜尋關鍵字最多 100 個字元`
- **上下文**: `throw new Error('搜尋關鍵字最多 100 個字元')`
- **修復方案**: 使用 `t('topics.search.maxLength')` 或類似的翻譯鍵

---

### 7. `frontend/src/components/features/ImageSearch.tsx` (1 個問題)

#### 問題 14: 第 540 行
- **類型**: Frontend hardcoded text
- **文字**: `追蹤 ID:`
- **上下文**: `<span className="ml-4">追蹤 ID: <code className="text-xs bg-gray-100 px-1 rounded\">{traceId}</code></span>`
- **修復方案**: 使用 `t('imageSearch.traceId')` 或類似的翻譯鍵

---

### 8. `frontend/src/components/layout/Header.tsx` (2 個問題)

#### 問題 15: 第 16 行
- **類型**: Frontend hardcoded text
- **文字**: `yyyy年M月d日 EEEE`
- **上下文**: `'zh-TW': 'yyyy年M月d日 EEEE',`
- **修復方案**: 這是日期格式字符串，可能需要移到 i18n 配置中，或使用 `t('dateFormat.zhTW')`

#### 問題 16: 第 18 行
- **類型**: Frontend hardcoded text
- **文字**: `yyyy年M月d日(EEEE)`
- **上下文**: `'ja': 'yyyy年M月d日(EEEE)'`
- **修復方案**: 這是日期格式字符串，可能需要移到 i18n 配置中，或使用 `t('dateFormat.ja')`

---

### 9. `frontend/src/stores/authStore.ts` (1 個問題)

#### 問題 17: 第 180 行
- **類型**: Frontend hardcoded text
- **文字**: `OAuth 認證失敗`
- **上下文**: `const message = error.response?.data?.detail || 'OAuth 認證失敗';`
- **修復方案**: 使用 `t('auth.oauthFailed')` 或類似的翻譯鍵

---

### 10. `frontend/src/utils/toast.ts` (1 個問題)

#### 問題 18: 第 78 行
- **類型**: Frontend hardcoded text
- **文字**: `處理中...`
- **上下文**: `export const showLoading = (message: string = '處理中...') => {`
- **修復方案**: 使用 `t('common.processing')` 或類似的翻譯鍵

---

## 📝 修復優先級

### 高優先級 (用戶直接可見的錯誤訊息)
1. ✅ 後端 API 錯誤訊息 (問題 3-10)
2. ✅ 前端錯誤訊息 (問題 12-13, 17)
3. ✅ 前端 UI 文字 (問題 11, 14, 18)

### 中優先級 (配置相關)
4. ⚠️ 日期格式字符串 (問題 15-16) - 可能需要特殊處理

---

## 🛠️ 修復步驟

### 後端修復步驟
1. 參考 `backend/app/services/channel_assist_service.py` 中的 `_get_error_message()` 實現
2. 為每個服務創建類似的錯誤訊息字典
3. 或使用 `backend/app/utils/i18n.py` 中的 i18n 工具函數
4. 替換所有硬編碼的錯誤訊息

### 前端修復步驟
1. 在 `frontend/src/i18n/index.ts` 中添加缺失的翻譯鍵
2. 在對應文件中導入 `useTranslation` hook
3. 使用 `t('translation.key')` 替換硬編碼文字

---

## ✅ 完成檢查清單

- [ ] 修復後端問題 1-10
- [ ] 修復前端問題 11-18
- [ ] 更新 i18n 翻譯文件（添加所有新鍵）
- [ ] 運行檢查腳本驗證修復
- [ ] 測試所有語言版本（zh-TW, en, ja）

---

**生成時間**: 2026-02-06
**檢查腳本版本**: 改進版（只檢查用戶可見文字）

