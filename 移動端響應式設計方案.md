# 移動端響應式設計方案（手機 & 平板）

## 📱 設計原則

### 一、響應式斷點設定

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      screens: {
        'xs': '375px',   // 小手機
        'sm': '640px',   // 大手機
        'md': '768px',   // 平板（豎屏）
        'lg': '1024px',  // 平板（橫屏）
        'xl': '1280px',  // 桌面
        '2xl': '1536px', // 大桌面
      },
      spacing: {
        'safe-top': 'env(safe-area-inset-top)',
        'safe-bottom': 'env(safe-area-inset-bottom)',
        'safe-left': 'env(safe-area-inset-left)',
        'safe-right': 'env(safe-area-inset-right)',
      }
    }
  }
}
```

### 二、觸控友善設計

- **最小觸控目標**: 44×44px（iOS）或 48×48px（Android）
- **按鈕間距**: 至少 8px
- **文字大小**: 最小 14px（手機），16px（平板）
- **安全區域**: 考慮 iPhone 的劉海和底部指示器

---

## 🎨 版面適配方案

### 一、主題列表頁（Topics）

#### 桌面版（≥1024px）
```
┌─────────────────────────────────────────┐
│  [篩選器]  [主題卡片] [主題卡片] [主題卡片] │
│  (3欄)     [主題卡片] [主題卡片] [主題卡片] │
│            [主題卡片] [主題卡片] [主題卡片] │
└─────────────────────────────────────────┘
```

#### 平板版（768px-1023px）
```
┌──────────────────────────────┐
│  [篩選器]  [主題卡片] [主題卡片] │
│  (2欄)     [主題卡片] [主題卡片] │
│            [主題卡片] [主題卡片] │
└──────────────────────────────┘
```

#### 手機版（<768px）
```
┌──────────────┐
│  [篩選器]     │
│  (可摺疊)     │
│  ─────────── │
│  [主題卡片]   │
│  (1欄)        │
│  [主題卡片]   │
│  [主題卡片]   │
└──────────────┘
```

**實現代碼**:
```tsx
// Topics 頁面 - 響應式設計
export default function Topics() {
  return (
    <div className="p-4 sm:p-6">
      <div className="grid grid-cols-12 gap-4 lg:gap-6">
        {/* 篩選器 - 響應式顯示 */}
        <div className="col-span-12 lg:col-span-3">
          {/* 手機版：可摺疊 */}
          <div className="lg:hidden mb-4">
            <CollapsibleFilter />
          </div>
          {/* 桌面版：固定顯示 */}
          <div className="hidden lg:block">
            <TopicFilters />
          </div>
        </div>

        {/* 主題列表 - 響應式網格 */}
        <div className="col-span-12 lg:col-span-9">
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
            {topics.map((topic) => (
              <TopicCard key={topic.id} topic={topic} />
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
```

---

### 二、主題詳情頁（Topic Detail）

#### 桌面版（≥1024px）
```
┌─────────────────────────────────────────────┐
│  [照片區]  [內容區]  [資訊區]                │
│  (4欄)     (5欄)     (3欄)                   │
│  [照片]    [文章]    [來源]                  │
│  [照片]    [劇本]    [互動按鈕]              │
│  [照片]    [編輯]    [版本歷史]              │
└─────────────────────────────────────────────┘
```

#### 平板版（768px-1023px）
```
┌──────────────────────────────┐
│  [照片區]  [內容區]           │
│  (4欄)     (8欄)              │
│  [照片]    [文章]             │
│  [照片]    [劇本]             │
│  ─────────────────────────── │
│  [資訊區]                     │
│  (12欄)                       │
└──────────────────────────────┘
```

#### 手機版（<768px）
```
┌──────────────┐
│  [照片區]     │
│  (12欄)       │
│  [照片輪播]   │
│  ─────────── │
│  [內容區]     │
│  (12欄)       │
│  [文章]       │
│  [劇本]       │
│  ─────────── │
│  [資訊區]     │
│  (12欄)       │
│  [來源]       │
│  [互動按鈕]   │
└──────────────┘
```

**實現代碼**:
```tsx
// TopicDetail 頁面 - 響應式設計
export default function TopicDetail() {
  return (
    <div className="p-4 sm:p-6 pb-20 lg:pb-6">
      {/* 響應式網格佈局 */}
      <div className="grid grid-cols-12 gap-4 lg:gap-6">
        {/* 照片區 - 手機版全寬，桌面版4欄 */}
        <div className="col-span-12 lg:col-span-4">
          <PhotoGallery photos={photos} />
        </div>

        {/* 內容區 - 手機版全寬，桌面版5欄 */}
        <div className="col-span-12 lg:col-span-5">
          <ArticleContent content={content} />
          <ScriptContent script={script} />
        </div>

        {/* 資訊區 - 手機版全寬，桌面版3欄 */}
        <div className="col-span-12 lg:col-span-3">
          <SourceInfo sources={sources} />
          <InteractionButtons topicId={topicId} />
          <VersionHistory topicId={topicId} />
        </div>
      </div>

      {/* 手機版固定底部互動欄 */}
      <MobileInteractionBar topicId={topicId} />
    </div>
  )
}
```

---

### 三、互動按鈕組件

#### 桌面版
```tsx
// 桌面版互動按鈕 - 水平排列
const DesktopInteractionButtons = ({ topicId }) => {
  return (
    <div className="hidden lg:flex gap-3">
      <button className="flex items-center gap-2 px-4 py-2 bg-green-50 hover:bg-green-100 rounded-lg">
        <ThumbsUpIcon className="w-5 h-5 text-green-600" />
        <span className="text-sm font-medium text-green-700">喜歡</span>
      </button>
      {/* 其他按鈕... */}
    </div>
  )
}
```

#### 手機版
```tsx
// 手機版互動按鈕 - 固定底部欄
const MobileInteractionBar = ({ topicId }) => {
  return (
    <div className="lg:hidden fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 shadow-lg z-50 safe-area-bottom">
      <div className="flex items-center justify-around px-4 py-3">
        {/* 喜歡按鈕 */}
        <button className="flex flex-col items-center gap-1 min-w-[60px] min-h-[60px] p-2 rounded-lg active:bg-green-50 touch-manipulation">
          <ThumbsUpIcon className="w-6 h-6 text-green-600" />
          <span className="text-xs text-gray-600 font-medium">喜歡</span>
        </button>

        {/* 不喜歡按鈕 */}
        <button className="flex flex-col items-center gap-1 min-w-[60px] min-h-[60px] p-2 rounded-lg active:bg-red-50 touch-manipulation">
          <ThumbsDownIcon className="w-6 h-6 text-red-600" />
          <span className="text-xs text-gray-600 font-medium">不喜歡</span>
        </button>

        {/* 編輯按鈕 */}
        <button className="flex flex-col items-center gap-1 min-w-[60px] min-h-[60px] p-2 rounded-lg active:bg-blue-50 touch-manipulation">
          <PencilIcon className="w-6 h-6 text-blue-600" />
          <span className="text-xs text-gray-600 font-medium">編輯</span>
        </button>

        {/* 替換照片按鈕 */}
        <button className="flex flex-col items-center gap-1 min-w-[60px] min-h-[60px] p-2 rounded-lg active:bg-purple-50 touch-manipulation">
          <PhotoIcon className="w-6 h-6 text-purple-600" />
          <span className="text-xs text-gray-600 font-medium">照片</span>
        </button>

        {/* 更多選項 */}
        <button className="flex flex-col items-center gap-1 min-w-[60px] min-h-[60px] p-2 rounded-lg active:bg-gray-50 touch-manipulation">
          <DotsVerticalIcon className="w-6 h-6 text-gray-600" />
          <span className="text-xs text-gray-600 font-medium">更多</span>
        </button>
      </div>
    </div>
  )
}
```

**CSS 優化**:
```css
/* 觸控優化 */
.touch-manipulation {
  touch-action: manipulation;
  -webkit-tap-highlight-color: transparent;
}

/* 安全區域支持 */
.safe-area-bottom {
  padding-bottom: env(safe-area-inset-bottom);
}

.safe-area-top {
  padding-top: env(safe-area-inset-top);
}
```

---

### 四、照片輪播組件

#### 手機版照片輪播
```tsx
// 手機版照片輪播 - 滑動切換
const MobilePhotoCarousel = ({ photos }) => {
  const [currentIndex, setCurrentIndex] = useState(0)

  return (
    <div className="lg:hidden relative">
      {/* 照片容器 */}
      <div className="overflow-hidden rounded-lg">
        <div
          className="flex transition-transform duration-300 ease-out"
          style={{ transform: `translateX(-${currentIndex * 100}%)` }}
        >
          {photos.map((photo, index) => (
            <div key={photo.id} className="min-w-full">
              <img
                src={photo.url}
                alt={photo.description}
                className="w-full h-64 sm:h-80 object-cover"
              />
            </div>
          ))}
        </div>
      </div>

      {/* 指示器 */}
      <div className="flex justify-center gap-2 mt-3">
        {photos.map((_, index) => (
          <button
            key={index}
            onClick={() => setCurrentIndex(index)}
            className={`w-2 h-2 rounded-full transition-all ${
              index === currentIndex ? 'bg-blue-600 w-6' : 'bg-gray-300'
            }`}
            aria-label={`查看第 ${index + 1} 張照片`}
          />
        ))}
      </div>

      {/* 左右切換按鈕 */}
      {photos.length > 1 && (
        <>
          <button
            onClick={() => setCurrentIndex((prev) => (prev > 0 ? prev - 1 : photos.length - 1))}
            className="absolute left-2 top-1/2 -translate-y-1/2 w-10 h-10 bg-black bg-opacity-50 rounded-full flex items-center justify-center text-white touch-manipulation"
            aria-label="上一張"
          >
            <ChevronLeftIcon className="w-6 h-6" />
          </button>
          <button
            onClick={() => setCurrentIndex((prev) => (prev < photos.length - 1 ? prev + 1 : 0))}
            className="absolute right-2 top-1/2 -translate-y-1/2 w-10 h-10 bg-black bg-opacity-50 rounded-full flex items-center justify-center text-white touch-manipulation"
            aria-label="下一張"
          >
            <ChevronRightIcon className="w-6 h-6" />
          </button>
        </>
      )}

      {/* 照片計數 */}
      <div className="absolute top-2 right-2 bg-black bg-opacity-50 text-white text-xs px-2 py-1 rounded">
        {currentIndex + 1} / {photos.length}
      </div>
    </div>
  )
}
```

#### 桌面版照片網格
```tsx
// 桌面版照片網格
const DesktopPhotoGrid = ({ photos }) => {
  return (
    <div className="hidden lg:grid grid-cols-2 gap-3">
      {photos.map((photo) => (
        <div key={photo.id} className="relative group cursor-pointer">
          <img
            src={photo.url}
            alt={photo.description}
            className="w-full h-48 object-cover rounded-lg"
          />
          <div className="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-30 transition-opacity rounded-lg flex items-center justify-center">
            <button className="opacity-0 group-hover:opacity-100 text-white">
              <PhotoIcon className="w-6 h-6" />
            </button>
          </div>
        </div>
      ))}
    </div>
  )
}
```

---

### 五、冷啟動問卷 - 移動端適配

```tsx
// 移動端友善的問卷設計
const MobileFriendlyQuestionnaire = ({ onComplete, onSkip }) => {
  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-end sm:items-center justify-center z-50 safe-area-top safe-area-bottom">
      <motion.div
        initial={{ y: '100%', opacity: 0 }}
        animate={{ y: 0, opacity: 1 }}
        exit={{ y: '100%', opacity: 0 }}
        className="bg-white rounded-t-2xl sm:rounded-2xl w-full sm:max-w-md max-h-[90vh] overflow-y-auto"
      >
        {/* 手機版：可拖拽關閉 */}
        <div className="lg:hidden pt-3 pb-2">
          <div className="w-12 h-1 bg-gray-300 rounded-full mx-auto" />
        </div>

        <div className="p-6">
          {/* 標題 */}
          <div className="flex justify-between items-center mb-4">
            <h3 className="text-lg sm:text-xl font-semibold">快速設定（30秒）</h3>
            <button
              onClick={onSkip}
              className="w-8 h-8 flex items-center justify-center rounded-full hover:bg-gray-100 touch-manipulation"
              aria-label="關閉"
            >
              <XIcon className="w-5 h-5 text-gray-400" />
            </button>
          </div>

          {/* 進度條 */}
          <div className="mb-6">
            <div className="w-full bg-gray-200 rounded-full h-2">
              <div
                className="bg-blue-600 h-2 rounded-full transition-all"
                style={{ width: `${progress}%` }}
              />
            </div>
            <p className="text-xs text-gray-500 mt-1 text-center">
              {currentStep + 1} / {questions.length}
            </p>
          </div>

          {/* 問題 */}
          <div className="mb-6">
            <p className="text-base sm:text-lg text-gray-700 mb-4 font-medium">
              {questions[currentStep].question}
            </p>
            <div className="space-y-3">
              {questions[currentStep].options.map((option) => (
                <button
                  key={option}
                  onClick={() => handleAnswer(option)}
                  className="w-full p-4 text-left border-2 border-gray-200 rounded-xl hover:border-blue-300 hover:bg-blue-50 active:bg-blue-100 transition-all touch-manipulation text-sm sm:text-base"
                >
                  {option}
                </button>
              ))}
            </div>
          </div>

          {/* 按鈕 */}
          <div className="flex gap-3">
            {currentStep > 0 && (
              <button
                onClick={() => setCurrentStep(currentStep - 1)}
                className="flex-1 px-4 py-3 border-2 border-gray-300 rounded-xl hover:bg-gray-50 active:bg-gray-100 font-medium touch-manipulation text-sm sm:text-base"
              >
                上一步
              </button>
            )}
            <button
              onClick={handleNext}
              className="flex-1 px-4 py-3 bg-blue-600 text-white rounded-xl hover:bg-blue-700 active:bg-blue-800 font-medium touch-manipulation text-sm sm:text-base"
            >
              {currentStep === questions.length - 1 ? '完成' : '下一步'}
            </button>
          </div>

          {/* 提示 */}
          <p className="text-xs text-gray-400 text-center mt-4">
            💡 完成問卷可解鎖「更準配方」！
          </p>
        </div>
      </motion.div>
    </div>
  )
}
```

---

### 六、文字內容顯示優化

```tsx
// 響應式文字內容組件
const ResponsiveContentDisplay = ({ content, type = 'article' }) => {
  return (
    <div className="bg-gray-50 rounded-lg p-4 sm:p-6">
      <h3 className="font-semibold text-gray-700 mb-3 text-base sm:text-lg">
        {type === 'article' ? '短文' : '腳本'}
      </h3>
      
      {/* 可滾動內容區 */}
      <div className="max-h-64 sm:max-h-96 overflow-y-auto">
        <p className="text-gray-700 whitespace-pre-line text-sm sm:text-base leading-relaxed">
          {content}
        </p>
      </div>

      {/* 統計資訊 */}
      <div className="flex justify-between items-center mt-3 pt-3 border-t border-gray-200">
        <p className="text-xs sm:text-sm text-gray-500">
          {type === 'article' ? `字數：${wordCount} 字` : `時長：約 ${duration} 秒`}
        </p>
        <button className="text-xs sm:text-sm text-blue-600 hover:underline touch-manipulation">
          編輯
        </button>
      </div>
    </div>
  )
}
```

---

### 七、表格和列表優化

```tsx
// 響應式表格 - 手機版轉為卡片
const ResponsiveTable = ({ data }) => {
  return (
    <>
      {/* 桌面版：表格 */}
      <div className="hidden lg:block overflow-x-auto">
        <table className="w-full">
          <thead>
            <tr>
              <th className="px-4 py-3 text-left text-sm font-semibold">項目</th>
              <th className="px-4 py-3 text-left text-sm font-semibold">狀態</th>
              <th className="px-4 py-3 text-left text-sm font-semibold">操作</th>
            </tr>
          </thead>
          <tbody>
            {data.map((item) => (
              <tr key={item.id}>
                <td className="px-4 py-3">{item.name}</td>
                <td className="px-4 py-3">{item.status}</td>
                <td className="px-4 py-3">
                  <button className="text-blue-600 hover:underline">查看</button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      {/* 手機版：卡片列表 */}
      <div className="lg:hidden space-y-3">
        {data.map((item) => (
          <div key={item.id} className="bg-white border rounded-lg p-4">
            <div className="flex justify-between items-start mb-2">
              <h4 className="font-semibold text-sm">{item.name}</h4>
              <span className="text-xs px-2 py-1 bg-gray-100 rounded">{item.status}</span>
            </div>
            <button className="text-sm text-blue-600 hover:underline touch-manipulation">
              查看詳情
            </button>
          </div>
        ))}
      </div>
    </>
  )
}
```

---

### 八、導航欄優化

```tsx
// 響應式導航欄
const ResponsiveHeader = () => {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false)

  return (
    <header className="bg-white border-b sticky top-0 z-40 safe-area-top">
      <div className="px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-14 sm:h-16">
          {/* Logo */}
          <div className="flex items-center">
            <h1 className="text-lg sm:text-xl font-bold">AI Agents</h1>
          </div>

          {/* 桌面版導航 */}
          <nav className="hidden lg:flex items-center gap-6">
            <NavLink to="/dashboard">儀表板</NavLink>
            <NavLink to="/topics">主題</NavLink>
            <NavLink to="/schedule">排程</NavLink>
          </nav>

          {/* 手機版選單按鈕 */}
          <button
            onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
            className="lg:hidden w-10 h-10 flex items-center justify-center rounded-lg hover:bg-gray-100 touch-manipulation"
            aria-label="選單"
          >
            <MenuIcon className="w-6 h-6" />
          </button>
        </div>
      </div>

      {/* 手機版下拉選單 */}
      {mobileMenuOpen && (
        <div className="lg:hidden border-t bg-white">
          <nav className="px-4 py-2 space-y-1">
            <NavLink to="/dashboard" className="block px-3 py-2 rounded-lg hover:bg-gray-100">
              儀表板
            </NavLink>
            <NavLink to="/topics" className="block px-3 py-2 rounded-lg hover:bg-gray-100">
              主題
            </NavLink>
            <NavLink to="/schedule" className="block px-3 py-2 rounded-lg hover:bg-gray-100">
              排程
            </NavLink>
          </nav>
        </div>
      )}
    </header>
  )
}
```

---

### 九、表單輸入優化

```tsx
// 移動端友善的表單
const MobileFriendlyForm = () => {
  return (
    <form className="space-y-4">
      {/* 輸入框 - 自動調整字體大小避免縮放 */}
      <div>
        <label className="block text-sm font-medium text-gray-700 mb-1">
          標題
        </label>
        <input
          type="text"
          className="w-full px-4 py-3 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
          style={{ fontSize: '16px' }} // 防止 iOS 自動縮放
        />
      </div>

      {/* 文字區域 */}
      <div>
        <label className="block text-sm font-medium text-gray-700 mb-1">
          內容
        </label>
        <textarea
          rows={6}
          className="w-full px-4 py-3 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 resize-none"
          style={{ fontSize: '16px' }}
        />
      </div>

      {/* 選擇器 - 大觸控目標 */}
      <div>
        <label className="block text-sm font-medium text-gray-700 mb-1">
          分類
        </label>
        <select className="w-full px-4 py-3 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">
          <option>時尚</option>
          <option>美食</option>
          <option>社會</option>
        </select>
      </div>

      {/* 提交按鈕 - 全寬，大觸控目標 */}
      <button
        type="submit"
        className="w-full px-4 py-4 bg-blue-600 text-white rounded-lg font-medium text-base hover:bg-blue-700 active:bg-blue-800 touch-manipulation"
      >
        提交
      </button>
    </form>
  )
}
```

---

### 十、全局樣式優化

```css
/* index.css - 移動端優化 */

/* 基礎重置 */
* {
  -webkit-tap-highlight-color: transparent;
  -webkit-touch-callout: none;
}

/* 防止文字選擇（按鈕） */
button {
  -webkit-user-select: none;
  user-select: none;
}

/* 觸控優化 */
.touch-manipulation {
  touch-action: manipulation;
}

/* 安全區域支持 */
@supports (padding: max(0px)) {
  .safe-area-top {
    padding-top: max(1rem, env(safe-area-inset-top));
  }
  
  .safe-area-bottom {
    padding-bottom: max(1rem, env(safe-area-inset-bottom));
  }
  
  .safe-area-left {
    padding-left: max(1rem, env(safe-area-inset-left));
  }
  
  .safe-area-right {
    padding-right: max(1rem, env(safe-area-inset-right));
  }
}

/* 防止 iOS 自動縮放輸入框 */
input[type="text"],
input[type="email"],
input[type="number"],
input[type="tel"],
input[type="url"],
input[type="search"],
textarea,
select {
  font-size: 16px !important;
}

/* 滾動優化 */
.overflow-y-auto {
  -webkit-overflow-scrolling: touch;
  scroll-behavior: smooth;
}

/* 響應式字體 */
@media (max-width: 640px) {
  html {
    font-size: 14px;
  }
}

@media (min-width: 641px) and (max-width: 1023px) {
  html {
    font-size: 15px;
  }
}

@media (min-width: 1024px) {
  html {
    font-size: 16px;
  }
}
```

---

## 📊 響應式測試檢查清單

### 手機測試（375px - 767px）
- ✅ 所有按鈕至少 44×44px
- ✅ 文字大小至少 14px
- ✅ 照片可滑動查看
- ✅ 互動按鈕固定在底部
- ✅ 表單輸入不會自動縮放
- ✅ 安全區域正確顯示
- ✅ 橫屏模式正常顯示

### 平板測試（768px - 1023px）
- ✅ 2欄網格佈局
- ✅ 照片網格顯示
- ✅ 互動按鈕水平排列
- ✅ 文字大小 15-16px
- ✅ 觸控目標足夠大

### 桌面測試（≥1024px）
- ✅ 3欄網格佈局
- ✅ 完整功能顯示
- ✅ 懸停效果正常
- ✅ 鍵盤導航支援

---

## 🎯 實施優先級

### Phase 1（立即實施）
1. ✅ 響應式網格佈局（grid-cols-1 md:grid-cols-2 lg:grid-cols-3）
2. ✅ 手機版固定底部互動欄
3. ✅ 照片輪播組件
4. ✅ 基礎觸控優化（touch-manipulation）

### Phase 2（短期實施）
1. ✅ 安全區域支持（safe-area-inset）
2. ✅ 響應式導航欄
3. ✅ 表單輸入優化（防止縮放）
4. ✅ 問卷移動端適配

### Phase 3（中期實施）
1. ✅ 橫屏模式優化
2. ✅ 手勢操作（滑動、捏合）
3. ✅ 離線支持提示
4. ✅ 性能優化（虛擬滾動）

---

**文件版本**: v1.0  
**最後更新**: 2025-12-30  
**用途**: 移動端響應式設計完整指南

