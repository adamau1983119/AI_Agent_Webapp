# 修復 node_modules 損壞問題

**問題：** `node_modules/date-fns` 目錄損壞，無法讀取  
**錯誤：** "The file or directory is corrupted and unreadable"

---

## 🔧 解決方案

### 步驟 1：停止開發服務器

在運行 `npm run dev` 的終端中，按 `Ctrl + C` 停止服務器。

### 步驟 2：清理並重新安裝依賴

```powershell
# 進入前端目錄
cd "F:\Adam 2025\Myproject\AI_Agent_Wbbapp_for_Social_Media_Content_Generation\frontend"

# 刪除 node_modules 和 lock 文件
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json

# 清理 npm 緩存
npm cache clean --force

# 重新安裝依賴
npm install
```

### 步驟 3：重新啟動服務器

```powershell
npm run dev
```

---

## 🚨 如果問題仍然存在

### 方案 A：檢查磁盤錯誤

```powershell
# 檢查磁盤錯誤（需要管理員權限）
chkdsk F: /f
```

### 方案 B：使用 yarn 替代 npm

```powershell
# 安裝 yarn（如果沒有）
npm install -g yarn

# 使用 yarn 安裝
yarn install
yarn dev
```

### 方案 C：手動修復 date-fns

```powershell
# 只重新安裝 date-fns
npm uninstall date-fns
npm install date-fns@^2.30.0
```

---

## 📋 完整修復流程

```powershell
# 1. 停止服務器（Ctrl + C）

# 2. 進入前端目錄
cd "F:\Adam 2025\Myproject\AI_Agent_Wbbapp_for_Social_Media_Content_Generation\frontend"

# 3. 刪除損壞的依賴
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json

# 4. 清理緩存
npm cache clean --force

# 5. 重新安裝
npm install

# 6. 啟動服務器
npm run dev
```

---

## ⚠️ 注意事項

1. **刪除 node_modules 是安全的**：可以隨時重新安裝
2. **可能需要幾分鐘**：重新安裝所有依賴需要時間
3. **確保網絡連接正常**：需要下載依賴

---

**請按照上述步驟執行，應該可以解決問題！**

