# 專案完整架構表

> **專案名稱**：Influencers AI（網紅 AI 助手）  
> **版本**：v6.0.0  
> **更新日期**：2026-05-19  
> **狀態**：🎉 Phase 1-5 全部程式開發完成，v5.0 靈感策劃 AI 助手核心功能 100% 完成  
> **相關文件**：`v4.0.0_完整需求規格書.md`, `v4.0.0_Checklist_TestList.md`, `docs/v5.0_靈感策劃_技術設計報告_第三方審查.md`, `docs/channel_create_new_scheme_checklist.md`（建立頻道新方案 **交付對照**）, **`docs/architecture_test_matrix.md`**（**全專案架構測試對照**）, **`docs/test_week_gap_closure_plan.md`**（**測試週架構缺口 12 項收口計劃**）, **`docs/test_week_daily_checklist.md`**（**測試週每日 Checklist**）, **`docs/環境重建指南與Checklist.md`**（**換機／硬碟遺失後本機環境重建**；測試週 Gate **A+B**；救回檔合併原則；**第十一～十三節** 易漏複核／壓縮重建／對比指令）

---

## 📋 專案功能總覽

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Influencers AI v4.0.0                            │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                        核心功能                                    │ │
│  │                                                                   │ │
│  │  📰 主打內容（Fashion/Food/Trend）  - 每 6 小時自動收集           │ │
│  │  📺 會員頻道（最多 3 個）           - 自定義類別和地區             │ │
│  │  🎨 風格學習代理                    - 👍/👎 評分 → AI 學習偏好   │ │
│  │  📤 分享代理                        - 一鍵發布到 4 個社交平台     │ │
│  │  💡 靈感策劃                        - 自定義主題搜尋 + AI 智慧助手 │ │
│  │  🤖 AI 智慧助手 (v5.0)             - 動態問題生成 + 內容生成     │ │
│  │  🔍 來源驗證 (v5.0)                - 三層驗證機制（邏輯+AI+比對）│ │
│  │  💰 成本監控 (v5.0)                - Token 使用量監控與警告      │ │
│  │  🔄 無限 Loop                       - 無限滾動、保留 15 天        │ │
│  │  🌐 多語言                          - 繁體中文/英文/日文           │ │
│  │  🔐 會員系統                        - Google OAuth + Email 登入   │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🛣️ 前端路由結構（重要！）

### 路由配置 (`frontend/src/app/App.tsx`)

```typescript
// 根路徑重定向邏輯（與 frontend/src/app/App.tsx 一致；2026-05-09 複核）
"/" → RootRedirect:
  - 有 token → "/dashboard"
  - 無語言偏好 → "/language"
  - 有語言、無 token → "/login"

// 認證與靜態頁（無 MainLayout）
"/language"         → LanguageSelection
"/login"            → Login
"/register"         → Register
"/forgot-password"  → ForgotPassword
"/reset-password"   → ResetPassword
"/oauth-callback"   → OAuthCallback
"/verify-email"     → VerifyEmail
"/terms"            → Terms
"/privacy"          → Privacy

// 主要頁面（有 MainLayout）
"/dashboard"           → Dashboard
"/topics"              → Topics
"/topics/:id"          → TopicDetail
"/preferences"         → Preferences   ← 側欄「設定」圖示進此頁（labelKey: nav.settings）
"/schedule"            → Schedule
"/settings"            → Settings      ← 個人帳號／檔案設定（與 /preferences 並存）
"/channels"            → Channels
"/channels/create"     → CreateChannel
"/channels/:id"        → ChannelDetail
"/channels/:id/edit"   → ChannelEdit
"/inspiration"         → Inspiration
"/style-profile"       → StyleProfile
"/publish"             → Publish
"/social-connect"      → SocialConnect
```

### Sidebar 導航配置 (`frontend/src/components/layout/Sidebar.tsx`)

```typescript
const menuItemsConfig = [
  { path: '/dashboard',      labelKey: 'nav.dashboard',      icon: 'home' },
  { path: '/topics',         labelKey: 'nav.topics',         icon: 'document' },
  { path: '/channels',       labelKey: 'nav.channels',       icon: 'channel' },
  { path: '/inspiration',    labelKey: 'nav.inspiration',    icon: 'lightbulb' },
  { path: '/style-profile',  labelKey: 'nav.styleProfile',   icon: 'sparkles' },
  { path: '/publish',        labelKey: 'nav.publish',        icon: 'rocket' },
  { path: '/social-connect', labelKey: 'nav.socialConnect',  icon: 'link' },
  { path: '/preferences',    labelKey: 'nav.settings',       icon: 'settings' },
  { path: '/schedule',       labelKey: 'nav.schedule',       icon: 'calendar' },
];
```

⚠️ **重要**：Sidebar 的 `path` 必須與 App.tsx 的路由定義一致！

---

## 📁 專案根目錄結構

```
AI_Agent_Wbbapp_for_Social_Media_Content_Generation/
│
├── backend/                          # 後端應用（Python FastAPI）
├── frontend/                         # 前端應用（React TypeScript）
├── docs/                             # 文檔目錄
├── scripts/                          # 腳本工具
│
├── README.md                         # 專案說明（含架構連結）
├── DEPLOYMENT.md                     # 部署指南
├── 專案完整架構表.md                   # 本文件
├── v4.0.0_完整需求規格書.md           # 完整需求文件
├── v4.0.0_Checklist_TestList.md      # 測試清單
├── .cursorrules                      # Cursor AI 行為規則
│
└── [其他文檔與腳本...]
```

---

## 🎨 前端架構（Frontend）

```
frontend/
│
├── src/
│   │
│   ├── app/                          # App 層
│   │   └── App.tsx                   # 主應用組件 + 路由定義 ⭐
│   │
│   ├── api/                          # API 客戶端 [受保護]
│   │   ├── client.ts                 # API 基礎客戶端（i18n 錯誤訊息）
│   │   ├── interceptors.ts           # API 攔截器（i18n 錯誤訊息）
│   │   ├── auth.ts                   # 認證 API（含 updateProfile）
│   │   ├── topics.ts                 # 主題 API
│   │   ├── contents.ts               # 內容 API
│   │   ├── images.ts                 # 圖片 API
│   │   ├── channels.ts               # 頻道 API（i18n 標籤）
│   │   ├── ratings.ts                # 評分 API（i18n 標籤）
│   │   ├── styleProfile.ts           # 風格檔案 API（i18n 標籤）
│   │   ├── social.ts                 # 社交分發 API（i18n 標籤）
│   │   ├── schedules.ts              # 排程 API
│   │   ├── inspiration.ts            # 靈感策劃 API（含 AI 助手）v5.0
│   │   └── ...
│   │
│   ├── components/
│   │   ├── layout/                   # 佈局組件
│   │   │   ├── MainLayout.tsx        # 主佈局（Sidebar + Header + Content）
│   │   │   ├── Sidebar.tsx           # 側邊欄導航 ⭐
│   │   │   └── Header.tsx            # 頁首（含語言選擇器）⭐ i18n ✅
│   │   │
│   │   ├── features/                 # 功能組件
│   │   │   ├── InfiniteTopicsList.tsx # 無限滾動列表
│   │   │   ├── TopicFilters.tsx      # 主題篩選器
│   │   │   ├── ContentGenerationPanel.tsx # 內容生成面板（風格/格式選擇）
│   │   │   ├── RatingPanel.tsx       # 評分面板 i18n ✅
│   │   │   ├── ImageSearch.tsx       # 圖片搜尋 i18n ✅
│   │   │   ├── ImageGallery.tsx      # 圖片畫廊 i18n ✅
│   │   │   └── ...
│   │   │
│   │   └── ui/                       # UI 組件
│   │       ├── TimeGroupLabel.tsx    # 時間分組標籤
│   │       ├── LoginPromptModal.tsx  # 登入提示模態框
│   │       ├── ConnectionErrorDisplay.tsx # 連線錯誤顯示 i18n ✅
│   │       ├── Pagination.tsx        # 分頁組件 i18n ✅
│   │       └── ...
│   │
│   ├── pages/                        # 頁面組件 ⭐
│   │   │
│   │   │── # 認證頁面
│   │   ├── LanguageSelection.tsx     # 語言選擇頁
│   │   ├── Login.tsx                 # 登入頁
│   │   ├── Register.tsx              # 註冊頁
│   │   ├── ForgotPassword.tsx        # 忘記密碼頁
│   │   ├── OAuthCallback.tsx         # OAuth 回調頁
│   │   │
│   │   │── # 主要頁面
│   │   ├── Dashboard.tsx             # 控制面板 → /dashboard
│   │   ├── Topics.tsx                # 主題列表 → /topics
│   │   ├── TopicDetail.tsx           # 主題詳情 → /topics/:id
│   │   ├── Channels.tsx              # 我的頻道 → /channels
│   │   ├── CreateChannel.tsx         # 建立頻道 → /channels/create
│   │   ├── Inspiration.tsx           # 靈感策劃 → /inspiration（含 AI 智慧助手 UI）v5.0
│   │   ├── StyleProfile.tsx          # 風格檔案 → /style-profile
│   │   ├── Publish.tsx               # 一鍵發布 → /publish
│   │   ├── SocialConnect.tsx         # 平台連接 → /social-connect
│   │   ├── Settings.tsx              # 設定 → /settings
│   │   ├── Preferences.tsx           # 偏好設定 → /preferences
│   │   └── Schedule.tsx              # 排程 → /schedule
│   │
│   ├── i18n/                         # 國際化
│   │   └── index.ts                  # i18n 配置 + 翻譯 ⭐
│   │
│   ├── stores/                       # 狀態管理（Zustand）
│   │   ├── authStore.ts              # 認證狀態 ⭐
│   │   ├── uiStore.ts                # UI 狀態
│   │   └── ...
│   │
│   ├── hooks/                        # 自定義 Hooks
│   │   └── usePageTitle.ts           # 頁面標題 Hook
│   │
│   └── utils/                        # 工具函數
│       └── toast.ts                  # 通知工具
│
├── index.html                        # HTML 模板
├── package.json                      # 依賴配置
├── vite.config.ts                    # Vite 配置（dev **port 3000**、`strictPort: true`—佔用時失敗，不自動改 3001）
└── .env                              # 環境變數
```

---

## 🔧 後端架構（Backend）

```
backend/
│
├── app/
│   │
│   ├── main.py                       # FastAPI 應用入口 [受保護]
│   ├── database.py                   # MongoDB 連接設定 [受保護]
│   ├── config_module.py              # 配置模組
│   │
│   ├── api/v1/                       # API 端點 [受保護目錄]
│   │   ├── auth.py                   # 認證 API（含 PATCH /profile）⭐
│   │   ├── topics.py                 # 主題 API
│   │   ├── contents.py               # 內容 API（支援 target_language）
│   │   ├── images.py                 # 圖片 API
│   │   ├── channels.py               # 頻道 API
│   │   ├── inspiration.py            # 靈感 API
│   │   ├── ratings.py                # 評分 API
│   │   ├── style_profile.py          # 風格檔案 API
│   │   ├── social.py                 # 社交分發 API
│   │   ├── schedules.py              # 排程 API（含 generate-today）
│   │   ├── feeds.py                  # RSS 健康監控 API
│   │   └── ...
│   │
│   ├── models/                       # 資料模型
│   │   ├── user.py                   # 用戶模型
│   │   ├── topic.py                  # 主題模型
│   │   └── ...
│   │
│   ├── services/                     # 業務邏輯
│   │   ├── auth_service.py           # 認證服務
│   │   ├── email_service.py          # 郵件服務
│   │   ├── inspiration_service.py    # 靈感搜尋服務
│   │   ├── style_learning_service.py # 風格學習服務
│   │   ├── distribution_service.py   # 社交分發服務
│   │   ├── channel_collector.py      # 頻道收集服務
│   │   ├── scoring_service.py        # 主題評分服務
│   │   ├── feed_health_service.py    # RSS 健康監控服務
│   │   ├── content_deduplication.py  # 內容去重服務
│   │   │
│   │   ├── inspiration_conversation_service.py      # 對話狀態管理 v5.0
│   │   ├── inspiration_question_generator_service.py # AI 問題生成 v5.0
│   │   ├── inspiration_source_verification_service.py # 來源驗證 v5.0
│   │   ├── inspiration_content_generator_service.py   # 內容生成 v5.0
│   │   ├── inspiration_cost_monitor.py               # 成本監控 v5.0
│   │   ├── inspiration_preference_service.py         # 偏好管理 v5.0
│   │   │
│   │   ├── ai/                       # AI 服務
│   │   │   ├── ai_service_factory.py # AI 服務工廠（DeepSeek/OpenAI/Gemini/Qwen/Ollama）
│   │   │   └── ...
│   │   │
│   │   ├── automation/               # 自動化服務
│   │   │   ├── scheduler.py          # 排程服務（每 6 小時收集）
│   │   │   ├── topic_collector.py    # 主題收集器（多語言支援）
│   │   │   └── workflow.py           # 自動化工作流
│   │   │
│   │   └── repositories/             # 資料存取層
│   │       ├── user_repository.py    # 用戶 Repository
│   │       ├── topic_repository.py   # 主題 Repository
│   │       ├── feed_health_repository.py  # RSS 健康 Repository
│   │       ├── source_list_repository.py  # 來源清單 Repository
│   │       ├── base_repository.py    # 基礎 Repository（供 CostMonitor 使用）
│   │       └── ...
│   │
│   ├── prompts/                      # AI Prompt 模板
│   │   └── article_prompt.py         # 文章生成 Prompt（支援 target_language）
│   │
│   ├── utils/                        # 工具函數
│   │   └── i18n.py                   # 後端 i18n 錯誤訊息
│   │
│   └── middleware/                   # 中間件
│       ├── jwt_auth.py               # JWT 認證
│       ├── rate_limit.py             # 請求限流
│       └── csrf.py                   # CSRF 防護
│
├── .env                              # 環境變數（不提交）
└── requirements.txt                  # Python 依賴
```

---

## 🌐 多語言系統 (i18n)

### 支援語言

| 語言 | 代碼 | 圖示 | 短名 |
|------|------|------|------|
| 繁體中文 | `zh-TW` | 文 | 繁 |
| English | `en` | A | EN |
| 日本語 | `ja` | あ | JP |

### 翻譯鍵結構 (`frontend/src/i18n/index.ts`)

```typescript
{
  // 品牌
  'brand.name': 'Influencers AI',
  'brand.tagline': '網紅 AI 助手',
  
  // 導航
  'nav.dashboard': '控制面板',
  'nav.topics': '主題',
  'nav.channels': '我的頻道',
  // ...
  
  // 問候語
  'greeting.hello': '你好',
  'greeting.guest': '訪客',
  
  // 篩選器
  'filters.title': '篩選條件',
  // ...
}
```

---

## 🔐 認證流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        用戶進入應用                               │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ "/" RootRedirect │
                    └────────┬────────┘
                             │
            ┌────────────────┼────────────────┐
            │                │                │
            ▼                ▼                ▼
    ┌───────────────┐ ┌───────────┐ ┌───────────────┐
    │ 有 Token      │ │ 無語言    │ │ 有語言無Token │
    │ → /topics     │ │ → /language│ │ → /login     │
    └───────────────┘ └───────────┘ └───────────────┘
                                           │
                          ┌────────────────┼────────────────┐
                          │                │                │
                          ▼                ▼                ▼
                   ┌──────────┐    ┌──────────┐    ┌──────────┐
                   │ Email    │    │ Google   │    │ 訪客模式 │
                   │ 登入     │    │ OAuth    │    │ → /topics│
                   └──────────┘    └──────────┘    └──────────┘
```

---

## 🗂️ 主題卡生成邏輯與多語言架構

### 主題卡生成流程

```
┌──────────────────────────────────────────────────────────────────────┐
│                     主題卡生成完整流程                                 │
│                                                                      │
│  ⏰ 排程服務 (SchedulerService v4.1)                                 │
│  │   每 6 小時觸發 (UTC 00:00, 06:00, 12:00, 18:00)                │
│  │                                                                   │
│  ▼                                                                   │
│  📡 主題收集器 (TopicCollector v3.2)                                 │
│  │   1. 角色分配策略 → 從 RSS Feed 收集原始內容                      │
│  │   2. 內容去重 (ContentDeduplicator) → MD5 + 相似度檢測            │
│  │   3. 多樣性評分 (DiversityScorer) → 確保來源多元                  │
│  │   4. 備用關鍵字方案 → RSS 失敗時自動補充                          │
│  │                                                                   │
│  ▼                                                                   │
│  🌐 多語言處理 (display_language + target_language)                  │
│  │   1. 用戶語言偏好 → display_language                              │
│  │   2. 標題翻譯為用戶語言（_translate_title）                       │
│  │   3. 內容生成語言 = target_language（zh-TW/en/ja）                │
│  │                                                                   │
│  ▼                                                                   │
│  💾 儲存到 MongoDB                                                   │
│  │   topics collection → 每個分類 10 筆                              │
│  │                                                                   │
│  ▼                                                                   │
│  🖼️ 自動化工作流 (AutomationWorkflow)                               │
│  │   1. 從 RSS sources[].images 提取預覽圖片                        │
│  │   2. 搜尋匹配圖片 (DuckDuckGo / Google / Unsplash)               │
│  │   3. 階段 1：每個主題 1 張預覽圖                                  │
│  │                                                                   │
│  ▼                                                                   │
│  📱 前端顯示                                                         │
│      Dashboard / Topics → 按用戶語言顯示 titles_i18n[lang]          │
│      TopicDetail → 支援「翻譯全文」按需翻譯                          │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 主題資料模型 (Topic)

```
Topic {
  id: string                    # 唯一識別碼 (topic_{category}_{timestamp}_{index})
  title: string                 # 主題標題（原始語言）
  category: Category            # 分類 (fashion / food / trend)
  status: Status                # 狀態 (pending / confirmed / deleted)
  source: string                # 主要來源名稱
  sources: SourceInfo[]         # 資料來源列表（含原文內容、關鍵字、圖片）
  description: string           # 摘要（約 30 字）
  preview_images: string[]      # 預覽圖片 URL（最多 5 張）
  is_expanded: bool             # 是否已展開完整內容
  generation_config: object     # 生成配置參數

  # 多語言欄位 [已完成 ✅]
  display_language: string      # 生成時的顯示語言 (zh-TW / en / ja)
  original_title: string        # 原始語言標題（翻譯前）
  original_language: string     # 來源原文語言 (en / ja / zh)
  titles_i18n: {                # 多語言標題
    "zh-TW": "繁中標題",
    "en": "English Title",
    "ja": "日本語タイトル"
  }
  description_i18n: {           # 多語言摘要
    "zh-TW": "繁中摘要",
    "en": "English summary",
    "ja": "日本語の概要"
  }

  # 時間戳
  generated_at: datetime
  updated_at: datetime
  created_at: datetime
}
```

### 核心產品理念：「資訊差」策略

```
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│   🌍 全球多語言 RSS 來源                                      │
│   ├── 英文來源：Vogue, WWD, TechCrunch, Eater...             │
│   ├── 日文來源：FASHIONSNAP, 食べログ...                      │
│   └── 中文來源：ELLE HK, 香港01...                           │
│                         ↓                                     │
│   🤖 AI 翻譯 → 標題/摘要 → 用戶語言                         │
│                         ↓                                     │
│   📱 用戶看到：外語內容的「本地化」版本                        │
│                                                               │
│   💡 價值：用戶無需懂外語，即可獲得全球趨勢資訊               │
│   💡 優勢：本地市場尚未報導的國際內容 = 資訊差 = 競爭力       │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

### 三大內容類別 × 收集配置

| 類別 | 每次收集 | 每日 4 次 | RSS 來源數 | 主要語言來源 |
|------|:--------:|:---------:|:----------:|:------------:|
| Fashion (時尚) | 10 | 40 | 23+ | en, ja, zh |
| Food (美食) | 10 | 40 | 19+ | en, ja, zh |
| Trend (趨勢) | 10 | 40 | 30+ | en, ja, zh |

### 相關服務模組

| 服務 | 檔案路徑 | 功能 | 版本 |
|------|---------|------|------|
| SchedulerService | `backend/app/services/automation/scheduler.py` | 排程管理（每 6 小時） | v4.0 |
| TopicCollector | `backend/app/services/automation/topic_collector.py` | RSS 收集 + 去重 | v4.0 |
| AutomationWorkflow | `backend/app/services/automation/workflow.py` | 圖片搜尋 + 內容生成 | v4.0 |
| DistributionService | `backend/app/services/distribution_service.py` | 社交平台分發 | v4.0 |
| StyleLearningService | `backend/app/services/style_learning_service.py` | 風格學習 | v4.0 |
| ChannelCollector | `backend/app/services/channel_collector.py` | 頻道內容收集 | v4.0 |
| ScoringService | `backend/app/services/scoring_service.py` | 主題評分 | v4.0 |
| ContentDeduplicator | `backend/app/services/content_deduplication.py` | 內容去重 | v4.0 |
| FeedHealthService | `backend/app/services/feed_health_service.py` | RSS 來源健康監控 | v4.0 |
| **ConversationService** | `backend/app/services/inspiration_conversation_service.py` | **對話狀態管理** | **v5.0** |
| **QuestionGeneratorService** | `backend/app/services/inspiration_question_generator_service.py` | **AI 動態問題生成** | **v5.0** |
| **SourceVerificationService** | `backend/app/services/inspiration_source_verification_service.py` | **三層來源驗證** | **v5.0** |
| **ContentGeneratorService** | `backend/app/services/inspiration_content_generator_service.py` | **多格式內容生成** | **v5.0** |
| **CostMonitor** | `backend/app/services/inspiration_cost_monitor.py` | **成本監控與警告** | **v5.0** |
| **PreferenceService** | `backend/app/services/inspiration_preference_service.py` | **偏好管理** | **v5.0** |

---

## 📊 排程配置

### 收集排程 (每 6 小時)

```
時間點 (UTC): 00:00, 06:00, 12:00, 18:00
每次收集: Fashion/Food/Trend 各 10 個主題
```

### 清理排程 (每日)

```
時間點 (UTC): 03:00
保留天數: 15 天
批次大小: 100 筆
```

---

## 🔒 受保護的核心文件

修改以下文件前必須先備份：

```
backend/app/main.py
backend/app/config.py
backend/app/database.py
backend/app/api/v1/*.py
frontend/src/api/*.ts
frontend/src/app/App.tsx        ← 路由定義
frontend/src/components/layout/Sidebar.tsx  ← 導航定義
```

---

## ⚠️ 開發注意事項

### 0. 本機開發埠（2026-05-19）
- **前端**：`npm run dev` → **`http://localhost:3000`**（`frontend/vite.config.ts` 已設 **`strictPort: true`**；與 README、`backend` 預設 **`FRONTEND_URL`／`CORS_ORIGINS`** 對齊）。
- **後端**：`http://localhost:8000`（`/docs`、`/health`）。
- 若 3000 被佔用：結束舊 **node** 進程後再啟動，勿改後端 CORS 去配合 3001。

### 1. 路由一致性
- Sidebar 的 `path` 必須與 App.tsx 的 Route 定義一致
- 修改路由時需同時更新兩個文件

### 2. 多語言
- 新增 UI 文字時必須同時添加三種語言翻譯
- 避免使用國旗圖示（政治敏感）

### 3. 認證
- `/` 是重定向，不是頁面
- Dashboard 在 `/dashboard`，不是 `/`

### 4. 測試 ID 規範 (data-testid)
- 所有按鈕、連結、輸入框必須添加 `data-testid` 屬性
- 命名格式：`{類型}-{位置}-{功能}`
- 詳細規範請參考：`按鈕測試ID架構表.md`

```typescript
// 類型前綴
btn-     // 按鈕
link-    // 連結
input-   // 輸入框
form-    // 表單
modal-   // 彈窗
menu-    // 選單

// 範例
data-testid="btn-login-submit"
data-testid="link-sidebar-dashboard"
data-testid="input-register-email"
```

---

## 🗄️ 資料庫集合（MongoDB）

### 核心集合

| 集合名稱 | 用途 | 版本 |
|---------|------|------|
| `users` | 用戶資料 | v4.0 |
| `topics` | 主題資料 | v4.0 |
| `contents` | 內容資料 | v4.0 |
| `images` | 圖片資料 | v4.0 |
| `channels` | 頻道資料 | v4.0 |
| `ratings` | 評分資料 | v4.0 |
| `style_profiles` | 風格檔案 | v4.0 |
| `social_connections` | 社交平台連接 | v4.0 |
| `schedules` | 排程資料 | v4.0 |
| `feed_health` | RSS 健康監控 | v4.0 |

### v5.0 新增集合

| 集合名稱 | 用途 | Repository |
|---------|------|------------|
| `inspiration_sessions` | 靈感策劃對話會話 | `InspirationConversationRepository` |
| `user_inspiration_preferences` | 用戶靈感策劃偏好 | `InspirationPreferenceRepository` |
| `cost_records` | 成本記錄 | `CostRecordRepository` |

---

## 🤖 v5.0 靈感策劃 AI 助手架構

### 核心服務架構

```
┌─────────────────────────────────────────────────────────────────┐
│                   靈感策劃 AI 助手系統 (v5.0)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  前端 UI (Inspiration.tsx)                               │  │
│  │  - 模式切換（搜尋 / AI 助手）                            │  │
│  │  - 開始對話 UI                                           │  │
│  │  - 問答流程 UI（問題顯示、快速選擇、自定義輸入）          │  │
│  │  - 內容生成結果顯示（驗證狀態、來源連結）                │  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │                                        │
│  ┌──────────────────────┼───────────────────────────────────┐  │
│  │  API 層 (inspiration.py)                                 │  │
│  │  - POST /assistant/start      → 開始對話                 │  │
│  │  - POST /assistant/generate   → 生成內容                 │  │
│  │  - GET/PUT /preferences        → 偏好管理                 │  │
│  │  - GET /cost/summary          → 成本摘要                 │  │
│  │  - GET /cost/statistics       → 成本統計                 │  │
│  └──────────────────────┼───────────────────────────────────┘  │
│                         │                                        │
│  ┌──────────────────────┼───────────────────────────────────┐  │
│  │  服務層                                               │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ ConversationService                            │  │  │
│  │  │ - 對話狀態管理（建立會話、添加訊息、完成會話） │  │  │
│  │  │ - 跨 Session 記憶（取得用戶對話歷史）          │  │  │
│  │  │ - 過期機制（30 天自動清理）                    │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ QuestionGeneratorService                       │  │  │
│  │  │ - AI 動態生成問題（1-5 個，根據主題複雜度）    │  │  │
│  │  │ - 根據主題個性化問題                          │  │  │
│  │  │ - 提供快速選擇選項                            │  │  │
│  │  │ - Fallback 機制（AI 失敗時使用預設問題）      │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ SourceVerificationService                      │  │  │
│  │  │ - Layer 1: 邏輯判斷（零成本）                 │  │  │
│  │  │ - Layer 2: AI 驗證（500-800 Token）           │  │  │
│  │  │ - Layer 3: 交叉比對（零成本）                 │  │  │
│  │  │ - 綜合結果計算（權重：30% / 50% / 20%）        │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ ContentGeneratorService                        │  │  │
│  │  │ - 多格式內容生成（影片講稿/文章/貼文/大綱）    │  │  │
│  │  │ - 應用用戶偏好（語氣、風格）                   │  │  │
│  │  │ - 包含指定模組（地址、歷史背景等）             │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ CostMonitor                                    │  │  │
│  │  │ - 成本監控（每日/每月 Token 限制）             │  │  │
│  │  │ - 成本計算（根據 AI 服務）                     │  │  │
│  │  │ - 成本警告機制（80% 閾值警告）                 │  │  │
│  │  │ - 成本統計（按服務、按日期）                   │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ PreferenceService                             │  │  │
│  │  │ - 用戶偏好管理                                │  │  │
│  │  │ - 分級確認策略（高/中/低頻）                  │  │  │
│  │  │ - 偏好清理服務（過期偏好自動清理）            │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  資料層 (MongoDB)                                      │  │
│  │  - inspiration_sessions                               │  │
│  │  - user_inspiration_preferences                       │  │
│  │  - cost_records                                       │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 工作流程

```
1. 用戶輸入主題
   ↓
2. 開始對話 (POST /assistant/start)
   - 建立會話 (ConversationService)
   - AI 生成問題 (QuestionGeneratorService)
   - 成本檢查 (CostMonitor)
   ↓
3. 用戶回答問題
   ↓
4. 生成內容 (POST /assistant/generate)
   - 多來源搜尋 (InspirationService)
   - 來源驗證 (SourceVerificationService)
   - 內容生成 (ContentGeneratorService)
   - 成本記錄 (CostMonitor)
   ↓
5. 顯示結果
   - 內容預覽
   - 驗證狀態標籤
   - 來源連結
```

### Token 成本估算

| 操作 | AI 調用次數 | Token 範圍 | 成本（DeepSeek） |
|------|:-----------:|:----------:|:----------------:|
| 問題生成 | 1 次 | 300-500 | $0.00004-0.00007 |
| 來源驗證 | 1 次 | 500-800 | $0.00007-0.00011 |
| 內容生成 | 1 次 | 2000-3000 | $0.00028-0.00042 |
| **標準流程** | **3 次** | **2800-4300** | **$0.0004-0.0006** |

---

## 📺 建立頻道 Step 2／RSS 自選頻道（規劃紀錄）

<a id="channel-step2-rss-planning"></a>

> **狀態**：**①～④**（`selected_feeds`、池子／已選、assist `exclude_urls`、`feeds/validate`／`feeds/search`）已落地；**⑤** 規格見 `docs/channel_create_ai_guided_spec.md`（**v0.2 §E**）；**UI**：`CreateChannel` **助手優先**版面已落地（預設開助手、摘要、lg 表單收合／展開、Phase C 互動）— 細項見 `channel_create_new_scheme_checklist.md`。  
> **路由**：`/channels/create` → `CreateChannel.tsx`（Step 1 類別 → **Step 2 地區 + 來源** → Step 3 命名）。  
> **目標**：客戶以 **最多 10 條 RSS（Feed URL）** 作為該自選頻道的訂閱／收集基礎；可多輪請 AI 補貨、自行搜尋或貼上 URL，最後一次確認。

### ① API 儲存 selected feeds（上限 10）

| 項目 | 說明 |
|------|------|
| **契約** | 頻道建立／更新請求擴充欄位（建議名）：`selected_feeds: Array<{ url: string; name?: string; role?: string }>`，**長度 ≤ 10**，URL 正規化（去空白、允許僅 https）。 |
| **驗證** | 後端 Pydantic：列表長度、`url` 格式；與現有 `ChannelCreate`／`channels` 集合 schema 對齊後再啟用嚴格拒絕。 |
| **持久化** | MongoDB `channels` 內嵌 **`selected_feeds`**（`ChannelCreate`／`ChannelResponse`）；舊頻道無此欄位或空陣列時收集邏輯 **fallback** 至「類別＋地區 → DEFAULT_RSS_SOURCES」。 |
| **收集行為** | `get_rss_sources_for_channel`：**有有效 `selected_feeds` 時 Layer1 僅用使用者選取**，Layer2 備援沿用；全失敗仍走既有 collector 層級策略。 |
| **預設池 API（Step 2）** | **`GET /channels/defaults/rss-sources?category=&region=`**（需登入）：回傳與 Layer1 相同候選，供前端候選池載入。 |
| **相關檔案（現況）** | `channels.py`、`channel.py`（`ChannelFeedEntry`）、`channel_service.py`、`channel_collector.py`（經 `get_rss_sources_for_channel`） |

### ② Step 2 UI：池子／已選兩區

| 項目 | 說明 |
|------|------|
| **版面** | **左（或上）候選池**：AI 推薦、搜尋結果、驗證通過的自填 URL。**右（或下）已選區**：最多 10 條，顯示 `已選 n／10`。 |
| **操作** | 候選 →「加入已選」；已選 →「移除」；達 10 條時禁用再加入（需 i18n 提示）。 |
| **狀態** | 與建立頻道表單同步；離開 Step 2 進 Step 3 時保留已選（可再加 session 草稿策略）。 |
| **相關檔案（現況）** | `CreateChannel.tsx`：候選池／已選、**貼 URL 驗證**、AI `exclude_urls` 再推薦（見 ③）、`selected_feeds` 提交 |

### ③ Assist 延伸：「再推薦」with exclude

| 項目 | 說明 |
|------|------|
| **目的** | 客戶首輪選畢後，可再請助手推薦一批 **不重複** 的 RSS／來源。 |
| **請求** | ✅ `POST /channels/assist` body 已支援 **`exclude_urls`**（最多 50）；與 `conversation_history` 並用。 |
| **後端** | ✅ `recommend_sources(..., exclude_urls)` 於白名單內往後補滿至多 5 筆。 |
| **前端** | ✅ 已選 URL + 上一輪已展示之推薦 URL 併入 exclude，**再次按「分析」**可換一批。 |
| **相關檔案** | `channels.py`、`channel_assist_service.py`、`CreateChannel.tsx` |

### ④ URL 驗證與搜尋

| 項目 | 說明 |
|------|------|
| **驗證** | ✅ `POST /channels/feeds/validate`：`feed_url_probe_service`（http(s)、**SSRF 主機／解析 IP**、GET + **feedparser** 粗判）；回傳 `error_code` 供前端 i18n。 |
| **搜尋** | ✅ **`GET /channels/feeds/search?q=`** 僅站內白名單；外部搜尋 API 仍待。 |
| **安全** | ✅ URL 長度上限、僅全球可路由位址（`getaddrinfo` 全段檢查）；全站 JWT + 既有 rate limit。 |
| **API** | ✅ `POST /channels/feeds/validate`，body `{ url }` → `{ valid, title?, suggested_name?, error_code? }`。 |
| **相關檔案** | ✅ `backend/app/services/feed_url_probe_service.py`、`channels.py`；`CreateChannel` Step 2 貼上區 |

### ⑤ AI 主導建立頻道＋長尾主題動態 RSS（規劃＋ v0.2 契約）

> **規格文件**：[docs/channel_create_ai_guided_spec.md](./docs/channel_create_ai_guided_spec.md)（A1～D2、**§E 助手優先 Phase A**、檢索 MVP ADR、API 契約）；**完整願景（一至四）**：[docs/channel_create_full_vision_一至四.md](./docs/channel_create_full_vision_一至四.md)。  
> **交付對照／勾選進度**：[docs/channel_create_new_scheme_checklist.md](./docs/channel_create_new_scheme_checklist.md)（「核心」、**A～I**、頂部總覽；與 [AGENTS.md](../AGENTS.md) 工作天對照）；`README.md` §專案架構亦列連結。  
> **已落地 MVP**：`wizard-options`、`assist` 之 **`exclude_urls` 再推薦**、`feeds/validate` 與 Step 2 **貼 URL**；助手內 Step 1／2 精靈列＋備援出口。  
> **版面目標（§E，UI 已落地 MVP）**：進入 `/channels/create` **預設主舞台為助手**、**無需先點「開啟助手」**；**大螢幕**下三步表單預設 **收合**（進階），一鍵展開；L2 進度／L3 摘要／L4 進階見規格 **§E2～E4** 與交付清單 **B～D**。

| 項目 | 說明 |
|------|------|
| **體驗** | 助手為版面主軸：開場自我介紹、說明三步驟即可完成個人化頻道；每步以 **可點選選項** 降低負擔，但 **非純固定模板**——冷門／特殊主題以 **自然語言** 驅動。 |
| **動態選項** | 後端結合 **理解（slot）→ 檢索（搜尋／索引／知識庫）→ 生成短列表** → 前端渲染為按鈕；**MVP** 檢索僅 **站內 RSS 白名單**；RSS／Feed 若來自外部探索須經 **probe／SSRF 安全**（與 ④ 銜接）。 |
| **備援 UX** | 保留「自行描述」「進階表單」類出口，避免 AI 誤判時卡住。 |
| **助手優先（Assistant-first）** | **§E Phase A（2026-05-08）** 已凍結；**`CreateChannel`**：預設助手、並排、摘要、**lg 表單收合／展開**、Phase C／D 交付項—見 **`channel_create_new_scheme_checklist.md`**。 |
| **相關檔案** | `CreateChannel.tsx`、`channels.py`、`channel_assist_service.py`、`feed_url_probe_service.py` |

### 追查索引（關鍵字）

`selected_feeds`、`exclude_urls`、CreateChannel Step 2、`channel_assist_service`、`channel_collector`、RSS validate、feed search、AI-guided channel creation、dynamic RSS retrieval

---

## 📝 版本歷史

| 版本 | 日期 | 更新內容 |
|------|------|----------|
| 架構表更新 | 2026-05-09 | **⑤** 同步 `CreateChannel` 助手優先 UI；狀態列與 **`channel_create_new_scheme_checklist.md`** B～D 對齊；頂部「相關文件」增測試週文件（**`architecture_test_matrix`**、**`test_week_gap_closure_plan`**、**`test_week_daily_checklist`**）。**`README.md`** 增 **交付驗收：以 Checklist 為唯一收貨依據** 與 **下週必跑 Checklist 總表**（與本表路由／模組對照）。**文件快照**：`docs/backups/2026-05-09_test_week_docs_snapshot`。**下週前抽查**：`frontend` **`npm run build`**（Vite 5.4）✅ 通過 |
| 架構表更新 | 2026-05-07 | **⑤** 對齊 `channel_create_ai_guided_spec.md`；`wizard-options` MVP；`CreateChannel` 助手 Step1 精靈列；加連結 **完整願景一至四** `docs/channel_create_full_vision_一至四.md`；**補** **交付對照** `channel_create_new_scheme_checklist.md`、頂部「相關文件」收錄 |
| 架構表更新 | 2026-05-08 | **⑤** 對齊規格 **v0.2 §E**（助手優先 Phase A 凍結、Phase B UI 未開工）；建立頻道小節狀態列同步 **①～④** 已落地 |
| 架構表更新 | 2026-05-06 | Step 2／RSS 節：對齊 ①② 實作（`selected_feeds`、`GET .../defaults/rss-sources`、池子／已選）；新增 **⑤ AI 主導三步驟＋長尾動態 RSS** 規劃列 |
| 架構表補充 | 2026-05-06 | 新增「建立頻道 Step 2／RSS 自選頻道」規劃：① API 存 feeds（≤10）② 池子／已選 UI ③ assist 再推薦 exclude ④ URL 驗證與搜尋 |
| v5.0.0 | 2026-02-25 | 🎉 v5.0 靈感策劃 AI 助手核心功能 100% 完成（10/10 服務） |
| v4.3.0 | 2026-02-07 | 🎉 全部程式開發完成 + 硬編碼根治 + API/服務架構更新 |
| v4.2.0 | 2026-02-06 | 新增主題卡生成邏輯與多語言架構 |
| v4.0.0 | 2026-02-03 | 多語言系統、品牌更新、路由修正 |
| v3.1.0 | 2026-01-29 | 初始架構規劃 |

---

**文件維護者**：開發團隊  
**最後更新**：2026-05-09
