# 配置完成說明

## ✅ 已完成

### 1. MongoDB 配置
- ✅ 已創建 `backend/.env` 文件
- ✅ 已設置 `MONGODB_URL=mongodb://localhost:27017`（本地 MongoDB）
- ⚠️ 如果使用 MongoDB Atlas，請修改 `.env` 文件中的 `MONGODB_URL`

### 2. DeepSeek API Key 配置
- ✅ 已在 `.env` 文件中添加 `DEEPSEEK_API_KEY` 欄位
- ⚠️ **請填入您的 DeepSeek API Key**：
  1. 訪問 https://platform.deepseek.com/api_keys
  2. 複製您的 API Key
  3. 在 `backend/.env` 文件中填入：`DEEPSEEK_API_KEY=sk-your-actual-key-here`

### 3. 測試手動生成主題
- ✅ API 端點已準備就緒：`POST /api/v1/schedules/generate`
- ⚠️ 需要 MongoDB 和 DeepSeek API Key 配置完成後才能成功生成

## 📝 下一步操作

### 步驟 1：填入 DeepSeek API Key
編輯 `backend/.env` 文件，填入您的 API Key：
```env
DEEPSEEK_API_KEY=sk-your-actual-api-key-here
```

### 步驟 2：重啟後端服務器
配置完成後，需要重啟後端服務器以載入新的環境變數：
1. 停止當前運行的後端服務器（Ctrl+C）
2. 重新啟動：`快速啟動後端.bat`

### 步驟 3：測試生成主題
使用以下方式測試：

**方法 A：使用 API 文檔（推薦）**
1. 訪問 http://localhost:8000/docs
2. 找到 `POST /api/v1/schedules/generate` 端點
3. 點擊 "Try it out"
4. 輸入：
   ```json
   {
     "category": "fashion",
     "count": 1
   }
   ```
5. 點擊 "Execute"

**方法 B：使用 curl 命令**
```bash
curl -X POST http://localhost:8000/api/v1/schedules/generate \
  -H "Content-Type: application/json" \
  -d '{"category":"fashion","count":1}'
```

## 🔍 檢查配置狀態

訪問 http://localhost:8000/api/v1/health 查看：
- `database`: 應顯示 "connected"（如果 MongoDB 連接成功）
- 其他服務狀態

## ⚠️ 注意事項

1. **MongoDB 連接**：
   - 如果使用本地 MongoDB，請確保 MongoDB 服務正在運行
   - 如果使用 MongoDB Atlas，請確保 IP 白名單已配置

2. **DeepSeek API Key**：
   - 必須填入有效的 API Key 才能使用 AI 功能
   - 開發環境中，API Key 未設置時會顯示警告，但不會阻止啟動

3. **重啟服務器**：
   - 修改 `.env` 文件後，必須重啟後端服務器才能生效

