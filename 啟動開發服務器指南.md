# 啟動開發服務器指南

**問題：** localhost:5173 無法連接  
**原因：** Vite 配置的端口是 3000，不是 5173

---

## 🚀 啟動開發服務器

### 方法 1：使用正確的端口（3000）

根據 `frontend/vite.config.ts` 配置，開發服務器運行在端口 **3000**。

**訪問地址：**
```
http://localhost:3000
```

### 方法 2：修改配置使用 5173

如果您想使用 5173 端口，可以修改配置：

**文件：** `frontend/vite.config.ts`

```typescript
server: {
  port: 5173,  // 改為 5173
  open: true,
},
```

---

## 📋 啟動步驟

### 步驟 1：進入前端目錄

```powershell
cd frontend
```

### 步驟 2：啟動開發服務器

```powershell
npm run dev
```

### 步驟 3：訪問應用

- **如果配置是 3000：** http://localhost:3000
- **如果配置是 5173：** http://localhost:5173

---

## 🔍 檢查服務器狀態

### 檢查端口是否被占用：

```powershell
# 檢查 3000 端口
netstat -ano | Select-String ":3000"

# 檢查 5173 端口
netstat -ano | Select-String ":5173"
```

### 檢查服務器是否運行：

如果看到端口被占用，說明服務器正在運行。

---

## ⚠️ 常見問題

### 問題 1：端口已被占用

**解決方案：**
1. 找到占用端口的進程並關閉
2. 或修改配置使用其他端口

### 問題 2：依賴未安裝

**解決方案：**
```powershell
cd frontend
npm install
```

### 問題 3：服務器啟動失敗

**檢查：**
- Node.js 版本是否正確
- npm 依賴是否完整
- 是否有錯誤訊息

---

## 🎯 快速啟動命令

```powershell
# 進入前端目錄
cd frontend

# 啟動開發服務器
npm run dev

# 服務器會自動打開瀏覽器
# 如果沒有，手動訪問 http://localhost:3000
```

---

## 📝 當前配置

根據 `frontend/vite.config.ts`：
- **端口：** 3000
- **自動打開：** 是
- **訪問地址：** http://localhost:3000

---

**請使用 http://localhost:3000 訪問應用！**

