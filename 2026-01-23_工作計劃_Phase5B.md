# 📋 工作計劃 - 2026-01-23
## Phase 5B: 智能圖片匹配

---

## 🎯 目標

**今天完成（5A）**: 卡片建立時有圖片 ✅  
**明天目標（5B）**: 內容生成後，智能匹配 8-10 張最佳圖片

---

## 📐 核心演算法

```
ImageScore = 0.5K + 0.3T + 0.2Q

K = Keyword Match (關鍵字匹配) - 50%
T = Trust Score (來源信任度) - 30%  
Q = Quality Score (圖片品質) - 20%
```

### 來源信任度分級

| 等級 | 分數 | 來源 |
|------|------|------|
| S | 1.0 | Vogue, Elle, NYT, BBC, WIRED |
| A | 0.8 | BoF, WWD, Hypebeast, TechCrunch |
| B | 0.6 | Unsplash, Pexels (外部搜尋) |
| C | 0.4 | 未知來源 |

---

## 📁 需要建立的新文件

### 1. `backend/app/services/images/image_matcher.py`

```python
class ImageMatcherService:
    """智能圖片匹配服務"""
    
    async def match_images(self, topic_id: str) -> List[MatchedImage]:
        """主入口：為主題匹配最佳圖片"""
        
    def compute_image_score(self, image: dict, keywords: list) -> float:
        """計算單張圖片的相關度分數"""
        
    def _compute_keyword_score(self, image: dict, keywords: list) -> float:
        """K: 關鍵字匹配分數 (0-1)"""
        
    def _compute_trust_score(self, source: str) -> float:
        """T: 來源信任度分數 (0-1)"""
        
    def _compute_quality_score(self, image: dict) -> float:
        """Q: 圖片品質分數 (0-1)"""
```

### 2. `backend/app/services/images/keyword_extractor.py`

```python
class KeywordExtractor:
    """從生成內容提取關鍵字"""
    
    def extract_keywords(self, title: str, content: str, category: str) -> List[str]:
        """提取關鍵字列表"""
        
    def _extract_chinese_keywords(self, text: str) -> List[str]:
        """中文關鍵字提取"""
        
    def _extract_english_keywords(self, text: str) -> List[str]:
        """英文關鍵字提取（品牌名、專有名詞）"""
```

### 3. `backend/app/services/images/__init__.py`

```python
from .image_matcher import ImageMatcherService
from .keyword_extractor import KeywordExtractor
```

---

## 📝 需要修改的文件

### 1. `backend/app/services/automation/workflow.py`

在內容生成後添加圖片匹配步驟：

```python
# 現有流程
async def process_topic(self, topic_id: str):
    # Step 1: Generate content
    await self._generate_content(topic_id)
    
    # Step 2: Search images (現有)
    await self._search_images(topic_id)
    
    # NEW Step 3: Match images (Phase 5B)
    await self._match_images(topic_id)
```

### 2. `backend/app/models/topic.py`

添加新的圖片結構：

```python
class MatchedImage(BaseModel):
    url: str
    score: float
    score_breakdown: Dict[str, float]
    caption: Optional[str] = None
    source: str  # "source_article" | "unsplash" | "pexels"
    matched_keywords: List[str] = []

class TopicImages(BaseModel):
    preview: List[str] = []        # Phase 5A
    matched: List[MatchedImage] = []  # Phase 5B
```

---

## ⏰ 時間規劃 (約 3 小時)

| 時間 | 任務 | 預估 |
|------|------|------|
| 09:00-09:30 | 建立 `keyword_extractor.py` | 30 分鐘 |
| 09:30-10:30 | 建立 `image_matcher.py` | 60 分鐘 |
| 10:30-11:00 | 修改 `workflow.py` 整合匹配 | 30 分鐘 |
| 11:00-11:30 | 測試與除錯 | 30 分鐘 |
| 11:30-12:00 | Git 提交與文件更新 | 30 分鐘 |

---

## ✅ 驗收標準

- [ ] 內容生成後，`images.matched[]` 有 8-10 張圖片
- [ ] 每張匹配圖片有 `score` 和 `score_breakdown`
- [ ] 圖片按分數降序排列
- [ ] 當預覽圖片 < 8 張時，自動從外部搜尋補充
- [ ] 匹配過程不影響現有內容生成流程

---

## 🔄 流程圖

```
┌─────────────────┐
│  內容生成完成   │
│  (title+content)│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  提取關鍵字     │
│  KeywordExtractor│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  收集候選圖片   │
│  preview_images │
│  + sources[]    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  計算每張圖片   │
│  ImageScore     │
│  = 0.5K+0.3T+0.2Q│
└────────┬────────┘
         │
         ▼
    ┌────┴────┐
    │ 圖片≥8? │
    └────┬────┘
     Yes │    │ No
         │    ▼
         │ ┌──────────────┐
         │ │ 外部搜尋補充 │
         │ │ Unsplash/    │
         │ │ Pexels       │
         │ └──────┬───────┘
         │        │
         ▼        ▼
┌─────────────────┐
│  排序並保存     │
│  Top 8-10 到    │
│  images.matched │
└─────────────────┘
```

---

## 📌 注意事項

1. **向後兼容**: 保留 `preview_images` 欄位
2. **效能考量**: 圖片匹配應 < 2 秒
3. **錯誤處理**: 外部搜尋失敗時不影響主流程
4. **日誌記錄**: 記錄匹配過程便於除錯

---

## 🚀 開始指令

```bash
# 1. 確認在正確分支
git checkout feature/phase5a-preview-images

# 2. 建立目錄
mkdir -p backend/app/services/images

# 3. 啟動後端服務
cd backend
.\venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

---

**準備完成！明天見 👋**
