# API 診斷結果報告

## 📋 診斷時間
2026-01-16

## 🔍 診斷目標
測試 `POST /api/v1/schedules/generate-today` API

## 📡 診斷結果

### 狀態碼
**500 Internal Server Error**

### 錯誤訊息
```
啟動生成任務失敗: name 'ConnectionFailure' is not defined
```

## 🔧 問題分析

### 根本原因
錯誤訊息 `name 'ConnectionFailure' is not defined` 表示在某個地方使用了 `ConnectionFailure` 但沒有正確導入。

### 已修復的文件
1. ✅ `backend/app/services/repositories/base_repository.py` - 已添加 `from pymongo.errors import ConnectionFailure`
2. ✅ `backend/app/api/v1/schedules.py` - 已正確導入 `ConnectionFailure`

### 可能的原因
1. **後端服務器未重新加載**：雖然代碼已修復，但後端服務器可能仍在使用舊代碼
2. **其他文件未導入**：可能有其他文件使用了 `ConnectionFailure` 但沒有導入

## 🎯 建議解決方案

### 1. 重新啟動後端服務器
```bash
# 停止當前運行的後端服務器
# 然後重新啟動：
cd backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 檢查後端日誌
查看後端 console 輸出，確認完整的錯誤堆疊追蹤。

### 3. 驗證修復
重新執行診斷腳本：
```bash
python diagnose_generate_today.py
```

## 📝 預期結果

修復後，API 應該：
- **資料庫未連接時**：返回 **400 Bad Request**，包含友好的錯誤訊息和修復建議
- **資料庫連接正常時**：返回 **200 OK**，任務在背景執行

## 🔄 下一步行動

1. 重新啟動後端服務器
2. 重新執行診斷腳本
3. 檢查後端日誌以確認錯誤是否已解決
4. 如果問題仍然存在，檢查是否有其他文件需要導入 `ConnectionFailure`

