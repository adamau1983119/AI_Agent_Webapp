# 開發服務器故障排除指南

**問題：** localhost:3000 無法連接（ERR_CONNECTION_REFUSED）

---

## 🔍 診斷步驟

### 步驟 1：檢查 Node.js 是否安裝

```powershell
node --version
npm --version
```

**如果沒有安裝：**
- 下載並安裝 Node.js：https://nodejs.org/
- 建議版本：LTS 版本（18.x 或更高）

### 步驟 2：檢查依賴是否完整

```powershell
cd frontend
npm install
```

### 步驟 3：檢查端口是否被占用

```powershell
# 檢查 3000 端口
netstat -ano | Select-String ":3000"

# 如果被占用，找到進程 ID 並結束
# taskkill /PID <進程ID> /F
```

### 步驟 4：嘗試啟動服務器

```powershell
cd frontend
npm run dev
```

**查看輸出：**
- 如果成功，會顯示 "Local: http://localhost:3000"
- 如果有錯誤，會顯示錯誤訊息

---

## 🚨 常見錯誤和解決方案

### 錯誤 1：端口已被占用

**錯誤訊息：** `Port 3000 is in use`

**解決方案：**
1. 找到占用端口的進程並關閉
2. 或修改 `vite.config.ts` 使用其他端口

### 錯誤 2：依賴缺失

**錯誤訊息：** `Cannot find module`

**解決方案：**
```powershell
cd frontend
rm -r node_modules  # 或 Remove-Item node_modules -Recurse
npm install
```

### 錯誤 3：Node.js 版本不兼容

**錯誤訊息：** 各種模塊錯誤

**解決方案：**
- 確保使用 Node.js 18.x 或更高版本
- 更新 npm：`npm install -g npm@latest`

### 錯誤 4：權限問題

**錯誤訊息：** `EACCES` 或權限錯誤

**解決方案：**
- 以管理員身份運行終端
- 或修改 npm 全局目錄權限

---

## 🎯 手動啟動步驟

### 完整流程：

```powershell
# 1. 進入前端目錄
cd "F:\Adam 2025\Myproject\AI_Agent_Wbbapp_for_Social_Media_Content_Generation\frontend"

# 2. 確保依賴已安裝
npm install

# 3. 啟動開發服務器
npm run dev

# 4. 等待服務器啟動
# 應該看到類似：
#   VITE v5.x.x  ready in xxx ms
#   ➜  Local:   http://localhost:3000/
```

---

## 🔧 替代方案

### 方案 1：使用不同的端口

修改 `frontend/vite.config.ts`：

```typescript
server: {
  port: 5173,  // 或其他可用端口
  open: true,
},
```

### 方案 2：檢查防火牆

Windows 防火牆可能阻止了連接：
1. 打開 Windows 防火牆設置
2. 允許 Node.js 通過防火牆

### 方案 3：使用生產構建預覽

```powershell
cd frontend
npm run build
npm run preview
```

---

## 📋 檢查清單

在啟動服務器前，確認：

- [ ] Node.js 已安裝（`node --version`）
- [ ] npm 已安裝（`npm --version`）
- [ ] 依賴已安裝（`node_modules` 目錄存在）
- [ ] 端口 3000 未被占用
- [ ] 防火牆允許連接
- [ ] 在正確的目錄（`frontend` 目錄）

---

## 🆘 如果仍然無法啟動

請提供以下信息：

1. **Node.js 版本：** `node --version`
2. **npm 版本：** `npm --version`
3. **錯誤訊息：** 運行 `npm run dev` 的完整輸出
4. **端口狀態：** `netstat -ano | Select-String ":3000"`

---

**請在新的終端窗口中手動運行 `npm run dev` 並查看錯誤訊息！**

